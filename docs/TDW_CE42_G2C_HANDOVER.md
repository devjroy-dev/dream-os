# CE-42 · SEAT G2 · G2-C · THE 4b RECORD + ROWS 16/17 — HANDOVER
base `548944a` · **docs only** · no `src/`, no `db/`, no migration, no bench

**r2 — supersedes the first `CE42_G2C_DOCS.zip`, which was never applied** (origin still read
`548944a` at 13:08 IST). The IDs follow-up and F-42.194's cure are folded in here rather than
shipped as a second ZIP onto an unpushed first: one apply, one push.

**Reading of the chair's numbers, stated so a wrong reading is caught on arrival.** F-42.191 is
the stale variable paragraph the chair named. **F-42.192 is read as the callback landing gap** —
the only number this seat asked for (4b·9, 12:38). If the chair meant another, this cut's §8 line
carries the wrong label and nothing else.

## WHAT THE CUT CARRIES

1. **The Google 4a/4b record** — `docs/filings/GOOGLE_API_DOMAIN_AND_SEARCH_CONSOLE.md` §7 (STEP 4
   closed; the consent screen and Verification-centre lines that were STILL OPEN, filled) and
   `docs/filings/GOOGLE_PUBLISH_AND_SCOPE_REVIEW.md` §7 card steps 4–6 closed, §8 record filled.
   Every value was read off a founder paste or the DEV440 grant row on 2026-09-10; the one
   exception is the phone footer check, recorded **as attested, not photographed** (B3·2 §6).
2. **The order as walked** (chair, 4b·5 ruling) — B3·2 §3 gains a head note: the console offers no
   *Verify branding* while in Testing, so publish runs first and the grant waits on *verified*.
   §7 carries the walked sequence with times.
3. **F-42.191** — B3·1 `:199–204` said the variable did not exist and nothing read it. Corrected with
   `googleOAuth.js:31/:57/:60–70/:76/:99`. **A second site of the same claim**, not in the filing:
   B3·2 §5's *"NOT EXECUTABLE TODAY"* paragraph — marked stale beside it, not deleted.
   **Left as written, on purpose:** `docs/TDW_INFRA_GOOGLE_OAUTH.md:35` (CE-40, true before the door
   existed) and `docs/TDW_19_G31_S2_HANDOVER.md:25` (the railway value, true when set). Dated
   records are not re-voiced to match a later tree.
4. **F-42.192, OPEN** — the callback returns to `/vendor/storefront` (`google.js:43–44`); only
   `your-website/screen.tsx:316` reads `?google=`. Recorded at B3·2 §8. Cure unruled.
5. **Citation drift** — B3·2 §2 row 5's `page.tsx:864/:866` re-derived to `:797/:799` at pwa
   `a96e2e2`, dated, the old numbers kept as true of `cc109bd7`.
6. **`TEMPLATES.md` rows 16 and 17** — `tdw_couple_broadcast`, `tdw_referral_broadcast`: §2 bodies
   with bytes and sha256 (both re-hashed from the file as written), button per §1a, §3 tracker rows
   filled: **row 16 `1397634775892426`, Active** (13:05) · **row 17 `1057756639996847`, In review**
   (13:06) · both **Marketing as filed**. Held bases **founder-attested, not photographed** — same
   button shape as the earlier `/v/` filings; the first live send's long-press is the wire witness.
   §1's *"opt-out on `marketing_opener` only"* is now stale by rows 13/16/17 — **named, not edited;
   §1 is the chair's** (c-41.4).
7. **F-42.194** — §1a's *"the sample takes the suffix alone"* struck through and corrected: the
   Manager's validator refuses a suffix-only sample (12:59); the sample is the full URL
   `https://thedreamwedding.in/v/DEV440`, review-only. Rows 16/17's button lines carry the correction.
   `docs/TDW_CE42_G2B_HANDOVER.md:9` repeats the old sentence and is left as a dated record.

## FOUNDER — THE TWO FILINGS (R-42.4) — DONE 2026-09-10, recorded as walked

WhatsApp Manager → **The Dream Wedding Direct** (`1739793260373677`) → **Message templates** →
**Create template**. Two new templates, not edits.

For each: Category **Marketing** (type **Default** if asked) · Language **English** (`en`) · no
header, no footer · body pasted exactly, one line · **Add button → Visit website** · text
`See my work` · URL type **Dynamic** · Website URL **`https://thedreamwedding.in/v/` only — never
type `{{1}}`** · samples: body `{{1}}` = `Dev Roy Photography`, button = **the full URL**
`https://thedreamwedding.in/v/DEV440` (F-42.194 — the suffix alone is refused) · check the preview
word for word · **Submit**.

- `tdw_couple_broadcast` — body at `TEMPLATES.md` §2 row 16.
- `tdw_referral_broadcast` — body at `TEMPLATES.md` §2 row 17.

IDs and categories returned and recorded (rows 16/17). The held-base check was attested by the
founder rather than photographed.

## OPEN / NEXT

- **Day 8 — 2026-09-18, after 12:34:53 IST.** The refresh-token check line is withheld until that
  day (conditional-withheld). B3·2 §8's last line closes the packet; publishing is not the proof.
- **House grant owed** (F-40.261, ruling D) — `/google/connect?house=1` as DEV440, signed in as
  `dev@`; a separate tap, not walked. `house_rows = 0` at 12:08.
- **F-42.192** — cure unruled; either `RETURN_PATH` moves to `/vendor/your-website` or Storefront
  learns `?google=`. The chair picks the side.
- **Row 17** — reads *In review* at 13:06; the flip to approved is a one-word update on the next
  Manager reading. Registry entries for 16/17 are R6's (4b-2).
- **Still carried:** F-42.129 open (the `/r/`–`/v/` gap) · F-41.120 · R-41.130 (09-16).
