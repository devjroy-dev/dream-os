# TDW · BLOCK 19 · G3.2 SITTING 3 — CLOSE
## The contracts room, designed as a vendor uses it — from kickoff to the first read signed copy on both phones

**Seat:** designer-LE · Fable · opened 2026-09-07 13:00 IST, closed 2026-09-08 02:26 IST
**Chair:** CE-40 · **Executor of every push, SQL and walk:** the founder
**Governed by:** R-40.120 (the founder's yes on the prototype; C1–C9) · R-40.121 (the lawyer's yes on clause 5) · R-40.118 · R-40.101 · R-40.117 · R-40.124–.127 (the founder's rulings on glass) · R-40.71 · R-31.1 · R-40.66 · R-40.88 · R-40.114 · R-40.44 · c-40.55

---

## 0 · THE CARD

> *The founder walks the prototype and says yes. Then the build, then he sends a real contract from Swati's phone to a new number in under five minutes without asking what any word means.* — kickoff §5

**Closed on data, 2026-09-08 02:22 IST**, on the room at `dreamos-pwa 5fcdf5d` and the doors at `dream-os` (4c, Railway build `27b9a574`): a client made from a name and a number → a manual function → fee and deposit → *Preview and send* with every blank filled where it was named → **Send** → `contract_sign · read` → the couple's OTP · read → the signature → `contract_copy · vendor · read` and `contract_copy · client · read`, every receipt `home=contract_send matched=1`. The founder read the signed agreement from an incognito session and received the PDF on both handsets.

`SELECT … FROM contract_sends WHERE contract_id = 'd8cc26ef-…'` → four rows, all `read`, all with a wamid, no error. `contract_profiles` holds **one** row — the first policies a vendor has ever saved in production, tonight. `contracts` holds five composed-and-signed. `contract_sends` holds five failures from the road here, kept.

---

## 1 · TIPS AT CLOSE

| Repo | Tip | Packets this sitting |
|---|---|---|
| `dream-os` | 4c on `7e0b6eb` (hash owed by the founder) | p1 `8c262cc` · rider `ae781f5` · p3 `52f0b2f` · p4a (owed hash) · p4b `7e0b6eb` · p4c |
| `dreamos-pwa` | `5fcdf5d` | p2 `10dbce8` · p3 `bfa3197` · p4 `6967978` · p4b rider `5fcdf5d` |

Every packet: fresh clone at the named base, CE-22 header, `tools/base_guard.sh` (from F-40.147 on), both-ways bench, founder-applied, founder-pushed. Three cuts withdrawn unapplied when a lane moved beneath them (`eefdd68`, `a4fdc92`, `f050160`) — the guard refused one of them on the founder's glass exactly as designed.

---

## 2 · WHAT THE ROOM IS NOW (the prototype, on glass)

Room with the policies card first · *Your contract policies* — 29 rows, a meaning line on every one, *You deliver on the day, so there's nothing to set here* · *See the standard agreement* — v4 with `[labelled placeholders]` before anything exists · *Someone new* — a name and a number, promoted through the compose door · the record with its progress thread; functions she adds herself; her trade's annex attached by default; *Your policies for {name}* as a card with one CTA that names what still blocks; *What's printed* in plain register · *Preview and send* — every blank filled where it is named, a policy blank as one field · *Waiting for … / signed / deposit received / the date is held* reachable from the list · autosave on blur; nothing a scrim can lose.

---

## 3 · RULINGS RECORDED THIS SITTING

R-40.120 (C1–C9) · R-40.121 (clause 5 as the Vendor's own words) · **R-40.124** a Needed row is filled inline where it is named · **R-40.125** a Needed policy row opens only that row · **R-40.126** the per-couple policies are one prominent tap · **R-40.127** (standing) the walk supersedes the prototype where the glass says so · F-40.265–.268 as ruled (OMIT-ROW; `named_professional` PROFILE; the role through the estate's labels; `signed` in the sealed filename) · c-40.55 (a read taken off the wrong panel).

---

## 4 · FINDINGS — ALLOCATED, AND WHERE EACH CLOSED

| # | What | Closed at |
|---|---|---|
| F-40.242 | `terms.venue`/`city` written, never read | pwa p2 |
| F-40.243 | Send unreachable with no lead-linked events | dream-os p1 (`functions_manual`) + pwa p2 |
| F-40.244 | R-40.73's accommodation never reached paper | dream-os p1 (register v3, clause 5) |
| F-40.245 | composed `sent` routed to the record | pwa p2 |
| F-40.246 | scrim tap discarded typing | pwa p2 |
| F-40.252 | seven PROFILE/DERIVED tokens read off `terms` — no Vendor line, no seal name, no tax block on every prior agreement | dream-os p1 |
| F-40.256 | `Rs Rs` | pwa p4 |
| F-40.257 | the sealed copy failed both ways | E.164 in `sendOne` (p4a) · public `wa-media` link (p4a) · **the Document header at Meta** (founder, 23:21; c-40.55) · readable-before-send (p4c) |
| F-40.258 | the OTP send had no row | dream-os p3 |
| F-40.259 | the sign door had no send arm | dream-os p3 |
| F-40.260 | the escape bleed, 35 JSX sites (this seat's own) | pwa p3 |
| F-40.264 | a private copy of the taxonomy in `contractAnnex.js` | dream-os p4b (bOB GREEN) |
| F-40.265 | 6.4's empty slabs | dream-os p4b |
| F-40.266 | 12.2's name had no home | dream-os p4b + pwa rider |
| F-40.267 | `shot_by` on paper | dream-os p4b |
| F-40.268 | `_draft` on a signed download | dream-os p4b |
| unnumbered | the vendor's copy raced the CDN (131053) | dream-os p4c — chair to allocate |

**Candidate, unnumbered, not concluded:** the sign link opened on the founder's desktop (vendor session) showed the vendor's room, not `/sign/…`. The signing succeeded from the couple's phone. Whether `middleware.ts` redirects an authenticated vendor away from `/sign/<token>` is a read not made.

---

## 5 · PROOFS AT CLOSE

`b56_contract_bench` 236 → **318/318** (§15–§20 authored this sitting; 22 mutations RED → GREEN across them) · `b57_contracts_wiring_bench` 153 → **192/192** (22 byte pins re-cut under R-40.120, five under R-40.124/.125; §11 authored) · `bOB_taxonomy_bench` RED since the s2 rider → **GREEN 78/78** · `tsc --noEmit` and `next build` clean at every pwa apply on the founder's glass. Ten dream-os benches red at the sitting's open remain red and identical to their state at `3b6ded3` — named in the p1 handover, never cured here.

---

## 6 · WHAT THIS SEAT GOT WRONG, ON THE RECORD

- Carried `CONTRACT_SIGN_SEND_ENABLED` as unset and said it as current (F-40.130's shape) — the flag was `1` and the door had no send arm.
- Carried `tdw_payment_reminder` as unfiled and the `tdw_contract_copy` header as present; both corrected by the chair, the second as c-40.55.
- Wrote 35 backslash-u escapes into JSX (F-40.260); wrote a second `Rs` beside `formatRs` (F-40.256); put a `require('./vendor/…')` in the pure renderer and let b56 §5 catch it before the cut.
- Expected `failed_sends_ever = 4`; the table said 5 and was right.

---

## 7 · OWED AFTER THIS SITTING

- The sign-link-on-a-vendor-session read (`middleware.ts`), if the chair wants it.
- The Send door still refuses on none of the six required fields — the room is the one home (sitting 2's note, still standing).
- Annex A's hours / team / deliverables have no surface (kickoff-adjacent, not ruled).
- `PUBLIC_SCHEMA.md` regen for 0139–0147.
- `docs/mocks/G32V4_VETO_SHEET.md` rows superseded by the prototype — unedited by rule.

**One sitting per session. Banked here.**
