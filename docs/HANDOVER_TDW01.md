# HANDOVER — TDW_01 HYGIENE (block close, Chief Engineer-completed)
**Shipped (commit 5d36f8d + this close-out):** legacy engine trio deleted · 12 .baks deleted (diff-gated) · dead frost chain deleted (tsc clean) · type-tree truth corrected (independent trees; lying headers fixed) · root index.js deleted (package.json is boot truth: src/index.js + src/brideIndex.js via start scripts) · 0068 archived (rename-preserving) · BASELINE.md created from the prod inventory · docs regime committed (specs at docs/specs/, protocol+masterplan at docs/) · drops per TDW01_DROPS.sql (guarded; founder's run output is the record) · cover_photos frontend: app/admin/cover/page.tsx + the control-room coming-soon line deleted this close-out · 0063 twin headers + SCHEMA pointers via tdw01_close.sh.

**Drift log (verbatim honesty):** v1's premises failed on: "missing 0063/0064" (0063 exists twice, 0064 exists) · "duplicate type trees" (independent) · "fold" (voided) · five-constraint cluster (seven) · candidate list required prod inventory the spec never demanded. Root cause: spec written without ground-truth audit — now protocol law §3.5.
**Deviations:** backup = counts-as-record (zero rows; ruled) · PROJECT_PROMPT_ADDITION.md was archived contra the stays-in-place ruling — let stand or `git mv docs/archive/PROJECT_PROMPT_ADDITION.md docs/` (founder's one-liner, zero urgency).
**Standing rulings born here (common law):** documented-not-renamed collisions · .bak diff-gate deletion · gaps get owners by dated addendum · zero-referenced ≠ abandoned (feature-level look required) · dependency clusters die by name, one transaction, cascade banned · audit-before-spec (§3.5).
**Assigned forward:** USE_MOCKS excision → TDW_03 P1 · frost mock excision → TDW_13 P5 · scope_org_id column verdicts + prod residue sweep → TDW_02 audit sitting.

**FOUNDER SMOKE (the block's close gate — run after the SQL):**
1. Railway: both services green post-redeploy (root index.js deletion validated; rollback = git revert of 5d36f8d's index.js hunk if boot fails).
2. One Victor turn in the vendor PWA chat.
3. One WhatsApp message on the vendor line; one on the bride line — both answered.
4. Paste the SQL notices + table count here. Status then flips to DONE.
