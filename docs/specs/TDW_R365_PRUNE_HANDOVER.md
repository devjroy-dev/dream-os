# repo: dream-os @ c538481
# R-36.5 RIDER (1) — THE PRUNE. LE DELIVERY HANDOVER

Built at `c538481`, fresh `git fetch -q origin`, tree clean before the NAMED BASE.
Sibling `dreamos-pwa` beside this repo at `a534329`; floor sibling-full.

Ruled: keep the file, prune it, re-found its identity. **One law, one instrument.**

**No production bytes move. No migration. Copy inventory: ZERO.**

---

## 1 · FILE TABLE

| file | state |
|---|---|
| `scripts/b05_f0583_race_bench.js` | MODIFIED — 35 cells → 15, header re-founded |
| `scripts/floor-manifest-r365-race-fence.txt` | **DELETED** — the stale manifest |
| `scripts/floor-manifest-r365-prune.txt` | NEW — this delivery's declared dirt |
| `docs/specs/TDW_R365_PRUNE_HANDOVER.md` | NEW — this file |

`scripts/floor-base.txt` is **UNCHANGED**: `b05_f0583_race_bench` was never entered in
base by the first seat, so clearing its red closes a **delta**, not a base line.

---

## 2 · WHAT WAS CUT, AND WHAT SURVIVED

**DELETED — §2, §3, §4, §5 (194 lines, 20 cells).** All F1: the agentBridge race cell,
the uncured-tree cross-check at `c7c1be1`, the agentBridge mutation arms, and the
createOwner dist/source legs. Three of them (4.2, 4.3, 5.8) were the anchor-dead reds;
the other seventeen were live but **duplicate** — F1's coverage is
`scripts/b05_r365_agent_race_bench.js` at `5951efa`, 17 cells, both-ways proven.
Deleted rather than re-anchored, as ruled: re-anchoring gives a live law a second
mouth, and two instruments for one law drift apart.

**SURVIVED — §6 (11 cells) + §7 (3 cells) + §0.Z (1).** The F2(b) degrade, which
nothing else covers.

**ALSO CUT, because the prune orphaned it:** `armRace` / `disarmRace` / `seedVendor`,
the `UNCURED_TIP` pin, the `child_process` import, and the fixture's race
choreography itself (`agentsSelects`, `onAgentsSelect`, `beforeAgentsWrite` and their
two call sites). Derived, not assumed — after the excision `agentsSelects` had **zero**
readers. A fixture that still advertises a race nobody can arm is machinery pretending
to be coverage.

**HEADER RE-FOUNDED.** It now states what the file is (the F2(b) instrument), why the
F1 cells died (anchors erased by the two-seat collision; coverage owned elsewhere),
and it **cites `b05_r365_agent_race_bench.js` at `5951efa` by name**. The run banner
moved from *"the race fence"* to *"F2(b), the resolve-wrap degrade"*.

**THE FILENAME STILL SAYS `f0583`.** The ruling kept the file, so I did not rename it —
a rename is a delete-plus-create and was not ordered. The header opens by saying the
name is now historical. **Flagged for the chair, not acted on.**

---

## 3 · ⚠ ONE TRAP THE PRUNE ALMOST SPRANG

Cell **4.4 was the `§0.Z` stripper-invocation cell** (F-07.99), and it lived inside the
§4 block being deleted. `b07_f0774_stripper_bench` §4.4 enforces that cell **by name**
on every bench holding a stripper — and §7 still strips `vendorInbound.js`. Cutting §4
cleanly would have turned this prune into a **floor delta one bench over**.

Re-sited into §7, where the stripping now happens, and re-founded on §7's own subject.
**Proven non-vacuous:** replacing `stripComments(...)` with a raw read reds it —
**14/15, RED: §0.Z**. `b07_f0774_stripper_bench` stays **20/20 GREEN**.

---

## 4 · BENCH

`node scripts/b05_f0583_race_bench.js` → **15/15 GREEN** (was 32/35 with 3 reds).

Cell movement, stated rather than counted: **35 → 15.** Twenty F1 cells deleted, zero
added, one (4.4) **renamed and re-sited** as §0.Z. No surviving cell changed its
assertion — §6 and §7 are byte-identical in behaviour to `e97019d`, which is the point:
the ratified cure's proof did not move.

---

## 5 · FLOOR

Instrument: this container. `npm ci` and `npm run build:engine` clean; **no chair-clone
substitution claimed.**

- **BEFORE**, NAMED BASE at `c538481`, clean tree: base 20, and
  **`b05_f0583_race_bench` a live DELTA** (red, never entered in base).
- **AFTER**, `--delivery scripts/floor-manifest-r365-prune.txt --check`: see §6.
- **Movement named: 21 actual reds → 20. One name removed (`b05_f0583_race_bench`),
  none added. `floor-base.txt` untouched at 20.**

The floor is now **delta-free against its own base for the first time this arc** — the
red that R-36.5's two-seat collision introduced is closed.

---

## 6 · FLOOR AFTER — RESULT

`[F-14.16] declared files unmoved — set and contents both verified.`
`FLOOR = NAMED BASE, no delta.`

---

## 7 · WHERE R-36.5 STANDS

- **F1** sealed at `5951efa`; arbiter singular and correctly named in the live estate.
- **F2(b)** sealed at `e97019d`; its instrument re-founded here.
- **F3** zero bytes as ruled; **F-05.84 stays OPEN**, cure re-pointed at the dark
  onboarding gate's own sitting.
- **F-05.88** (`idx_agents_user`) filed, cure queued cold — census SELECT on
  `pg_stat_user_indexes`, then a DROP block if dead. Rides a future engine-plane SQL
  sitting; **not hot**.
- **The walk** is chair-held and organic: next real signup.
