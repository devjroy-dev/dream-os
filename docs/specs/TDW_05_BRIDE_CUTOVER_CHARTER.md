# TDW_05 — BRIDE-LANE CUTOVER CHARTER (eighth chair, 2026-07-21)
Authored gate-open (CE-45). This charter fixes the SEQUENCE and the LAWS; the exact
paste-blocks are authored LIVE by the seated CE with the real values at execution
(the resolution-night §3 precedent — zero placeholders ever ship in runnable form).
**THE NUMBER (canonical, founder-held SIM, CE-52): `+917011788380` — virgin/WhatsApp-clean,
founder-witnessed.** Every paste-block at execution carries these digits literally.
(The MARKETING number, same charter pattern: `+918810531764` — see the memo.)

## 1. WHAT THIS IS
The bride lane goes live on WABA-DIRECT `1739793260373677` — the same clean-born WABA
(SELF · Asia/Kolkata · INR + MC ····0986 · App-LIVE `1425513376067685` sole subscriber ·
system user `61592314612760` Full control) that carries the vendor lane. No new WABA,
no new token, no new app. Cutover-by-addition, not rebirth.

## 2. STANDING LAWS THAT GOVERN (deltas bolded)
- **Token-follows-assets: NO re-mint.** THE NUMBER joins a WABA the token already
  governs; assignment is inherited. Re-minting is forbidden and cures nothing.
- **Provenance is inherited clean** (the WABA's birth certificate is already correct);
  no billing writes occur — the once-only currency write is long spent, correctly.
- **CE-45's code law: `BRIDE_PHONE_NUMBER_ID` and `BRIDE_WHATSAPP_NUMBER` are SET
  TOGETHER or not at all.** The bride branch is unreachable without the explicit
  number (metaLaneFor symmetry). Setting the PNID alone is the F-05.16 disease and
  the code now refuses to route it — but the env stays clean regardless.
- One change → one restart → one test (the F-05.16 relief discipline).
- Two-step PIN for THE NUMBER: a NEW pin, founder-held, OFF-REPO (never the vendor's).
- W-1: this cutover is transport-only; zero bride-soul/voice work rides it.

## 3. THE SEQUENCE (founder-hand console + CE-authored paste-blocks, in order)
1. **Add THE NUMBER to WABA-DIRECT** (Business Manager → WhatsApp Manager → the WABA →
   Add phone number) → SMS/voice verify against the SIM → record the NEW PNID by-screen.
2. **Display name filing:** proposed "The Dream Wedding" (brand-rides-display-name;
   FOUNDER VETO — if the bride-facing brand should differ, rule before filing).
   Review may pend; the vendor precedent says sends work while pending — do not block on it.
3. **Cloud API register** THE NUMBER (the CE authors the register paste-block live:
   `POST /{new-PNID}/register` with the new two-step PIN).
4. **Templates: NOTHING TO FILE.** Templates are WABA-scoped — all 11 APPROVED keys
   (5 AUTH incl. `tdw_couple_login_otp`/`tdw_couple_reset_otp`/`tdw_circle_join_otp`,
   6 content) already serve THE NUMBER the moment it registers.
5. **Witness A — raw template send** from the new PNID to the test handset
   `+918757788550` (CE-authored curl, founder-run): accepted + delivered.
6. **Railway env — ALL THREE services, one pass each, then redeploy all three from one
   tip:** `BRIDE_PHONE_NUMBER_ID=<new PNID>` **AND** `BRIDE_WHATSAPP_NUMBER=whatsapp:+91<THE NUMBER>`
   — together, per CE-45. Nothing else changes. (`OTP_WA_NUMBER` stays unset;
   `MARKETING_PHONE_NUMBER_ID` stays sandbox until the marketing ruling executes.)
7. **Witness B — production template path:** couple `send-otp`/`forgot-pin` smoke
   (CE-authored, founder-run from the Codespace) → OTP arrives on the handset as
   `tdw_couple_*_otp`.
8. **Witness C — production free-form path:** a text TO THE NUMBER from the handset →
   the bride agent's reply arrives back (`line=bride` in the log, wamid, sent→read).
   This witnesses metaLaneFor's cured bride branch live — bench case (b) in production.
9. **SEAL** — the CE re-derives, banks the cutover record, F-05.2 goes ACTIVE for the
   bride lane. The M2b gate ARMS (both lanes live).

## 4. WHAT UN-GATES ON THE SEAL
- **F-05.11-γ** — the couple forgot-PIN reset rail (δ-shaped session write MANDATORY;
  couple server contract pre-witnessed at CE-40 — no re-derivation owed).
- **The bride media-adapter sitting** — the lane-agnostic resolver awaits its adapter;
  `imagePipeline.js:289/293` (F-05.15's sibling fence-fragility, CE-45) rides this
  sitting as a second disclosed cure.
- **M2b** (kickoff issues in-chat at that moment, §10 form): Twilio code sunset ·
  WABA-PROD `1299109268220358` retirement + its 3 `_hx` legacies (FOUNDER VETO) ·
  the three Twilio app unsubscribes · App-DEV `1073940998303309` deletion. Irreversibles
  ALL founder-ruled at the gate, none before.
- **F-05.7 execution** — the var reconciliation (`*_WA_NUMBER` vs `*_WHATSAPP_NUMBER`,
  `TWILIO_WHATSAPP_NUMBER`'s rename/retirement, `+14787788550`'s retirement decision)
  — PROMOTED at CE-43, executes as its own small sitting once both lanes are stable.

## 5. ROLLBACK REALITY (owned in advance)
There is no Twilio rollback for the bride lane (it never had a Meta-era one; the old
+1 number's pipe is Twilio-dead estate-wide). Fallback if THE NUMBER misbehaves:
unset the BRIDE_* pair (lane returns to dormant — couples get nothing rather than
errors), fix forward. Acceptable pre-launch; recorded.
