# TDW_06 — THE CLOSING ARC · EXECUTOR HANDOVER
## The wire measured as delivered · the probe classified · the drawer removed · the block committed

**Base:** `dream-os @ 08c923c` · `dreamos-pwa @ 5b8c1c8`, both re-derived at origin fetch-first from fresh clones, both `git status` clean at the charter's tips (freeze witnessed, both repos).
**Role:** LE / executor, code-capable. **Delivery:** TWO ZIPs. **Apply order binding: ENGINE FIRST, PWA SECOND.**
**Authority:** CE ruling on read-first Part Two (A-2 · A-4 force-arm + control · A-5(ii) · B-3→B-2 · C-1(b) · C-2 · D-1) + the founder's veto 「 approve all 」 and 「 Its gone :( 」.

**Engine delta — TWO code files, TWO docs, nothing else:** `scripts/b06_gauntlet.js` · `scripts/b06_forkc_wireguard_bench.js` · `docs/specs/TDW_06_CARD_THREE_BLOCK.md` (new) · this handover (new).
**Pwa delta — TWO files:** `components/vendor/ChatThread.tsx` · `scripts/tdw06_f06133_drawer.proof.mjs` (new).

**W-1 SHUT — command-asserted 0-line against origin:** `donnaSoul.ts` · `harveySoul.ts` · `consultantHarveySoul.ts` · `advisorLens.ts` · `loop.ts` · `donna.ts` · `relaySeam.ts` · `donnaLead.ts` · `vendorInbound.js` · `chat.js`. **Zero production bytes moved this arc — the movement is entirely instrument, doc and display.**

**SQL NIL.** Zero DDL, zero migration, zero founder-run SQL in this delivery. The Card-Three block ships a READ-ONLY SELECT as documentation, run at the founder's discretion at Evening Seven, never by this ZIP.

**Copy: ZERO ADDED VENDOR BYTES.** Three vendor-facing strings DELETED under the founder's 「 approve all 」. The five Stage-2 strings are **required from their exported home** (`chat.js:2764-2766`) and authored nowhere in the rig.

---

## 1 · ITEM 1 — THE THROUGH-DOOR MOVEMENT (fork A-2)

**The disease, re-derived at my own tip:** the lanes call the compiled `runTurn` at `b06_gauntlet.js:2054` and the grader reads its result at `:2059`. Production vendors talk to a DOOR that can replace the reply. Stage 2 was invisible to every lane verdict ever taken.

**What shipped.** The lane's turn output now passes the **PWA-JSON seat's composition** — classify → specimen → intercept → delivered form, exactly as `chat.js:2547-2570` assembles it — before any verdict reads it. The door's Stage-2 bindings are **required from their one home** on the same require the rig has always made; `chat.js` is a `src/` module so the hygiene fence never touched it and **F-06.112's liftConst is NOT the governing precedent here** — these are real exported bindings, not lifted literals.

**THE STATED LIMIT, never a coverage claim.** The **WA seat's Fork-D retry leg is NOT in the measured path.** A-3 was refused as disproportionate; the retry leg is sealed both-ways at its own bench. Declared here so it is not discovered.

**A-4 — the arming.** `stage2Armed()` reads `process.env.WIRE_GUARD_STAGE2` at call time and treats **absent as armed**. A run inheriting a shell carrying `off` would score un-intercepted fabrications as reds the movement promised to score green — a hollow red. The rig **sets** the variable at every lane's seam and never inherits; `delete` is refused because an unset variable is indistinguishable from an inherited empty shell. Both the set and the state assert (§[31]①).

**A-5(ii) — `r` is byte-untouched.** The delivered form travels as a THIRD ARGUMENT to `sc.verdict`. Graders that ignore it are byte-identical to the day they were sealed; §[31]④ asserts on shipped source that nothing assigns to `r.reply` and that SD-REL's verdict body names no door. **CE-91's grader law is provably intact.**

**The one labeled amendment, count preserved.** SD-C5 gains a door limb: hand fired → green as ever · no hand, INTERCEPTED → green (the vendor never received it; the estate working) · no hand, WALKED → **the only red this line can now draw**. Evening Six's unanimous SD-C5 is the fixture.

**The no-live-row guarantee, and where it does NOT come from.** My read-first conviction of `chat.js:1747` was ratified and is load-bearing: the lane double IS `{ from, schema: () => db }`, so it **satisfies** that null-handle test and the specimen path proceeds to `.insert`. Protection comes from the double's own closure. §[31]⑤ asserts the absence directly.

---

## 2 · ITEM 2 — THE B-2 DIVERGENCE PROBE · VERDICT DECLARED

**B-1 is CLOSED-UNAVAILABLE.** Evening Six's log is gone (founder's word 「 Its gone :( 」). The night's specific SD-REL cause therefore records **UNRESOLVED-NAMED**: the class verdict below stands; the instance's bytes are unrecoverable. Stated plainly, never papered.

**THE PROBE'S QUESTION:** can `refusedFacts` and `notWritten` diverge on a real single-match return? If they can, `loop.ts:749`'s `if (!dc.plain) continue` is a live third exemption neither F-06.115 nor CE-108 enumerated.

**THE VERDICT: THEY CANNOT. MY THIRD READING IS REFUTED AT THIS COMMIT.** All four guards in `donnaLead.ts:290-317` push to **both** arrays inside one block, and the wedding-date guard's inner `if (stands && given)` (`:305`) gates both pushes together. `plainClause` and `refusedOut` are therefore `undefined` together or defined together, always. **`loop.ts:749` is VACUOUS over the only two sites that author `refused`** — fail-safe, not a defect, and it must never be counted as coverage.

**Shipped as cells (§[31]⑥)**, four ways: equal push counts · strict pairwise ordering · both gates reading the same emptiness · the two/three boundary unmoved. Mutation M-6 breaks the lockstep and reds them. **The day a door authors `refused` without `plain`, this reds.**

**The two original readings of Evening Six therefore stand unnarrowed** — no-carrier fail-open vs an uncovered surface — and the arc adds no third.

---

## 3 · ITEM 3 — THE DRAWER, REMOVED OUTRIGHT (C-1(b))

`PairWork` is **DELETED WHOLE**, both branches. `TypingDots` alone is the working state — one animation, one home, no caption. Three vendor-facing strings died with it, all vetoed 「 approve all 」.

**Orphan census, asserted not intended.** `deliberation` had three consumers; only one was the drawer. **FilingChip is a different surface and is untouched.** Orphaned on the display plane: `handoff`, `operator_report`, and summary-less `operator_action`. **DISPLAY DIES, DATA LIVES** — the fields stay, the engine is 0-line, and §6 of the harness asserts every one of them survives rather than trusting intention.

**One disclosed micro-decision:** two comments elsewhere in `ChatThread.tsx` named `PairWork` and would have been left lying. Their **expressions are byte-unmoved**; only their prose is corrected, and the correction says so in-file.

---

## 4 · ITEM 4 — THE CARD-THREE BLOCK, COMMITTED (D-1)

`docs/specs/TDW_06_CARD_THREE_BLOCK.md`, with the sanctioned `Still open — Operator asked:` render as a **named negative fixture in the file itself** and the real sighting beside it as the deaf-cure test. **I ran both fixtures before shipping**: (i) returns 0, (ii) returns exactly one hit.

**A derivation the record should carry:** the rig's `speakerSightings` **never** convicted that line — verified limb by limb (limb 4 needs a leading imperative; limb 5 needs a comma after `Operator`; the speech-act limb carries `says/said` but not `asked`). **The defect was never in shipped code.** It was in the hand-authored evening block, all three times. That is why D-1 — not D-2 — is the cure: the class ends where its transport ends.

---

## 5 · PROOF

**Floor at the delivered tree, my own hand, both repos:**

```
selftest 351 -> 386 (+35 DISCLOSED GROWTH: section [31] whole)
guard 100/100 (count PRESERVED, ONE labeled amendment - §5.1)
relay 40 · m0 50 · m1 45 · m2 43 · m3 37 · m4 33 · m4b 24 · m4c 20 · m4d 16
f0658 20 · f0667 16 · f0681 17 · f0692 23 · advisor 16 · advisor_route 16 · 0081 12
sonnet 13 · donna_cache 16 · b0461_p6 25 · b6_floors 47 · b6_s1 24 · b6_sitting2 22
door_rider 15 · f0550 31 · arc_m1 53 · arc_m2 27 · arc_m3 11 · arc_m4 18 · arc_m5 11
arc_m6 20 · wa_words 19 · downgrade 12 · fresh_thread 10 · pwa_flip_seam 9
meter 28/29 KNOWN RED (F-06.41) carried, not this sitting's
pwa: tdw06_f06133_drawer 41/41 NEW · tdw06_m3_report_chip ALL GREEN
     whole-tree tsc --noEmit ZERO on an installed cleared-cache tree
engine: npm run build green · tsc --noEmit zero · node --check clean both scripts
```

**Every count byte-stable except the two disclosed.**

**LABELED AMENDMENT, count preserved — `b06_forkc_wireguard_bench` §5.1.** It pinned the destructure's LITERAL SHAPE; this movement extends that same destructure and the literal stopped matching while the law it protects was untouched. Re-authored **stricter**: each of the four families must be required from `chat.js` **individually** (dropping any one now reds, where before only a shape change did), and the two-homes guard now covers all four rather than `ACTION_CLAIM_RE` alone. Non-vacuity proven — dropping a name reds 99/100.

**BOTH-WAYS — SIX MUTATIONS OF PRODUCTION CODE, each `cmp`-restored byte-identical:**

| # | mutation | result |
|---|---|---|
| 1 | `stage2Intercept`'s costume-alone gate removed (**chat.js**) | **first GREEN → §[31]②b added → RED 384/386** |
| 2 | the arming seam inherits instead of setting | RED 380/381 |
| 3 | the disarmed control silently re-arms | RED 376/381 |
| 4 | the verdict loses its delivered context (A-5(ii) severed) | RED 383/386 |
| 5 | SD-C5's interception limb removed | RED 384/386 |
| 6 | `donnaLead`'s lockstep broken — a refusal with no plain clause | RED 383/386 |

Plus five pwa mutations, all RED, all `cmp`-restored: drawer call site restored · deleted label returns · `TypingDots` guard inverted · FilingChip filter narrowed · `deliberation` no longer collected.

---

## 6 · FOUR DEFECTS OF MY OWN, FILED NOT PAPERED

**(a) MUTATION M-1 CAME BACK GREEN — and it is the arc's most useful red.** Removing `!verdict.specimen` frees the interceptor to replace **every** classified turn, honest ones included, and my first cut of §[31] passed 381/381. Every cell asserted a costume IS caught; **none asserted an honest turn is NOT.** A guard that intercepts everything would have shipped through this movement. Cured with §[31]②b — the honest lane, interceptor armed, zero replacements. **Fourth time this arc's lineage the mutation floor has caught what an eye passed.**

**(b) MY OWN COMMENT CONVICTED BY MY OWN CELL.** The removal harness asserts the three deleted strings are absent from `ChatThread.tsx` **comments included** (M-2c §5.9's stricter arm). My first comment *quoted two of them* to explain the deletion. The cell reddened, correctly. **I kept the strict cell and stripped my prose** — naming a deleted string is not the same act as keeping it.

**(c) A CELL WHOSE PREMISE WAS WRONG, FOUND BY RUNNING.** §[31]⑤ first asserted the in-sim specimen came back **id-less**, resting on `chat.js:1747`. It came back RED: the double's default insert branch (`:1548`) **manufactures a synthetic id for any unknown table**, so `wireGuardSpecimen` returns a `run_id` naming **no row anywhere**. The premise was wrong, not the code. Corrected to assert **row**-absence, which is the real guarantee. **Minted forward: an in-sim id that names no row is F-06.113's class wearing a return value.**

**(d) A SIGHTING I EXPECTED TO BE A DEFECT AND WHICH IS NOT — recorded, not tidied.** The honest lane's **S5** (advisor room) **is** intercepted, by limb 4's construction: an act claim in the advisor room convicts on sight (F-06.4's prey). The redirect's claim is **conditional** — nothing was filed and the sentence does not say it was — so on these bytes limb 4 reads a conditional as a completion. **This is a PRECISION datum for the wire-guard thread, not a movement defect**; S5 is off the bar (CE-99/R-1) and §[31]②b excludes it **by R-1's own flag**, never by convenience. §[31]②c asserts it in both directions so it cannot silently change.

---

## 7 · FINDINGS FILED THIS SITTING (unnumbered, per the law — the chair mints)

1. **AN IN-SIM ID THAT NAMES NO ROW.** The lane double's default insert branch manufactures a synthetic id under `.single()` for any table it does not model. Nothing in-sim reads it today, so nothing is broken — but a future cell that treated a `run_id` as evidence of persistence would be greening over a row that never existed. F-06.113's class, wearing a return value. Greppable: `return mode ? { data: { id: nid('row') }, error: null }`.
2. **THE ADVISOR LIMB READS A CONDITIONAL AS A COMPLETION** on the honest redirect's shape. One fixture, not a live row — but the live advisor room ships redirects, and the wire-guard thread's precision read should look for this shape. Recorded with its cell.
3. **`loop.ts:749`'s PLAIN GUARD IS VACUOUS** over both `refused` sites (§2's verdict). Fail-safe, never coverage. Named so no future reader counts it.
4. **THE MEASUREMENT-READ LAW EARNED A SPECIMEN THE HARD WAY.** Evening Six's log died in a scrollback and took item 2's instance-level answer with it. The founder's standing instruction now covers evening runs: **gauntlet runs tee to a file as LAW.** Recorded here because the arc paid for it.

---

## 8 · WHAT THE FOUNDER MUST DO — and what he must not

**Dashboard/console acts: NONE.** No env var, no Railway change, no Meta change, no SQL in this delivery.

`WIRE_GUARD_STAGE2` in **production** is untouched by this ZIP — the rig sets it only inside its own process, at its own lane seams. **The tripwire law stays live: one false interception is a STOP.**

Apply **ENGINE FIRST, PWA SECOND**. The engine verify runs `npm run build` before the floor — a count taken on an unbuilt tree is how a wrong number becomes a handover sentence. Each verify ends in the D-10 STOP; **each git line is its own paste-block; do not run one over a red verify.**

**Live verdicts are DECLARED, never claimed. EVENING SEVEN is the witness** — the first of the two closing greens under the amended bar, on a fresh kit against account **9888294440** (vendor `23165e38-6510-4639-ab6a-9f35bab93742`, agent `d02c7a9a-8622-4543-aa40-13d0911faf9b`). Evening Seven's kit **points at** `TDW_06_CARD_THREE_BLOCK.md` rather than restating it.

**Sequencing beyond this sitting is the founder's.**
