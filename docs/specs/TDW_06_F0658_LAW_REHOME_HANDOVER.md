# TDW_06 — F-06.58 MICRO · THE LAW RETURNS TO EVERY ROOM · EXECUTOR HANDOVER

Base: `68b7f28` (re-derived at origin, fetch-first, at delivery). Delta: **6 files** — two
production (`harveySoul.ts`, `loop.ts`), three benches amended, one bench new, plus this
handover. **W-1 opened, chartered and bounded: position/scope only, zero authored bytes.**
Findings travel unnumbered from this session; F-06.59 and F-06.60 were minted by the chair.

---

## 0. THE ONE THING TO READ FIRST — A DISCLOSED DEVIATION FROM THE RULING

**The ruling's §1 compose order reads `NO_MACHINERY_LAW` unconditional. I shipped it
`(isConsult ? '' : NO_MACHINERY_LAW)`. Ratify or reverse — the reversal is one term.**

I found this while wiring, not while reasoning, and it is reported under §0.2 rather than
adapted around quietly:

| witness | bytes |
|---|---|
| `consultantHarveySoul.ts:6` | *"NO DONNA. He has no operator, no back office, no records, no books."* |
| `consultantHarveySoul.ts:21` | *"NO NAMED INTERNAL CAST. No Donna, no named partner."* |
| `consultantHarveySoul.ts:37` | *"you work alone… Overhead breeds compromise… Your isolation is not loneliness — it is armor."* |
| the law's own bytes | *"Whatever passes between you and Donna is the two of you at work behind the curtain."* |

Composed unconditionally, the law would **hand a named colleague to a man whose entire
soul is that he has none** — a soul contradiction shipped into a live room, on a file this
sitting was never chartered to touch. It also protects nothing there: no Donna, no estate,
no tools, so there is no machinery to narrate and no wire to delegate on. `loop.ts:369-371`
already carries the estate's own ruling that consult is standalone for exactly this reason.

**Why I chose to ship the narrower shape rather than hold for your word:** the two error
directions are not symmetric. Ship-unconditional-and-be-wrong puts a soul contradiction on
a live room; ship-excluded-and-be-wrong leaves consult exactly as it is today, which is the
state every M-4 walk ran against. I took the reversible error. **This is the only place the
delivery departs from the ruling as written, and it is asserted as a floor with its reason
attached at `b06_f0658_bench` §1.6 — not left as a silent gate, which is the very thing
F-06.58 was filed for.**

The cure's claim is therefore exact: **the law reaches every VENDOR-BUSINESS room,
category-independently.** Not "every room".

---

## 1. WHAT SHIPPED, BY FINDING

### F-06.58 — the planner-gated law, cured by re-home

`2b89b5c` honoured 「 move it 」 as *terminal in the file*, and the file's last export was
`PRODUCTION_WEAVE` — planner-gated at `loop.ts`. **A constant-boundary crossing wore a
position move's clothes and the law inherited a gate nobody ruled it under.**

Re-derived at my own clone, by command:

```
2b89b5c^   cluster TRUE  in HARVEY_SOUL · PRODUCTION_WEAVE  549
2b89b5c    cluster FALSE in HARVEY_SOUL · PRODUCTION_WEAVE 1060
soul 32227 → 31717  delta 510 = 509 + 1 space
weave  549 →  1060  delta 511 = 509 + 2 newlines
```

**Two precision corrections to the filing, both banked in-code:**

1. **`"behind the curtain"` is not a probe.** It greps TWICE at HEAD — `HARVEY_SOUL`'s
   one-mind paragraph carries *"what runs behind the curtain was never theirs to manage"*.
   A tree that lost the law entirely would be **acquitted** by that string. Named at
   `b06_f0658_bench` so nobody re-derives with it.
2. **The pre-`2b89b5c` position was mid-paragraph, not terminal, and not even a
   paragraph.** The cluster sat spliced inside `HARVEY_SOUL` paragraph 24 of 35, between
   *"It runs deeper than her name."* and *"And his records you speak of…"*. `2b89b5c` did
   not move a paragraph; it **extracted a mid-paragraph clause run and re-homed it**.

**Built as (C):** `NO_MACHINERY_LAW`, the file's last export, the 509 bytes byte-identical;
`PRODUCTION_WEAVE` returned to its pre-`2b89b5c` **549 bytes exactly**; `loop.ts` composes
SOUL → *(planner ?* roster *: '')* → **law** → *(advisor ?* LENS *: '')* → fieldBlock.

**The property that decided the shape, asserted at §1.5:** for a planner the concatenation
is **byte-identical to the pre-cure prefix**. Weave-then-law is exactly what `2b89b5c`
produced, so a planner account's cached static prefix does not move by one byte. **E-1 cost
stated honestly:** the 24 non-planner fields *do* each pay one prefix re-warm, once — the
law is +511 chars inside the cached prefix. That is the price of the cure, per account, not
per turn.

**(B) was refused on measurement, not argument.** Returning the cluster to `HARVEY_SOUL`'s
close displaces *"Every reply is simply the next line in an ongoing relationship."* — the
voice-run terminal CE-77's founder-vetoed revert restored. Measured on a scratch tree:
**five cells RED across two benches** (m4c §1.1/§2.3/§5.0, m4d §2.1/§4.0). No cure of
F-06.58 may be bought with CE-77's revert. `b06_f0658_bench` §3.3 asserts both terminal
properties still hold, so the shape cannot be quietly re-proposed.

### F-06.60 — the instrument cured, not the fixture (`b06_m4c` §1.1, count preserved)

The cell's own comment claimed it tested *"what the model READS, not where the template
happens to close."* **It did not.** Its normaliser split the whole file on `(?<=[.!?])\s+`
and stripped `` `; `` only at token-end — but a constant boundary's preceding characters are
`` `; ``, not `[.!?]`, so the split never fires there and the terminator **fuses with
everything after it**:

```
"…relationship.`;\n\n// ── TDW_04.5 P6 — THE PRODUCTION-MANAGER WEAVE…"
```

So a cell advertised as position-insensitive REDs on any change to **where a constant
closes**, with not one readable byte moved. Both candidate fork shapes tripped it and
neither had a content delta. F-06.55's class — the instrument convicting the innocent — one
layer deeper.

**Cured by construction, not by regex:** §1.1 now compares the **bodies of the exported
template literals** as a set. Comments, declarations and terminators are not prompt bytes
and are excluded because they are never read, rather than because a pattern hoped to catch
them. **Teeth proven, not assumed:** `b06_f0658_bench` §4 deletes a readable sentence from
`HARVEY_SOUL` and `b06_m4c` REDs — verified to red at **§1.1 specifically**, 19/20.

### F-06.57 — the two `b6_sitting2` cells (20 → 22)

Re-derived at my own tip against `git show <c>:donnaFind.ts`:

```
1d211ea GREEN GREEN   ·   5ea0153^ GREEN GREEN
5ea0153 RED   RED     ← first red, both cells   ·   HEAD RED RED
```

**Both dead predicates came from ONE sentence** at `5ea0153^:donnaFind.ts:412`. F-06.14's
own chartered reframe rewrote it whole ("records" → "binders", plus the recognition-not-
results framing the Sana specimen earned) and never amended the cells. Amended to the
**property**, against the living prose at `donnaFind.ts:515-521`.

### The scrub cells (`b0461_p6`, 24 → 25)

The wire assertion **follows the Donna-bearing string** to `NO_MACHINERY_LAW`
(`/Donna/` case-exact, the F-06.35 masking ruling carried). Left on the weave it would have
matched a string that can no longer fail it — F-06.54's own finding, one arc later. **New
cell:** `!/Donna/i.test(PRODUCTION_WEAVE)` raw — roster talk carries no law that must name
her, and `/i` is correct *there* because it judges source content, never scrub behaviour.
**The cap: 1200 stands, founder-confirmed 「 ok 」, annotated and not re-asked** — noting only
that the 511 chars it was raised for have left the constant. Re-tightening a signed cost
number unasked is the same discourtesy as raising one, in the other direction.

### F-06.59 — recorded, ZERO bytes toward it (per your §3)

`coupleSystemPrompt.js:18`'s returning-bride early return composes a 2062-char prompt with
**no HARD RULES block at all** — so no V-4 register rule, on the one plane the firewall
cannot reach. Rule 12's absence is deliberate-and-stated; rule 11's is not (the branch's own
comment reads *"unchanged, preserved"* — it predates M-4). Live at `engine.js:103`. No cell
has ever composed it: `b06_m4c` §2.4 builds `isReturningBride: false` only. **→ F-06.51's
chartered sitting. Untouched here.**

---

## 2. THE PROOF

### The new coverage bench — `b06_f0658_bench` 20/20

The compose expression is **LIFTED from `loop.ts` source, never retyped** — a retyped
expression tests the typist. If the expression changes shape the extraction REDs rather
than quietly testing a fossil (`b6_s1_bench`'s eval-the-real-body pattern, one layer up).

```
photography · decor · jewellery · makeup · catering   → law PRESENT  (was ABSENT)
planning                                              → law PRESENT  (unchanged)
planner prefix                                        → BYTE-IDENTICAL to pre-cure
consult                                               → excluded, reasoned (§0 above)
```

### Both-ways — five mutations, every one on a PRODUCTION file

```
RED ✓  loop.ts    law re-gated planner-only        → §1.1 (F-06.58 itself, reproduced)
RED ✓  harveySoul cluster deleted outright         → §1.1 (no room carries it)
RED ✓  harveySoul law re-seated inside the weave   → §3.1 (the disease's exact shape)
RED ✓  harveySoul soul close displaced             → b06_m4c §2.3, the EXISTING tooth firing
RED ✓  harveySoul a readable sentence deleted      → b06_m4c §1.1, F-06.60's teeth kept
all mutated files restored byte-identical (§4.0 asserts it on the bytes)
```

### Floor at the cured tree — full run, credentials supplied

**Moved by this ZIP, all labeled:** `b0461_p6` 24 → **25** · `b6_sitting2` 20 → **22** ·
**NEW `b06_f0658` 20**.
**Amended in place, count preserved:** `b06_m4c` **20** (§1.1 re-aimed).
**`b06_m4d` 16 — ZERO amendments, exactly as measured at the read-first. No surprise to
disclose.** **`b06_m4c` §2.3 UNTOUCHED**, stated as your §5.5 required.

**Byte-stable at CE-80's counts:** gauntlet `--rig-selftest` **122/122** · b06 m0 50 · m1 45
· m2 39 · m3 37 · m4 33 · m4b 24 · m4d 16 · arc m1 53 · m2 27 · m3 11 · m4 18 · m5 11 · m6 20
· f0550 31 · crons 48 · sendwa 55 · webhookcore 11 · otp_meta 24 · b0498 58 + punct 17 ·
meta_router 31 · transport 10 · m1b 4 · m2_vendor 2 · onboarding 27 · wa_door 32 ·
couple_soul 21 · f0532 9 · f0515 4 · f0516 4 · f059 10 · media_shim 14 · f0555 23 ·
b6_door_rider 15 · f79 19 · f80 24 · open_question 28 · referent 36 · rider 32 · s1 24 ·
s2 48 · floors 47 · witness 22 · b3_rider 20 · b5_describe 18 · bands 46 · crew_page 111 ·
collab 52 · collab_wiring 71 · owner_assignments 19 · money_loop 73 · assign 30 · crew 21 ·
crud_crew 19 · gap 10 · pinlogin 11 · checker 101 · b5c 47 · b5b 47 · advisor 16 ·
advisor_route 16 · donna_cache 16 · downgrade 12 · sonnet 13 · 0081 12 · fresh_thread 10 ·
pwa_flip_seam 9 · wa_words 19.

**KNOWN RED, carried loudly: `b06_meter_bench` 28/29** (F-06.41, pre-existing, homed to the
modelRouter micro — unchanged by this ZIP).

### CE-80's floor-method law — the benches this run did NOT silently skip

**Nothing was skipped.** `b6_door_rider_bench` throws at `db.js:11` for want of
`SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY`; the throw is module-scope only and dummy
values run it fully offline — **15/15, named here as CLOSED rather than skipped**, per your
predecessor's own closing of this gap. Every other bench ran with the same dummy env.
**Not run by design and declared:** the gauntlet's LIVE lanes (founder's keys, founder's
run) — only `--rig-selftest` is a desk artifact and it is the 122/122 above.

### Gates

`node --check` clean on all four `.js` files · engine **typecheck** and full **engine build**
(the D-10 engine limb) both exit 0, zero output, at the repo's **pinned tsc 5.9.3**. Declared
from the read-first and unchanged: this container's global tsc 6.0.3 errors on the repo's
`moduleResolution=node10` — a tooling mismatch, never repo code; **5.9.3 governs**, per your §5.4.

### W-1

`HARVEY_SOUL` is **0-line** (asserted at `b06_f0658` §2.5 against `68b7f28`). `donnaSoul`,
`advisorLens`, `consultantHarveySoul`, and all four bride/couple prompts are **0-line**. The
estate's one live-forward W-1 guard (`b05_arc_m4` §4.1) is pathspec-scoped to the four bride
files and is **not tripped** — green at 18/18. **The re-pin-with-attribution ritual therefore
fires ZERO times this sitting**, and that is the correct outcome, not an omission: the ritual
exists for soul *bytes* moving, and this sitting moved a constant boundary.

---

## 3. COPY INVENTORY — **ZERO**, and SHOWN not claimed

No user-facing string, no model-voiced string, not one authored byte. The 509 chars are the
founder's own ruled bytes travelling unedited. **Proven three ways in the bench, at the
delivered tree:** §2.1 the law equals the cluster as it stood at `2b89b5c`, length 509 ·
§2.2 the shortened soul paragraph plus the law **reconstructs the pre-`2b89b5c` paragraph
exactly** · §2.3 the law exists in the file exactly once.

**The veto slot, current vs proposed: IDENTICAL. Position and scope only.** One founder item
already banked and not re-asked: the 1200 cap 「 ok 」.

---

## 4. CORRECTIONS OWNED BY THIS SESSION

1. **I invented an attribution.** My read-first quoted CE-80 as saying "twenty-four of
   twenty-five fields." No such sentence exists in the log — chair-caught, grep-verified.
   The reconciling instinct was right and the citation was fabricated, which is the worse
   half. **The phantom phrase appears nowhere in this deliverable**, and the fact is stated
   the way it survives audit: the gate is `=== 'planning'` by string equality, so the
   exposure was every other category, whatever any ladder holds.
2. **My first wiring of `loop.ts` composed the law into the consult room and I wrote a
   comment calling it "inert, not wrong."** That was wrong on its face and I had already
   read the file that disproved it. Caught at self-review before any bench ran; the
   reasoning is now in the file and in §0 above.

---

## 5. OPEN, AND NOT THIS SITTING'S

- **F-06.59** — the returning-bride branch's missing V-4. → F-06.51's chartered sitting.
- **F-06.41** — `b06_meter_bench` 28/29, pre-existing. → the modelRouter micro.
- **The consult deviation (§0)** — ratify or reverse; one term either way.
- **The live behavioural witness. DECLARED, NOT CLAIMED.** This ZIP proves the law is now
  *composed* for every vendor-business room. It cannot prove the model obeys a law it can
  now read. **Every M-4 walk ran on the planner fixture, which is precisely why this was
  invisible** — so Evening One's card needs a **non-planner curtain probe**, and the
  category SELECT on the smoke card exists to tell you which of the three test accounts can
  serve as one.
