# repo: dream-os @ 5951efa
# R-36.5 RIDER — LE DELIVERY HANDOVER

Built at `5951efa` (the composite tip), fresh `git fetch -q origin`, tree clean before
the NAMED BASE. Sibling `dreamos-pwa` beside this repo at `a534329`, floor sibling-full.

**GUARD, discharged:** `origin/main` is `5951efa`. Nothing landed beyond my tip; this
relay seats one session and no second build for this scope exists at origin.

**RIDER STATUS: (2) and (3) BUILT. (1) IS HELD — the retirement premise is true of 3
cells out of 35, and executing it as written would delete the only instrument the
ratified F2(b) cure has.** §4 below, with counts.

**F2 fork WITHDRAWN**, and not merely on instruction — witnessed at `e97019d`:
`resolveAgentOrDegrade` keys on `meta.turns_cap === 0`, names 0115's lesson in-comment,
logs `[agent:resolve] RESOLVE FAILED` before anything else, rethrows for
pre-onboarding (F3 as ruled), and reuses the vetoed `WA_CAP_ZERO_LINE`. It is the
dial-scoped arm I proposed as a third path, shipped better: `NO_AGENT_USAGE_PROBE`
lets `buildMeta` run its cap read without a resolved agent. Both collisions I filed
are answered — collision 1 by keying on the dial rather than the tier word, collision
2 by the §6.4 repair in this ZIP.

---

## 1 · FILE TABLE

| file | state |
|---|---|
| `scripts/tdw10_combined_cap_bench.js` | MODIFIED — §6.4 re-founded |
| `scripts/floor-base.txt` | MODIFIED — one line removed, 21 → 20 |
| `scripts/floor-manifest-r365-rider.txt` | NEW — declared dirt |
| `docs/specs/TDW_R365_RIDER_HANDOVER.md` | NEW — this file |

No production bytes move. No migration. **Copy inventory: ZERO.**

---

## 2 · RIDER (2) — §6.4 RE-FOUNDED, F-15.12 CLASS CLEARED

**WAS:** `assert.strictEqual(nums[nums.length - 1], '0121', …)` — a pinned ADDRESS.
Every migration appended after 0121 reddened it, so by 0129 the bench had been red at
base for eight migrations. That is worse than noise: **the bench had gone red-blind.**
A real break in its cap cells would have hidden inside an expected red and the floor
would have reported *no delta* — LESSON 3's shape at bench granularity, and half the
reason I held F2 last seat.

**NOW:** LD-8's actual property is append-only — **the tip is SUPPOSED to move.** What
must never happen is a hole below it being filled. So the tip is not asserted at all,
and the cell's weight sits on the two invariants that carry it.

**TWO ASSERTS WERE WRITTEN AND BOTH DELETED AS UNREACHABLE — disclosed because they
would have shipped as coverage that could not fail:**
- `nums.every(4-digit)` — the `readdir` filter already admits only `^\d{4}_`, so the
  3-digit mutation never reaches the array. Proven: M-C did not red it.
- `tip >= '0116'` — `nums.includes('0116')` four lines up already implies it. Proven:
  M-D reddened the *includes* assert, never this one.

Contiguity was considered and refused: the ladder carries **seventeen** holes today
(0024, 0026, 0027, 0029, 0037, 0038, 0058, 0068, 0079, 0089, 0091–0095, 0097, 0113 —
derived at this tip, not recalled). Enumerating them re-pins addresses one level up.
Only 0113 is named, because only 0113 is reserved by ruling.

**NON-VACUITY, mutations on the real ladder:**

| # | mutation | §6.4 |
|---|---|---|
| M-A | `touch db/migrations/0113_fake_fill.sql` | **RED** |
| M-B | second `0128_*` file added (duplicate ordinal) | **RED** |
| M-C | 3-digit `013_short.sql` | green — assert unreachable, **deleted** |
| M-D | ladder above 0116 removed | RED, but on the *includes* assert — tip assert unreachable, **deleted** |

`tdw10_combined_cap_bench`: **37 passed, 0 failed.**

---

## 3 · RIDER (3) — FLOOR

Instrument: this container. `npm ci` and `npm run build:engine` both clean; **no chair-clone
substitution claimed.**

- **NAMED BASE at `5951efa`**, clean tree: 21 in base, **`b05_f0583_race_bench` a live
  DELTA** — red and never entered in base by the first seat.
- **AFTER**, `--delivery scripts/floor-manifest-r365-rider.txt --check`:
  `[F-14.16] declared files unmoved — set and contents both verified.`
  `tdw10_combined_cap_bench` **cleared from the red set**; `floor-base.txt` 21 → 20.
  **The one remaining delta is `b05_f0583_race_bench` — rider (1), held.**
- Count movement: **22 actual reds → 21**, one name removed (`tdw10`), none added.

---

## 4 · ⚠ RIDER (1) IS HELD — THE PREMISE IS TRUE OF 3 CELLS OF 35

The relay's ground for deletion: *"its mutation anchors grip erased source."* **Verified
by running it, and it is true — of three cells.** `b05_f0583_race_bench` is **32/35
GREEN**, and the three reds are exactly the F1-implementation arms whose anchors my
5951efa replaced:

- `4.2 loser re-read severed` · `4.3 winner-only guard dropped` · `5.8 winner-only guard dropped`

**The other thirty-two are live, and fourteen of them are the only proof F2(b) has:**

| section | cells | subject | status |
|---|---|---|---|
| §2 | 7 | 0129 / the arbiter | green |
| §3 | 1 | ladder | green |
| §4 | 4 | agentBridge | 2 red (dead anchors) |
| §5 | 9 | createOwner | 1 red (dead anchor) |
| **§6** | **11** | **F2(b) degrade — drives the exported `resolveAgentOrDegrade`** | **green** |
| **§7** | **3** | **F2(b) severance arms (rethrow, loud-log, dial predicate)** | **green** |

**My bench carries ZERO F2 cells.** I held F2 last seat and built nothing for it, so
"your bench is the law's instrument now" is true for F1 and false for F2(b). Deleting
this file as written retires a **ratified** cure's entire instrument — including §7.3,
which proves that loosening the dial predicate makes a paying vendor hear a false
refusal. That is the exact failure the binding clause exists to prevent.

`e97019d` exported `resolveAgentOrDegrade` and `NO_AGENT_USAGE_PROBE` specifically so
the bench drives the shipped seam rather than a copy (R-29.34). Those exports have no
other reader.

**FOR THE CHAIR — three arms, evidence attached:**
- **(a)** delete as ruled, and accept F2(b) ships uninstrumented;
- **(b)** surgical: drop only §4.2 / §4.3 / §5.8, keep the file, re-found its §4/§5
  anchors on my implementation — the manifest file goes either way;
- **(c)** port §6 + §7 into `b05_r365_agent_race_bench.js` and then delete, making my
  bench genuinely the one instrument the relay assumes it already is.

**My lean is (c)**, because it ends with one home rather than two overlapping ones —
but it is a build, not a deletion, and it needs a seating. I did not take it unasked.

---

## 5 · ⚠ LIVE-ESTATE FINDING — TWO SEATS NAMED THE INDEX DIFFERENTLY

Not in the rider; found while discharging the guard, and it touches production.

- `e97019d`'s 0129 creates **`agents_user_id_key`**
- my 0129, which is the file at tip, creates **`agents_user_id_unique`**

You ran mine, and BLOCK 1 returned `agents_user_id_unique … UNIQUE`. **But BLOCK 1
filtered on `indexname = 'agents_user_id_unique'`, so it could not have shown a
sibling.** If the first seat's block was ever pasted, `engine.agents` now carries two
unique indexes on the same column — harmless to correctness (the `ON CONFLICT
(user_id)` inference resolves against either) but a redundant index, and the ladder
file at tip would then misdescribe the estate.

**That was a weakness in my own verify block: I scoped it to the name I expected.**
One unfiltered SELECT settles it:

```sql
select indexname, indexdef
  from pg_indexes
 where schemaname = 'engine' and tablename = 'agents'
 order by indexname;
```

Expect **one** unique index on `(user_id)`. If two appear, drop `agents_user_id_key`
— it is the superseded seat's name and no file at tip creates it.
