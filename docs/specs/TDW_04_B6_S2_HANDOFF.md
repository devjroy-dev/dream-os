# TDW_04 — SURFACES S2 → RIDER HANDOFF (the migration sitting's record)

**CE-22:** dream-os `dcad413` = `origin/main`, fetched and re-derived at sitting open → **ZIP E delivered NOT yet applied** · dreamos-pwa `3a92fd0` = `origin/main`, same derivation → **ZIP F delivered NOT yet applied** — no deploy-green claimed on either; proofs run after the founder applies both. **Apply order: ZIP E first, ZIP F second, then 0078 in the Supabase editor BEFORE any slot toggle is touched** — the new create door for slot blocks writes rows the OLD unique index refuses; the code degrades honestly against the old index (its 23505 reads as 'Already blocked.'), but the feature is 0078's, so the migration gates the smoke, not the deploy.

**Read beside:** `TDW_04_B6_KICKOFF.md` · the B6 sitting handoffs · the surfaces paper · `TDW_04_B6_S1_HANDOFF.md` · `TDW_04_B6_S1_READER_CENSUS.md` · FINDINGS_LOG's "SURFACES S2" sections (the sitting-open rulings R-B6-25..31 verbatim + the delivery record + the veto set + F-04.75).

---

## 0. WHAT THIS SITTING DID (charter order; full detail in the log)
1. **Sitting open recorded** — R-B6-25..31 verbatim · the S1 veto set DISCHARGED (founder: fine as is) · F-04.73 CLOSED at the CE's desk (honest 400 chain; no cousin filed; `kind is required.` joins the veto list; the toast-legibility line added to this sitting's browser pass — executed, see F-04.75).
2. **`0078_slot_blocks.sql`** — R-B6-17's shape + R-B6-26's DROP inside + the old two-column unique retired (create-before-drop; revert one-liners in-file; run-time pre-flight asserts). THE SITTING'S ONE FOUNDER-RUN MIGRATION.
3. **The write-path law** at `eventWrite::findExistingBlock` (enforcement-site proposal WITH evidence in the log — one home, every block writer routes through it; cross-slot race disclosed, gist-EXCLUDE alternative proposed not taken) + mirrored-CHECK sentences.
4. **The slot-aware checker** (blockedCheck via rowHolds — capacityCheck's own predicate; full_day byte-preserved, 101/101 co-witness; partial refusals name their slot) + `describeDate`'s additive `blocked_slots` (a partial block is never a blocked day nor a free one) + the ONE disclosed bench amendment (b5's whole-shape line, R-B6-15 convention).
5. **R-B6-25 executed:** the wire filter (both queries, tell stays honest) + `refuseBlockedRow` at cancel + generic PATCH (state-update travels the PATCH — evidence in the log), 404-shaped like LOCK 2, naming the block machinery. DELETE named-not-guarded, the CE's to widen.
6. **Item 4 whole:** `GET /vendor/day/:vendorId/:date` (spine fails hard, decorations fail soft, repeat_every rendered-never-expanded — declared gap) + `CalendarDaySheet` (P5 entire, incl. **Move's inline verdict: the 409 body's sentence verbatim + holders**; advisory-over-landed-write prints as heads-up over success).
7. **Item 6 completed + R-B6-28:** ESSENTIAL = title + event_date + kind (Kind visible, required, defaultless; Date always renders — interpretation disclosed) + the Block offer (in-page callback on the calendar; `?block=` from the list, window.location not useSearchParams).
8. **F-04.75 filed + one-line-cured ratify-or-revert:** the porcelain error-toast text was 1.4–1.5:1 against its own dark-red background (theme-dependent var); literal cream cures it to 11.5–13.1:1 computed; dark theme byte-identical. The screenshot rides the founder smoke (the executor has no browser; the arithmetic is in the log).

## 1. OPEN AT BANKING
1. **🔴 THE FOUNDER RUN** — apply E → F → run 0078 (verify block in-file) → the smoke card (delivered with the ZIPs) incl. **the cancelled-block count read** (R-B6-25's ruled read; repair ruled if any exist) + **T19 unmoved-unless-a-fixture-moved-it** + **the toast screenshot** (F-04.75's witness).
2. **🔴 RATIFY-OR-REVERT set:** the enforcement-site proposal · the DELETE-door exception · the day-tap routing (popup → day sheet) · the ESSENTIAL Date-always-renders interpretation · the b5 bench amendment · F-04.75's one-liner · the partial-hold grid treatment (ring undimmed, interim until pips) · the veto-on-sight copy set (log, delivery section).
3. **🔴 R-B6-29's rider is NEXT, before the block handover:** F-04.72's disambiguation hold (shape (a); shape (b)'s cross-plane collision as the named first test case), its OWN engine-gated ZIP after this seal. **The block handover writes AFTER that rider** — not this sitting's, not the founder-smoke's.
4. **🟡 F-04.69 + R-B6-12's single-home move** — no S2 file opened `leads.js` (said as ruled); still first-in-line for the first ZIP that does.
5. **🟡 Proposed, not taken:** the gist-EXCLUDE constraint closing the cross-slot block race at the DB (btree_gist cost) · widening the DELETE-door guard · the pips rider speaking `blocked_slots` (the field waits with its consumer).
6. **🟢 R-B6-27 executed** — the C4 correction note is in the calendar spec; the paper's table stands retracted by name.

## 2. EXECUTOR DISCLOSURE
In the log's delivery section, items (a)–(h) — including the two SQL-comment-in-JS slips caught mechanically before any gate. Tool-verified work held throughout; nothing ran against production; no banner claimed unseen.

---

**Next session opens on: the founder's apply + 0078 + smoke (this delivery's card) → the CE's ratify-or-revert pass on §1.2 → the F-04.72 rider ZIP (R-B6-29) → the block handover (R-B6-22 as amended).**
