# TDW_04 — B6 SITTING 2 → NEXT HANDOFF

**CE-22:** dream-os `7e6983c` = `origin/main`, fetched and re-derived at the moment of writing (F-04.67's cure, kept) → **this ZIP (ZIP B), delivered NOT yet applied** — no deploy-green claimed; proofs run after the founder applies · dreamos-pwa `552646d` — **READ-ONLY this sitting** (`page.tsx` read WHOLE, 552 lines — §0 item 7's owed read discharged; **zero PWA files edited**, R-B6-2's law holding until the surfaces ruling).

**Read this beside `TDW_04_B6_KICKOFF.md` and the sitting-1 handoff. Priority order R-B6-8: sitting 1 discharged (1) + the smoke card; this sitting discharged (2) the ladder, (3) the surfaces paper (authored, with the CE), (5) Q-B4-6(b)'s ZIP — and staged (4) T19 for the founder's run.**

---

## 0. WHAT THIS SITTING DID

1. **RULINGS RECORDED AT SITTING OPEN** (R-B6-10..15, verbatim in FINDINGS_LOG's sitting-2 section): ZIP A's five deviations RATIFIED · **F-04.42 / F-04.44 / T12 CLOSED in place** citing the smoke sitting, T12's card wording ratified into kickoff §3 step 4 · the divergence line ruled into this ZIP · the donna_find word-sweep ruled in (dump-size → 06, recorded not acted) · the lead-key diagnosis ruled (no cure before it) · **the edit/cancel veto answered: YES, recorded verbatim.**
2. **§0 ITEM 7'S LADDER — FINISHED.** Census whole (T19's oracle SQL and the /4-vs-/1 dispute read at source) · PUBLIC_SCHEMA (vendors 37 cols incl. `slot_capacity` at ordinal 45; events 16 cols; the constraints addendum — the 13-kind CHECK, the slot/state CHECKs, `0075`'s unique partial index read WITH its predicate, and the redundant-looking `events_vendor_date_blocked_idx` beside it) · ENGINE_SCHEMA whole · the consolidated spec + audit findings whole · the calendar spec whole, P4/P5 twice · `eventWrite.js` whole · both doors whole · **`page.tsx` whole-file** (the day-tap branch :296, `onAdd` :141, edit-only AddSheet mount :542 — the founder's testimony verified in code). Q-B5-4's "finish" ruling, undischarged for two sittings, is discharged.
3. **THE SURFACES PAPER — AUTHORED, WITH THE CE** (`docs/specs/TDW_04_B6_SURFACES_PAPER.md`). Eight items, each census-read at HEAD: `0078` per-slot blocking (build sitting's, with a full_day ruling needed) · the capacity row (smallest item) · the horizon contract (the regression-prone one) · the day sheet + inline Move verdict (rides the 409 body whole, zero-line backend diff; force affordance stays a computed-boolean PROPOSAL) · the pill default **verified already-cured and struck** · AddSheet's Block offer + the +Booking hedge for the founder's priced gap · the 57-reader census with the ratio re-derived at the build sitting (and R-B6-7's index question gaining its free evidence there) · **a two-sitting split, the migration as the seam.** No PWA file edited; the paper is the ask.
4. **ZIP B — Q-B4-6(b) + the three ruled riders.** The composed-reply save (F-04.41's cure: `memory.ts::saveMessage` returns the witnessed row id — disclosed signature widening; `TurnResult.assistant_message_id`; `chat.js`'s `composedTail` + `persistComposedReply`, both routes, exact-row targeting, never a guess) · R-B6-15's softened edit/cancel strings (veto YES) · R-B6-13's word-sweep in `donnaFind.ts` (exact new string on the record for veto-on-sight) · R-B6-12's divergence line (`b6_sitting2_bench` §3). Gates: full engine suite (tsc · build · smoke · tombstone 15/15) · `node --check` clean · **101/101 · 20/20 · 18/18 · 36/36 · 22/22** · regression-proofed both ways · `eventWrite`/`scrub`/`calendarSignals`/`occupancy` 0-line diff. One disclosed bench amendment: `b6_referent_bench`'s "exactly two" clause count → four, under R-B6-15, ruling named in the label.
5. **F-04.68 FILED (R-B6-14 discharged: diagnosed, confirmed, NOT cured).** The ruled suspect pair (`patchLeadSnapshot` vs `leadItem`) is **both clean** — the item-writer census found a **fourth** writer: `harvest.js:118 leadSnapshotItem`, missing ST-3b's `name`/`phone_key`, reaching `patchNote` at :201, which replaces items **wholesale**. Fresh-lead key loss is harvest contact, not age. `phone_key` has no render fallback — the strong fusion key dies silently per touch. Cure proposed (mirror the keys, one line + one require) with the structural sibling named (four homes for one item shape — F-04.36's family, same as R-B6-12's list); **the cure is one ruling away.** One founder-run read settles the specimen attribution: `harvest_patch` rows naming the three lead ids in the smoke window.

## 1. OPEN AT BANKING

1. **🔴 THE SURFACES RULING** — the paper is with the CE; the build sitting(s) open on it. **No PWA file is edited before it.** The paper's own "does not decide" list is the ruling's agenda.
2. **🔴 T19 — THE BLOCK-CLOSE ORACLE, FOUNDER-RUN, GREEN BANNER, FULL HEADER + PASTED ROWS.** The SQL is the census doc's own L-8 block (`docs/TDW_03_CROSSPLANE_CENSUS.md`, "THE ORACLE — L-8's STANDING LINE"), re-issued to the founder with this delivery. **The triple is never carried** — the charter's `/4`, the census's earlier `/1`: the `on_calendar` leg is date-window-dependent (`event_date >= today`) and the smoke sitting created/cancelled fixtures (Rhea's shoot cancelled; the Nisha recce `80b66d4e` may stand or be cleaned) — **the run settles it and the discrepancy is EXPLAINED or FILED, never averaged.** The census doc and the masterplan row update to whatever the run says, in the delivery that carries the result. *A green oracle is not a clean estate* — the sentence rides the entry verbatim, per R-B6-6.
3. **🟡 F-04.68's cure** — one ruling away (this sitting's filing carries the proposed shape + the structural question). The stored key-less residue heals on the next keyed patch per the census's own precedent; no re-normalization proposed.
4. **🟡 R-B6-12's single-home move** — a named candidate for whichever future ZIP touches `leads.js` or the snapshot. The divergence line guards the estate meanwhile.
5. **🟢 Block close (R-B6-8), scored:** R-B6-1 founder-witnessed live ✅ · smoke card's three inherited items **CLOSED** ✅ · Q-B4-6(b) shipped ✅ (founder-witness owed post-apply: a refused turn, then a refresh — the honest sentence must SURVIVE it; that is F-04.41's live obituary) · the surfaces paper **authored** ✅ / **ruled** ⬜ · T19 re-derived green ⬜ · block handover ⬜.

## 2. FOUNDER SMOKE FOR THIS ZIP (plain steps; the executor reads the evidence)

1. Apply the ZIP, push, wait for Railway green.
2. In the vendor chat, ask Victor to move a booking onto a date you have blocked (or onto a full slot). Read his refusal in the reply.
3. **Refresh the page.** The refusal sentence must STILL BE THERE in the scrollback — before this ZIP it evaporated and only the optimistic prose survived. Paste what the scrollback shows.
4. (Optional, one read) For F-04.68's specimen attribution: the `vendor_activity_log` rows with `action='harvest_patch'` in the smoke window — the executor reads them against the three lead ids.

## 3. EXECUTOR DISCLOSURE (sitting 2)

Tool-verified work held throughout: every code claim in the paper and the filing was read at `7e6983c`/`552646d` by command; the benches ran, both ways. Authored items disclosed rather than discovered: **(a)** the `b6_referent_bench` count amendment shipped ratify-or-revert with the ruling named in the assertion label — stated here BEFORE ratification, per the F-04.61-correction lesson; **(b)** `composedTail` recomputes pure builders rather than restructuring the routes — a disclosed redundancy chosen so the wire paths carry a 0-line diff; **(c)** R-B6-14's ruled suspect pair was one suspect short and the filing says so — the census habit (enumerate the writers, never trust the named pair) is what found the fourth.

---

**Next sitting opens on: the surfaces ruling (build S1 per the paper's split, if ruled) · T19's pasted result recorded into census + masterplan · the block handover if R-B6-8's conditions have all landed.**
