# TDW_04 — THE SURFACES PAPER (B6 sitting 2, 2026-07-17) — **PAPER ONLY. RULED BEFORE BUILT.**

**CE-22:** dream-os `7e6983c` = `origin/main`, fetched and re-derived at the moment of writing · dreamos-pwa `552646d` — **READ-ONLY** (`page.tsx` read WHOLE, 552 lines, this sitting; **zero PWA files edited**, R-B6-2's law) · Railway green: not claimed — this file is paper.

**Charter:** R-B6-2 — Q-B5-4's ladder (§0 item 7) finished before this was authored, and the ladder's reads are this paper's evidence base: the census whole · `PUBLIC_SCHEMA.md` (vendors + events + the constraints addendum) · `ENGINE_SCHEMA.md` whole · `TDW_04_LEDGER_AND_CALENDAR_FINAL.md` whole · `TDW_04_AUDIT_FINDINGS.md` whole · the calendar spec whole (P4/P5 twice) · `eventWrite.js` whole · both doors whole · `page.tsx` whole-file. Subset-proposal pattern: **paper → CE ruling → build.** Every line-number below was read at the heads named, not carried from a charter.

**The founder's priced testimony, carried verbatim from the smoke sitting (banked per the handoff):** *there is no CRUD way to put a booking on the calendar page.* Verified against `page.tsx` at `552646d`: the + FAB and the day-popup's + both route to **chat** with a primer (`onAdd`, :141–146); the AddSheet mounts **edit-only** (:542–549 — `addOpen` is set only from a card's Edit button, `existing=editRow`); day-tap on an empty or blocked date opens the **Block sheet** (:296–298, by design). **Priced live in one sitting:** two chat-create attempts failed on the model's disposition; the only real event came through the LIST page's AddSheet. When the model's disposition fails, the vendor has **no mechanical path to his own calendar from the calendar.** Not a regression — a gap the P5 spec always intended to fill; today it has a price tag.

---

## THE EIGHT ITEMS (each with its ground truth, its dependency, and its cost)

### 1. Per-slot blocking — `0078` (F-04.63's STOP)
`0075`'s UNIQUE partial index is one block per **DATE** (`(vendor_id, event_date) WHERE kind='blocked' AND deleted_at IS NULL` — constraints addendum §3, read with its predicate per the addendum's own warning). P5's ratified day-sheet actions say `Block morning/noon/evening/day` — **four toggles against an index that permits one.** No door writes a per-slot block today; `events.slot` exists and its CHECK admits the four values, so the COLUMN is ready and the INDEX is the stop. `0078`'s shape (proposed for the ruling, not drafted as DDL here): the unique predicate gains slot — one live block per `(vendor_id, event_date, slot)`, with `full_day` semantics ruled explicitly (does a `full_day` block coexist with a slot block? the checker's `blocked consumes all capacity of its slot(s)` rule suggests full_day excludes the other three — **that is a ruling, not a migration comment**). **Belongs to the surfaces BUILD sitting per R-B6-2's own words — one founder-run migration per sitting, and F-04.63's entry says it is not chartered at B6.** Blocks item 4's slot toggles; blocks nothing else.

### 2. The capacity settings row (F-04.64's cure, first half)
`vendors.slot_capacity` exists (PUBLIC_SCHEMA, ordinal 45, `0076`), `capacityCheck` reads it (`??` not `||` — 0 is a lawful posture), and **nothing writes it, nothing renders it**: `me.js`'s PATCH allowlist is twelve entries, zero `slot_capacity` (B-7, re-verified at HEAD); `settings/page.tsx` has no capacity row. The cure is exactly P3's sentence: the allowlist gains one entry (smallest change, B-7's confirmed-viable path) + a "Working capacity" stepper, function artists only, Jost labels, NULL = category default (the ruled table: photography 1 · makeup 1 · decor 1 · venue 1 · florist 3 · other 1). **The second half — the force affordance — stays a PROPOSAL** and is deliberately NOT in this paper's ship list: if chartered, the door exposes a checker-computed boolean, never the rule (`isOverridable` never rides the wire, Q-C-3). Depends on nothing; smallest item on the list.

### 3. The horizon contract (Q-B3-12; F-04.47's real cure)
`DEFAULT_WINDOW_DAYS=400` is a ruled **interim** ("DO NOT treat this number as the design") and `HARD_CAP .limit(200)` **silently truncates** — no count, no `has_more`; at 400 days a busy studio exceeds it and vanishes without a tell. The client (`lib/vendor/api/vendor.ts:241`) sends no `from/to` ever, and `page.tsx`'s month-nav moves React state without re-fetching (verified whole-file: `useEventsData(vendorId)` is called once; ‹/› only set `year/month`). **The contract:** the PWA sends `from/to` for the visible month ± a buffer; month-nav re-fetches; the GET returns `total` + a truncation tell (it already computes a `countQuery` — the wire carries it or the cap becomes honest). Backend + frontend in one motion or the interim stays the design by inertia. **This is the item that touches the most existing behaviour — it is the one to smoke hardest.**

### 4. The day sheet (P5's centrepiece) with the inline conflict verdict on Move
P5 verbatim: bookings by slot with binder chips · followup projection (C7, read-time) · muhurat note · milestones due (`payment_schedules`, B-9 confirmed) · actions. The Move action's inline verdict **rides the 409 body whole** — `conflictOr400` already returns `{ok:false, error: conflict.message, conflict}` at 409, and `_base.ts`'s `handleResponse` never throws on non-2xx, so the sheet renders `conflict.message` + `holding` with a **zero-line backend diff**. A force affordance, if proposed, exposes a checker-computed boolean, never the rule (the wire adds `overridable: boolean` computed door-side from `isOverridable(conflict)` — the client never learns `kind !== 'date_blocked'`). The Block slot-toggles inside the sheet are **gated on item 1's ruling** — until `0078`, the sheet ships `Block day` only, honestly. Needs the new `GET /day/:vendorId/:date` endpoint (one round trip, spec P5's own shape). **Biggest item; the platform thesis in one surface.**

### 5. The `reason:'Blocked'` pill default — ALREADY CURED, verified, struck from the ship list
F-04.35's cure shipped at the B1 seal rider (`552646d`'s own commit message is the record): the Block sheet's default pill is 'Blocked', the picker opens with no pill highlighted (intended; incidentally the "no default" UX). **Named here so the build sitting does not re-do it** — the charter's eight-item list predates the verification. What REMAINS in this family: nothing on the block path; the item collapses to a recorded nothing (B-2's precedent).

### 6. AddSheet's Block offer (F-04.37's CRUD-door class) + the calendar page's `+ Booking`
Two halves, one testimony. (a) The founder's priced gap: the calendar page gets a mechanical create — the day sheet's `+ Booking` (AddSheet draft-first, per 03's law) covers it **if item 4 ships**; if item 4 splits to a later sitting, the interim is the day-popup's + opening the AddSheet in create mode with the date prefilled (a ~5-line page.tsx change against the sheet already mounted) — **proposed as the cheap hedge, CE to pick.** (b) The AddSheet Block offer: the events-slice AddSheet must not silently mint `kind='blocked'` (`ALLOWED_KINDS` is 12 and excludes it, door policy ratified) — the sheet offers "Block this day instead →" routing to the block flow, so a vendor typing "blocked" as a title gets the real mechanism, not a fake block. F-04.37's class: teach the right door, don't coerce.

### 7. The 57-reader census, with the blocked-majority ratio re-derived
The charter's 5:3 ratio is **NOT carried** (R-B6-2's own instruction: re-derived from the census file, not the charter). The count of `public.events` readers is dreamos-pwa-side and **the read is the build sitting's opening act** (this paper's clone read `page.tsx` only, under the ladder's clause; a whole-tree reader census is a code read of ~57 sites and belongs beside the fixes it feeds). What this paper CAN state from the reads done: two readers are already ruled (`nextThree` excludes blocks — F-04.36, shipped; the grid's `blockMap` renders them — by design); the census's job is the remaining ~55, each classified **sees-blocks-correctly / leaks-blocks / blind-by-design**, fixes ruled before touched. **`events_vendor_date_blocked_idx` (R-B6-7's "someone should look") gets its free evidence here**: the census walks every read path, so whether anything uses the non-unique index (redundant beside its unique sibling, which covers the same key with a tighter predicate) falls out — DDL still not proposed; the finding gains its evidence, as R-B6-7 predicted.

### 8. What ships vs. hands forward — THE SPLIT PROPOSAL
**Two sittings, and the seam is the migration.**
- **Sitting S1 (no migration):** item 2 (capacity row — smallest, self-contained) · item 3 (horizon contract — the most-regression-prone item gets the freshest session) · item 6(a)'s hedge if ruled (the +Booking interim) · item 7's reader census (opening act, findings ruled not fixed unless trivially in scope) · item 5's strike recorded. Gates: tsc against a cleared build cache (protocol §6's deletion clause is moot but the cache rule stands), executor browser pass, founder phone smoke on the horizon (November shoots visible without `from/to` gymnastics — the F-04.47 specimen reversed).
- **Sitting S2 (the migration sitting):** `0078` founder-run (item 1, with the full_day ruling in its header) · item 4 whole (day endpoint + sheet + Move verdict + slot toggles) · item 6 completed on the sheet's `+ Booking`. Gates: the amended founder smoke (spec §5, the block demo teaching `date_blocked`'s vocabulary) + T19's oracle unmoved unless a fixture moved it.
- **Hands forward past both:** the force affordance (F-04.64's second half, PROPOSAL) · heat-grid pips + agenda rail (P5's remaining chrome — real but not load-bearing; proposed as S2 stretch or a polish rider) · B7/B8 (imports + Snap-a-Date, `0095`, 04's parked tail, chartered separately per R-B6-8).

**One sitting instead of two is NOT proposed:** item 3 and item 4 each carry the block's worst regression class (a silently-truncated calendar; a new endpoint feeding a new surface), and the estate's own record says one phase per sitting for exactly this shape.

---

## WHAT THIS PAPER DOES NOT DECIDE (the CE's, listed so silence is not consent)
`0078`'s full_day-vs-slot semantics · whether the +Booking hedge ships in S1 or waits for the sheet · whether the force affordance is chartered at all this block or parks with F-04.64 · the census's fix budget (ruled per-finding or batch-ruled) · whether heat-grid/agenda-rail ride S2 or a rider.

**No PWA file is edited before the ruling. This file is the ask.**
