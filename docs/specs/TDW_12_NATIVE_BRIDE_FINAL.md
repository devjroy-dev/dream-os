# TDW_12_NATIVE_BRIDE_FINAL — The Bride App: Second Target, Same Soul
**Block:** 12 (built AFTER 13–18 per the founder's sequencing — numbered by its original reservation) · **Repo:** `dreamos-pwa` workspace, second target `native/bride/` (F-6: two listings, one codebase) · dream-os untouched (all riders landed in 11)
**Gates:** TDW_13 complete (extraction + `frost`/`dark` tokens + choreography recordings) — SATISFIED on 13's close · consumes 14–18's contracts as shipped
**Author:** Chief Engineer session, 2026-07-14 · **Doctrine:** TDW_BUILD_PROTOCOL.md + TDW_11's permanent guardrail govern

---

## 0. READ FIRST
| Source | Verifying |
|---|---|
| `native/vendor/` (11) | The foundation being reused: workspace/Metro config, canon-in-RN, session/secure-store, stream reader, push, purchases (RevenueCat), splash machinery, guardrail CI gate — REUSE, never re-scaffold |
| `components/frost/blooms/*` + FROST_BLOOMS.md + the 13 choreography recordings | The screens (1:1) and the motion reference the canvas is re-authored against |
| `lib/design/tokens.ts` — `frost` + `dark` sets | Both bride themes, already semantic |
| `lib/frost-api/*` (+ any browser seams) | The shared client; seams-not-forks law from 11 §3 |
| 14 (coplanner web constitution, visibility resolver) · 15 (inbox kinds, envelopes, masthead, Meridian SSE) · 16 (signals consent, proposals) · 17 (chapters, salons, gestures, sounds/haptics, Full Table, Marriage Day) · 18 (Trousseau room, the guest frame) | The feature contracts this app renders |
| couple auth routes + bearer path (11 RIDER-1, landed) | Native session |
| Apple/Play current + RevenueCat one-time products | The Full Table's dual-rail (the bride app's ONLY purchase) |

## 1. LOCKED FOUNDER DECISIONS
| # | Ruling |
|---|---|
| BN-1 | Second target in the same workspace (F-6); its OWN store listing (bridal identity: screenshots, description, reviews all bride-world) |
| BN-2 | Cold-open = the admin-curated BRIDE collection with Ken Burns (the audience-tagged system; same choreography law as 11 N-2) |
| BN-3 | Both themes ship (`frost` default, `dark`); the accent law per set |
| BN-4 | Push is a launch feature (11 N-12 precedent) — the 15 inbox kinds + 16 proposals ring the lock screen |
| BN-5 | **Dual-rail Full Table:** iOS = RevenueCat one-time IAP; Android = Play via RC; web = Razorpay — one entitlement flip (`couples.full_table`), three feeders, the 11 money laws verbatim (parity-and-absorb, anti-steering string audit) |
| BN-6 | Constitutionally web surfaces LINK OUT, never re-implement: coplanner (the member key), board-share pages, the Story, crew-analog none, the callmeZ handoff (18) |
| BN-7 | The canvas choreography is re-authored in Reanimated **against the 13 recordings as the spec** — the one sanctioned motion re-authoring in the whole program; founder approves the side-by-side before P2 closes |
| BN-8 | The permanent login-crash guardrail + CI gate apply verbatim (shared gate with the vendor target) |

## 2. PROPOSED — AWAITING FOUNDER RULING
(none)

## 3. MIGRATIONS
None. The app renders existing contracts; push reuses the platform-aware `push_subscriptions` (0083 amended in 11) with the couple XOR from 0089.

---

## PHASE TABLE (one phase per sitting)

### P1 — Target, auth, splash, themes
`native/bride/` target stood up on the 11 foundation (shared packages, Metro config extended, EAS profiles for the second listing; a web deploy + both EAS builds must all stay green — the tri-build gate). Bride OTP → PIN against couple auth (bearer via the landed rider); secure-store session. **Splash (BN-2):** the shared AnimatedSplash with the bride collection (audience tag), cached-offline, tap-skip, guardrail-safe overlay. Both theme sets live; her `couples.theme` is server truth. expo-router structure mirroring the frost IA: the canvas home + rooms — NOT the vendor five-door tab bar; the bride app keeps the canvas identity.

### P2 — The canvas + the blooms (BN-7)
The conductor rebuilt in RN: bloom registry, open/close in Reanimated authored against the 13 recordings frame-by-frame (easings/timings matched by eye and stopwatch; the founder's side-by-side approval gates the phase). The twelve blooms as RN screens off their extracted twins — presentational ports over the same frost-api contracts (a fetch outside the shared client is a failed session, per the 11 law). Gesture handoffs (canvas pan ↔ bloom scroll) via Gesture Handler, conflict-tested.

### P3 — The journey renders
The Dream masthead with the 17 chapter engine (Cormorant 44 count, chapter voice, milestone cards with save-to-photos) · envelopes with fill hairlines + drag-to-file (GH) · bookings with the milestone mirror + record-payment sheet · receipts via camera (expo-image-picker → the Vision classify path) · pages/moments (photo attach, the gallery) · tasks with the assignee picker · Marriage Day Mode (the veil, silenced notifications — expo-notifications channel respect) · the After (gallery masthead, thank-you prompts, the Story + Stylist doorway LINKING OUT per BN-6).

### P4 — Discover theatre + the chats
Salons (hero wings, curated rail, the feed), Chosen-for rail, availability whispers · the signature **double-tap bloom** (GH double-tap + expo-haptics + the muse chime via expo-av — the two-asset sound law travels) · long-press quick-look · the compare tray + ask-the-circle (spawns the 14 poll; voting renders in threads read-surface — coplanner itself stays web, the bride's own circle bloom is native) · **DreamAi chat** (stream reader shared with 11's Hub; voice hold-to-talk via the Sarvam endpoint; draft cards/undo grammar where the bride wire carries them) · **Meridian** (SSE, her concierge, the lane-handoff intact) · Surprise reveals (Ken-Burns cards, attribution links out).

### P5 — Muse, signals, the envelope, the one purchase
Muse + boards (create, drag-in, covers; share generates the web link and hands to the share sheet — the page itself stays web) · muse→signal consent · the signals consent card in settings/onboarding · **sealed envelopes** in the proposals tray (haptic tick, 300ms unfold — Reanimated) · open-contact → WA handoff via Linking · **The Full Table (BN-5):** the 4th-chair unlock card → RevenueCat one-time sheet (iOS/Android) → webhook → `full_table` flips → seats open; web Razorpay remains the third rail; anti-steering string audit on the iOS binary; sandbox proof on BOTH store rails against the same flip function · the Trousseau room (18): camera cataloguing, function cards, gap reports with attributed shop cards (link out), the Stylist guest frame + the explicit z link flow (pure API — renders native, account ceremony intact).

### P6 — Push, onboarding, offline, the stores
**Onboarding as the first gift (17 E-7):** names+date → the taste quiz full-bleed → signal consent → the masthead reveal — the app's opening minutes ARE the brand. **Push (BN-4):** registration → couple-XOR subscriptions; the 15 kinds + proposals deep-link to the owning room; prefs honored; Marriage Day silence respected at the channel level. Offline: stale-with-stamp, honest mutation failures. Stores: the bridal listing (screenshots from the real app in both themes, the description in the house register — E-1 audit), privacy forms from real behavior, account-deletion visible (the 11 rider's endpoint), Apple dry-run with a test bride, Play internal first; on approval the store links join the landing + the After per W-8's pattern. Full sweep on physical iPhone + mid-range Android, both themes.

---

## 4. GUARDRAILS
The 11 laws travel whole: guardrail + CI gate (shared, both targets) · shared api only, seams never forks · secure-store/MMKV discipline · zero web-pricing or steering strings on iOS · one entitlement flip, three feeders · BN-6's link-out constitution (a native reimplementation of coplanner, board pages, or the Story is a failed session) · BN-7's recordings-as-spec with founder sign-off · the two-asset sound law + E-1 language law on every authored string · both themes on every screen (double screenshot pass) · no storage APIs beyond the sanctioned pair · dream-os and web surfaces untouched.

## 5. ACCEPTANCE CRITERIA
1. Tri-build gate: Vercel + vendor EAS + bride EAS all green from one repo state.
2. Cold-open: bride collection Ken-Burns from admin, offline-cached, <4s to interactive canvas; guardrail CI proven (injected violation fails).
3. BN-7: the founder approves the side-by-side canvas recordings; gesture conflicts zero across canvas↔bloom↔feed.
4. Chapter fixtures pass natively (masthead voice, milestone card saves to photos once-only, Marriage Day silences push at the channel).
5. An envelope drag-files a receipt shot on camera; the vendor's milestone mark lands as her lock-screen push deep-linking to the booking.
6. Double-tap blooms with haptic + chime; the sound toggle silences; long-press peeks; compare→circle-poll round-trips with the second phone.
7. A sealed envelope unfolds; open-contact lands in WhatsApp; her contact never left the payload pre-open (audit).
8. Full Table purchased on iOS sandbox AND Play sandbox → the same flip → seat 8 admits; Razorpay web rail unaffected; anti-steering audit clean.
9. Trousseau camera-catalogues; the Stylist z-link ceremony completes natively; the After doorway links out.
10. Onboarding: a fresh bride reaches her number in the gift flow with all writes landing; E-1 register audit passes the listing + every string.
11. Store builds complete; review checklists filled; `tsc` clean across the workspace; MASTERPLAN gains BN-1…BN-8.

## 6. FOUNDER SMOKE (physical phones, both platforms + a second phone)
Cold-open to your curated album → onboard a fresh bride through the gift → double-tap a photographer in the salon, feel the bloom → compare three, poll the circle from phone two → break an envelope's seal, open the door, land in WhatsApp → camera a receipt into Attire → buy The Full Table with Face ID → photograph a lehenga into the Trousseau, run the sangeet gap report → set the date to today; watch her app go quiet → day after: the gallery, the Story link, the Stylist's doorway.

## 7. SESSION BOUNDARIES
Six sittings P1→P6 strictly; P2's founder sign-off gates P3+. Handover per protocol into the workspace docs; MASTERPLAN updated — and with it the board closes: 01–18 specced, two apps, one house.


---

## ADDENDUM (2026-07-14, founder-approved) — Social sign-in (mirrors TDW_11's addendum)
Google + Apple together, no Meta, on the bride auth screens (P1); the mandatory phone-verify beat precedes the onboarding gift and prefills nothing until the number is verified; lazy-init law + CI gate identical; link-by-verified-phone identical (a bride's social door finds her existing couple account). The 17 onboarding gift gains the social doors as its first screen — verified name/email prefill the names beat AFTER the phone verify, never before.
