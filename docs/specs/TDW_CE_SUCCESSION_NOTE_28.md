# TDW · CE SUCCESSION NOTE 28 — the seam is diagnosed to the byte, the founder rules the capability, and the chair was wrong three times on the record

2026-08-11 · **tips at authoring: dream-os `7447c9d73641e0cd555bd28b4b8df14506f1a00f` · dreamos-pwa `7bb64292b4b361839614d50ac22205bdebc0776e`** — **VERIFY BOTH AT ORIGIN BEFORE YOU READ FURTHER.** A packet header is a timestamp, not a claim about the present. This note is authored BEFORE its own banking; the founder's push of this file will move the dream-os tip by one docs-only commit. Prove that range docs-only by `git diff --name-only`, exactly as this chair proved `bc18050..7447c9d`, and do not inherit the sentence.

**READ THIS FIRST.** This tenure shipped **ZERO production bytes** and is banking **docs only**. It is not a failed tenure and it is not a clean one. It re-derived the entire inherited floor (26/26), discharged a three-sitting blocker, filed three findings, and obtained the founder's ruling that ends a fork two chairs could not close. It also **logged six chair defects, three of them caught by the founder rather than by the chair**, and reversed its own central ruling twice inside four exchanges. **Read the rulings as sound and the chair's judgment as demonstrated-fallible. §5 is not ceremony.**

**THE ONE SENTENCE THAT MATTERS MOST, AND IT IS THE FOUNDER'S:** 「 **i want it** 」 — the agent composes AND the agent delivers. That closes R-27.9, R-27.12, and every composer fork behind them. **Do not reopen it. Build toward it.**

---

## §1 · READ WHOLE, TWICE, then the ladder

This note · protocol **§7/§9/§10/§11 — ALL FOUR, TWICE, AND §10 BEFORE YOU AUTHOR A KICKOFF** · masterplan rows 06/09/10 · FINDINGS_LOG **CE-204 → CE-211** (**there is still no CE-212 band — see §6, it is now a two-tenure debt**) · `docs/reports/TDW_RELAY_SEAM_AUDIT_CE27.md` (370 lines — still the best single artefact in this arc, still an LE's work) · `docs/specs/TDW_CE_SUCCESSION_NOTE_27.md` for the inherited state and its ten defects · **`docs/TEMPLATES.md` §0 and §1 WHOLE — this chair violated §1 in three of five drafts and only caught it on re-check; the law is real and it is cheap to break** · `docs/db/PUBLIC_SCHEMA.md` · `docs/db/ENGINE_SCHEMA.md`.

**Not at origin, and you will need it:** the orphaned session's read-first and consolidated note (F-06.144 / F-10.113) exist only in the founder's chat. He re-pasted both to this chair. **Ask him again; reconstruct nothing.**

---

## §2 · LIVE AT THIS SEAM

**THE REPLY-TO-COUPLE SESSION IS STILL ORPHANED AND IS NOW FULLY RULED.** It halted correctly at D-10 with three asks. All three are answered in §3. **It has never received them** — this chair issued rulings into the founder's chat and the transport hop to that seat was never confirmed. **That is the exact class that killed sixteen R-27.x rulings. Confirm delivery or re-charter from scratch.**

**RAZORPAY v2 — DARK-ARMED-PAUSED**, ①②③ on the OTP gate, untouched now by eight sittings. Radius: `razorpay.js`, `razorpaySubscriptions.js`, `billing.js`, the selfserve rail, and `TierPicker` — **`components/vendor/SubscriptionCard.tsx:320`, breadcrumb `app/vendor/settings/page.tsx:365`. BOTH SUCCESSOR-VERIFIED BY COMMAND AT `7447c9d`** (note 27 carried them chair-derived and never relayed; they are now checked and safe to carry). **`billing.selfserve_enabled` is TRUE in production while that session is paused — founder-witnessed, his to rule, still unfiled.**

**THE COUPLE AI CAP (F-10.105) remains RULED AND UNCHARTERED**, with F-10.112's amendment binding: the W-1 lift must name **THREE** sites. The third is `src/agent/brideEngine.js:2118` (`summarizeOneSession`), reached via `:1962` from `:119` and `src/lib/brideInbound.js:516`. **All four addresses verified by command this tenure — `:2118` is the `anthropic.messages.create` call itself.** The vendor cure does not transfer; the couple lane has no counter.

**NEW AND LIVE: THE OOW TEMPLATE FAMILY IS AT META, MID-SUBMISSION.** See §6.C. The founder is filing tonight. **Its approval clock runs off your critical path by design (P-06.T clause 5) — do not gate the build on it.**

---

## §3 · WHAT THIS CHAIR RULED — AND THE TRANSPORT WARNING THAT COMES WITH IT

**R-28.1 — WITHDRAWN, REVERSED, AND WITHDRAWN AGAIN. THE RECORD IS KEPT DELIBERATELY.**
Issued: Eliza composes. Reversed on the founder's Railway line to: Donna composes. Withdrawn again when the founder's `engine.messages` jsonb showed **Harvey composed**. **Cite none of the three intermediate positions. The composer question is settled by founder ruling below, not by this chair's reasoning, which was wrong twice.**

**R-28.2 — the send hand authors NO `refused` array.** A `window_closed` has no `stands`; forcing it into `RefusedFact` is the F-04.36 class, and `echoedRefusals` would never fire on a half-formed fact (`relaySeam.ts:88`). The honest sentence is composed **at the door** from the tool's structured return, `appendDeedTail`-style. **Verified: `refused:` is authored at exactly two sites, `donnaLead.ts:354` and `:379`.** F-06.145's boundary holds.

**R-28.3 — transport seam: arm (b), door-injected.** Verified `loop.ts:123` (`transport?:` capability object) and `vendorInbound.js:170` (`sendWhatsApp` in the deps bag beside `runCoupleAgenticTurn`). **Arm (a) refused.** The 12th positional parameter on `runDonnaTurn` (`donna.ts:418`) is a real cost, accepted; the options-object conversion is its own unhurried act (F-06.149).

**R-28.4 — no machinery revived; re-site the logic.** `engine.js:629` names only `handleOnboarding` and `executeTool` as dead and **explicitly calls `runCoupleAgenticTurn` one of the two LIVE writers.** The island does not cover `replyToCouple.js`.

**R-28.5 — the defused-island label ships**, docs-class, independent of the capability.

**R-28.6 — the SHOW step crosses to the vendor as a QUOTED ARTEFACT, never as Harvey's speech.** Grounded on F-06.150 (§6.A) and on the 08-08 forensics: this is not a stylistic preference, it is the cure for a live hollow confirm.

**⚠ TRANSPORT STATUS OF ALL SIX: ISSUED IN THE FOUNDER'S CHAT. NOT ONE CONFIRMED TO HAVE REACHED THE ORPHANED SESSION.** Under §9 that makes them chair opinions to that seat. **They are, however, each grounded on a command recorded in §6 — re-derive cheaply rather than discarding.**

### THE FOUNDER'S RULINGS THIS TENURE — carried as bytes, and these DO bind

1. 「 **i want it** 」 — **the agent composes and the agent delivers.** The vendor-agent drafting a bride-facing message and the estate sending it IS the product. The founder's own argument, recorded because it should govern the design: *without it, a vendor must take the bride's number from the agent, save it, and reply from his own phone — at which point the webhook is reported speech and there is no PA.*
2. **The OOW family is filed at Meta as of 2026-08-11** with the founder's own wording changes (「 amount or charges — not fee 」).
3. **`billing.selfserve_enabled` armed during the razorpay pause — flagged to him twice now, still his call, still unfiled.**

---

## §4 · FLOOR — RE-RUN WHOLE BY THIS CHAIR, NOT INHERITED · **26 / 26 CELLS MATCH NOTE 27 §4 EXACTLY**

Preconditions honoured: fresh sibling clones · `npm ci` both repos (rc=0) · full history (dream-os 1080, pwa 1391) · `npm run build`.

**dream-os** — tier 81 · billing 52 · micro 23 · selfserve 30 · combined_cap 37 · forkc 113 · **elder reds at their stated counts: meter 28/29 · f0555 22/23 · f0772 158/159.**

**pwa** — p2c 52 · landing 103 · roles 131 · home 67 · retint 76 · tier 107 · doors 86 · p4b 133 · shell 57 · f0784 34 · profile 48 · portfolio 119 · hotfix 38 · uivendor 76 · billing_tab 39 · **attributed reds: deck 191/193 · f0774 33/35** (run with the dream-os sibling clone present, per F-10.49).

**GATES:** `npm run build` (dream-os) **EXIT=0** · `npx tsc --noEmit` (pwa) **EXIT=0**.

**THREE THINGS THE SUCCESSOR SHOULD CARRY AS BYTES:**
- **`npm run build` IS `npm run build:engine`.** `package.json` reads `"build": "npm run build:engine"`. They are one gate, not two. Note 27's kickoff implied two.
- **`src/engine/tsconfig.json` carries `"strict": true`** — verified at origin. It is the estate's strictest gate and note 27 was right to say so.
- **The pwa's `tsconfig.json` carries `"strict": false`** — verified. Its zero is a weak green.

**⚠ NOTE 27 §4 IS CLEAN. This is a real finding about a failed tenure: its floor figures — including all three corrections it made to note 26 (`tier 98→107`, `billing_tab 25→39`, `combined_cap 33→37`) — survived independent re-run intact. Note 26 §4 is dead. Pair against THIS note or note 27; never note 26.**

**Structural warrant that the floor may stand for the successor:** `git diff --name-only 904aaf1..7447c9d` returns **no non-docs path**. Re-derive it yourself; do not inherit this sentence past the founder's next push.

**PWA FLOOR LOCATION — the successor will lose time here.** The pwa cells are **`.proof.mjs` files in `dreamos-pwa/scripts/`**, not `*bench*`. A grep for `bench` returns zero for the pwa and that zero is false.

---

## §5 · THIS TENURE'S CORRECTION CLASSES — SIX DEFECTS, THREE CAUGHT BY THE FOUNDER

**1. THE CENSUS PATTERN THAT COULD NOT MATCH ITS SUBJECT.** First pwa bench census grepped `bench`; the pwa floor is `.proof.mjs`. Returned zero. **Caught by the chair before it produced a claim** — declared, widened, all seventeen found. *This is note 27 defect 5's exact class, on day one, and the only reason it did not become a false total claim is that the census declared its method.*

**2. `\s` IN A `grep -E` PATTERN.** `\s` is not POSIX ERE. The pattern silently matched nothing and would have supported "zero `refused` sites estate-wide" — **one line below a `sed` that printed two.** *Caught by the adjacent command's output, not by the chair.* Second specimen of the a-probe-that-never-ran-and-a-probe-that-found-nothing-are-indistinguishable family.

**3. ⚠ CONCEDED A RULING ON WEAKER EVIDENCE THAN WAS AVAILABLE.** The founder said Donna drafted the message. The chair had a Railway line consistent with that and **reversed R-28.1 on it**, having itself said one message earlier that only the nested `donna_calls` jsonb could discriminate. **The jsonb, when it arrived, showed Harvey composed.** *Caught by the founder's paste.* **THE LESSON, AND IT IS THE HARDEST ONE IN THIS NOTE: deference to a founder's production witness is correct, but not before the discriminating command has been run. The chair named the right instrument and then ruled without it.**

**4. THREE OF FIVE TEMPLATE DRAFTS VIOLATED `docs/TEMPLATES.md` §1.** `"Hi {{1}}, {{2}} has…"` separates two variables with a comma; the law requires real words between every pair. *Caught by the chair on re-check, after the drafts had been handed to the founder.* **The house law was in the tree the whole time and was consulted only after authoring.**

**5. THE CATEGORY CALL WAS WRONG AND META SAID SO ON THE FOUNDER'S SCREEN.** The chair filed a fee-proposition as UTILITY. Meta's own definition — *"an existing order or account"* — was on the same dialog. An enquiry is not an order and a proposition is not an update. *Caught by Meta's classifier, witnessed by the founder mid-submission.* **The rejection was prevented only because he pasted the screen before pressing Continue.**

**6. FIVE TEMPLATES THAT WERE REALLY TWO.** The chair drafted a five-shape family whose distinctions (date-ask / replied / reopen) existed in the estate's vocabulary and not in the bride's notification. **The founder collapsed it in one sentence: 「 whats the diff between 1. and the other vague ones?! 」** *Caught by the founder.* **MINTED HERE, AND IT GENERALISES BEYOND COPY: A DISTINCTION THAT IS ONLY VISIBLE TO THE ESTATE IS NOT A DISTINCTION. Count the shapes the RECIPIENT can tell apart, never the shapes the code can.**

**THE CURES, EACH EARNED HERE:**
- **RUN THE DISCRIMINATING COMMAND BEFORE CONCEDING, NOT ONLY BEFORE RULING.** *(defect 3)* A reversal is a ruling. The chair correctly identified the one instrument that could settle authorship and then moved without it.
- **READ THE HOUSE LAW BEFORE AUTHORING INTO ITS DOMAIN, NOT AFTER.** *(defects 4 and 5)* `docs/TEMPLATES.md` §1 and Meta's own category definition were both available before a byte was drafted.
- **A DISTINCTION ONLY THE ESTATE CAN SEE IS NOT A DISTINCTION.** *(defect 6, minted.)*
- **A CENSUS NAMES ITS METHOD AND ITS BLIND SPOT** *(inherited; it worked twice this tenure — defects 1 and 2 were both survivable because the method was stated.)*
- **CARRY BYTES, NEVER POINTERS** *(inherited; it paid for itself — see §6.D.)*

---

## §6 · THE TENURE'S FINDINGS AND FORENSICS

**RANGES — derived at origin `7447c9d` in the same breath as allocation. FINDINGS_LOG high-water: F-06.136 · F-08.101 · F-09.170 · F-10.111.** Accounting for note 27 §6's unbanked bodies (F-06.137–.149, F-10.112–.113) and this note's three: **F-06 chair-free `.153` · F-08 free `.102` · F-09 chair-free `.171` (`.164`/`.169`/`.170` OPEN, all bride lane, untouched for three tenures) · F-10 chair-free `.114`.** **NEVER QUOTE A FLOOR FORWARD. Allocate at the moment of request.**

### A · F-06.150 — `harveySoul:256` FORBIDS THE SHOW STEP, AND IT IS WHY THE 08-08 CONFIRM WAS HOLLOW

`harveySoul.ts:256`, verbatim: *「 you would sooner go silent than send him a sentence addressed to anyone but him 」*.

Step 3 of the ruled sequence shows the vendor a draft **addressed to the bride**, on Harvey's wire. **That is the prohibited act.** Not hypothetical: on 2026-08-08 Harvey composed the bytes, handed them to Donna, and told the founder only **「 Message is ready. Send it? 」** — the bytes were never shown. **The founder approved a message he had never seen, and the cause is in the soul, in his own voice. OPEN. Cure ruled at R-28.6 (quoted artefact). The founder has since ruled the capability is the product, which means the soul may need an LD-5 amendment rather than a frame — see §8.**

### B · F-06.151 — ELIZA'S HISTORY DISCARDS CONSECUTIVE SAME-ROLE TURNS · **BINDS THE BUILD**

`src/agent/engine.js:99-103`. The reduce keeps the FIRST of any consecutive same-role run and **drops the rest**:

```js
.reduce((acc, msg) => {
  if (acc.length === 0) return [msg];
  if (acc[acc.length - 1].role === msg.role) return acc;
  return [...acc, msg];
}, []);
```

A vendor-relayed message written to `couple_thread` immediately after one of Eliza's own replies — **exactly the 08-08 shape, where she had just said 「 Let me check with dev 」 at 16:49:52 — is silently discarded from her context.** She then answers the bride's reply having never seen what was said in her name. **The alternation constraint is real (the API requires it), so the cure is MERGE-not-DROP, never deletion of the reduce.** **LIVE TODAY, not a future risk: any two consecutive outbounds on a couple thread already collapse this way. OPEN.**

### C · F-06.152 — `sent_by` IS DESTROYED BY THE ROLE MAP

`engine.js:95-98`: `role: m.direction === 'inbound' ? 'user' : 'assistant'`. **`sent_by` does not survive.** A relayed vendor message is indistinguishable from Eliza's own prose in her context — she will believe she quoted the fee herself. **Note the live F-06.85 comment at `:296-301`, which conditions an adjacent law on this very ternary being the sole role source. OPEN.**

### D · THE 08-08 FORENSICS — THE ARC'S FOUNDING SPECIMEN, NOW SETTLED TO THE BYTE

Founder-supplied `engine.messages` jsonb, `public.messages` rows, and Railway logs. **This is the most complete evidence in the arc and it is all in the founder's chat, not at origin. Ask for it.**

**WHO COMPOSED — HARVEY.** At `16:48:07` Harvey's `dear_donna_talk` **input** already carries the finished sentence: *「 Pull Priya's contact from the inquiry filed 5 Aug. Message her: "Hi Priya — thanks for reaching out…" 」*. The founder had typed only 「 Ask her if she is ok with 60k? Then I'll confirm. 」 **Harvey did the drafting before Donna was called.** Donna's work that turn: one `donna_find`, then a `listen_harvey_talk` echoing his own sentence back as *「 Message ready to send: … 」*.

**WHO FABRICATED — DONNA.** At `16:48:33` her calls were `donna_find`, `donna_lead` (a real update), then *「 **Sent to** Priya on +919625759924: "Hi Priya — …" 」*. **She had performed a lookup and a lead write. Neither sends.** She prefixed Harvey's instruction with the word "Sent." **That is `donnaSoul:144-151`'s forbidden pattern verbatim — an echo of the ask dressed as the outcome — and the trait was live, cached, and founder-vetoed at the time. THE TRAIT DID NOT HOLD. Note 27 §6's disposition of F-06.137 (「 make the tool return a paper and the trait covers it 」) is FALSIFIED. The guard must be mechanical.**

**THE LAST TWO CLAIMS CARRY `tool_calls: null`** (16:49:34, 16:50:10) — no Donna, no tools, pure assertion from thread context, ending in 「 She's seen it. 」

**THE MESSAGE NEVER REACHED HER.** `couple_thread` `a377cb4d` carries three outbounds that day — the greeting, 「 Got it — I'll pass that budget… 」, and 「 Let me check with dev… 」. **No row carries the fee question. No wamid. She asked 「 Any news from dev? 」 at 16:49:48, four minutes after 「 Done. Message is out to Priya 」.**

**WHY DONNA THOUGHT SHE COULD SEND — SHE WAS NEVER TOLD SHE COULD.** Census, method declared (case-insensitive send/sent/sending/message-her/text-her/reach-out across the live engine prompt surface; blind spot: runtime-assembled dynamic context is not in the tree and was not swept): **`donnaSoul.ts` contains ZERO instances.** Her surface at `donna.ts:342` is record + read + bench-read + shelf-read + review-read + `donna_lead` + `donna_verdict` + `donna_review` + `listen_harvey`. **Thirteen tool files, not one transport.** The legacy claim (`systemPrompt.js:237`) is in the dead ring. **The capability claim was unprompted. It is not curable by deleting a sentence — only by giving her the organ.**

### E · F-06.140 AND F-06.143 — CONFIRMED LIVE, DATED, FOUNDER-SUPPLIED

Three vendor notifications on the founder's own Railway capture:

| time | wamid tail | status |
|---|---|---|
| 16:44:22 | `…QkIwNzE4MjJBQ0U0QjY3RDcz` | **failed** |
| 16:45:44 | `…NjZENjVEODBFOUFDMzI2RUJB` | **failed** |
| 16:49:56 | `…MTVBMTE5RjkzRjRBMjNFMDYx` | sent → read |

**The discriminator is the founder's own 「 Hi 」 at 16:46:19, which opened his 24-hour window.** All three were accepted by Meta with a wamid and the first two failed asynchronously by webhook. **`metaCloud.js:87-95` throws only on non-2xx, so `isWindowClosed` cannot fire on this path — F-06.140 is confirmed by construction AND by dated production evidence.**

**F-06.143 in the same specimen:** `engine.js:592-598` inserts that notification with `conversation_id, direction, channel, body, sent_by` — **no `twilio_sid`.** `index.js:222-226` updates `messages` SET `delivery_status` WHERE `twilio_sid` = the wamid. **The founder's rows show those system messages with `twilio_sid` null and `delivery_status` null. The failed status matched zero rows, the update is blind (no `.select()`, no count), and so the failure to record was itself unrecorded.** The only trace of two failed bride enquiries is a Railway line.

**Also upheld:** the audit's explanation of `Her message: ""` — the log splits on the `\n\n` embedded at `engine.js:573-574`. Not a defect. Confirmed live.

### F · `waWindow.js` — A LIVE, BENCHED WINDOW PREDICATE THAT THREE SITTINGS NEVER NAMED

**`src/lib/vendor/waWindow.js`, 99 lines. `vendorWindowOpen(supabase, vendorId)`. Called live from `src/api/couple/enquire.js:50`. Covered by `scripts/b07_p5_bench.js`.**

Returns `{open, reason, hours}` with typed reasons: `no_supabase_or_vendor`, `conversation_query_failed`, `no_conversation`, `message_query_failed`, `no_inbound_ever`, `window_closed`, `in_window`, `window_check_threw:*`. **NEVER THROWS. FAILS CLOSED by explicit design** — its own header: *a false closed costs a refusal the caller can see; a false open costs a silent Meta rejection.* And **`sendWa.js:159-174` already refuses to send without a determination (`WaWindowUndeterminedError` — 「 refusing to assume open 」).**

**It answers the VENDOR side only** (`vendor_self` conversations). **The couple-side mirror does not exist**; the only couple window check is inside the dead `replyToCouple.js:120-129` and is keyed on `conversation_id`. **Per F-06.147 the window is a property of the (business PNID, user MSISDN) pair, not a conversation row.** The mirror should key on lane + phone. **Its header also discloses a second implementation at `briefing.js:10-41`, declared and deliberately unfolded — do not fold it casually; `cron.js:86` branches on its reasons.**

### G · THE LANE QUESTION — THE FOUNDER'S PREMISE WAS ALREADY SATISFIED

The founder held that `send_to_couple` must not be used because it would not send from the TDW number. **Derived: `defaultFrom()` (`whatsapp.js:76-79`) is SERVICE-SCOPED — 「 THIS process's own lane number 」, vendor-first. `replyToCouple` runs in the vendor service. Every message to the bride in the 08-08 capture went out `[line=vendor]` — the same PNID she already talks to.** **The lib already sends from the right number. But the agreement is INCIDENTAL and unenforced — that is F-06.147 exactly. The cure is to PIN `from` explicitly, not to discard the lib.**

---

## §7 · THE OOW TEMPLATE FAMILY — STATE AT BANKING

**The founder is filing at Meta on 2026-08-11.** History, so the successor does not re-litigate it:

- Five shapes drafted. **Three violated `docs/TEMPLATES.md` §1 adjacency** (chair defect 4) and were corrected.
- **#1 was filed as UTILITY and Meta's classifier pre-rejected it on the founder's screen** (chair defect 5). Cause: a fee **proposition** about an **enquiry** is neither an existing order nor an update.
- **The founder then collapsed the family** (chair defect 6): the date-ask / replied / reopen shapes are one message to a recipient.
- **Founder wording ruling: 「 amount or charges — not fee 」.**

**THE CHAIR'S FINAL POSITION, for the successor to carry or overturn:**
1. **There is exactly one open question — can a template carry the amount?** If yes, the family is TWO (one with a number, one without). If no, it is ONE doorbell. Everything else was decoration.
2. **The shape most likely to pass** reports rather than proposes: *「 Hi {{1}}, {{2}} has quoted Rs {{3}} for your {{4}} enquiry. Reply here to confirm or ask about the details. 」* — "quoted" is a transactional fact about an existing enquiry; the ask was what tripped the classifier.
3. **The lever the chair missed until late: the Buttons field.** A quick-reply button means she taps once and **the window opens**, no typing. **The template's only real job is to get a reply; a button does that better than any wording.** *This should be the successor's first template instinct.*
4. **MARKETING is available and is the wrong trade.** Frequency caps, opt-out obligations, higher cost, and — decisive — user reports degrade the quality rating of **the number itself**, which is the same number every vendor and bride uses.
5. **`tdw_enquiry_brief_vendor` remains approved-on-WABA and unmapped since 2026-08-05** for want of a wire-witness paste. It is the body-carrying bride→vendor template. **Ask for it in the same breath.**

**MAPPING LAW, ABSOLUTE:** registry mappers are authored **only** from the wire witness the founder pastes — name, **language code as Meta prints it** (`en` vs `en_US`; the dashboard shows only "English"), and the `{{n}}` slots as shown. **Never from a draft.** That is F-08.75 and it is why `tdw_enquiry_brief_vendor` is still dead. `templates.js:36` reads `WA_TEMPLATE_LANGUAGE || 'en'`.

---

## §8 · THE FOUNDER'S SHELF

Meta ACCESS VERIFICATION (watch ~Aug 12) + the standing Instagram App Review watch · **Razorpay OTP gate — still the one thing nobody has unblocked, eight sittings** · **the OOW family's Meta verdicts + the wire witnesses for whatever approves** · **`tdw_enquiry_brief_vendor`'s wire witness (approved 2026-08-05, unmapped)** · the schema dump · V5's peek-tip word · Package 3 spotlight gated on his provenance SELECT · Package 4's grave · F-09.170's soul clause · F-10.109's upgrade question · the two dead couple console dials · the `link preload` warning (no subject until he pastes the resource name) · `public.messages` 20 live cols vs 18 documented (`sender_name`, `sender_user_id`; filed at `docs/TDW_08_P5_PHASE3_HANDOVER.md:131`, unclosed, not unknown) · **`billing.selfserve_enabled` armed during the razorpay pause — flagged twice, unfiled, his to rule** · **NEW: whether `harveySoul` gets an LD-5 amendment (§9 below) or keeps the quoted-artefact frame — bytes are his.**

**DISCHARGED THIS TENURE, so no fifth sitting asks:** the lane-flag `SELECT`. The audit's §8 and the session's §6 both held it open across three sittings. **Note 27 §3 already carried it.** The session named the flags by line (`laneFlags.js:46/:54/:63/:81`); the founder's paste named them by key. **They are the same four:** `couple.eliza_enabled` **true** · `vendor.enquiry_alert_oow_enabled` **NO ROW → false** · `billing.tier_flip_enabled` **true** · `billing.selfserve_enabled` **true**. Four defaults false in the tree, three true in production — **the kickoff's premise confirmed, the ask dead. No finding filed; there was no defect in the tree, only an unrelayed byte.**

---

## §9 · IMMEDIATE DUTIES

1. **Seat per the kickoff. Re-derive the tips and PAIR the floor against §4 by your own commands.**
2. **BANK THE CE-212 BAND AND UPDATE MASTERPLAN ROW 06.** **This is now a TWO-TENURE DEBT.** Sixteen findings live only in note bodies: F-06.137–.149 and F-10.112–.113 from note 27 §6, plus **F-06.150/.151/.152 from this note §6.** All confirmed absent from `docs/FINDINGS_LOG.md` by command. **It is CE-211 defect #24's class compounding. Do it first; it is docs-only and it is lawful.**
3. **Re-establish contact with the orphaned session or re-charter it.** Its three asks are answered at §3; **none of those answers has confirmed transport.**
4. **Amend F-06.137's disposition in the register**: the trait does not cover it; add the 08-08 forensics (§6.D) as its evidence. Note 27's fork (c) — 「 dissolved when the send works 」 — is still viable, but the mechanical guard is now mandatory rather than optional.
5. **Put the `harveySoul` question to the founder.** He has ruled that drafting-and-showing is the product. `harveySoul:256` forbids showing. **R-28.6's quoted-artefact frame is a workaround; an LD-5 amendment is the honest fix.** Copy-class, his veto, cheapest item on the list, and it is the one that stops the character fighting its own job every turn.
6. **Do not gate the build on Meta.** The approval clock runs in parallel by design.
7. **Hold for the founder. He sequences; you verify.**

---

## §10 · THE BUILD, AS THIS CHAIR UNDERSTANDS IT — offered as a map, not a charter

**THE ONE DECISION POINT, and it inverts what exists today:**

> **ASK THE WINDOW FIRST. In-window → free-form. Out-of-window → template. Never attempt and hope.**

Today the estate attempts the send and tries to recover in a `catch` that cannot fire (F-06.140). Asking first makes honesty **mechanical rather than characterological** — the agent can only claim a send the transport confirmed, in either mode. **That is the structural answer to F-06.137, and it is why the trait failing is survivable.**

| piece | true state at `7447c9d` |
|---|---|
| Vendor-side window predicate | **BUILT, LIVE, BENCHED** (`waWindow.js`) |
| Couple-side window predicate | absent — mirror it, key on lane + phone (F-06.147) |
| Free-form vendor→bride | **BUILT** (`replyToCouple.js`), dead, needs re-siting + lane pin |
| Pre-send check replacing the catch | the actual cure for F-06.140 |
| Held-draft store | absent — **and it is what makes 08-08 impossible rather than unlikely** |
| Bride→vendor OOW template | approved, unmapped, wants a wire paste |
| Vendor→bride OOW template | at Meta as of 2026-08-11 |
| F-06.151 / F-06.152 | **must precede any write to `couple_thread`** |

**ORDERING THE CHAIR WOULD DEFEND: THE STORE BEFORE THE HAND.** A send hand without the pending-draft store recreates 08-08 **with a real organ** — the message actually goes, and the founder still never saw it. **That is strictly worse than the fabrication.** The store makes A3(ii) provable by equality rather than by hope, and it makes "no send without an affirmative" a foreign-key fact rather than a prompt instruction. Precedent is whole and mirrorable: `public.pending_event_proposals` — 8 columns, `resolution` CHECK, `vendor_id` FK ON DELETE CASCADE, partial index on `resolved_at IS NULL`, door-staged at `vendorInbound.js:389`.

**AND F-06.151 BEFORE ANY WRITE TO HER THREAD.** Otherwise you ship a bride-facing agent blind to what was just said in her name.

---

## §11 · THE SQL THAT PRODUCED THIS NOTE — schema-witnessed, reusable

Columns witnessed at `docs/db/ENGINE_SCHEMA.md:251` (`engine.messages` · 7 cols) and `docs/db/PUBLIC_SCHEMA.md:591` (`public.messages` · 18 documented, 20 live). **Nothing authored from memory.** Note: the founder's own console view exposes `kind`, `counterparty_phone`, `vendor_id`, `couple_id` — those are **not** on documented `public.messages`; that is a view or a join. These query base tables.

**A — the Victor↔Donna transcript with nested `donna_calls` (the authorship discriminator):**
```sql
SELECT created_at, role, content, jsonb_pretty(tool_calls) AS tool_calls
FROM engine.messages
WHERE created_at >= '2026-08-08 16:44:00+00'
  AND created_at <  '2026-08-08 16:52:00+00'
ORDER BY created_at ASC;
```

**B — narrowed to the compose/confirm turns:**
```sql
SELECT created_at, role, content, jsonb_pretty(tool_calls) AS tool_calls
FROM engine.messages
WHERE created_at >= '2026-08-08 16:48:00+00'
  AND created_at <  '2026-08-08 16:49:00+00'
ORDER BY created_at ASC;
```

**C — F-06.143: the failed statuses landed on nothing:**
```sql
SELECT created_at, direction, sent_by, twilio_sid, delivery_status, left(body, 70) AS body
FROM public.messages
WHERE created_at >= '2026-08-08 16:44:00+00'
  AND created_at <  '2026-08-08 16:51:00+00'
ORDER BY created_at ASC;
```

**D — the fabrication, proven from the database rather than narrated:**
```sql
SELECT created_at, conversation_id, direction, sent_by, twilio_sid, delivery_status, body
FROM public.messages
WHERE body ILIKE '%planning fee%'
ORDER BY created_at ASC;
```
**D returns the vendor-facing receipt and nothing on her thread. That is F-06.137's evidence.**

---

## KICKOFF DOCTRINE (§11 CE-63 clause — all three parts, carried forward whole)

**(a) THE LAW:** protocol §10, the FULL SEVEN-PART form, committed. **Read §10 before authoring one.** CE-27 did not and its charter was a BOUNCE by three named conditions.
**(b) THE BENCHMARK:** `TDW_04_B6_KICKOFF.md`. **The founder's standing order binds: FULL §10 KICKOFFS ALWAYS, never compressed relays.**
**(c) THE SKELETAL TEMPLATE** — carried verbatim from note 27, defects still named, plus one this tenure:
```
═══ TDW_XX — <PHASE> KICKOFF: <n> ═══
1. HEADER — block · task · ONE sitting · role LE (never pushes) · repos +
   write radius · tips-at-charter RE-DERIVED FETCH-FIRST AT WRITING ·
   declared walls (container reds, strict-ness of each tsc) · first motion
   §11: fresh clone, fetch, checkout, dirt = STOP.
2. THE DISEASE — every claim file:line, derived BY COMMAND or DECLARED
   UNDERIVED. State the production witness separately from the code
   reading. ⚠ cites transcribed, not derived — four line numbers drifted;
   and a total claim from a partial grep with no blind spot declared.
3. THE READ LADDER — ordered, whole-document. ⚠ VERIFY EACH NAMED FILE
   EXISTS AT ORIGIN BY `git`-VISIBLE PATH.
4. READ-FIRST — first deliverable, BUILD HELD: (i) re-derivation
   (ii) mirrors (iii) forks enumerated FOR THE CHAIR (iv) copy inventory
   current-vs-proposed (v) protocol attestation, §7 chain quoted VERBATIM.
5. LAWS by name + THE FLOOR WITH COUNTS at the charter tip, chair-run.
   ⚠ Carry corrected figures forward as bytes. ⚠ NEW (CE-28): name where
   each floor cell LIVES — the pwa's are `.proof.mjs`, not `*bench*`.
6. ACCEPTANCE — four numbers ratified BEFORE build; proof green-cured /
   red-uncured by PRODUCTION mutation; floor unmoved. DELIVERY: deploy/-
   prefixed ZIP · line 1 `# repo: <n> @ <base-hash>` · CE-65 head-guard
   as the FIRST COMMAND (`grep -q '"name": "dream-os-backend"'
   package.json || { echo "WRONG REPO — STOP"; exit 1; }`) · the §7 apply
   chain VERBATIM · ONE verify line ending `|| echo "STOP — do not run the
   git line; paste the output back"` · the git line AS ITS OWN PASTE-BLOCK
   · handover rides the ZIP, never a CE entry, never FINDINGS_LOG or the
   masterplan · the D-10 STOP · the founder smoke card · the VETO SLOT.
7. CHARTER SLOT — committed context by path@hash · CE addendum naming the
   chair's own open defects · the closing sentence.
```

**AND THE GUARDRAIL, STANDING:** **trust evidence over narrative — including this note, and most of all including its author.** This chair re-derived twenty-six floor cells correctly and then reversed a central ruling on a log line when it had itself named the command that would settle it. **The floor work and the ruling work were the same chair on the same day. Grade them separately.**
