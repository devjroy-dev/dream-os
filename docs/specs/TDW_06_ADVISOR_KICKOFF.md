# TDW_06 — THE ADVISOR SITTING KICKOFF (P6a, S-10; the third chair's first charter)

**CE-22:** authored at dream-os `ab08bea` = `origin/main`, fetched and re-derived at the moment of writing (`git fetch -q origin && git rev-parse --short origin/main`). **Status: STANDING CHARTER.** This kickoff executes Succession Note II §4's P6a seat — **the SPEC'S scope (`TDW_06_WA_AGENT_FINAL.md` P6.1/S-10), which supersedes the field note's thinner charter BY NAME** — grounded on the field note §3's witnessed codex census, which is KEPT as ground truth. One executor session, one sitting (split lawful at any seam per the banking law). The founder runs the one migration and every smoke; copy veto on every vendor-facing word.

## §0 — READ LADDER (in order, END TO END, before any edit)
1. `docs/TDW_BUILD_PROTOCOL.md` — every law binds, §9 included (D-10's verify+git fusion; the conditional-withheld rule).
2. `docs/TDW_00_MASTERPLAN.md` — the 06 row whole.
3. `docs/specs/TDW_06_WA_AGENT_FINAL.md` — **the block spec, whole** (the second chair's closing lesson: the spec joins every sitting's read ladder). P6.1 + S-10 + migration table + §3 guardrails are this sitting's contract.
4. `docs/specs/TDW_06_MANUAL_PAPER.md` — the doctrines the advisory room lives under (§2.3 the speaker doctrine; §2.1 sentence 3 shape-vs-existence).
5. `docs/specs/TDW_06_CE_FIELD_NOTE_2026-07-18.md` §3 — the codex census (seven handbooks witnessed in `engine.domain_handbooks`, incl. `social_media_management` "The Operator's Codex", 95,253 chars) + §3.5's doctrine note. Scope sentences superseded by this kickoff where they are thinner than the spec; census facts stand.
6. `docs/specs/TDW_CE_SUCCESSION_NOTE_2.md` §4 — the P6a row, verbatim.
7. `docs/FINDINGS_LOG.md` — from the 06 sitting-0 charter to the end, including THE THIRD CHAIR SEATING entry (this kickoff's two drift reports live there).
8. Code, by command at your own fetched HEAD: `src/engine/src/core/loop.ts` (the prefix assembly, the consult switch at `:349`-era, the VENDOR SUIT smmLens block ~`:270–300`, `staticPrefix`) · `src/engine/src/core/handbook.ts` whole (index+pull vs `getHandbookFull`) · `src/engine/src/core/professions.ts` · `src/engine/src/core/consultantHarveySoul.ts` (READ-ONLY; untouched by name) · `src/api/vendor-engine/chat.js` (the door, `composedTail`, the mode read seam) · `docs/db/ENGINE_SCHEMA.md` + `docs/db/PUBLIC_SCHEMA.md` (witnessed columns — the only lawful SQL source) · `src/lib/modelRouter.js` (config vocabulary) · the PWA chat header component set (chip siting) + the WA pre-engine intercept seam (the nudge-words precedent, `engine.js` lane — READ, not edited; see guardrail 6).

## §1 — WHY THE READ-FIRST IS THE SITTING'S FIRST DELIVERABLE (§0.2 + §3.5, both drifts already on the record)
The third chair's seating verify found two spec-vs-code facts, filed in FINDINGS_LOG; the sitting OPENS by resolving them with a code-read REPORT to the CE, and **migration 0080 is authored only after the CE's siting ruling returns**:

**R-1 — the mode-column collision.** `engine.agents.mode` EXISTS at HEAD, `text NOT NULL default 'advisory'` (ENGINE_SCHEMA.md:59) — the dreamai-era consult/advisory switch, the very column sitting-0 read to acquit hypothesis (c). The spec's 0080 charters `agents.mode text not null default 'business' check (mode in ('business','advisor'))` — same name, colliding semantics, written against a pre-consult-era snapshot. The report answers from the code: which table/column can carry the TDW vendor's Business·Advisor toggle without repurposing or shadowing the consult switch (candidate shapes to price, none pre-chosen: a NEW column on `engine.agents` e.g. `victor_mode`; a `public.vendors` column beside `assistant_name`; or widening the existing check — the last is suspect on its face, two eras in one column). The CE rules the siting; 0080 is then authored ONCE, founder-run, with in-file verifies and the revert commented per §9.

**R-2 — the lens-shape drift.** The field note ruled "index-in-context + pull-section-on-demand is the cache-lawful shape — keep it." The code at `loop.ts:274–295` loads the SMM codex **WHOLE** (`getHandbookFull('social_media_management')`) as always-on standing mastery in the existing advisory path, with index+pull serving only the trade codex. The report states, with honest chars→tokens→₹ arithmetic (chars/4, labeled estimate, both providers' cache rates from `models.ts`): the cost of whole-SMM + whole-or-indexed trade codex in advisor mode's static prefix, cold-write per window and warm, versus the index+pull shape for both. **The CE rules the shape on the numbers** — both are cache-lawful (static is static); the ruling is a cold-write-cost decision, not a doctrine one. Until ruled, no prefix assembly is authored.

**The read-first report also answers:** where the chat door learns the agent's mode today (the seam `chat.js` → engine args) · whether `professions.ts`'s hook vocabulary covers all seven censused handbook fields (name the gaps, invent nothing) · the WA pre-engine intercept's exact mechanics for the mode words.

## §2 — THE CHARTER (the spec's P6a, executed in this order after the R-1/R-2 rulings return)

1. **`advisorLens.ts` — Victor as SMM + domain expert, authored per LD-5** (the self with its reasons, NEVER a rules-list): the consultant who also ran brand for houses like this — content cadence per trade (what a photographer posts vs a decorator), reel/portfolio judgment, enquiry-to-DM craft, seasonal calendars (muhurat clusters as content moments), honest platform mechanics. Category-calibrated via per-profession hooks off `professions.ts`. **The lens does not duplicate the codex** — the codex shelf is the knowledge (per the R-2 ruled shape); the lens is HIS relationship to it and to the vendor's trade. CE sight-check on the excerpted lens before the founder applies (the S2 convention).
2. **Migration `0080`** (LD-8's reserved number): `vendors.assistant_name text` (S-3's Mira fallback — shipped now, consumed by P3 later; the spec's own rung) + the mode column AS RULED at R-1. Founder-run in the Supabase editor, witnessed-columns only, in-file verify SELECTs, revert commented.
3. **The chat door reads mode:** `advisor` → the advisor lens replaces the operational handbook in his prefix · **Donna dispatches DISABLED — the door does not offer `dear_donna_talk`** — except the single lightweight `jot_advice` capture into his notes so good counsel isn't lost (`jot_advice` is a NEW minimal hand: text in, note filed, witness line per the standing chip vocabulary — its result sentence on the veto list). Mode state stamps message meta for the ledger.
4. **The surfaces:** PWA — the two-word mode chip in the chat header (`Business · Advisor`, Jost, ink, design system; ≤3 gold per screen stands), PATCHing mode SERVER-SIDE (no localStorage — native clause; the settings-allowlist convention). WA — the mode words ("advisor mode" / "business mode") intercepted pre-engine like the nudge words; the WA engines are otherwise untouchable (protocol §8), so the intercept lands at the lawful seam the read-first named.
5. **The business-facts redirect, verbatim as chartered:** anything operational the vendor says in advisor mode gets ONE in-character redirect — *"that one's for the ledger — flip me to business mode and it's filed"* — soul-authored (the reason attached: counsel doesn't pretend the ledger away; he tells you which room it lives in), never a detection rule. On the veto list.
6. **The doctrine note bound in (field note §3.5, kept):** the advisory room has no hands — the dispatch and claim doctrines' subjects are absent — but the SPEAKER doctrine and shape-vs-existence bind whole: the advisor speaks from the codex and the owner's words this thread, never from pretended account access ("your 5k followers" must have been told him or read through a lawful surface; no estate reads exist in this room).

## §3 — GUARDRAILS (inviolable this sitting)
1. The soul law is absolute — a rules-list or forbidden-phrase block anywhere is a failed session.
2. `consultantHarveySoul.ts` untouched by name. Both business souls (harveySoul v8, donnaSoul) untouched — the doctrine pass is sealed; this sitting adds a LENS, not a soul edit. Any prefix reorder is disclosed as the cache law demands; doctrine text batched into one authored pass.
3. **The cache law:** all static content before the breakpoint; the lens lands in the cached prefix once, one window spend; dynamic content never touches the static prefix.
4. **Every vendor-speakable minted string pre-read through the REAL `scrubText`** (minted + rendered both in front of the founder — the D-6 convention) and on the veto list: the redirect sentence · the chip's two words · the WA mode-word confirmations · `jot_advice`'s result sentence.
5. `leads.js` 0-line (F-04.69 + R-B6-12 stand first-in-line for the first ZIP that opens it — this one does not; say so at delivery). `eventWrite.js`, `occupancy.js`, `scrub.js`, `calendarSignals.js` 0-line.
6. The WA engines are live product surfaces — the mode-word intercept is the ONLY WA touch, at the pre-engine seam, and only if the read-first confirms the nudge-word precedent's seam carries it; else REPORT (§0.2), never improvise on a live wire.
7. Full engine gates on every ZIP (tsc · build · tombstone · smoke · every standing bench count byte-stable or labeled-amended per R-B6-15); benches both-ways at the uncured tree; D-11's dist gate where dist testifies.
8. Delivery per §7 + §9 whole: `deploy/`-prefixed ZIPs, `# repo:` line 1, verify line ending in D-10's STOP sentence, git line beneath it, commit messages via `git commit -F -` heredoc; conditional blocks withheld or fully commented. Fixtures self-contained, zero placeholders, Vera-era names.

## §4 — ACCEPTANCE (the spec's criterion 9, made checkable before the build — never ruled after the fact)
1. Filing verifiably PAUSED in advisor mode: a scripted operational message in advisor mode produces ZERO Donna dispatches in the ledger (nested `donna_calls` the only convicting reader, D-1) and the redirect sentence verbatim; the same message in business mode files with its witness line.
2. `jot_advice` captures counsel: one advisory turn's jot lands as a note, witnessed.
3. The mode word works on WA (founder phone smoke); the chip persists server-side across a refresh and a second device.
4. Advice register evident on a founder ear-test transcript (his verdict, his words, recorded verbatim).
5. Mode stamps in message meta, row-witnessed.
6. No estate claim in advisor mode: an absence/figure probe ("how many enquiries did I get this week?") draws the redirect or an honest I-can't-see-the-ledger-from-here sentence — NEVER a number.
7. 0080 proven by the founder's in-file verifies; `node --check` + engine tsc + PWA tsc (cleared cache) clean.

## §5 — SESSION BOUNDARIES + WHAT THIS KICKOFF DOES NOT DO
Order: read-first report → CE rulings (R-1 siting · R-2 shape) → 0080 authored + founder-run → engine ZIP (lens + door + jot_advice + meta) → PWA ZIP (chip) + WA intercept → founder smoke + ear-test → handover per protocol (masterplan 06 row + this log; SCHEMA docs if 0080 moved them). Banking lawful at every seam; every packet carries its commit hash in line 1. **Not this sitting:** P1 the TDW MANUAL (schedulable any sitting on the founder's word — authoring work, no transport dependency) · P2/P3/P4 (05-dependent per the dependency law) · the acceptance evenings (M-7(iii), founder-side, running in parallel — reds file to the CE as findings) · shape (b) (M-5 deferral stands) · any WA transport work (05's).
