# TDW_06 — THE SD-REL SITTING · EXECUTOR HANDOVER
## The paper that can support an honest relay, and the existence family's other mouth

**Base:** `0267e10` (re-derived at origin, fetch-first, at delivery).
**Role:** LE / executor, code-capable. **Repo:** `devjroy-dev/dream-os` alone (dreamos-pwa untouched).
**Authority:** the thirteenth chair's kickoff + Header Amendment №1 (2026-07-28, chat paste-blocks per the transport law) · chair rulings R-1/R-2/R-3 on read-first Parts One and Two · **the founder's copy veto, executed in chat the same day on the chair-amended plain-speech form.**
**Delta:** `src/engine/src/core/tools/donnaLead.ts` · `scripts/b06_gauntlet.js` · `scripts/b6_door_rider_bench.js` · `scripts/b06_f0692_bench.js` (new) · this document. **Nothing else.**
**W-1 HELD SHUT** — zero soul, prompt, voice or persona bytes; `donnaSoul.ts`, `harveySoul.ts`, `advisorLens.ts`, `consultantHarveySoul.ts`, `loop.ts`, `donna.ts` all 0-line. **No SQL, no migration, no env var, no dashboard act.**

This document carries **no CE number**, touches **no FINDINGS_LOG entry** and **no masterplan row** — those are the chair's (clobber law). The chair cuts the log entry from this handover.

---

## 1. WHAT SHIPPED

### R-1 / fork 1(e) — THE DOOR'S PAPER STATES WHAT IT REFUSED (F-06.92)

**The disease, captured by command through the rig's own db double before anything was written.** On the SD-REL / F-04.78 specimen the shipped door returned:

```
Updated existing lead "Tara Relay Test" (id=lead-tara-seed) — raw_message. (Typed lead — this
id is not a binder; …) Note: matched by NAME to the lead already on file — if this is a
different person with the same name, tell me and I'll file them separately.
```

A success verb, one column key, and **silence on everything that mattered**: nothing about Jaipur, nothing about 5 March 2027, nothing about Udaipur and 5 December having been refused. Every enrich guard at `donnaLead.ts:192/:198/:200/:202/:208/:209/:210` is `if (input.X && !cur.X)` — **a silent no-op; not one of them reports.**

**And the sharpest statement of it, folded into F-06.92 by ruling:** `donnaSoul`'s relay paragraph orders her in one breath to speak the result's own sentence *and* to speak *"no workings, no machinery"* — and the only substantive thing in that paper **was** a column key. **The two clauses could not both be obeyed.** The mouth completed from the only meaningful facts left in its context: the dispatch's. F-04.78's relay is a paper that cannot support an honest sentence, not a mouth refusing to speak one. **The soul is not the fault and is not to be re-opened as one.**

**The cure.** A `notWrittenNote` composed from the *same guard conditions already evaluated*, appended LAST on both single-match returns. Scope as ruled: **owner-meaningful fields only** — name · city · wedding date · referrer. `source` and `raw_message` are **excluded by ruling** (Victor-set plumbing; admitting them re-admits the machinery that caused the disease). The phone is excluded — Q-R-1 shape (c) already speaks it. A field is reported **only where the two values actually differ**: re-sending what stands is not a refusal.

**The paper now reads, verbatim, captured post-cure through the same double:**

```
… Note: matched by NAME to the lead already on file — if this is a different person with the
same name, tell me and I'll file them separately. Not written — the record already stands: the
city stays Jaipur (you said Udaipur), the wedding date stays 5 March 2027 (you said 5 December
2027). If either should change, say so and I'll change it.
```

**The copy is the founder's bytes**, chair-amended form, veto executed in chat. **The `:229`/`:254`/`:284` legacy strings are byte-unchanged.**

**PLACEMENT WAS A DERIVATION, NOT A PREFERENCE.** `b6_door_rider_bench:147` pins the source shape with `/nothing new to add\.\$\{nameMatchNote\}/` and `/\)\$\{flag\}\$\{nameMatchNote\}/`. Appending the note **last, after `nameMatchNote`**, leaves both pinned substrings byte-exact — so that bench needs **no amendment and its count does not move**. Any other placement breaks it. Asserted as a cell.

**TWO LAWS PAID FOR INSIDE THE CURE, both benched:**
- **PRECISION IS NOT INVENTED.** `parseWeddingDate:111` stores a month-precision date as the **1st-of-month sentinel** (`2027-03` → `2027-03-01`, precision `'month'`). A renderer blind to `wedding_date_precision` prints *"1 March 2027"* for a row that only ever held *"March 2027"* — **a day the estate never had, minted inside the very cure that exists to kill false certainty.** Both forms on the veto table carried this defect, the executor's own included; it was reported at read-first and cured here. `humanWeddingDate` reads the precision column and speaks *"March 2027"*.
- **F-06.27's UTC slice.** `new Date('2027-03-05')` is UTC midnight and formats to the previous day west of Greenwich. The renderer reads the **string parts** and never round-trips a clock. Asserted by source cell.

**EXECUTOR INTERPRETATION, DISCLOSED — ratify-or-revert.** The approved sentence was rendered on a **two-refusal** specimen (*"If either should change"*). One refusal ships *"If that should change"*; three or more ship *"If any should change"*. Nothing else in his bytes moved. Disclosed in-file at the cure site, not only here.

**RATIFIED-AS-RULED, restated:** `:284` create path **expected-zero** — no standing row, no refusal, no note. Benched.

### R-2 — THE EXISTENCE FAMILY LEARNS THE OTHER MOUTH (F-06.91)

R-1's per-mouth idiom **ported, not reinvented**, to all three sites through one home, `relayMouths(r)`:
- **`absenceFidelity` (`:333`-family)** judges `[Victor's outward prose, ...every listen_harvey_talk voiced text]` **per mouth, never merged** — a merged blob lets a fabrication in her sentence be acquitted by an honest clause in his, and loses the WHO the cures live by. `fabricated` is **worst-of-mouths**; the conviction **names its mouth**.
- **The two fail-closed tests** (SD-ABS, SD-EXIST) read every mouth: an unreadable plane honestly reported *by her* was scored as if nobody had reported it.

**R-1's REDUCTION PROPERTY KEPT:** on a single-mouth turn there is no WHO to name, so the arm returns the **pre-F-06.91 sentences byte-for-byte**, both directions. Asserted as a cell — and it was earned: the first draft prefixed unconditionally and the bench caught it.

**SCALARS AND VOCABULARY UNTOUCHED, asserted as cells never prose:** `ABSENCE_CLAIM_RE` and the three `_FID_*` regexes are byte-unchanged — this cure widens the **corpus**, never the words. F-06.84's acquitting phrases and F-06.35's case gap stay their own findings' jobs. **CE-91's grader precedent asserted structurally:** SD-REL's verdict is self-contained, calls neither fidelity arm, and is byte-untouched by this sitting.

**THE ZERO-FLIP CENSUS, run at read-first on a scratch tree and re-confirmed on the shipped tree:** **not one pre-existing cell changes verdict.** The widening is purely additive — so its teeth are entirely in the new cells, and the non-vacuity proof below is the whole of its evidence.

### F-06.94 — THE FLOOR UNDER-COUNTED ITSELF, CURED

`b6_door_rider_bench` was carried as the estate's **named credential-drop specimen** — permanently un-runnable — through CE-80's own floor-method law, NOTE_12 §3 and two committed handovers. **It was never a credential need.** `dist/core/db.js` throws at *module load* if two env vars are absent; the bench reads and writes nothing live and runs wholly on an in-memory double, with `../db.js` stubbed out from under the module under test. It self-stubs now (dead loopback port, on purpose, so any real reach fails loudly; a real environment's vars are never overwritten), and **joins the sealed floor at 15/15 from this delivery.** The "named specimen" language retires from future floor records. It had been declared skipped **while guarding exactly the surface this sitting opens.**

### F-06.93 — CURED WITH ZERO FIXTURE BYTES MOVED

The rider asked that SD-REL's honest fixture be re-derived against the shipped post-1(e) paper. **It did not need to move.** Its sentence — *"Matched the existing Tara Relay Test — her record holds Jaipur, 5 March 2027; the new city and date were not written"* — is now **fully supported** by the door's own return, which speaks both standing facts and both refusals. **No labeled amendment owed; the fixture is byte-untouched.** That is the cleanest possible discharge of the rider and it is stated as a fact, not a claim: the post-cure paper is pasted above.

---

## 2. PROOF

**`gauntlet --rig-selftest` 297 → 311** (+14, all in the new section **[28]**; every pre-existing cell green and **unamended**). **`b06_f0692_bench` 23/23 NEW.** **`b6_door_rider_bench` 15/15, count preserved, zero cells added.**

**Both-ways, six mutations of SHIPPED code — never test setup — each run out-of-process, tree `cmp`-restored after each, reds enumerated:**

| Mutation | Result |
|---|---|
| **M-A** — the `relayMouths` extraction reverted (relay name never matches) | **305/311 — 6 RED**, exactly ①②④⑤ + ⑧ + ⑨ — all three sites' cells and nothing else |
| **M-B** — SD-ABS's fail-closed reverted to reply-only | **309/311 — 2 RED**, ⑧ + STRUCTURAL |
| **M-C** — SD-EXIST's fail-closed reverted to reply-only | **309/311 — 2 RED**, ⑨ + STRUCTURAL |
| **M-D** — `${notWrittenNote}` deleted from the update return | **`b06_f0692` 15/23 — 8 RED** |
| **M-E** — the precision branch stripped from `humanWeddingDate` | **`b06_f0692` 21/23 — 2 RED**, the precision cell + its source pin |
| **M-F** — the F-06.94 self-stub removed | **`b6_door_rider_bench` CRASHES** on the bare env, reproducing the tenure-long skip exactly |

M-A **is** the uncured extraction; M-D **is** the uncured door. The uncured tree fails on exactly the cures. M-B/M-C each RED their own arm *and* the structural pin — which is the pin doing precisely the job it was written for: a site retired at one place cannot leave the estate believing all three still see her.

**THE SEALED FLOOR, every count run by command at the cured tree:**

```
b06 m0 50 · m1 45 · m2 39 · m3 37 · m4 33 · m4b 24 · m4c 20 · m4d 16
f0658 20 · f0667 16 · f0681 12 · advisor 16 · advisor_route 16 · 0081 12
sonnet 13 · donna_cache 16 · b0461_p6 25 · b6_floors 47 · b6_s1 24 · b6_sitting2 22
b05 f0550 31 · arc_m2 27 · arc_m4 18 · arc_m5 11
b6_door_rider 15/15  (F-06.94 — NO LONGER SKIPPED; joins the floor this delivery)
gauntlet --rig-selftest 311/311  (was 297; +14, section [28] — the disclosed growth)
b06_f0692 23/23  (NEW)
KNOWN RED, not this sitting's: b06_meter_bench 28/29 (F-06.41), carried loudly
```

Beyond the list, run anyway because `donnaLead.ts` is a hot file: `b05_arc_m1` 53 · `arc_m3` 11 · `arc_m6` 20 — all green.

**SKIPPED, NAMED (floor-method law):** live gauntlet lanes — founder's keys, live-lane. Block 04 bench families and `checker_bench` — not run; no shipped byte reaches their surfaces (this delta is one engine TS file and three `scripts/` files). A declared judgement, not a claim of coverage.

**Engine gates:** `tsc -p src/engine/tsconfig.json` clean, zero errors.

**LABELED FLOOR AMENDMENTS: ZERO.** No pre-existing cell was amended, re-pinned or re-aimed in this sitting. `b06_m2_bench:198`'s full-line pin on `ABSENCE_CLAIM_RE` was the named adjacency at read-first — **the constant did not move, so the pin did not either.**

---

## 3. THE LIVE VERDICT IS OUTSTANDING — stated plainly, as acceptance requires

**No bench in this ZIP witnesses the model choosing honestly.** No desk cell can: `loop.ts:710` hands Victor her voiced text alone, and whether she now speaks the paper she has been given is a model behaviour. **SD-REL's verdict on the next live gauntlet is the only verdict this cure will ever get**, and it is outstanding at delivery.

**And per CE-93 that run is a FIRST MEASUREMENT on the corrected question, comparable to nothing before it.** The two derivable SD-REL red records at origin conflict with each other (RUN 5 says both Donnas; the later CE-25 evening attributes it to Donna-Haiku alone and acquits the production hand) and both are prior-era under NOTE_12 §9.6. **This cure's justification is structural, not frequency**, and the record must not be read as though a tally existed. F-06.87's TIME DRIFT watch remains the first thing that run reads.

---

## 4. WHAT THE FOUNDER MUST DO — and what he must not

**Dashboard/console acts: NONE.** No env var, no Railway change, no Meta change, no SQL. This sitting reads no table and writes none.

The apply block, the verify line and the git line ride the chat message this ZIP came with. The verify line ends in the D-10 STOP; the git line is its own paste-block. **Do not run the git line over a red verify.** The verify's `npm run build` step rebuilds the engine — `donnaLead.ts` moved, and the gauntlet refuses to run distless by design.

**Live witness: none this sitting**, by the kickoff's own acceptance. Declared, not claimed.

---

## 5. FINDINGS FILED THIS SITTING (unnumbered, per the law — the chair mints)

1. **The reduction property had to be earned twice.** The first draft of `absenceFidelity` prefixed the mouth unconditionally, which moved every historic single-mouth sentence for no information gain. Its own cell caught it. Disclosed because the cell ran, not because it was noticed by eye.
2. **The structural pin catches a partial retirement, and that is not incidental.** M-B/M-C each RED their own arm *and* the structural cell. Three sites drawing from one home is what makes a half-retired cure visible; recorded so a future sitting does not "simplify" the three call sites back into three local expressions.
3. **F-06.95 stayed shut with the file open.** `donnaLead:212` writes `budget_max` unguarded while every fact field is standing-value-guarded — money overwrites silently where facts drop silently. The file was open in front of me for the whole sitting and it was **not** touched, per the chair's ruling: a money-write semantics change must be derived against the provenance hold and the F-04.13 money laws first. Named here so the restraint is on the record as deliberate.

---

## 6. WHAT THE NEXT SITTING PICKS UP

Nothing from this one. The board, founder-sequenced: **F-06.13's fan-out** → **F-06.84** → the evenings. **F-06.83 returns to the board** carrying its derived fourteen-field census (`describeRow` — client · amount · direction · received · pending · payment_status · date · stage · phone · doc · note · self · **filed** · archived, with `filed`/`arrivalStamp` the one instrumented) and the **two-holes distinction**: F-06.83 is the *find* surface, F-06.92 was the *door* surface, and **neither subsumes the other**. **Fork C stays refused** — its return condition is unmet until the relay law has one post-repair measurement.

The clock is at ZERO; nothing in this sitting moves it. Sequencing is the founder's.
