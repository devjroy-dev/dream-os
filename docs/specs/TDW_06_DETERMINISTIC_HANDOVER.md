# TDW_06 — THE DETERMINISTIC SITTING · EXECUTOR HANDOVER
## The floor that cannot flip, and the relay that cannot echo alone

**Base:** re-derived at origin, fetch-first, at delivery (line 1 of the apply block carries it). Code base `1924656`; the CE-102/CE-103 docs sit above it and this delta is the first code movement since the freeze lifted.
**Role:** LE / executor, code-capable. **Repo:** `devjroy-dev/dream-os` alone (dreamos-pwa untouched).
**Authority:** the thirteenth chair's DETERMINISTIC SITTING kickoff + Charter Addendum №1 · the fourteenth chair's ruling on read-first Part One (**A-1(b) · A-2(b) · A-3 · B-1(a) · B-2(α) · B-3 · C-1**) and its VETO EXECUTED packet · **the founder's copy veto, 「 ill go with both the recomendations 」**.
**Delta — NINE files, nothing else:** `src/engine/src/core/historyGate.ts` (new) · `src/engine/src/core/relaySeam.ts` (new) · `src/engine/src/core/snapshotTypes.ts` · `src/engine/src/core/donna.ts` · `src/engine/src/core/loop.ts` · `src/engine/src/core/tools/donnaLead.ts` · `scripts/b06_gauntlet.js` · `scripts/b06_forkc_wireguard_bench.js` · `scripts/b06_f0613_relay_bench.js` (new) · this document.

**W-1 HELD SHUT — command-asserted, all 0-line:** `donnaSoul.ts` · `harveySoul.ts` · `advisorLens.ts` · `consultantHarveySoul.ts` · `scrub.js` · `leads.js` · `harvest.js` · `chat.js` · `vendorInbound.js` · `memory.ts`. **`DONNA_STATIC_PREFIX` byte-identical to `1924656` (md5 `1d99924…` both sides) — THE CACHE WINDOW IS NOT INVALIDATED BY THIS DELIVERY.**

**No SQL, no migration, no env var, no dashboard act.** SQL scope NIL, as attested at read-first. Both cures read only what the turn already holds; no new table, column, or query.

This document carries **no CE number**, touches **no FINDINGS_LOG entry** and **no masterplan row** — those are the chair's (clobber law).

---

## 1 · WHAT SHIPPED

### 1.1 CURE (A) — THE SD-WEEK MECHANICAL FLOOR (`historyGate.ts`, seated in the work loop)

CE-25 deliberately HELD this floor pending evidence. Evening Four supplied it — the fan-out at 2-of-5 on the production split **with money riding** — and the founder REVERSED the hold.

**The predicate is chain-shaped and count-blind (fork A-2(b)).** It refuses exactly one shape: a `donna_history` whose `binder_id` a **breadth sweep** handed over, on a turn whose ask names no record and carries neither provenance nor write intent. A count floor was refused at ruling because it would tax CE-25's own protected case; the four lawful shapes are properties here, not thresholds:

| | Shape | Opener |
|---|---|---|
| **L1** | the owner named the record | `askNamesRecord` — an id outright, a client name the sweep surfaced (full or given), or any name-shaped capitalised token past a sentence's first word |
| **L2** | provenance, "how do you know that" | `PROVENANCE_ASK_RE` |
| **L3** | reconciliation before a write | `WRITE_INTENT_RE` (ask-side: the read PRECEDES its write, so the write hand cannot be the evidence) |
| **L4** | the id came from a NAMED find | `namedIds`, and a named find **outranks** an earlier sweep on the same id |

**IT FAILS OPEN ON AMBIGUITY, NEVER CLOSED — the inverse of every other floor in this block, deliberately.** The provenance hold sits at the same seam a few lines below and fails CLOSED, because a false pass there writes a fabricated figure into the record. Here the asymmetry runs the other way: a false refusal costs the owner an answer he asked for; a false pass costs one fan-out that Evening Five's SD-WEEK catches. **F-04.27 is named on the predicate's face** with its five false-refusal risks enumerated and each one's opener stated in-file.

**Siting (fork A-1(b), ruled).** The predicate's evidence — the ask, the chain, the ids' provenance — exists at `donna.ts`'s `for (const tu of work)` seam and nowhere else without invented plumbing. `executeHistory` is handed an agentId and an input and has **exactly one caller**, so the tool layer would have had to receive all of it as threaded parameters. The chain is fed where it is formed: each find's yield populates `breadthIds`/`namedIds`/`knownNames`.

**THE REFUSAL RECORDS UNDER ITS OWN HAND NAME — `donna_history_refused`, never `donna_history`.** This is mechanical, not cosmetic, and it is the sitting's one substantive design decision beyond the rulings. Every reader in the estate that counts `donna_history` hands — SD-WEEK's verdict first — means by it *"a deep read actually happened."* A refusal keeping that name would have preserved the count and left SD-WEEK **RED over a wire that refused correctly**: a hollow red, the exact mirror of the hollow green this block exists to refuse. Under its own name the refusal is still AMONG THE HANDS (C-1's lane cell finds it there) while the deep-read count honestly reads zero.

**F-04.62, discharged in the paper's first four words.** The founder-vetoed bytes, shipped with zero edits:

> `REFUSED, and this is not a failure: the binder was not opened. The turn named no record, and this id came from a recents sweep rather than a search by name — so opening the binder whole would carry one engagement's entire story back to answer a question about the shape of the week. Take the shape from what you recognise and what is due. If the owner named this record, or you find it by name first, the read is lawful and nothing here stops it.`

Its register follows the seam's own shipped precedent (`provenanceHold.ts:96`'s `HELD — …`) so the estate speaks one dialect where it refuses; the hand's two genuine failures (`ERROR: donna_history needs binder_id.` / `ERROR opening history: …`) are **byte-unchanged** and structurally distinguishable, asserted both directions. **A-3, ruled: the plain leg is ABSENT, not dormant** — `historyGate.ts` authors no `plain` clause, greppable as a negative cell, so the paper cannot reach Victor's composer or a vendor by any path.

### 1.2 CURE (B) — THE RELAY SEAM (`relaySeam.ts`, seated at `loop.ts`'s voiced/plain seam)

**The carrier (fork B-1(a)).** `ToolOutcome` gains `refused?: RefusedFact[]`, authored at the door from the **identical guard conditions in the identical order** — one evaluation, two renderings, no second home to drift. Fork (b), re-deriving at the seam, was refused on F-04.36's class. The date fact reads `humanWeddingDate`, the same renderer the sentence uses, so a month-precision row can never enter the carrier as a day the estate never had (F-06.92's law, carried into the carrier).

**The predicate reads structured fields, never prose (F-06.102).** A relay contradicts its own receipt when it asserts the value the door REFUSED and does not name the value that STANDS. Both limbs are required, and the second is what keeps the honest relay honest — the shipped honest fixture speaks *"her record holds Jaipur, 5 March 2027 … were not written"* and is therefore never a contradiction, however many facts were refused. **No inference fallback exists and none may be added:** a hand with no `refused` array can never be detected and its relay ships byte-untouched.

**The form (fork B-2(α), amended from (c) on the read-first's blocking report; chair correction №1).** Her sentence is **verbatim-first and byte-exact**; then `RELAY_DEED_SEAM`; then the door's already-vetoed sentence. This is the composedTail/deed-line precedent, thrice shipped. The seam label is the founder's vetoed **V1**: newline + `— from the file: `.

**THE SEPARABILITY THIS BUYS AND OWES.** Because the tail is door speech living inside a relay field, every per-mouth arm MUST strip at the seam before judging, or an appended honest tail could **acquit a fabricating relay**. The hazard was enumerated in advance, not discovered. `RELAY_DEED_SEAM` has ONE home; `relayMouths` (the gauntlet's one per-mouth corpus, three call sites) strips at it; both directions are cells, and **M-E** below is the proof.

**THE DISTIL CONSEQUENCE — half the point, and it costs zero reader bytes.** `memory.ts`'s `donnaMessages` re-injects `listen_harvey_talk.result` into Harvey's context on every later turn of the conversation; an uncorrected echo is therefore re-taught for the life of the thread — this block's founding conviction that **the thread teaches the shape**. Because the annotated text IS the persisted result, the correction re-teaches instead and **`memory.ts` is 0-line**. Fork C no longer repeats a receipt the seam already carried.

**B-3 — THE COVERAGE BOUNDARY, STATED AS LAW** in the seam's F-06.85 comment, written against `refused` and never conflated with `plain`. At this commit: `refused` is authored at **TWO** sites (donnaLead's two single-match returns); `plain` at **THREE** (those two plus donnaFind's arrival line). Everywhere else the seam FAILS OPEN, relay byte-identical. The boundary is not only stated but **derived by command in the bench (§3.5b)**, so a new door that earns a `refused` array cannot extend the guard silently.

---

## 2 · PROOF

### 2.1 THE FLOOR AT THE CURED TREE — every count run by command

```
b06 m0 50 · m1 45 · m2 43 · m3 37 · m4 33 · m4b 24 · m4c 20 · m4d 16
f0658 20 · f0667 16 · f0681 17 · f0692 23 · advisor 16 · advisor_route 16 · 0081 12
sonnet 13 · donna_cache 16 · b0461_p6 25 · b6_floors 47 · b6_s1 24 · b6_sitting2 22
b6_door_rider 15 · b05 f0550 31 · arc_m1 53 · arc_m2 27 · arc_m3 11 · arc_m4 18
arc_m5 11 · arc_m6 20
b06_forkc_wireguard 38/38   (count PRESERVED; FOUR labeled amendments — §2.3)
b06_f0613_relay_bench 40/40 (NEW)
gauntlet --rig-selftest 351/351  (was 338; +13, sections [7] amendment + [30] — disclosed growth)
KNOWN RED, not this sitting's: b06_meter_bench 28/29 (F-06.41), carried loudly
```

**Engine gates:** `tsc -p src/engine/tsconfig.json --noEmit` clean, zero errors; `npm run build` green; `node --check` clean on all three touched `scripts/` files.

**SKIPPED, NAMED (floor-method law):** live gauntlet lanes — founder's keys, live-lane. Block 04 bench families and `checker_bench` — not run; no shipped byte reaches their surfaces. A declared judgement, never a claim of coverage.

### 2.2 BOTH-WAYS — SIX MUTATIONS OF SHIPPED CODE, never test setup; each run out-of-process after `npm run build`, tree `diff -r`-restored after the set

| Mutation | Result |
|---|---|
| **M-A** — the gate's `return HISTORY_REFUSAL_PAPER` killed (**the uncured wire**) | bench **39/40**, selftest **349/351** — reds ① and ③, the gate's own cells |
| **M-B** — the chain feed reverted (sweep ids never recorded as sweep-supplied) | selftest **348/351** — reds ①②③ |
| **M-C** — L1 disabled (`askNamesRecord` unreachable) | bench **36/40** — reds §1.2, §1.2b, §1.2c, §1.7, exactly the L1 family |
| **M-D** — the seam's detection killed at `loop.ts` | selftest **348/351** — **the pre-cure SD-REL conviction RETURNS**, the amendment's whole floor |
| **M-E** — `stripDeedTail` removed from `relayMouths` | selftest **350/351** — reds the separability cell alone |
| **M-F** — the door's `refusedOut` forced undefined (the carrier gone) | bench **39/40**, selftest **348/351** — the detection goes blind exactly as designed |

**M-A + M-B together ARE the uncured gate; M-D + M-F together ARE the uncured relay**, and each fails on exactly its own cures. Tree restored byte-clean after the set (`diff -r` against the pre-mutation copy: **IDENTICAL**), floor re-run green at 351/351 · 40/40 · 38/38.

**M-A EARNED ITS KEEP IMMEDIATELY, disclosed rather than tidied:** selftest cell ② was passing **vacuously** — an `every` over an empty refusals array. The mutation caught it, the length limb was added, and the reason is in-file beside the cell. It was found by RUNNING, not by eye.

**TWO HONEST LIMITS ON THE MUTATION SET, DECLARED, NOT PAPERED OVER:**
1. **M-C reds the bench and NOT the lane.** Section [30]④'s lawful lane carries three openers at once (L1 + L2 + L4), so killing L1 alone leaves it lawfully open. That is correct behaviour and it means **per-limb both-ways lives in the bench (§1.2 family) and lane coverage proves the composite**. Stated as the split it is, never counted as lane coverage of L1.
2. **M-D and M-F red the lane and NOT the bench.** The bench drives the pure predicates out of `dist`; the wiring is what those mutations break. The two instruments are complementary by design and neither is claimed to cover the other's ground.

### 2.3 LABELED FLOOR AMENDMENTS — FIVE, all disclosed, no count lost

**`scripts/b06_gauntlet.js` §[7] — ONE amendment, the sitting's headline.** The cell `the echo relay … FAILS SD-REL` was true for four evenings and is now false, **because the wire changed and the grader did not**. It is replaced by five substance cells: SD-REL passes on the echo lane · stripping at the seam returns her echo **byte-exact** · the tail is the door's vetoed sentence carrying both standing facts and both refusals · `relayMouths` strips so the honest tail can never acquit her relay · a clean turn carries no seam at all. **SD-REL's verdict body is BYTE-UNTOUCHED** (CE-91's grader precedent), asserted structurally in §[30]⑤. The pre-cure conviction is not lost — **M-D preserves it by mutation**.

**`scripts/b06_forkc_wireguard_bench.js` — FOUR amendments, count PRESERVED at 38/38.** §2.2 and §2.3's pins now name `refused` beside `plain` (**stricter**: dropping either now reds, where before only `plain` was named). §2.5 and §2.6's behavioural extraction begins one step earlier (`const carriedAtSeam = …`) and the sandbox is handed the seam's **clean-turn** values — `echoedPlain` empty, `voicedOut === voiced`, which is precisely the state every pre-cure turn was in. Those cells therefore still prove exactly what they proved, over a **strictly larger** slice of shipped bytes.

**No other pre-existing cell was amended, re-pinned or re-aimed.**

### 2.4 THE CHAIR'S TWO DISCREPANCIES, DISCHARGED IN THE RECORD

**(i) The census, re-derived at this tip:** `grep -c "donna_history" scripts/b06_gauntlet.js` → **21** (lines 234 1692 1694 1697 1700 1705 2214 2216 2220 2300 2737 2739 2742 2924 3030 3032 3350 3871 3957 3961 3967, pre-delta numbering). The read-first stated 20 from a table it had narrated rather than counted; the chair was right and the miss is owned. **The classification is unchanged: 0 of 21 sat in a lane-run path** — which is why C-1 existed.

**(ii) The pin-survival cell ships (§[30]⑤):** `HISTORY_FANOUT_FLOOR = 2` is byte-unmoved, asserted full-line, because `b06_m1_bench:187-188` and `b06_m2_bench:380-382` pin it and this cure moved **the wire, never the grader**. The blast-radius-zero claim now rests on that cell, not on "no lift".

### 2.5 C-1 — THE LANE-RUN CELLS (selftest section [30])

The `fanout` profile lived in the rig for a tenure and was **never passed to `runLane`** — the gap the read-first found. It now runs through the real wire against a seeded store (additive `lane.seed` hook; no existing lane supplies one, so every pre-existing lane is byte-identical). Result: **eight sweep-supplied deep reads, eight refusals, ZERO `donna_history` executed**; the paper byte-exact among the hands; the F-06.13 arm green on the real turn; and a second lane proves the **lawful** read passes untaxed through the same wire.

**③b — an honest side-effect, recorded rather than tidied.** The fan-out profile's prose quoted the eight binders it used to pull. With the binders unopened those figures have no hand behind them, so **F-06.63's money arm convicts the lane** — a different arm, correctly firing, and the visible proof that the contagion axis the fan-out fed has stopped being fed. The cell asserts the **arm**, never the lane aggregate, and says so in-file so no later reader mistakes a red lane for a failed gate.

---

## 3 · THE LIVE VERDICT IS OUTSTANDING — stated plainly, as acceptance requires

**No bench in this ZIP witnesses a model choosing honestly, and none can.** What these cells witness is that the WIRE no longer offers it the choice on the two convicted shapes.

**Evening Five's comparability sentence, for the seal's record, exactly as ruled:** SD-WEEK's green will mean **the gate refused and the shape answer stood** — not that the model chose well. SD-REL's green will mean **her relay could not ship the echo alone**. Both are declared here and neither is claimed. The residual CE-102 named a coin-flip is not cured in the mouth; it is removed from the wire on these two shapes, and that is the whole of what Path A bought.

---

## 4 · WHAT THE FOUNDER MUST DO — and what he must not

**Dashboard/console acts: NONE.** No env var, no Railway change, no Meta change, no SQL. This sitting reads no new table and writes none.

The apply block, the verify line and the git line ride the chat message this ZIP came with. **The verify's `npm run build` step rebuilds the engine** — five engine TS files moved and the gauntlet refuses to run distless by design. The verify line ends in the D-10 STOP; **the git line is its own paste-block; do not run it over a red verify.**

**Live witness: none this sitting**, by the ruling's own acceptance. Declared, not claimed.

---

## 5 · FINDINGS FILED THIS SITTING (unnumbered, per the law — the chair mints)

1. **F-06.85's REVERSE POINTER IS OWED IN THE DIRECTION W-1 SHUT.** `donna.ts`'s `DONNA_STATIC_PREFIX` — model-visible, `cache_control: ephemeral` — tells her *"every write leaves a dated line in the event trail that donna_history reads back."* After this gate that is **conditionally false**: on a breadth ask over sweep-supplied ids it will not read back. F-06.85 requires that sentence to name its mechanism in-comment. **W-1 was shut, and writing it would have invalidated the cache window for a comment.** The forward pointer is written mechanism-side (`historyGate.ts`); the reverse pointer is FILED AS OWED — the F-06.97 precedent exactly, one comment inside a protected string, needing a ruled opening and not a build.

2. **A CELL THAT PASSES VACUOUSLY IS INDISTINGUISHABLE FROM A CELL THAT PASSES.** Section [30]②'s first draft was an `every` over a filtered array with no length limb; it greened at the cured tree and greened again under M-A, where there were no refusals at all. **The mutation found it; nothing else would have.** Recorded because the estate's mutation discipline earned a specimen today, and because `every`-over-filter is a shape worth grepping for elsewhere.

3. **THE RIG CANNOT `require` A PRODUCTION CONSTANT FROM ENGINE DIST, AND THAT SHAPED THE SEAM'S BORROWING.** This file's own hygiene fence NOOPs every `engine/dist/` request — the F-RIG-1 cure. A `require` of `RELAY_DEED_SEAM` would have handed the rig `undefined` and **silently blinded every per-mouth arm**, which is F-RIG-1's own class one layer over. The literal is therefore LIFTED from its one home with a stale-throw, the `liftConst` precedent `b06_m1/m2/m3_bench` already use on this file. **Executor siting, disclosed** — the constant still has exactly one home and a drift throws rather than degrades.

4. **THE `fanout` PROFILE GUARDED NOTHING FOR A TENURE.** It was authored as F-06.13's disease specimen and was never lane-run; every SD-WEEK conviction in the estate's record came from hand-built turn objects. Named so the class is greppable: **a profile defined and never passed to `runLane` is documentation, not coverage.**

5. **`donnaLead:212`'s UNGUARDED `budget_max` WRITE STAYED SHUT WITH THE FILE OPEN.** F-06.95, untouched by rule — the file was open in front of me for the whole sitting and a money-write semantics change must be derived against the provenance hold and the F-04.13 money laws first. Named here so the restraint is on the record as deliberate, the CE-94 precedent.

---

## 6 · WHAT THE NEXT SITTING PICKS UP

Nothing from this one. **EVENING FIVE** is next by the founder's sequence: gauntlet + all four cards on the banked fixture kit (Sonal Verghese · Payal Trivedi · Rukmini Deshpande), the freeze RE-ARMED at this seal. The board's standing items are unchanged: F-06.83 · F-06.95 · F-06.99 · F-06.105 (the W-1 shelf) · F-06.106/107/108 (the wire-guard ladder rework, Stage 2 gated behind it) · F-06.109.

The clock is at ZERO; nothing in this sitting moves it. **Sequencing beyond this sitting is the founder's.**
