# TDW_19 · THE WEDDING SERVICES AGREEMENT · v3

**Status:** THE SIMPLIFIED INSTRUMENT. For the lawyer's one-line yes.
**Seat:** CE-40 · G3.2-pre · Fable-Desk, docs only · 2026-09-05
**Base:** `dream-os @ 9235849`
**Governed by:** R-40.43 (this simplification, under the lawyer's guidance) · R-40.36 · R-40.37 (deposit 30%, editable) · R-40.9 (the couple's publication switch, off by default) · R-40.7 (the credit-roll roles) · master §7 (the refusals).

## WHAT THIS FILE IS, AND WHAT v1 STILL IS

`TDW_19_CONTRACT_GENERIC_v1.md` is **the record the lawyer approved** — eighteen clauses, seven annexes, approved unchanged as carrying no detrimental liability on the vendor. It stays on the tree, untouched, and nothing here supersedes it as a record.

**This is v3: the same agreement, said the way a photographer would actually send it.** Twelve clauses. Every section fits one screen of a phone. No definitions clause — each term is defined where it is used. No boilerplate. The protections the lawyer approved are all still here; they are shorter.

**v3 is what a vendor sends. v1 is what the file cabinet holds.**

### What was dropped, and why it was safe to drop

| Dropped from v1 | Why |
|---|---|
| Clause 2, definitions | Nine terms defined in one place and used in another. Each is now defined in the sentence that uses it. |
| Clause 16, notices | A couple reading this on WhatsApp is already receiving notice on WhatsApp. |
| Clause 16, severability · entire agreement · waiver · no partnership · no third-party rights · counterparts | Boilerplate that changes no outcome between these two parties. Indian law supplies most of it without a clause. |
| Clause 15's DPDP citation | The obligation stands whether or not the contract recites the statute. One plain sentence replaces it at clause 11. |
| Clause 17's mediation ladder | Two steps before a court that neither party will use. One line of law and jurisdiction replaces it. |
| Clause 18's five sub-clauses | The mechanism is one paragraph, not five. |
| Appendix 1 | Already a separate file, `TDW_19_CONTRACT_FIELD_REGISTER_v1.md`. |

### Where each v3 clause came from

1 ← v1 clause 1 · 2 ← v1 clause 3 · 3 ← v1 clause 4 · **4 ← v1 clauses 5 + 6 merged** · **5 ← v1 clauses 7 + 8 merged** · 6 ← v1 clause 9 · 7 ← v1 clause 10 · 8 ← v1 clause 11 · 9 ← v1 clause 12 · 10 ← v1 clause 13 · **11 ← v1 clauses 14 + 15 + 17, one sentence each** · 12 ← v1 clause 18.

### Two things that have not changed

**Every number is a blank**, written like `{{deposit_pct}}`, filled by the vendor. TDW chooses none of them.

**Money reads `Rs 1,25,000`** — Indian grouping, the letters `Rs`, never the rupee sign, never `K`, `L` or `Cr`.

---

# THE AGREEMENT

## 1 · WHO THIS IS BETWEEN

Made on `{{agreement_date}}`, between:

**`{{vendor_business_name}}`** — a `{{vendor_category_words}}` business at `{{vendor_address}}`, `{{vendor_city}}`, signing through `{{vendor_signatory_name}}`, on `{{vendor_phone}}`. Called **"we"** below.

and

**`{{partner_1_name}}` and `{{partner_2_name}}`**, on `{{couple_primary_phone}}`. Called **"you"** below.

Either of you may agree things with us, and either of you can be held to what is agreed here — unless you tell us in writing that only one named person may instruct us.

## 2 · WHAT WE'RE DOING

We'll do what the attached annexes describe: **`{{annexes_attached}}`**. Those annexes are part of this agreement.

**If an annex isn't attached, that service isn't included.** Anything not written in this agreement or an attached annex isn't included either — in particular: `{{exclusions}}`.

We'll do the work with the skill and care you'd expect of a `{{vendor_category_words}}` business, ourselves or through our team.

**Changing the plan.** Either of us can suggest a change. It counts once you've both agreed it in writing *and* we've told you what it costs and you've said yes to that price.

We can say no to something that would be unsafe, unlawful, or against the venue's rules. That isn't us breaking this agreement.

## 3 · THE DATES

| Function | Date | Time | Venue | City |
|---|---|---|---|---|
| `{{function_1_name}}` | `{{function_1_date}}` | `{{function_1_time}}` | `{{function_1_venue}}` | `{{function_1_city}}` |
| `{{function_2_name}}` | `{{function_2_date}}` | `{{function_2_time}}` | `{{function_2_venue}}` | `{{function_2_city}}` |

*(one row per function)*

**These dates are the whole point of this agreement.** From the moment your deposit reaches us, we hold them for you and won't take another booking that stops us being there. Before the deposit reaches us, we hold nothing.

We'll be there and ready at the time above. If your function starts late and that isn't our doing, we stay for the hours we agreed, counted from the agreed start — anything beyond is overtime at clause 4.

A change of date is a postponement (clause 5). A change of venue in the same city is a change to the plan (clause 2). A change of city is a postponement.

## 4 · MONEY

**The fee is `Rs {{fee_total}}`** — `{{fee_breakdown}}`.

**[If we're GST-registered]** That fee is `{{gst_treatment}}` of GST. GST at `{{gst_pct}}%` is `Rs {{gst_amount}}`, so you pay `Rs {{fee_payable_with_gst}}`. Our GSTIN is `{{vendor_gstin}}` and you'll get a tax invoice for every payment. If the rate changes by law before your wedding, the amount changes with it.

**The deposit is `{{deposit_pct}}%` — `Rs {{deposit_amount}}` — paid when you sign.** It's what holds your dates. There's no separate booking amount, retainer or advance; this is that money, under one name.

**Then:**

| | | | |
|---|---|---|---|
| Deposit, on signing | `{{deposit_pct}}%` | `Rs {{deposit_amount}}` | now |
| `{{milestone_2_label}}` | `{{milestone_2_pct}}%` | `Rs {{milestone_2_amount}}` | `{{milestone_2_due}}` |
| `{{milestone_3_label}}` | `{{milestone_3_pct}}%` | `Rs {{milestone_3_amount}}` | `{{milestone_3_due}}` |

**Pay us directly** — UPI `{{vendor_upi_id}}`, or `{{vendor_account_name}}` / `{{vendor_account_number}}` / IFSC `{{vendor_ifsc}}`, or cash against a receipt. **No money under this agreement goes through any platform, and no one takes a commission on it.**

**Travel and extra hours.** For functions outside `{{vendor_base_city}}`, travel and stay are `{{travel_terms}}`. Hours beyond what the annex says are `Rs {{overtime_rate}}` per `{{overtime_unit}}`, and we'll tell you before the clock starts wherever we can.

**If a payment is late.** More than `{{late_grace_days}}` days late, we may charge `{{late_interest_pct}}%` a month on what's overdue, and if it stays unpaid we may pause work until it's cleared — but never in the week before a function, and we'll always tell you first. Time lost to a pause is added to the delivery time at clause 6. **We hand over the finished work once the full fee has reached us.**

## 5 · IF PLANS CHANGE

**Postponing.** Weddings move. Tell us in writing at least `{{postpone_notice_days}}` days before the first function and we'll move everything you've paid to new dates within `{{postpone_window_months}}` months, if we're free. **This works once.** If we're not free, you can either hold the money on account for those `{{postpone_window_months}}` months, or treat it as a cancellation below — at the slab for the date you asked to postpone, not the date of the function. If the new dates are in a busier season at our rates, you pay the difference; if quieter, there's no reduction. A postponement caused by clause 9 is handled there instead.

**Cancelling.** Tell us in writing; that date is the date of cancellation.

| You tell us | You pay |
|---|---|
| more than `{{cancel_tier_1_days}}` days before | `{{cancel_tier_1_pct}}%` of the fee |
| `{{cancel_tier_2_days}}`–`{{cancel_tier_1_days}}` days before | `{{cancel_tier_2_pct}}%` |
| `{{cancel_tier_3_days}}`–`{{cancel_tier_2_days}}` days before | `{{cancel_tier_3_pct}}%` |
| fewer than `{{cancel_tier_3_days}}` days before | `{{cancel_tier_4_pct}}%` |

What you've already paid comes off what you owe. If you've paid more, we refund the difference in `{{refund_days}}` days; if less, you pay it in `{{refund_days}}` days.

**Is the deposit refundable? `{{deposit_refundable}}`**

**If we cancel** — which we'd only do for a reason at clause 9, or if you haven't paid, or if the work would be unsafe or unlawful — we refund everything you've paid within `{{refund_days}}` days and do our best to find you someone as good for the same dates, though we can't promise we'll succeed.

## 6 · WHAT YOU GET, AND WHEN

We deliver what the attached annexes list.

**Within `{{delivery_days}}` days of your last function**, unless an annex gives a different time for a particular item. Days lost to a payment pause or to clause 9 are added on.

**How:** `{{delivery_method}}`. If it's a download link, it stays live `{{link_live_days}}` days and we'll tell you the date it expires.

**Changes:** `{{revision_rounds}}` round(s) included. Further rounds are `Rs {{revision_rate}}` each.

**We keep the original files for `{{archive_months}}` months, then we may delete them. Please take and keep your own copy.** We're not a backup service.

## 7 · WHAT WE NEED FROM YOU

- One named person we can call at each function, with their number.
- The venue's permission for us to work — including for our equipment, lighting, power, and a drone if one is in the annex — and a heads-up on any restriction they place on us, as soon as you know.
- Safe access to the spaces we need, at the times agreed, and somewhere secure to keep equipment.
- Your decisions and approvals when we ask for them. If a delay on your side pushes a deadline, clause 6's clock moves by the same amount.
- Meals and water for our team on a long day — `{{meals_provision}}` — and a word with us about anything that affects our safety or our work, or anyone who mustn't be photographed.

We'll always be courteous to you and your guests, and we ask the same. If someone on our team is threatened or harassed, we may withdraw from that function and we'll tell your named contact why, at the time.

## 8 · YOUR PHOTOGRAPHS, AND THE WEDDING PAGE

**We own the copyright in what we create.** Once the full fee is paid, **you have a permanent, worldwide licence to use it for yourselves and your family** — print it, share it, frame it, post it — forever, at no further cost. Guests you share with get it on the same personal-use terms.

**You can't** sell it, license it to anyone else, enter it in a competition as your own, or let it be used in advertising, without our written permission.

**[Optional — our portfolio]** We'd like to show your wedding in our portfolio, on our website and social media, and to people considering booking us. **You can say no now, or change your mind later** — tell us and we'll stop within `{{takedown_days}}` days, though we can't recall what's already printed or already shared by someone else.

**[Optional — the wedding page]** The platform can publish a page for your wedding, with photographs and a credit to everyone who worked on it. **Signing this agreement does not switch that on.** It is switched on only by you, in your own account, by the setting called **"Publish our wedding"** — which is **off unless you turn it on**, and which you can turn off again at any time, for any reason, without asking anyone. When it's off, the page comes down within `{{takedown_days}}` days. Nothing above overrides that switch.

**The credit.** If that page is published, everyone who worked on your wedding is credited by their role — we'd be credited as `{{vendor_credit_role}}`, and we'll name the others so they're credited too. **Nobody pays to be credited, and no credit is placed or ranked by payment.**

**Your guests.** Nobody is named in anything published without their own say-so, and any guest who asks to be taken out of a published photograph is taken out within `{{takedown_days}}` days.

**Anything you give us** — photographs, music, artwork, text — you're telling us you have the right to let us use it.

## 9 · IF SOMETHING BEYOND ANYONE'S CONTROL HAPPENS

Floods, fire, an earthquake, an epidemic or a public-health restriction, a government order or curfew, riot or unrest, a strike or a failure of transport or power we couldn't work around, serious illness or injury to us or a key member of our team, or a death in your family or ours — if any of that stops a function happening, **neither of us has broken this agreement and neither owes the other damages.**

**We'll move the wedding first, not cancel it.** Clause 5's once-only limit doesn't apply here, and everything you've paid moves with it. If after genuinely trying we can't agree new dates within `{{fm_window_months}}` months, either of us can end this in writing: we keep the value of work already done and costs we've committed and can't get back — itemised for you — refund the rest within `{{refund_days}}` days, and neither of us owes the other anything more.

This clause isn't available to someone who could have gone ahead and chose not to, and it doesn't cover a change of plan, a change of mind, or running short of money. Whoever relies on it tells the other as soon as they reasonably can, and says what they're doing about it.

## 10 · IF SOMEONE CAN'T MAKE IT

We may use employees, assistants and freelancers, and we stay responsible to you for their work and their conduct as if it were ours.

**[Optional — where you're booking a particular person]** `{{named_professional}}` will personally be there. If illness, injury or clause 9 prevents that, we'll send someone of comparable skill at no extra cost and tell you as soon as we know. **If you'd rather not accept the substitute, you can cancel that function and we'll refund what it was worth — clause 5's slab won't apply to that refund.**

Where no individual is named, we'll allocate our team as we judge best, with the number of people the annex states.

## 11 · THE SHORT ONES

**What we're liable for.** We'll work with reasonable skill and care. **Our total liability under this agreement, for everything, is limited to the fee we've actually received** — and neither of us is liable to the other for indirect or consequential loss. Cameras, lights and memory cards do fail; we use professional equipment and back up as the annex says, and if something is still lost that way and not through carelessness, we'll refund a fair share of the fee for what was lost. None of this limits liability for death or personal injury caused by negligence, for fraud, or for anything else the law won't let us limit.

**Your details stay private.** We keep your information, your guest list and your family's business to ourselves, use it only to do this work, keep it no longer than clause 6 and the law require, and never sell it or share it for marketing. You can ask us what we hold, ask us to correct it, and ask us to erase it — except for what clause 8 licenses us, what we're required to keep, and what's already published with your consent.

**Law and where.** Indian law applies, and the courts at **`{{vendor_city}}`** decide anything we can't settle between us — which we'll always try to do first.

## 12 · SIGNING

We'll send this to you on WhatsApp. You read it on your phone, we send a one-time password to `{{couple_primary_phone}}`, and you enter it and tap **"I agree"**. That is your signature. We then seal a PDF of what you agreed — carrying the date, the time, both our numbers and a digest of the document — and both of us get that PDF on WhatsApp. **The sealed PDF is the signed agreement.** This is a contract made electronically under section 10A of the Information Technology Act, 2000, and neither of us will later say it doesn't count because it was signed this way.

**If you'd rather sign on paper, say so and we will** — a signed paper copy has exactly the same effect. *(Until the flow above is live, this is how this agreement is signed.)*

---

**`{{vendor_signatory_name}}`**, for `{{vendor_business_name}}` — `{{vendor_phone}}`

**`{{partner_1_name}}` and `{{partner_2_name}}`** — `{{couple_primary_phone}}`

**Attached:** `{{annexes_attached}}`

---

# THE ANNEXES

*Attached only where the service is engaged. One page each.*

## ANNEX A — PHOTOGRAPHY AND FILM

**What.** `{{a_team}}` at each function, covering `{{a_coverage_hours}}` hours, with professional cameras and a backup body on site. Drone: `{{a_drone}}` — and where it's engaged, it flies only where the law and the venue allow; if permission is refused or it isn't safe, it doesn't fly, and that isn't a breach or a ground for a refund.

**How much.** `{{a_edited_count}}` edited, colour-corrected photographs, delivered `{{a_photo_format}}`. Film: `{{a_film}}`. Album: `{{a_album}}`.

**How long.** Teaser within `{{a_teaser_days}}` days; everything else within clause 6's time. If you're choosing images for an album, please choose within `{{a_selection_days}}` days of us sending the gallery.

**What's extra.** `{{a_extras}}`. Raw and unedited files: **`{{a_raw_files}}`**.

**One honest line.** We back up every card to `{{a_backup_scheme}}` the same night. We use licensed music in films; if you want a specific commercial track, the licence is yours to get and we may have to say no.

## ANNEX B — MAKEUP AND HAIR

**What.** `{{b_looks}}` look(s) each for `{{b_persons}}` — `{{b_persons_named}}` — by `{{b_artist_name}}` with `{{b_assistants}}` assistant(s). Trial: `{{b_trial}}`. Professional products, `{{b_product_brands}}`, sanitised brushes, disposable applicators for lips and eyes.

**How much.** Included: `{{b_included}}`.

**How long.** `{{b_time_per_person}}` per person. We arrive `{{b_arrival_before}}` before you need to be ready, and we need a clean, well-lit spot with a plug point and a chair at a workable height.

**What's extra.** `{{b_extras}}` — which may cover extensions, hairpieces, draping, nails, and lashes beyond the first pair. Travel is clause 4's.

**One honest line.** Please tell us about any allergy, skin condition, recent treatment or medication at least `{{b_disclosure_days}}` days before — **a patch test is free on request and we'd rather you had one.** We can't be responsible for a reaction to something you knew about and didn't mention, or to a test you turned down.

## ANNEX C — DÉCOR AND PRODUCTION

**What.** Décor for `{{c_areas}}`. We'll survey the venue and send you `{{c_drawings}}` drawings by `{{c_drawings_date}}`; please approve in writing by `{{c_approval_date}}`. **After that, a change is a change under clause 2 and we'll price it before we make it.**

**How much.** Everything in `{{c_inventory}}`. Items marked hired come back to us; items marked purchased are yours. Nothing that isn't listed becomes yours.

**How long.** We set up over `{{c_setup_hours}}` hours before the function and clear within `{{c_strike_hours}}` hours after. Please get us that access from the venue.

**What's extra.** Anything the venue won't permit that we then have to solve another way. Security deposit on hired items: `{{c_security_deposit}}`, back within `{{refund_days}}` days if they return undamaged. Damage caused by you, your guests or your other suppliers is charged at `{{c_damage_basis}}`; ordinary wear isn't damage, and damage we cause to the venue is ours.

**One honest line.** **Flowers are grown, not manufactured.** Colour, size and availability change with the season, the market and the day, so we may swap in a flower of equivalent quality and value without asking, and we'll tell you if it's a big one. That isn't a fault and isn't a ground for a reduction.

## ANNEX D — PLANNING AND COORDINATION

**What.** `{{d_service_level}}` — `{{d_scope}}`.

**How much.** We can commit up to `Rs {{d_authority_limit}}` on your behalf without checking each time; above that we ask you first, in writing.

**How long.** `{{d_onday_team}}` coordinator(s) from `{{d_onday_start}}` to `{{d_onday_end}}` at each function. Run-sheet to you by `{{d_runsheet_date}}`, your approval by `{{d_runsheet_approval_date}}` — the approved run-sheet is what we work to.

**What's extra.** Whatever your other suppliers charge — you pay them directly, except for `{{d_pass_through}}`, which we pay and you reimburse at actual cost.

**One honest line.** **We engage other suppliers as your agent, in your name — you contract with each of them.** We're not liable for another supplier's default or bad work unless we contracted with them on our own account, and where we have, they're named here: `{{d_own_account_suppliers}}`. If a decision can't wait on the day and we can't reach your named contact, we'll decide in your best interests.

## ANNEX E — MEHENDI

**What.** `{{e_design_complexity}}` for `{{e_bride}}`, covering `{{e_bride_coverage}}`, plus `{{e_guest_design}}` for `{{e_guests}}` guest(s). `{{e_artist_count}}` artist(s): `{{e_artist_names}}`.

**How much.** Cones are **natural henna, made fresh — henna, essential oil, sugar and lemon, and nothing else. No black henna, no PPD**, and we'll say so to any guest who asks.

**How long.** About `{{e_bride_hours}}` hours for the bride, `{{e_guest_minutes}}` minutes a guest, within `{{e_guest_total_hours}}` hours in total. Leave the paste on at least `{{e_paste_hours}}` hours. We'll need good light and a seat at a workable height.

**What's extra.** Guests beyond `{{e_guests}}` or time beyond `{{e_guest_total_hours}}` hours, at clause 4's rate.

**One honest line.** **Henna stains differently on everybody.** Colour depends on body heat, skin, how long the paste stays on, aftercare and the weather. We'll give you written aftercare and tell you what to expect, but **we can't promise a particular shade, and a lighter stain isn't a fault or a ground for a refund.** A patch test is free on request `{{e_patch_days}}` days ahead, and worth it if you've not had henna before.

## ANNEX F — VENUE

**What.** `{{f_spaces}}`, from `{{f_hours_from}}` to `{{f_hours_to}}`, for up to `{{f_capacity}}` guests. Included: `{{f_inclusions}}`. Not included: `{{f_exclusions}}`. Parking for `{{f_parking}}` cars.

**How much.** Numbers to us by `{{f_headcount_date}}`, final by `{{f_final_headcount_date}}`. **[If we're catering]** `{{f_catering}}` — menu agreed by `{{f_menu_date}}`, `Rs {{f_per_plate}}` a plate, charged on `{{f_guaranteed_minimum}}` or the final number, whichever is higher.

**How long.** The hours above. Beyond them, `Rs {{f_overstay_rate}}` an hour.

**What's extra.** Your other suppliers may work here subject to `{{f_outside_vendor_policy}}`, and any access charge is `{{f_outside_vendor_charges}}` — stated now, not raised later. Security deposit `Rs {{f_security_deposit}}` by `{{f_deposit_date}}`, back within `{{refund_days}}` days less damage, overstay and any cleaning beyond the ordinary.

**One honest line.** We hold `{{f_venue_licences}}`. **You'll need `{{f_couple_licences}}`** — which may include music rights, a liquor permit, and permission for fireworks or open flame. Without one, we have to stop that activity, and that isn't a breach by either of us. Amplified music stops at `{{f_music_curfew}}`, and we'll hold that line.

## ANNEX G — OTHER SERVICES

*For a service the annexes above don't cover — a designer, a jeweller, a performer, a content creator.*

**What.** `{{g_service_name}}` — `{{g_service_description}}` — at `{{g_functions}}`, by `{{g_team_count}}` person(s): `{{g_team_named}}`. We provide `{{g_vendor_provides}}`; you provide `{{g_couple_provides}}`.

**How much.** `{{g_deliverables}}`, delivered `{{g_delivery_method}}`.

**How long.** `{{g_hours}}` hours from `{{g_start_time}}` at each function; delivery within `{{g_delivery_days}}` days of `{{g_delivery_trigger}}`. Fittings or rehearsals: `{{g_fittings}}`, `{{g_fittings_count}}` session(s) at `{{g_fittings_location}}`.

**What's extra.** Alterations after the final fitting, `Rs {{g_alteration_rate}}`. Items hired rather than sold — `{{g_hired_items}}` — come back to us, and Annex C's damage line applies.

**One honest line.** `{{g_special_terms}}`.

---

# APPENDIX — THE QUESTIONS, CLOSED

v1's Appendix 2 asked the lawyer seven. This is where they stand.

**Q1 — the electronic signature, and what the audit record must contain.** **Answered at R-40.43.** Clause 12 is drafted to that ruling. *The ruling's text is not on the tree at base `9235849`; this seat records the closure and does not restate the answer, because a legal answer paraphrased from a relay and sent back to a lawyer is worse than an open question.* The chair's docs cut banks it.

**Q7 — stamp duty.** **Answered at R-40.43**, same terms as Q1.

**Q3 — the GST block.** **STANDS — and it is G3.2's one product question.** Whether the switch at `{{gst_treatment}}` and the optional block are right for a registered vendor *and* for one below the threshold decides what the composer prints and what a tax invoice must carry. It is a product question, not a contract question, and the contract text does not change on either answer.

**Q2 — the cancellation slab.** **Struck.** The lawyer approved v1's clause 7 as carrying no detrimental liability on the vendor, and v3's clause 5 is that slab with fewer words and the same tiers.

**Q4 — the liability cap.** **Struck**, for the same reason: v1's clause 14 was approved, and clause 11 is it, shorter.

**Q5 — the credit roll and consent.** **Struck.** Clause 8 makes publication depend on a switch the couple owns, off by default, withdrawable, and says in terms that signing does not turn it on. The lawyer approved that construction.

**Q6 — data protection.** **Struck as a contract question.** The obligation is the vendor's whether or not the contract recites the statute; clause 11's privacy sentence states what she does. Reciting the DPDP Act taught a couple nothing and lengthened the document.

**What the lawyer is asked for now: one line.** Does this shorter instrument carry the same protection for the vendor as the version already approved? If yes, v3 is what a vendor sends.

---

## PROVENANCE

Authored at `dream-os @ 9235849` by the CE-40 G3.2-pre docs seat under **R-40.43**. The lawyer-approved record is `TDW_19_CONTRACT_GENERIC_v1.md` and is unchanged by this file.

**No byte here is vendor-facing product copy.** The product's bytes are minted at G3.2's mock, under the copy law's founder veto. The blanks are catalogued in `TDW_19_CONTRACT_FIELD_REGISTER_v1.md`, which tracks **this** file from this cut forward.
