# TDW — CE SUCCESSION NOTE 34
(authored by the banking thirty-fourth chair, 2026-08-15. You are the
thirty-fifth chair. TRUST EVIDENCE OVER NARRATIVE — including this
note; catching its drifts is credited, as my seat's catches of the
CE-34 kickoff were, and as the LE seats' catches of ME were. This
chair was corrected thirteen times in one seating. Read §6 before
you believe anything here.)

## §1 · SEAT
Role: CE. You verify by command at your own clones, rule on forks,
author kickoffs, write the register, and NEVER execute — the founder
pushes, LE seats build. Fetch-first fresh SIBLING-FULL clones of
devjroy-dev/dream-os + devjroy-dev/dreamos-pwa; derive both tips
(expected dream-os b473651 · dreamos-pwa c6e631d, plus this note's
own docs push — the founder's push output is the only landing claim).

NO LE SEAT IS ACTIVE. You inherit an empty floor and two charters
the founder has already named as your first two acts (§3① and §3②).

## §2 · THE READ LADDER, whole, in order
1. docs/TDW_BUILD_PROTOCOL.md — §7/§11 twice.
2. TDW_CE_34_SEATING_KICKOFF.md + TDW_CE_SUCCESSION_NOTE_33.md
   (both in docs/specs/) — the seat you inherit and the one before.
3. docs/specs/TDW_15_ROOMS_FINAL.md WHOLE — its CE-34 amendment
   (the R-4/P2.3 stale-resolver strike; envelopes take THE WALL)
   is part of the contract, read twice.
4. docs/specs/TDW_BRIDE_TRACK_AMENDMENT_ONE.md WHOLE — CE-30's
   §3.5 pass over blocks 13–17. §2.15 governs Block 15; §4 is the
   COLLISION REGISTER (standing, carried in every bride-track
   kickoff until each line dies by name); §7 is THE LIVE-USAGE
   REFRAME — real couples, the test-accounts-only assumption is
   DEAD on the couple side. THIS DOCUMENT WAS MISSING FROM MY OWN
   SEATING LADDER AND I RULED FOR TWO SITTINGS WITHOUT IT.
5. docs/BRIDE_PARITY_MATRIX.md + docs/FROST_BLOOMS.md (BOTH IN
   dreamos-pwa, not dream-os) — the matrix is THE CONTRACT; rows
   tick in the document itself. FROST_BLOOMS is stale by 34 and
   carried by name (F-13.12's class).
6. docs/handovers/ — TDW_15_P1_ZIP1, TDW_15_P1_HOTFIX_F1506,
   TDW_M_SCHEMA_REG (dream-os) · TDW_15_P1_ZIP2 (pwa). The ZIP2
   handover's STRIPPER section is required reading before you rule
   on any comment-stripped cell.
7. docs/specs/TDW_09_QUEUE_ADDENDUM_CE33.md — Row 9's SIX items;
   ITEM 6 is the envelope WhatsApp arm, re-homed there on the
   founder's word at CE-34.
8. docs/FINDINGS_LOG.md — bands to CE-220, plus everything CE-34
   minted that is not yet banked (§4).
9. docs/db/PUBLIC_SCHEMA.md — REGENERATED 2026-08-15 at ladder tip
   0125, repo tip af3cb04. It is CURRENT. This is the first note in
   some time that can say so.

## §3 · FIRST MOTIONS, in order
① GATES + FLOORS both repos before anything (npm ci; dream-os
  build; pwa tsc; both governed runners --check by name, clean
  trees, sibling-full — three benches REFUSE without the sibling
  clone present and that refusal is not a delta).
② CHARTER TDW_15 P2 ZIP 2 — THE PWA HALF, AND READ §5 FIRST. The
  work was BUILT and typechecked at 0 by a seat that banked before
  cutting; the pwa tip is UNMOVED at c6e631d, so NONE OF IT IS IN
  THE TREE. Treat it as a rebuild against standing rulings, not a
  resume. Every ruling it needs is listed at §5; four bench
  amendments ride it (R-34.52/.53/.54 and F-14.25's declaration).
③ CHARTER THE UNNAMED-BRIDE AUDIT — the founder's words: "find out
  why the bride name is not a sine qua non for opening a new
  account, as even now brides are signing up unnamed." I derived
  the mechanism before writing this and I am handing you the
  derivation, NOT the ruling (§7). Do not let a seat re-derive from
  scratch; do not let one cure it without a copy veto.
④ THE FOUR AMENDMENT-BEARING CENSUS: R-34.53 requires an ITEMISED
  control delta before you ratify any new parity figure. Do not
  pre-approve a number you have not seen derived.
⑤ M-CELLSWEEP, chartered by CE-34, UNSTARTED — read-only census
  across both repos for cells pinning WHERE a constant lives
  rather than WHAT holds about it (F-15.12, two instances, one per
  repo). Findings only. Sequence at the founder's word.

## §4 · WHAT CE-34 MINTED AND WHAT IS UNBANKED
MINTED, ALL UNBANKED IN THE REGISTER — your first docs push owes
these lines, re-derived at origin against the UNION before you
allocate anything above them:
  F-15.5   POST /couple/events silently falls back to kind='other'
           (events.js:63) while PATCH 400s the same vocabulary
           (:126). One vocabulary, two doors, two behaviours.
           F-OB.3's class, couple plane. OPEN.
  F-15.6   CURED (P1 hotfix). Its lesson stands as doctrine: a
           bench cell can be green, non-vacuous and mutation-proven
           while pinning a value the database has never accepted.
           Column-constraint census is now part of R-33.1.
  F-15.7a  The payment date is collected on both planes and stored
           on neither. FOUNDER-PARKED, P2's.
  F-15.7b  brideTools.js:416 tells the model the date "Defaults to
           today" — false capability in a live agent's mouth, on a
           lane with real brides. OPEN, rides Row 9 ITEM 6's lift.
  F-15.8   expenses.tsx:71 fmtDate doubles the suffix on a
           timestamptz and returns the raw string; a photo receipt
           has no receipt_date, so it prints a database timestamp.
           OPEN, live, visible.
  F-15.9   The photo affordance sends no typed fields. OPEN; it is
           P2's seam, not a P1 defect.
  F-15.10  THE COUPLE PLANE'S CATEGORY VOCABULARY IS PRE-0123 —
           couple_bookings_category_check carries photographer/
           videographer/mua/venue/caterer/florist/music and LACKS
           hairstylist, jewellery, content_creator. Only designer,
           decor, other agree with the canonical eleven. Second
           limb: ALLOWED_CATEGORIES is re-declared inline at
           bookings.js:87 instead of importing the one home. A
           bride cannot log a jewellery booking. OPEN — needs a
           migration, a backfill on live rows, and a plane-crossing
           census. ITS OWN MICRO.
  F-15.11  CURED (P2 ZIP1). The receipt column list had three homes
           under a comment claiming one; two client paths read
           different doors for the same rows. Cured to
           receiptColumns.js. The two-doors question is NOT cured.
  F-15.12  A cell that pins WHERE a constant lives is a tripwire
           against ever giving that constant a better home. Two
           instances (dream-os 2.4, pwa obp_vendor_form 1.5/5.1).
           M-CELLSWEEP chartered.
  F-14.25  --delivery manifest mode exists in dream-os's
           run-floor.sh and NOT in dreamos-pwa's. There is
           currently NO HONEST WAY TO FLOOR A PWA DELIVERY TREE.
           F-14.16's cure landed in one repo of two. Port is a
           micro, queued. OPEN.
  F-SW.6   CLOSED, self-cured at the 2026-08-15 regen, no hand-edit.
  F-SW.7   CLOSED. The register survives its own generator,
           demonstrated on the real document.
  F-SW.8   The header is variable-height and the tree line-cites it
           (brideEngine.js:193/:1681/:2159, crew.js, admin/*,
           requireCircleMemberAuth.js, 20+ handovers). Every
           register row shifts every anchor below it; the sentinel
           moved 1141→1142→1180 in one session. OPEN, unruled. It
           bites when the register grows a third row.
  R-33.10  A non-vacuity claim is made from the RUN'S OWN OUTPUT,
           never from a grep over it. A mutation reporting no red
           is either a passing cell or a broken instrument, and the
           two are indistinguishable until you read the run.
           (§ is multi-byte; . matched one byte; the grep could not
           have hit. An LE caught this in its own instrument.)

## §5 · TDW_15 P2 ZIP 2 — THE RULINGS IT MUST BE REBUILT UNDER
Every one of these was ruled at CE-34 and none is in the pwa tree.
A seat that rebuilds without them will re-open settled forks.
  R-34.28  PRESS-TO-FILE, not drag. The estate has one drag surface
           (admin, desktop, file upload) and it ships a press
           fallback beside it. HTML5 DnD does not fire on touch.
           Drag is deferred, additive, chartered separately. The
           spec's word "drag" is AMENDED by the chair, not
           reinterpreted by the seat.
  R-34.29  The hairline fill is inkSoft below the threshold and
           accent at full strength above; the rail stays line.
           NOT ink→accent: accent is the arc, the name and the
           rules, and the rail is that same terracotta at 0.14, so
           the crossing would have been a brightness change on a
           decorative hue. No new token.
  R-34.22  Two emptinesses, never conflated: unfiled (envelope_id
           NULL) and untyped (amount NULL). A filed receipt can
           contribute zero. The 90% signal is a claim about TYPED
           spend only.
  R-34.30  COPY, founder-ruled: NO seed envelope names (the room
           opens empty) · the tray's empty line says the good state
           quietly · the 90% signal is WORDLESS, no label authored
           · the photo-receipt line sits ON THE RECEIPT ROW, once,
           quiet, never at the envelope, never an error state. The
           exact strings return to the chair before the ZIP cuts.
  R-34.31  The canonical eleven are THE PICKER, not seeded data.
  R-34.33  CAT_LABEL + labelFor lift VERBATIM from
           app/vendor/onboarding/page.tsx to lib/frost/
           categoryLabels.ts — a MOVE, not an edit. ZERO category
           label strings authored. The eleven are FOUNDER-SIGNED
           (2026-08-13): Event Planner · Designer · Photography &
           Videography · Make up Artist · Hairstylist · Jewellery ·
           Decor · Venue & Catering · Performer (Anchor, DJ,
           Choreography) · Content Creator · Something else.
           I ONCE RULED THESE BE TITLE-CASED FROM THE TOKENS. FOUR
           WOULD HAVE BEEN WRONG. An LE refused and cited the
           founder's signature. See §6.
  R-34.34  The couple plane gets a smallest additive READ door
           serving allowed: VENDOR_CATEGORIES imported from
           src/agent/categories.js. The picker iterates the
           RESPONSE, never Object.keys(CAT_LABEL). It does NOT cure
           F-15.10 and the handover must say so.
  R-34.52  obp_vendor_form 1.5 and 5.1 amend to assert the
           INVARIANT (declared in categoryLabels.ts · imported by
           the page · rendered through labelFor) with the eleven
           still byte-frozen AT THEIR NEW HOME. 1.2 untouched.
  R-34.53  tdw09_frost_parity 3.0/3.1/3.2 re-baseline by LABELLED
           amendment, delta itemised control-by-control,
           ratify-or-revert stated. The FIGURE is ratified by the
           chair against the itemisation, never pre-approved.
  R-34.54  tdw13_d4_extraction 2a's ruled-edit allowlist grows by
           ONE — the ExpenseSlice type line, verified present in
           the pre-extraction corpus before it is honoured. The
           prose moves seven→eight. A NINTH still reds the cell.
  F-14.25  The pwa floor cannot honestly floor a delivery tree. The
           seat DECLARES the probe's STOP with base reds named and
           delivery files declared; it does not resolve it.
Also standing: journey.ts:161's column census updates with
envelope_id (the file's own census, not bookkeeping) and no
/*-forming literal enters any string in a counted file.

## §6 · WHAT I GOT WRONG, SO YOU DO NOT REPEAT IT
Thirteen corrections in one seating, and the pattern in almost all
of them is ONE THING: I asserted from narrative — a spec's ink, a
prior kickoff's parenthetical, a file-level import, a careful
mental model — instead of running the command.
  · I ruled the P1 record-payment sheet as new build. It had
    shipped, and the matrix I had read said so.
  · I said P1's envelope-tag shipped behind a flag. It never
    shipped; there was no flag.
  · I ruled the eleven labels be title-cased. They were signed.
  · I said "two live consumers" from a file-level import when the
    symbol had zero callers.
  · I said the ladder was four behind with 0126 landed. There is
    no 0126.
  · I said the holes list would go 19→18. It went 19→17, and I had
    reasoned it instead of running it — which is exactly what a
    typed integer does to every reader who meets it.
  · I ruled a hand-edit of script-owned prose, then ruled a
    byte-unchanged copy move that carried two claims the same
    delivery falsified.
  · I miscounted a five-word regex as six inside the ruling that
    made count-honesty acceptance-blocking.
EVERY ONE was caught by an LE seat reading at its own tip. The
protocol works when the chair is wrong. It only works if the seats
are told, in the kickoff, that catching the chair is CREDITED —
say so in every kickoff you write.

## §7 · THE UNNAMED-BRIDE AUDIT — THE FOUNDER'S CHARGE, AND MY
DERIVATION HANDED FORWARD (not a ruling; the fork is yours)
Derived at dream-os b473651 / dreamos-pwa c6e631d, by command:
  · app/(landing)/page.tsx:801-806 — "Your first name" is a PLAIN
    INPUT. No required, no validation, no error path.
  · :833 — the Send-code button's disabled expression is
    `phone.length < country.maxDigits || (role === 'Maker' &&
    !joinCategory)`. The VENDOR's category IS gated. The bride's
    NAME IS NOT. A vendor cannot pass that button without a
    category; a bride can pass it with an empty name.
  · :489 — the body sends `name: joinName.trim() || undefined`.
    Empty name becomes ABSENT, not empty-string.
  · src/api/couple/auth.js:477 — `((req.body && req.body.name) ||
    '').trim() || null` — absent becomes NULL, and provisionRole
    is called with it. No 400, no refusal.
So the name is optional at THREE layers and refused at none, and
the pattern is asymmetric: the same screen gates the vendor's
category and not the bride's name. The account is created
successfully, unnamed, and the estate then has a bride whose every
surface — Mira's address, the home screen, the join screen (TDW_14
D-5's whole subject) — has nothing to render.
WHAT I DID NOT DERIVE, and the audit owes: how many live rows are
affected (founder SELECT, and it is his to run) · what those
surfaces DO today with a null name (fallback, blank, or crash) ·
whether the WhatsApp lane can recover a name conversationally
after the fact · whether TDW_14 D-5c's home-screen-name cure was
built assuming a name always exists.
THE FORK YOU MUST NOT LET A SEAT PICK ALONE: making the name
required is a GATE ON A LIVE SIGNUP FUNNEL with real couples
arriving (Amendment One §7). A gate that refuses is a gate that
loses signups. The alternatives — ask later in the WhatsApp lane,
ask on first app open, accept null and render honestly — are
product decisions with copy attached, and the copy is the
founder's veto. Charter the audit READ-ONLY: derive, count, and
enumerate the forks with costs. Do not charter a cure in the same
sitting.

## §8 · LAWS you inherit whole
The Block 14 seal §6 (R-33.1–.9) + R-33.10 (§4) + NOTE 33 §5's
sentences + NOTE 32 §5's — they compound, never replace. W-1 shut
everywhere except where a charter lifts it byte-by-byte (Row 9's
ITEM 6 will; nothing in Block 15 does). R-31.2 on brideTools.js /
brideEngine.js / miraSoul.js — Row 9's files, read-only to every
other seat. LD-8: append-only, and a RESERVED address is redeemed
below the tip legitimately — 0098 and 0088 are both witnessed
precedent, and PUBLIC_SCHEMA.md's own header paragraph exists to
stop you convicting one by arithmetic. NEVER HAND-EDIT
PUBLIC_SCHEMA.md, including to seed. The founder's standing
instructions: vetoes simplified with recommendations; one-word
rulings where possible; his sequencing absolute; his push output
the only landing claim; SQL and tests as placeholder-free paste
blocks. REGISTER-IS-THE-CHAIR'S — re-derive ranges at origin
against the UNION (log + tree + commit subjects) before any
allocation; handover ink is a committed home and is not
renumbered.

## §9 · CANDIDATE TO RIDE YOUR SEATING PUSH
This note, and the FINDINGS_LOG band for §4's mints — cut as a
docs-only ZIP, no closure line the founder has not witnessed.
Beyond that: P3 (masthead, and Amendment One §2.15 re-pointed it
off briefing.js — F-15.3), then P4/P5/P6; F-15.10's micro; the
F-14.25 port; M-CELLSWEEP; Row 9 on the Fable seat.
Sequencing beyond this note is the founder's.
