# TDW_07 P2 — EXECUTOR HANDOVER
**Block 07 (DISCOVER) · P2: Discover Profile + the folded F-07.5 cure + the F-07.4 reconciliation at the raise, with the template-approval rider.**
**Session role:** EXECUTOR (Opus-LE) · **Base:** `dream-os 97e1c1a` · `dreamos-pwa 2399c74` (both re-derived fetch-first from fresh clones at sitting open; trees clean, no predecessor residue).
**Authority:** the fifteenth chair's P2 kickoff + CE addendum → READ-FIRST PART ONE → the CE ruling §A–§I → READ-FIRST PART TWO → the CE ruling on Part Two → the founder's fixture ruling, his name ruling, and his copy word 「 go 」.

> This document rides the ZIP. It is not a CE-numbered entry and it touches neither `FINDINGS_LOG.md` nor the masterplan (the clobber law).

---

## 1 · WHAT SHIPPED

### dream-os (13 files)

| File | State | What |
|---|---|---|
| `scripts/b06_m0_bench.js` | MODIFIED | F-07.5 cure 1/3 — §7.3 range-pinned to `M0_RANGE`, title re-worded. |
| `scripts/b06_m2_bench.js` | MODIFIED | F-07.5 cure 2/3 — §6.6 range-pinned; `SEAL` hoisted to file scope (one hash, two readers). |
| `scripts/b05_f0550_ping_drain_bench.js` | MODIFIED | F-07.5 cure 3/3 — §6.4 range-pinned. **Title untouched**; only its assertion message named 0101. |
| `scripts/b6_s1_bench.js` | MODIFIED | Labeled amendment — the allowlist cell re-aimed from tail-position to membership. Count preserved. |
| `src/lib/vendor/discover.js` | MODIFIED | The 5→6 raise at the ONE constant · `min_portfolio_images` on the status response · F-07.4's reconciliation stated at the gate. |
| `src/lib/vendor/profileScore.js` | MODIFIED | The travel term at 0.10 + the proportional re-normalisation · `meterHints()` + `SECTION_ORDER`. Extended in place, never twinned. |
| `src/api/vendor/me.js` | MODIFIED | Three additive allowlist entries · `BOOLEAN_FIELDS` guard · GET/PATCH shape extension (F-07.9's server half). |
| `src/api/couple/discover.js` | MODIFIED | The feed's select and score call gain the travel pair. |
| `src/lib/templates.js` | MODIFIED | `demo_lead_alert.status` → `'approved'`; both stale comment paragraphs re-authored (F-06.85). |
| `docs/TEMPLATES.md` | MODIFIED | §3 tracker row → approved, 2026-07-29, Utility retained. |
| `scripts/b07_p1_bench.js` | MODIFIED | §2.3 gains the travel field; §8.4 re-aimed to the new truth; M-8 inverted. Count preserved at 72. |
| `scripts/b07_p2_bench.js` | NEW | The P2 floor, 48 cells. |
| `docs/specs/TDW_07_P2_HANDOVER.md` | NEW | This file. |

### dreamos-pwa (9 files)

| File | State | What |
|---|---|---|
| `app/vendor/discover/profile/page.tsx` | NEW | **Discover Profile** — the screen. |
| `lib/vendor/discoverFloor.ts` | NEW | The floor's one home on this side of the wire, server-preferred. |
| `components/vendor/AtelierForm.tsx` | NEW | Pure move of the five form primitives + `A`/`F`. Settings' rendered output unchanged. |
| `hooks/vendor/useSettings.ts` | MODIFIED | **F-07.9's client cure** + the three P2 fields. |
| `lib/vendor/types/vendor.ts` | MODIFIED | The me-response and update-request shapes; `min_portfolio_images` optional on the status. |
| `components/vendor/Header.tsx` | MODIFIED | The "Discover Profile" entry re-points to the screen that now exists. |
| `app/vendor/settings/page.tsx` | MODIFIED | The §C split; imports the moved primitives; the vetoed "Moved to your Discover Profile." card. |
| `app/vendor/discover/page.tsx` | MODIFIED | Both floor copies read one source; the vetoed gate line. |
| `scripts/tdw07_p2_profile.proof.mjs` | NEW | The pwa half, 34 cells. |

**Not touched, by ruling:** every soul / lens / loop / engine file (W-1) · the interceptor, the tripwire and the vendor-engine wire · `/vendor/portfolio` and its nine inbound edges · `src/api/demo/*` · the BottomNav · `src/api/admin/discover.js` (F-07.10 is named, not cured) · `lib/vendor/cabinet.ts` and `PeekNav.tsx` (the off-register twin, named not touched per CE §I).

---

## 2 · THE FOUNDER'S FIXTURE PASTES — VERBATIM (the walk's evidence base)

Both run by the founder in the Supabase editor and pasted into the sitting. The smoke card below was authored **from** these rows, never before them.

**Paste 1 — `9888294440`:**
```
vendor_id,stored_phone,business_name,vendor_tier,agent_tier,discover_eligible,discover_request_state,discover_paused,rate_display,rate_min,rate_max,about_filled,tag_count,open_to_travel,travel_notes_filled,instagram_handle,photos_total,photos_approved,photos_pending,photos_rejected,hero_rows
23165e38-6510-4639-ab6a-9f35bab93742,+919888294440,null,prestige,entry,true,approved,false,true,null,null,false,0,false,false,null,0,0,0,0,0
```

**Paste 2 — the Swati account (the founder's fixture ruling re-aimed the walk here):**
```
vendor_id,user_name,business_name,stored_phone,category,city,vendor_tier,agent_tier,discover_eligible,discover_request_state,discover_paused,rate_display,rate_min,rate_max,about_filled,tag_count,open_to_travel,travel_notes_filled,instagram_handle,routing_handle,photos_total,photos_approved,photos_pending,hero_rows
a8c52506-d363-4a36-9cec-09b50cc32c4c,Swati Roy,null,+918595356978,makeup,Delhi,trial,entry,true,approved,false,true,null,null,false,0,true,false,Makeupbyswatiroy,MAKEUPBYSWATIROY,2,2,0,0
```

**Two facts settled by them and banked for every future sitting:** phones are stored `+91`-prefixed, so a loose `LIKE '%digits%'` is the correct match; and Swati's tiers **agree** (trial/entry), so unlike `9888294440` her walk has no CE-109 mismatch — step 0 is a clean control, not a hazard.

**Her score, derived at the desk from paste 2:** `0.220` at P1's tip → `0.200` after the raise → `0.180` after the travel re-normalisation. Both real eligible accounts keep their relative order (the other is 0.000 throughout).

---

## 3 · THE FLOOR, AT THE EXECUTOR'S OWN HAND

Method as always: **exit code is the verdict, count is the number.** `npm ci` + `npm run build` green before any bench ran. *(NOTE_14 §1's staleness warning earned its keep: my first `m0` run without the engine dist reported a false 34/37 with two phantom mutation reds — disclosed at the time, not carried.)*

**THE THREE EX-REDS RETURN WHOLE — F-07.5 CLOSED:** `m0 50/50` · `m2 43/43` · `f0550 31/31`.

**Byte-stable, matching the sealed list:** `selftest 386` · `guard 113` · `relay 40` · `m1 45` · `m3 37` · `m4 33` · `m4b 24` · `m4c 20` · `m4d 16` · `f0658 20` · `f0667 16` · `f0681 17` · `f0692 23` · `advisor 16` · `advisor_route 16` · `0081 12` · `sonnet 13` · `donna_cache 16` · `b0461_p6 25` · `b6_floors 47` · `b6_sitting2 22` · `door_rider 15` · `arc_m2 27` · `arc_m4 18` · `arc_m5 11`.

**One labeled amendment, count preserved:** `b6_s1 24/24`. See §6.

**NEW:** `b07_p2_bench 48/48` GREEN · `tdw07_p2_profile 34/34` GREEN. Both run from `/tmp` (Q-SP-5).
**Carried green:** `b07_p1_bench 72/72` (two cells reconciled) · `tdw07_p1_discover 35/35`.

**KNOWN RED, carried loudly:**
- `meter 28/29` (F-06.41) — as always.
- **`b05_f0555_media_dedupe_bench 22/23` — F-07.11, newly disclosed.** §6.2, *"the guard speaks to NOBODY."* Reproduced at the clean tip with this delta stashed: identical. Not this sitting's, on no prior disclosed list, duration-red unknown. Chair-minted F-07.11; cure is its own founder-sequenced micro.
- `scripts/test-shape.js` exits 1 at clean origin — it calls live Haiku. **Diagnostic, not a floor bench**; named, not counted.

**pwa:** whole-tree `tsc --noEmit` **ZERO** on an installed, `.next`-cleared tree.

---

## 4 · NON-VACUITY — FOURTEEN MUTATIONS, ALL AGAINST PRODUCTION CODE

Each applied to a **production source file** (never a fixture, never test setup), the bench re-run, the file `cmp`-restored byte-identical.

| # | Production byte mutated | Observed |
|---|---|---|
| P2-1 | `discover.js` — `MIN_PORTFOLIO_IMAGES` 6 → 5 | RED 41/48 |
| P2-2 | `profileScore.js` — `TERM_WEIGHTS.travel` 0.10 → 0 | RED 45/48 |
| P2-3 | `profileScore.js` — travel term reads `open_to_travel`, not the notes | RED 47/48 |
| P2-4 | `profileScore.js` — `SECTION_ORDER` re-ordered (hero after about) | RED 47/48 |
| P2-5 | `me.js` — `'about'` removed from `ALLOWED_FIELDS` | RED 46/48 |
| P2-6 | `me.js` — the `BOOLEAN_FIELDS` guard loop defanged | RED 44/48 |
| P2-7 | `me.js` — `instagram_handle` dropped from the GET response | RED 47/48 |
| P2-8 | `discover.js` — `min_portfolio_images` removed from the status | RED 46/48 |
| W-1 | `discoverFloor.ts` — the constant 6 → 5 | RED 33/34 |
| W-2 | `discover/page.tsx` — `{floor}` reverted to the word "five" | RED 31/34 |
| W-3 | `useSettings.ts` — `instagram_handle` blanked again | RED 32/34 |
| W-4 | `Header.tsx` — the entry re-pointed at `/vendor/settings` | RED 33/34 |
| W-5 | `profile/page.tsx` — a second `#C9A84C` added | RED 33/34 |
| W-6 | `profile/page.tsx` — `formatRs` swapped for a `₹` template | RED 33/34 |

**Plus the cure's own both-ways proof:** widening `M0_RANGE` to swallow `97e1c1a` returns `0101_profile_controls.sql` and reddens §7.3 (50 → 49). And the `b6_s1` amendment's honesty proof: deleting `'slot_capacity'` from the allowlist returns that bench to 23/24.

---

## 5 · WHAT THE SITTING RETIRES

**(i) The P1 §8.3 founder-run pause `UPDATE` is RETIRED.** P1's smoke step ⑤ paused the test vendor by SQL because the vendor-facing switch did not exist. It exists now, at Discover Profile's Pause section, and the round trip is benched server-side (`b07_p2_bench` §6: set → GET reads it back → the real feed excludes → approval retained). **No founder SQL is required by this delivery's walk.**

**(ii) The pwa's second and third copies of the photo floor are RETIRED.** `portfolioTotal < 5` and the word *"five"* are gone; both read `photoFloor(status?.min_portfolio_images)`, which prefers the number the server enforces.

---

## 6 · DECLARED DEVIATIONS AND SELF-CATCHES

**(i) The screen is named Discover Profile, at `/vendor/discover/profile`.** The spec's P2 heading says "Profile Studio" and the charter says `/vendor/profile`. **Founder-ruled, veto discharged in-session** — 「 profile studio should be in discover. not studio 」 · 「 it shouldnt be named profile studio 」 · 「 And discover profile is perfect 」. The estate already used STUDIO for the top-level mode and for the prestige-gated Studio Suite; a third would have been one word doing three jobs. `Header.tsx:191` had shipped the label "Discover Profile" long before the screen existed.

**(ii) `b6_s1_bench`'s allowlist cell — labeled amendment, count preserved.** Its regex matched `[…'slot_capacity']` **through the closing bracket**, so it asserted "slot_capacity is allowlisted AND nothing follows it." P2's three lawful additions reddened it at 23/24 — B6-S1's own R-B6-16 precedent (*the smallest change is a list entry*) punished by a bench bound to the list's shape rather than its content. Re-aimed at membership; deletion still convicts.

**(iii) Two bench cells I authored vacuous and caught by mutation before delivery.** `b07_p2_bench` §3.4 (the tie-break) greened under its own mutation twice: draft one because the array is built by `SECTION_ORDER.filter()` and JS sort is stable, so the comparator limb is belt-and-braces; draft two because it derived its prediction **from the constant under test** — tautological. Draft three writes the expectation out and reddens correctly.

**(iv) Two harness cells that convicted their own comments.** `tdw07_p2_profile` §4.2 and §4.4 grepped the bare words `localStorage` and `cabinet` and failed on this delivery's own explanatory prose — the same defect P1's executor filed as its deviation (iv), reproduced one sitting later. Cured by a comment-stripper so both cells judge code; **softening the comments was refused by name** — a green bought by deleting evidence is not a green.

**(v) A cut that removed two fields the ruling kept.** Trimming settings' moved sections took `name` and `style_notes` with them. Both are SETTINGS fields by the ruled criterion (users.name is the engine's greeting; style_notes is written by WhatsApp onboarding and read by the admin detail view). Restored before tsc.

**(vi) A citation corrected.** I cited `cron.js:140` unqualified in the verdict table; the chair reasonably expanded it to `src/agent/cron.js`, which does not exist. The real readers of `briefing_enabled` are **`src/cron.js:140`** and **`src/api/admin/vendors.js:150`**.

**(vii) My own walk step corrected by the code.** I told the founder the travel hint "must be among" Swati's top three. It is **sixth** — the four-way 0.135 tie beats it. Corrected in §7.

---

## 7 · THE SMOKE CARD — Swati Roy, reconciled step by step against the as-built list

**Step 0 — the meter check.** Her tiers **agree** (trial/entry). A clean control, not a hazard. *(Executor reads: paste 2 above.)*

| # | Step | Thumb-path at this delivery | Executor reads |
|---|---|---|---|
| ① | Deploys green | Railway + Vercel | the founder's word |
| ② | Log in as Swati → **DISCOVER** chip → **Discover Profile** | `/vendor/discover/profile`, or the ◈ Header entry — BUILT THIS SITTING | the meter reads **18**; the IG section shows `Makeupbyswatiroy` **filled** |
| ③ | Read the three hints | — | photos · hero · about. **The IG hint must be ABSENT** — a met term is silent, the control cell |
| ④ | Read the gate line | Portfolio section | "6 photos required for Discover — you have 2 uploaded, 2 approved." |
| ⑤ | Write the About, save | — | the meter climbs; the About hint leaves |
| ⑥ | **Write the travel policy**, save | Travel policy section | **the meter climbs by exactly 10** while the toggle is untouched — Fork 3's ruling, live. *(The travel HINT is 6th and will not have been on screen; the section and the score are the proof, not the hint list.)* |
| ⑦ | Set a rate range, save | Starting rate | the preview reads **Rs 1,50,000** — never `₹`, never 1.5L |
| ⑧ | Toggle the rate off | Starting rate | "Your starting price is hidden from Discover." · the card's price vanishes in Frost |
| ⑨ | Upload 4 more photos, set a hero | link out to `/vendor/portfolio` (P3's slot, byte-untouched) | gate line now reads 6 uploaded |
| ⑩ | **Admin approves 4 of the 6** | `/admin/approvals/photos` — **NOT** `/admin/vendors/portfolio`, which auto-approves on upload and destroys the specimen | the gate line shows uploaded ≠ approved: F-07.4 rendered honest instead of contradictory |
| ⑪ | **Pause** from the Studio switch | Pause profile — BUILT THIS SITTING | her card leaves the Frost feed; a SELECT shows `discover_eligible` still `true`. Unpause; it returns. **This is the retired §8.3 SQL** |
| ⑫ | Open Settings | `/vendor/settings` | five sections gone, one line: "Moved to your Discover Profile." · **Instagram no longer shows a placeholder over a stored value** — F-07.9's live cure |
| ⑬ | Live witness | — | **DECLARED, never claimed** |

**Every step has a thumb-path. No founder SQL. The build's proof stands independent of the walk.**

---

## 8 · WHAT P3 PICKS UP

The portfolio manager itself (grid, drag reorder, cover star, per-photo caption, delete-with-confirm) at the slot this screen links to · the 20-photo cap raise wherever it lives · Cloudinary named transformations + LQIP · the IG import per `TDW_07_IG_IMPORT_ADDENDUM.md`, which CE-62 sites at the photo step.

**Open for the chair / founder-sequenced:** **F-07.10** — the eligibility grant bypasses the floor; ruled option (b) warn-and-allow, admin-surface work, open-named until built · **F-07.11** — `b05_f0555_media_dedupe_bench` 22/23, its own micro · the off-register `cabinet.ts` / `PeekNav.tsx` twin, named not touched · P4's `VendorProfileView` singularity, which nothing here pre-empts.
