# TDW_06 · M-4 · ELIZA — EXECUTOR'S HANDOVER
**Sitting:** the block's one chartered soul sitting · **base:** `72b0418` (CE-74's seal)
**Role:** executor · **repo:** `dream-os` · **rulings executed:** R0–R8 + the register addendum + the F-06.30 resolution

This is the executor's handover. It carries no CE number and touches neither
`FINDINGS_LOG.md` nor `TDW_00_MASTERPLAN.md` — those are the chair's.

---

## 1. WHAT SHIPPED

**The opener (R1 · V-7/V-8/V-9).** `coupleSystemPrompt.js` gains a WHO-YOU-ARE stanza
(LD-5, character with the reason attached) ahead of YOUR JOB; HARD RULE 4 is demoted
from a blanket do-not-answer to the vendor-only boundary; FLOW step 1 branches on
whether her first message carried a substantive question. `capture_couple_lead`'s
HARD RULE 11 is **byte-untouched** and benched.

**The money register (R2 · the founder's 「 forbids both 」).**
- *(A) the soul plane* — `harveySoul:179` amended in place (the symbol law gains the
  grouping clause and its reason); mirrors minted on the three surfaces the census
  found silent: `donnaSoul`, `advisorLens`, `coupleSystemPrompt` (rule 12).
- *(B) the mechanical plane* — `rs()` opened as the TS engine's gloss-free door beside
  `moneyWords`, and every donor-pool site routed through it (`donnaFind` ×4 ·
  `donna.ts` ×3 · `recordPrimitives` ×3 · `donnaLead` · `donnaBench` ×7 ·
  `provenanceHold`). The CJS wire routes through `witnessLine.rupees`
  (`moneyGuard` ×2 · `harvest` · `api/vendor/leads`). Per-runtime as ruled, no
  cross-runtime reach invented — **and the two homes are asserted to agree**
  (`b06_m4 §2.1`), which is the only thing that makes a per-runtime ruling safe.
- *F-06.38* — `brideSystemPrompt:176`'s `Rs 50k` → `Rs 50,000`; the file no longer
  teaches two registers against itself.

**F-06.30 — the withholding tell.** `donnaFind` gains `RECOGNITION_WITHHOLDING_TELL`,
sited on **both** recognition gates (`:382` zero-match · the `recentsShape` main
return) and on **neither** matched path. It declares the fact the payload's shape
hides: money and phones are not rendered on recognition lines, so the list cannot say
whether a budget is on file. Zero soul bytes, exactly as ruled — M-1's P1 precedent
(make the withheld fact legible), applied to a fact withheld by design.

**The riders.** *F-06.35* — `/gi` at **all four** sites (`:159 · :161 · :163 · :164`;
the charter named two). *F-06.36* — `witnessWireScrub` files `persona_scrub_on_wire`
under its own action name, wired at all five WhatsApp sites **and the web door**.

**F-06.31** — the name-provenance watch-arm: observes and scores, never holds.
**F-06.37** — `systemPrompt.js` labeled as a defused island.
**The opener scorer** — `openerFidelity` in the gauntlet, driven both ways.

---

## 2. WHAT IS PROVEN, AND WHAT IS NOT

**The floor, re-derived from a clean build:**

| bench | count |
|---|---|
| `b06_m0_bench` | 50/50 |
| `b06_m1_bench` | 45/45 |
| `b06_m2_bench` | 39/39 |
| `b06_m3_bench` | 37/37 |
| `b06_m4_bench` | **33/33 (new)** |
| `b06_gauntlet --rig-selftest` | **120/120** (was 110) |
| 0081 · advisor · donna_cache · downgrade · sonnet · wa_words · fresh_thread | 12 · 15 · 16 · 12 · 13 · 19 · 10 — all byte-stable |

`b06_m4` carries six production mutations, each REDding on exactly its cure, every
mutated file restored and asserted byte-identical.

**NOT PROVEN, stated plainly:** the opener is a prompt paragraph, and this estate has
learned twice that a desk can say nothing about one. `b06_m4 §1` proves only the
mechanical preconditions — the stanza reaches the composed prompt, the deflection was
demoted not deleted, the capture rule survived. **The behavioural verdict is the
founder's walk.** Any cell claiming otherwise would be the hollow green M-2 exists to
refuse.

**A DECLARED GAP:** `openerFidelity` is written and driven both ways, but **it is not
seated in any live scenario**. The gauntlet drives the *vendor* lane; the opener lives
on the *couple* lane, which has no live rig in this estate. Building one is a sitting
of its own and was not chartered here. The arm exists so the walk has a mechanical
verdict rather than an eyeball, and so a future couple-lane rig finds it ready.

---

## 3. WHAT DRIFTED FROM THE CHARTER

**Correction №10 (the fixture).** The ruling's F-06.30 fixture — `{}` then
`{"tokens":["Ritu"]}` — is not the production shape. `donnaFind:356` builds `tokens`
from `client` + `note` **alone**, so `{"stage":"new"}` (what the 02:38:57 turn actually
sent) is tokens-empty and takes the recognition path. The gap was **54 seconds**, not
47; the contrast limb is the 02:39:51 client-token turn, not the July-17 `donna_lead`
Ritu write (an honest write-echo, eight days earlier). **The bench drives
`{"stage":"new"}`** — benching `{}` alone would have left the shape production sent
unproven. The mechanism ruling itself is unaffected and confirmed.

**Eight labeled floor amendments, ratify-or-revert:**
1–7. **F-06.34's class had seven cells, not one.** M-3 cured the floating-tip defect at
`§6.8` alone. Six more — `m0 §7.1/§7.2 · m1 §6.6 · m2 §6.1/§6.2 · m3 §4.1/§4.2` — diff
the *working tree*, so they convicted M-4 of a W-1 breach the CE had authorised. All
range-pinned to their own sitting's seal. Counts preserved.
8. **`m3 §2.9` under the both-sides clause** — `scrubModelFrame` gained an optional
third argument, so every call site's bytes changed. The old exact-string green is
retired; the cell now asserts the new shape **and is strengthened** to require the
witness context.
Plus three gauntlet cells: the tell excluded from the row-grep (a tell that names what
it withholds must not be counted as a leak), and two matched-path money expectations
moved to the grouped form — which are themselves the register cure's proof on the
matched path.

---

## 4. CARRIED LOUDLY, NOT SILENTLY

**`b06_meter_bench` is 28/29 — RED AT THE CLEAN TREE.** Stash-confirmed at `72b0418`
with my work removed: the failure (`§3.2 no OTHER route moved — the whole DEFAULTS map,
key-for-key`) **pre-exists this sitting**. It is not mine and I have not touched
`models.ts`. Naming it because CE-74's floor line reads "all else at CE-73's counts" and
a red bench sitting inside that phrase is exactly the class this block keeps convicting.
**Finding candidate, unnumbered — the chair's to mint.**

**Two authoring defects I built and the apparatus caught**, banked because they are the
same disease twice: `§5.4` sliced `recognitionRow`'s body to `executeFindTool`, which now
contains the tell — so the cell greped the tell's own vocabulary and convicted the cure
of the disease it cures. I had already amended the identical shape in the gauntlet an
hour earlier and repeated it. `§5.3` source-greped a phrase spanning a string
concatenation seam. Both found only because the cells refused to go green.

---

## 5. WHAT THE NEXT SITTING PICKS UP

- **The founder's walk** — its spine per the ruling: the 50k-class first question asked
  **cold**, answered **first**; a money line read in the grouped register; a "do I have
  any budgets on file?" turn over token-less recognition answered honestly; threads by
  `conversation_id`.
- **Escalated, not decided here:** the fee-band product ruling (R1(b), collides with
  HARD RULE 3) · naming the agent (correction №B — naming and the opener cure are two
  acts) · F8, the soul-layer observation.
- **F-06.39's micro** — latent-trap, founder-sequenced.
- **A couple-lane live rig**, if the chair wants `openerFidelity` seated rather than
  waiting.
