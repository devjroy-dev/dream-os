# TDW_05 — SEND-(#200) RESOLUTION SESSION HANDOVER (the Dream Wedding Direct saga)
_Session: 2026-07-20 ~23:00 → 2026-07-21 ~02:00 IST. One sitting. Opened on the seventh chair's handoff ZIP (`tdw_send200_handoff.zip`); closed with the vendor lane LIVE on Meta direct and CE Succession Note VII authored. Companion record: `docs/specs/TDW_CE_SUCCESSION_NOTE_7.md` (the successor-facing summary; THIS doc is the session's own detailed record)._

## 0. DISCLOSURE (derive-or-declare, applied to this session itself)
- This session's container could NOT reach graph.facebook.com and never held any token. **Every Graph/console fact below is founder-hand** — commands authored by the session, run by the founder, outputs pasted back verbatim; console steps witnessed by founder screenshot in-chat.
- **Every repo fact was derived by command** on a fresh clone of `devjroy-dev/dream-os`, re-fetched at origin (tip at close: `6b42f65`; unshallowed to verify the seventh tenure's range `b59452d..6b42f65`).
- The session was chartered as investigator; in practice it also authored the cure runbook, guided the founder's console hands step-by-step, and authored Note VII at the founder's direction. It executed nothing itself.
- Two of the session's own working hypotheses were falsified mid-flight and retracted by name in-chat: (i) "subscription alone cures the #200" — [A] was real missing wiring but proven INSUFFICIENT by [C]; (ii) "bare granular_scopes means no WABA was ever granted" — falsified by [D] (a fresh mint against witnessed Full-control assets still shows bare scopes; bare = not-asset-restricted). Evidence beat narrative both times, per the kickoff's own law.

## 1. THE PROBLEM (as inherited)
Template sends from production numbers on WABA-PROD `1299109268220358` returned `(#200) "no permission to send on behalf of this WABA"` while every Graph field read healthy and the numbers had historic deliveries. The seventh chair's handoff correctly shaped it as a wiring/identity tangle and exonerated: app code (F-05.9 `abbcfe4`), the shared receiver (`907814b`), token scope, number health.

## 2. THE INVESTIGATION TRAIL (labels [1]–[K2] as run; outputs in chat)
- **[1] debug_token:** Railway token → `app_id 1425513376067685` (App-LIVE), SYSTEM_USER, `expires_at 0`, scopes present, granular_scopes BARE. App-DEV `1073940998303309` exonerated (later also shown absent from the business portfolio entirely).
- **[2] WABA-PROD subscribed_apps:** ONLY three Twilio apps (`7140899846025065`, `713285137508430`, `178145949230870`). **[3] sandbox contrast:** App-LIVE is the sandbox's sole subscriber — which is why P3's sandbox smoke was green while production #200'd.
- **[4]:** WABA-PROD `ownership_type: CLIENT_OWNED` (owner + on_behalf_of both business `995204059832918`/SELF) — the Twilio-birth fingerprint, reconciled: partner-created WABA, business messaging on its own behalf.
- **[A] cure attempt 1:** `POST /1299…/subscribed_apps` → success; **[B]** verified App-LIVE now listed. **[C] clean test → #200 persists** (`fbtrace AjZl40j5k4ouNOnuOmNTnpJ`). Necessary, not sufficient.
- **[D] fresh System User token** (minted after witnessing `tdw-whatsapp` Full control on the WABA + the app pinned to `1425…` by-screen via the business Apps list): same bare granular scopes. **[E] clean test with it → #200 again** (`fbtrace AN1L8WHniv-74Ul1OG4-Rmg`). Token layer fully exhausted.
- **[F]/[G]** owned/client WABA reads → failed on `business_management` (a scope the token lacks and App-LIVE never requested — evidence `10_39_39`). A DIFFERENT #200; explicitly NOT evidence about the WABA. The session's own defective command, disclosed as such.
- **THE ROOT, by-screen (Billing hub, `asset_id=1299…`):** WABA-PROD's only payment instrument = **Twilio Inc. credit line (Default)**; billing profile **USD-locked** ("currency cannot be changed once set"; ad-account currency change → "contact support"); founder's MasterCard ····0986 is INR-only → cannot attach. **No payer of record for a direct send → #200.** Management reads never touch billing → all-green reads all along. The "Payment configuration → India" rail item was checked and is WhatsApp Pay (customers-pay-you), NOT message billing — a dead end, disclosed (the session sent the founder there once, wrongly).

## 3. THE CURE — PATH B′, REBIRTH (founder-hand, ruled eyes-open)
1. **WABA-DIRECT `1739793260373677`** ("The Dream Wedding Direct") created directly under business `995…` via Business Settings → WhatsApp accounts → Create (display-name-only path, so no number was consumed at creation). Born: SELF, Asia/Kolkata, business-verification inherited (Verified/Approved at birth).
2. **Billing set at birth:** India / INR / MasterCard ····0986 attached and Default — the once-only currency write, made correctly. Witness: Billing hub shows ₹0.00, "You'll pay using MasterCard ····0986".
3. **Wiring day-one:** system user `tdw-whatsapp` (`61592314612760`) assigned Full control (console); App-LIVE subscribed (**[H]** → success).
4. **The night's ONE irreversible act (ruled, briefed, then executed):** +91 79821 59047 **deleted from WABA-PROD** — old PNID `1143702945489388` + its Insights (17/17) permanently gone. Safety case: physical SIM in the founder's handset = always re-verifiable. Re-added to WABA-DIRECT — no OTP challenge (same-business re-add) — **NEW PNID `1197664646766743`**; **[I]** Cloud API register → success; two-step PIN set (founder holds it OFF-REPO — never commit it).
5. **Templates re-filed on WABA-DIRECT via API** (bodies byte-identical to `src/lib/templates.js`; examples from the standing fixtures): **[J]** `tdw_vendor_login_otp` → **APPROVED instantly** (`889453490444121`); then the batch — 4 more AUTH all APPROVED instantly (`tdw_vendor_reset_otp 2141803752887353` · `tdw_couple_login_otp 1807080077120201` · `tdw_couple_reset_otp 897152556224351` · `tdw_circle_join_otp 1045197921243934`) + 6 content templates PENDING at close (`tdw_marketing_opener 1336770165243551` · `tdw_morning_nudge_vendor 1561878605718468` · `tdw_morning_nudge_bride 1011828918129845` · `tdw_crew_assignment 1586609669515026` · `tdw_payment_due 1933718834006817` · `tdw_demo_invite 1460307722523713`). The 3 `_hx…` Twilio-legacy templates were deliberately NOT refiled (recommendation: die with WABA-PROD; founder veto pending). **CE-36's named auth-button unknown CLOSED:** `sub_type:'url'` accepted by live Meta.

## 4. VENDOR CUTOVER — SEALED
- **[E-DIRECT]:** template send from PNID `1197664646766743` → test handset `+918757788550` → **accepted**, `wamid.HBgMOTE4NzU3Nzg4NTUwFQIAERgSNzcwN0FEMjRGNEI4RTQxOUU2AA==`; delivery witnessed on the handset.
- **Railway (all three services):** `META_WABA_TOKEN` = the fresh token (Railway + founder only); `VENDOR_PHONE_NUMBER_ID=1197664646766743`. All three redeployed green from the same tip (clears the three-deploy-hashes blocker from the handoff ZIP). `BRIDE_PHONE_NUMBER_ID` deliberately UNSET · `OTP_WA_NUMBER` unset (dormant fallback per CE-36) · `MARKETING_PHONE_NUMBER_ID` untouched (sandbox, P3's sealed state).
- **Production smokes, founder-run from the Codespace** (the founder's local ISP DNS can't resolve `*.up.railway.app` — see F-05.12 below — so the browser was bypassed): `POST /api/v2/vendor/auth/forgot-pin` and `POST /api/v2/vendor/auth/send-otp` for `+918757788550` → **two OTPs delivered** as Meta auth templates. Site-API → `otpSend.js` → PNID → Meta → handset, end to end. **Vendor lane LIVE on Meta direct; F-05.2 active for the vendor lane.**
- **Rollback reality (owned):** Twilio rollback for the vendor lane is DEAD (old PNID deleted; Twilio deactivated). Fallback story = `OTP_WA_NUMBER` (would need a live Twilio number) or forward-fix. Ruled acceptable at 3-test-accounts scale.

## 5. FINDINGS PROPOSED (CE numbers them in the log)
- **F-05.10 (this saga):** Twilio-born WABA — CLIENT_OWNED + partner apps + partner credit line + USD-locked billing = structurally uncurable for direct sends; cured by rebirth (WABA-DIRECT). Two laws earned: token-follows-assets (bare granular scopes = live asset evaluation; re-minting cures nothing) and provenance-before-permissions (diagnose a WABA's birth certificate first; billing currency is a once-only write).
- **F-05.11 (dreamos-pwa):** Forgot-PIN flow navigates to the landing page instead of the reset screen. Server path proven healthy (smoke hit the API directly) → purely frontend routing. Needs a pwa-clone triage session.
- **F-05.12 (infra hardening):** founder's ISP DNS fails on `*.up.railway.app` (domain verified alive from an independent resolver). If the founder's ISP trips, vendor ISPs will. Durable cure: `api.thedreamwedding.in` CNAME → Railway. Post-cutover.

## 6. STATE AT CLOSE / OPEN ITEMS (the eighth chair's queue is Note VII §7; deltas here)
- FINDINGS_LOG tip = CE-37 — the log owes: `907814b` + `abbcfe4` seal entries, then F-05.10/11/12. Masterplan 05 row likewise.
- 6 content templates PENDING (re-check: `GET /1739793260373677/message_templates`). Display name "The Dream Wedding" on the reborn +91 "In review" (sends working regardless).
- WABA-PROD retained: holds the +1 (Twilio-held, dead), 14 old templates, Twilio apps + credit line. Retirement rides M2b. App-DEV `1073…` deletion = hygiene, also M2b-adjacent.
- Bride lane: recommend a fresh Indian number on WABA-DIRECT (charter per Note VII §7.3). Marketing lane: still sandbox; decision owed (§7.4). P3 opener live test OPEN pending `tdw_marketing_opener` approval.
- fbtrace_ids of record: `AjZl40j5k4ouNOnuOmNTnpJ` · `AN1L8WHniv-74Ul1OG4-Rmg` (the two true #200s) · `A9RegfbE8arkGYvIGL7Dipy` / `A_pD-MbxbAqbY39nNvWyRgB` (the [F]/[G] scope-miss class, not WABA evidence).
