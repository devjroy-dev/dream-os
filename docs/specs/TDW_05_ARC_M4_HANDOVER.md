# TDW_05 — COUPLE-LANE MECHANICAL ARC · MOVEMENT M4 · HANDOVER
**The arc's one W-1 opening. Built at base `6acafd2`, §11 first motion clean.**

## 1. THE WALL OPENED FOR EXACTLY FOUR THINGS

**RIDER (i) — REDIRECTED BY THE FOUNDER, and the redirect inverted the shape.**
His word: 「 every event is a ceremony. haldi ceremony, mehendy ceremony etc 」.
So `brideSystemPrompt.js:86` — the line the chair and I both proposed changing — **was right all along and is 0-line.** The edit moved to the tool doc:

- `ceremony` now owns **every wedding ritual — mehndi, haldi, sangeet, the wedding day itself, reception.**
- `family` **re-scopes explicitly** to occasions that are NOT rituals — relatives arriving or staying, a lunch at home for the visiting side.
- **Enum values untouched.** A re-scope, never a schema change: no stored row becomes unreadable.

**THE "ALL HOMES" DEMAND, ANSWERED BY CENSUS NOT ASSUMPTION.** The CE named `:214`/`:254` as suspected siblings. Derived: they carry the enum **array** but their descriptions govern *filtering* ("all my trials") and *changing* ("trial → fitting") — **neither defines the taxonomy.** `vendorCalendarImage.js:101` is a different enum on a different plane (a mehndi *is* a shoot for a photographer). **The taxonomy has exactly ONE home**, and §1.3 now asserts that count so F-05.43 cannot be reborn one file over.

**RIDER (ii)** — the confirm rule gains its reason, appended inside the money paragraph. Byte-exact to the approved text. Its closing line puts the character where **M2's mechanical hold** stands: *"the answer is not to write a smaller one — it is to ask."*

**RIDER (iii)** — the first-person house, new paragraph after the naming one. Byte-exact. §3.2 guards the clause that matters: first-person house **without** widened knowledge-claims.

**C10-loop** — `loop.ts:536`'s `// clean re-run on Sonnet` dies on a zero-Sonnet estate. **D-10 engine-build step run and attested** (§5.2).

**`brideSystemPrompt.js`: 0-line. The wall is shut.**

## 2. PROVEN
`b05_arc_m4_bench` **18/18**, four production mutations RED. The cell that matters is **§4.1 — the wall was shut everywhere else**, asserted as an *exact expected file list* over the diff, so no fourth soul byte could ride in behind the three.

Floor + all four arc benches + `b06_sonnet` + `b06_advisor`: **all rc=0**, `f0532` untouched.

## 3. THREE LABELED AMENDMENTS — ONE CLASS, MY OWN CONSTRUCTION, OWNED
`b05_arc_m1_bench` §6.2 + §6.4 · `b05_arc_m2_bench` §5.3 · `b05_arc_m3_bench` §5.2. **Counts preserved; amended in place, none removed.**

**The defect:** each read `git diff --name-only HEAD` — the **live working tree** — and so asserted *"no soul file is dirty anywhere,"* a property those movements have no standing to claim. M4 then opened the wall lawfully, on the founder's veto, and **all four cells convicted it for doing what it was chartered to do.** A guard that can only stay green by forbidding chartered future work is a cell surviving its own subject — **CE-63's class, third instance in this arc, and this one nobody else built.**

**Re-aimed** to the property that was always the real one and is true permanently: **this movement's own delivered files contain no soul/prompt/engine byte** — a fixed list, unbreakable by any later movement. The live-tree W-1 guard now has **exactly one home**, in the movement that opens the wall (M4 §4.1), where an exact expected file list makes it meaningful. M1/M2/M3's W-1 property stands sealed at their own commits, chair-verified.

## 4. NEXT
**M5** — C6, last, `f0532`'s labeled amendment (deletions-pure).
**M6** — C9(a) + F-05.49 + F-05.51 + the ruled `resolveAgentForVendor` **reject-loudly** hardening.
**Smoke S6 is M4's:** *"Is my sangeet on the calendar?"* → the hand queries the **reconciled** kind (now `ceremony`), and the absence line stays honest.
