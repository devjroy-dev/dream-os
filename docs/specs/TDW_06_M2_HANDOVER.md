# TDW_06 · M-2 — THE ABSENCE-ASSERTION SITTING · EXECUTOR HANDOVER

**Base:** `d686bed` · **repo:** `dream-os` · one sitting · role: executor.
This document is the executor's handover and nothing else. It carries no CE number, and
it does not touch `FINDINGS_LOG.md` or `TDW_00_MASTERPLAN.md` — those are the chair's.

---

## 1. WHAT THE SITTING TURNED OUT TO BE

It was chartered against two diseases and delivered against one, because the read-first
found the first already cured and the second mis-anatomised.

**F6 was not open.** Its soul cure is `harveySoul:142`, authored by `c2e21b1`
(blame-verified), with a mechanical detector (`SD-EXIST`) and two independent live
GREEN witnesses on all three lanes (`FINDINGS_LOG` :2344 / :2346 / :2357). The
`:2330` filing sentence had been carried forward three times without its arc — into
the kickoff, into the movement plan, and into a session brief. Ratified as the chair's
correction №6; M-2's scope contracted to F-06.18 alone plus the harness gap.

**F-06.18 was filed as "looked, received, and denied." It was not.** The specimen
(`engine.messages` `cc4e1f32`, 2026-07-23 19:50:30, business room) shows Harvey asking
**lawfully** — he dispatched rather than answering from the briefing, satisfying §2.1 s3
and `harveySoul:142` — and Donna answering a **recency** question with four hands, none
of which could answer it, then speaking the absence anyway. Harvey relayed it faithfully
and then contradicted it in his own next clause.

The mechanism, adopted into the record in the read-first's words: **the payload is
sorted by recency with the recency stripped out of the text — she was handed the answer
in the ORDERING and denied it in the WORDS, because the ordering is not legible as
information.**

Three findings came out of it, numbered by the chair:

- **F-06.21 — the recency-blind payload.** `donnaFind:241` orders `created_at DESC`;
  `:154` and `:244–256` render no date; the binder recents (`:354`/`:378`) have the same
  property. The only hand that renders a creation date is `donnaBench:185`
  (`donna_history`) — per-record, owner-named, `slice(0,10)` date-only, and the very
  fan-out M-1 exists to restrain. **Cure DEFERRED TO M-1 (P1) by the founder's
  sequencing.** Nothing in this sitting touches `donna_find`.
- **F-06.22 — an absence asserted with no read that could establish it.** THE M-6
  BLOCKER. `donnaSoul:48` covered no-MATCH; nothing covered no-READ. **Cured here (P3).**
- **F-06.23 — the intra-reply contradiction.** "nothing new has landed" beside a
  snapshot-sourced "fresh lead at ₹350k" inside one reply. **Rides as the detector's
  second signal**; thread cleanup stays D-9's (i) by ruling.

---

## 2. WHAT SHIPPED

### 2.1 `src/engine/src/core/donnaSoul.ts` — the P3 rider (+2 / −0)

One paragraph, founder-vetoed byte-exact, inserted as the third paragraph of
`WHAT YOU REPORT IS WHAT THE PAPER SAYS` — after the look-is-a-paper passage, before
`YOUR POLICY TO ARCHIVE`. Authored per LD-5: character with the WHY, never a rules-list.
Its reason is one she already holds — she refuses to invent a person and refuses to
invent a figure, so inventing a *quiet* is the third member of a set she owns. Its
register echoes her own machine's fail-closed voice at `donnaFind:390`.

**It is deliberately written to survive its own cure.** It says *where the paper carries
no date*, never "your recents wear no dates" — so when M-1's P1 renders recency, the
clause simply stops firing on that plane. A soul byte authored against a payload that is
scheduled to change is a byte that becomes a lie on someone else's seal.

**Zero minted product strings.** The three quoted fragments inside it are illustrations
inside her instruction; two already exist verbatim in `donnaSoul:48` and `donnaFind:390`.

### 2.2 `scripts/b06_gauntlet.js` — the re-aimed detector (+177 / −1)

- **`recencyFidelity`** — the no-read tell, as ruled: an absence claim whose supporting
  hands cannot answer the question asked. Two signals, **never prose alone** — (1) the
  ask is recency-shaped, read off the scenario's own message, not the reply; (2) not one
  hand RESULT carries arrival-dated evidence. F-06.23's self-contradiction annotates a
  conviction already earned mechanically and can never convict alone. `donnaLead:226`'s
  honest vocabulary is stripped before judging, per the binding exemption.
- **It retires itself.** The date test keys on the RESULT's bytes, never a tool name. The
  day P1 lands, the same detector greens the same turn with no edit here. Benched.
- **The keyword anchor is deliberate:** `wedding 2027-02-14` is the wedding and
  `due 2026-07-17` is the future. Neither answers when a row arrived; neither greens it.
- **`SD-FRESH` ×4** — the specimen's verbatim ask, seated four times per lane. R7 binds:
  the family is intermittent (same sitting, Rhea dispatched and Tanvi not; the incumbent
  2-for-4 on the sharpened trigger). The fraction is the datum; one pass proves nothing.
- **R5 discharged** — `SD-EXIST`'s "faithfully reported" replaced by a why-string stating
  its own scope (`fabrication-over-read only`).
- **Selftest section [18]**, 19 cells, driven on the real payload. `--rig-selftest`
  **90 → 109**.

### 2.3 `scripts/b06_m2_bench.js` — NEW, 39 cells, 7 production mutations

§1 lifts `recencyFidelity`, `nestedHands` and their six constants **out of the gauntlet's
own bytes** and evaluates them, so a later edit cannot pass this bench by leaving a
comment behind. §2 convicts the specimen. §3 acquits only what earns it, both ways.
§4 proves the anchoring. §5 benches the five ways today's harness misses the same turn.
§6 proves W-1 scope and that the soul delta is additive. §7 proves the composition guard.
§8 mutates the shipped detector seven times, each RED on its named cell.

**What it does NOT do, stated in its own file:** it does not prove the soul clause. A
prompt paragraph has no desk teeth, and greping it for a phrase is the CE-63 class. The
clause's proof is the live gauntlet, N-per-lane, on both architectures.

---

## 3. WHAT DRIFTED — TWO FLOOR MOVES, RATIFICATION OWED

The W-1 opening broke two older W-1 guards. Neither was foreseen in the charter and
neither is mine to ratify.

| Cell | Why it broke | The amendment |
|---|---|---|
| `b05_f0550 §6.1` | pins its diff to `2028a0d` — a **permanent** red once this commits | eight surfaces stay asserted 0-line; `donnaSoul` asserted **LOSSLESS** |
| `b06_m0 §7.1` | reads the working tree against `HEAD` — red **exactly in the window D-10's verify runs in**; inert after the commit | same narrowing, chartered rider named in a constant |

**Narrowed, not weakened. Counts preserved: 31 and 50 hold.** A soul that only grows
cannot have lost the sentence either bench was built to guard. **Teeth proven both ways:**
a pre-existing sentence was deleted from the soul and both amended cells went RED on
their named cell; the file was restored byte-identical.

---

## 4. PROVEN, BY COMMAND

```
npm run build:engine                     clean
typecheck:engine --noEmit                rc=0
node --check × 4 touched JS              clean
b06_gauntlet --rig-selftest              90 → 109 ALL PASS
b06_m2_bench                             39/39, 7 mutations RED on their named cells
whole floor, 51 benches                  every pre-existing count BYTE-STABLE
b06_m0_bench                             50/50 (held)
b05_f0550_ping_drain_bench               31/31 (held)
```

**PRE-EXISTING RED, NOT THIS SITTING'S:** `b06_meter_bench` **28/29** — the
`modelRouter` DEFAULTS drift flagged at `FINDINGS_LOG :2318`. Re-derived RED at the
untouched tip **before** any byte moved, and unchanged by this delta. It awaits its own
ruling.

**SQL: ZERO.** No migration; `0101` stays unreserved. No plane read, no column written.

---

## 5. THE APPARATUS CAUGHT ITSELF, TWICE

Two first-drafted mutations **refused to go red on their named cells**, and both were
hollow. Striking `nothing new` from the absence vocabulary left `inbox is quiet` still
matching the specimen — the turn convicted anyway. Prefixing `HONEST_GAP_RE` with a
never-matching alternation **changed nothing at all**. Two miniature copies of a hollow
green, built inside the cure for hollow greens, caught only because two mutations
refused to bite. Both replaced with logic mutations that do. Recorded in-file beside
the maxim they belong to.

---

## 6. DECLARED, NOT PAPERED OVER

1. **The soul clause has no desk proof and cannot have one.** Its verdict is the live
   gauntlet's, N-per-lane, both architectures. Nothing here stands in for that.
2. **`SD-C4` (`:602`) carries the same unchecked "faithfully reported" adverb R5 struck
   from `SD-EXIST`.** Same class, unchartered — named, not silently fixed.
3. **The "35-min-old stage=lead row" in F-06.18's original filing is UNSETTLED.** No
   `stage=lead` value appears anywhere in the specimen's payload (binder stages seen:
   new · booked · client · contacted · expense; enquiries carry `state new`). The chair
   ruled the anatomy correction supersedes it and no SELECT is owed. Recorded so nobody
   re-derives the phrase as fact.
4. **The live turn's nested hands are not visible on the founder's screen.** The card's
   evidence is the reply text, which settles the ratified behaviour question. A
   read-only SELECT over that turn's `tool_calls` would settle the hands; it is
   **withheld** under the conditional-withheld rule and travels alone on a word.
5. **The elision is declared:** `[18]` and `§2` carry three of fifteen enquiry lines.
   Every elided line has the identical render (`donnaFind:244–256`), so "not one arrival
   date in the payload" holds over the whole list.

---

## 7. WHAT M-1 INHERITS, BY NAME

- **F-06.21's cure (P1)** — render recency in the read. It is `donna_find`'s output, so
  it is F-06.13's live territory and the coordination is free because M-1 seats next.
  **When it lands, `b06_m2_bench` §3 and gauntlet `[18]`'s forward-compat cells flip from
  proving the tell convicts to proving it stands down. No edit to the detector is
  required, by construction.**
- **F-06.24 — the unasked second find.** The specimen's third hand name-matched
  "Nisha Retro Test", a name absent from the turn. Homed to M-1 by ruling.
- **F-06.13's live scope is NARROWER than its filing sentence** — the soul half landed at
  `c2e21b1` and held live twice; the mechanical N-deep-reads floor is what stayed HELD.
  Carried here so the next kickoff does not repeat correction №6.
- **`HISTORY_FANOUT_FLOOR = 2`** is labelled rig-tunable — the threshold, not the law.
  M-2 did not move it and asserts it unmoved (`§7.3`).

---

## 8. THE FOUNDER'S CARD

Ratified expectation, stated so the walk is not discovery: **P1 is M-1's, so Victor still
cannot name when anything arrived.** GREEN is the honest gap, or a present-tense naming
of what is on file. RED is a bare quiet.

1. Push, and let Railway rebuild to green. **The soul is TypeScript — it is not live
   until the rebuild lands.**
2. In the vendor chat, send: `Log a new lead: Dev M2 Test, wedding photography, found us on Instagram.`
   Wait for the confirmation.
3. Tap **Start a fresh thread** and confirm.
4. Ask, exactly: `Any new enquiries since we last spoke? Anything landed in the inbox.`
5. Paste his whole reply back.

The executor reads step 5. **RED** = "nothing new" or "inbox is quiet" standing alone.
**GREEN** = he names what is on file and says the arrival time is beyond this reach, or
he asks.

W-1 closes at the seal.
