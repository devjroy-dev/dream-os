# TDW · CE-33 — ROW 14 D-3 ARC SEAL: POLLS, WHOLE
(authored by the thirty-third chair, 2026-08-14, on the founder's word. Where this
seal and the CE-219 band disagree, this seal wins; where this seal and the tree
disagree, the tree wins. TRUST EVIDENCE OVER NARRATIVE — including this seal.)

## §1 · TENURE AND TIPS, derived at the chair's own clones
Seated 2026-08-13 at dream-os `c4773a6` · dreamos-pwa `403faf9` (seating floors:
dream-os 117 green / 21 non-zero, red set characterized by name; pwa
`FLOOR = NAMED BASE`). Arc closed at dream-os **`b605155`** · dreamos-pwa
**`eb75327`**, verified fetch-first on sibling-full clean trees: build 0 ·
`tsc --noEmit` 0 · dream-os **118 green / 21 non-zero, red set IDENTICAL BY NAME
to the seating base across every measurement of the arc** · pwa
`FLOOR = NAMED BASE, no delta`. Landing claims throughout were exclusively the
founder's push output; every post-origin pass ran on the chair's own clones.

Commits this arc — dream-os three: `44fb6ba` (D-3a) · `120fe6a` (D-3b server) ·
`b605155` (D-3e server). dreamos-pwa six: `3f1cb7f` (D-3b client) · `8fbcddc`
(F-14.10) · `7476b1a` (D-3c) · `c351751` (D-3d) · `e028d20` (D-3e client) ·
`eb75327` (D-3f). Plus the docs push carrying this seal.

## §2 · THE DELIVERIES

| seat delivery | subject | bench at close |
|---|---|---|
| D-3a | two tables at `0124` · three Class B doors · the resolver widened at the gate (R-D3.3) · §5.1 four-by-charter | `b14_d3` green |
| D-3b | `eligible_count` — the denominator byte ⑤ demanded · `pollCopy.ts` the one client home · two renderers, one vocabulary | 57/57 at landing |
| D-3c | F-14.8 defused · F-14.9 rendered · ⑨ dressed · the create sheet, bride-only | 97/97 at landing |
| F-14.10 | the muse FAB clamp — arithmetic-evaluating proof, a cell kind the estate did not have | 18/18 |
| D-3d | ⑨ off the hero rung · ① made a CTA | 103/103 |
| D-3e | delete — a question taken back, never rewritten; no activity row; cascade at the plane | 67/67 · 124/124 |
| D-3f | the poll stops waiting on reads it does not use | 131/131 at close (the note's 130 corrected at origin) |

`b14_d3_polls_bench` at close: **67/67**. The pwa polls proof at close: **131/131**.

## §3 · THE MIGRATION — 0124, on the record
In order (ladder tip was 0123; F-SW.3 does not apply, stated in-file because 0098
made out-of-order look like the block's habit). F-OB.6-shaped whole: every
statement its own paste boundary, the constraint verify reading `pg_constraint`
out of the catalogue, all four blocks founder-pasted and witnessed in-comment.
`circle_poll_votes_pkey` = `PRIMARY KEY (poll_id, voter_user_id)` — R-D3.2 at the
plane. The spec's own address line ("per 0087") is dead ink twice over: 0087 was
seized by applied code (F-04.108) and the founder re-homed 14 to 0098, which D-1
then spent. Recorded in the migration's head. The V2 expect-line defect (predicted
8 constraints; the catalogue held 9 — the DDL right, the prediction short one FK)
is seat-owned in-file, and cell §7.7 now counts the DDL's own declarations so the
next such line cannot be wrong quietly. **Standing sentence it minted: assert the
artifact, never a predicted count.**

## §4 · THE RULINGS

**R-D3.1 — Class B.** Polls on the resolver, refuse-in-handler, mounted bare
beside the three. The spec's "C-9 pattern" sentence STRUCK (Amendment One §2.14's
class — a spec sentence written before the auth arc existed); the strike rides
Amendment One, the departure labeled at the handover.

**R-D3.2 — voter key `voter_user_id uuid NOT NULL`, PK `(poll_id, voter_user_id)`.**
No sentinel, no text ref. Founder sealed the re-invite semantic at the sheet:
KEEP — a returning member keeps her vote.

**R-D3.3 — the userId gap cured at the GATE.** `resolveCoupleIfPresent` already
computed the bride's `users.id` and discarded it; its return widened additively,
arm 2 passes it through. A per-handler couples hop would have been a second
implementation of a resolution the gate already performs.

**R-D3.4 — visibility through the ONE home.** Polls consult `circlePermissions`;
`b14_d1` §5.1's enumeration grew to four BY CHARTER, the sitting owning the cell.
Default semantic: a poll is SHARED BY CREATION — options render whole to every
active member; anything a poll JOINS reads through the resolver. Founder ratified
line (b): a flagless member sees a linked event's name and date, never its vendor.

**R-D3.5 — `thread_id` nullable; two surfaces, no third; one timer.** Standalone
polls render at the circle bloom and the coplanner strip; refresh piggybacks the
existing 10s interval.

**R-D3.6 — the close nudge OUT.** D-2 ruling ③'s population grounds hold. The
template, its cron caller, and F-14.7's cure land TOGETHER in the delivery that
ships the send — F-14.7 is owed at the first circle send site's birth, and the
nudge is that site. Acceptance-6's nudge clause HELD to that delivery. Narrow
supersession of D-2 §7's "D-3 files its own": a template files at its FEATURE's
birth, and the feature is the nudge.

**R-D3.7 — ties: the founder's word.** Sealed at the sheet: no crown on a tie;
byte ⑧ states it plainly and extends by comma to n-way.

**R-D3.8 — DDL F-OB.6-shaped at 0124.** Honored (§3).

**FOUNDER RULINGS, all built and cell-guarded:** the veto sheet ratified whole
(「approve all recommendations」, 2026-08-13) · ⑩ ratified mid-walk as
unreachable-by-design — the disabled control is the message · **create and delete
are BRIDE-ONLY** — a member votes, she does not convene or unmake · **no edit
door anywhere** — a question is taken back, never rewritten · **no activity row
on delete** — the circle never sees a withdrawn question · **⑨ gets weight, same
ZIP** — with the boundary held: the byte is frozen at the character; what moved
was its dress. Styling is not copy and needs no re-veto; new words would.

**CHAIR RULINGS MINTED THIS ARC:**
- **R-33.1** — a read-first's census enumerates READERS as well as callers: every
  bench, doc, and import that reads a file in the radius, found by walking,
  before the first byte. (Evidence: reader cells found by the floor, not the
  read-first, four deliveries running — `b14_d1` §6.1, `b07_auth_crossover`'s
  four shape-pinners, `f0772` §3.5/§3.2/M-9, `frost_parity` twice.)
- **R-33.2** — a cell asserts the RULING, not the implementation; a cell that
  pins a form cannot tell a cure from a regression. (Specimen: §5.2 reddened by
  a change honouring R-D3.5.)
- **R-33.3** — an absence cell's radius equals its claim: bound to the surface
  the claim names, strip comments before bounding, count render sites and never
  mentions. (Evidence: five over-convictions; the slice canary's first-run fire;
  the §10.8→§11.9 repeat, same file, same arc, lesson carried in its own comment.
  In the seat's own sentence, kept in this body because it is the reason benches
  exist: **writing a lesson down is not the same as having learnt it.**)

## §5 · THE WALK — banked whole, production, real devices
All twenty-six frozen bytes accounted: the sheet's twelve device-witnessed
character-exact (①③④⑤⑥⑦⑧⑨ on glass on both surfaces · ② ⑩ at their server homes ·
⑪ ⑫ expected-zeros holding — no wall advertised, no word of ours in her poll),
plus D-3c's nine and D-3e's five, one home per plane, a cell that reds if either
surface drifts.

Load-bearing witnesses: **`2 of 2` voted** — `eligible_count` reaching a screen,
the datum that exists solely because byte ⑤ demanded a denominator no client
could count (a `0 of 1` would have meant the bride missing from her own poll) ·
**the vote row that MOVED** on a changed mind — one row, `created_at`
byte-unchanged, never a second row · **`Gold won`** and **`It's a tie — Gold and
Ivory`**, both on glass · the delete cascade verified at the rows (`polls 0 ·
votes 0`, no orphans) · the ⑨/silence asymmetry as ruled — the empty state
speaks only on the surface whose subject is polls · and **D-1's muse-switch
witness PAID**: column → resolver → guard → payload → page, vanishing and
returning on a handset — the one link no bench in either repo can reach. The
create and delete doors both had their first live use on the founder's own
screen; he used the first to poll his circle on whether polls should have
delete, which carried 2–0.

The React #418 on the founder's console: NOT this arc's — both top hazards
present at `403faf9` pre-delivery, React recovered, disposed as F-13.11.

## §6 · FINDINGS MINTED THIS ARC (chair's register)
- **F-13.11** — pre-existing hydration hazards in the circle bloom: the
  `contactsSupported` server/client branch in a `useState` initializer
  (React cause #1 verbatim, likeliest #418 culprit) + three `Date.now()` feed
  sites (cause #2). Cosmetic-but-real; React recovers. MICRO SHELF, unsequenced.
- **F-14.8** — `toLocaleString(undefined,…)` at the closes line: cause #3's shape
  in the tree, **corrected by the chair to landmine-class** — the poll subtree is
  gated on `!loading && polls.length>0` and never enters the hydration
  comparison, so the seat's "first poll with a close time would trip it" was
  contradicted by the seat's own derivation #1. **DEFUSED at D-3c** anyway: a
  landmine cleared cheap is a landmine cleared.
- **F-14.9** — `linked_event` served by the payload, rendered by neither surface;
  R-D3.4's vendor gate proven at the payload with no consumer. Fork CLOSED by
  the veto record itself: ratified line (b) describes a SCREEN, so dropping the
  field would falsify a founder-ratified behavior. **RENDERED at D-3c**, name and
  date, vendor behind the flag, ⑪'s expected-zero untouched. The sheet's
  "already built" was payload-true and screen-false — the walk is the instrument
  that caught it, which is what walks are for.
- **F-14.10** — the muse FAB clamp: a viewport expression that went negative
  below 440px, invisible on every desktop. **CURED**; its proof evaluates
  arithmetic rather than matching strings — a cell kind the estate did not have.
  **The number is RATIFIED RETROACTIVELY**: it entered through a commit message
  while the chair-free floor happened to be `.10`, so no collision — and
  **REGISTER-IS-THE-CHAIR'S is restated once**: the number waits for the chair,
  even mid-arc, even when the natural-next is obvious. That the gamble paid is
  not the law permitting it.
- **F-05.80** reproduced twice this arc, unmoved.

## §7 · CORRECTIONS LEDGER — the chair's and the seat's, one table
Chair catches of the seat: the `byCoupleUserId` cite (the function is
`byCoupleId`) · F-14.8's live-radius overclaim (§6) · the close note's stale
"D-4 still held" line (the chair had delivered the ordered derivation two
sittings prior) · the D-3f figure (130 stated; 131 derived at origin).
Chair catches of the chair's own inheritance: NOTE 32 §2's conductor figure
(1,120; derived at the note's own stated tip: 1,169) — the №1 species,
committed ink repeated without re-derivation.
Seat self-catches, banked with credit: the V2 expect-line (owned in-file,
§7.7 authored) · the slice canary's first-run fire · §10.7's family-for-weight
check · §5.2's form-pin · §11.9's mentions-count repeat. Five absence-cell
over-convictions enumerated at the seat's close note ④. Every count re-derived,
never adjusted until it passed.

## §8 · D-4 UNBLOCKED — the ordered derivation, on the record
`events.assigned_member_ids` (`uuid[] NOT NULL default '{}'`, GIN-indexed) is
**VENDOR-PLANE WHOLE**: twelve consumers, every one vendor-side (`crew.js` — its
own header calls it the SOURCE OF TRUTH for crew · `vendor/events.js` refuses
non-team-member ids in its own words · `eventWrite` / `occupancy` /
`crewSnapshot` / `calendarSignals` / `roster` / `day` / `bands` /
`studio/payments` / Victor's write path / the engine's recordPrimitives) —
**zero readers under `couple/`, `circle/`, or `brideTools`**. Its id-space is
`team_members.id`. **D-4 must not touch it**: a circle member's id in that
column is readable by every crew `.contains` query — the mixed-key disease the
voter key refused. The delegation seat is **new couple-plane machinery:
`couple_tasks.assigned_member_id`** on the table the bride's live task writer
(`brideEngine.js`) actually writes (witnessed: 9 columns, ordinal-5 hole is a
dropped column). One product line rides D-4's sheet, recommendation attached:
key on `circle_members.id` with `ON DELETE SET NULL` — a task is delegated to a
SEAT in the circle; removal returns it to the pool visibly, never a ghost
(deliberately opposite the vote ruling, for stated reasons). G-5's split
tasks-vocabulary is the backdrop; the reader moves if G-5 ever resolves it.

## §9 · CARRIED FORWARD, the founder's to sequence
① **The vendor gate is bench-only** — §5.1–§5.3 proven, never walked; no
production event carries a `vendor_id`. Scope any walk to `9888294440`.
② **D-4 delegation** — unblocked (§8), sheet-ready.
③ **Row 9 kickoff** on the founder's word, Fable seat, per NOTE 32 §3②.
④ **Track 1 verdict duties** ~2026-08-19, per NOTE 32 §3④.
⑤ **The masthead seed** — derived, costed, DECLINED (hoisting lets a cached
value correct itself on screen); the derivation is banked at the close note so
the next sitting starts from it, not from scratch.
⑥ **F-13.11** on the micro shelf.
⑦ A poll has no edit — BY RULING — and no reopen for a closed one. Named, not
a defect.
⑧ The BRIDE_AUDIT SUPERSEDED banner LANDS IN THIS PUSH — c12's cure; that
standing trap closes the day this seal lands.

## §10 · RANGES AT THIS SEAL — derived at `b605155` · `eb75327`, both repos walked
Chair-free floors: F-05 `.81` · F-06 `.197` · F-07 `.127` · F-08 `.103` ·
F-09 `.190` · F-10 `.121` · F-13 `.12` · F-14 `.11` · F-OB `.13` · F-SW `.5`.
Never quote a floor forward — re-derive at origin before any allocation.

Row 14: D-1 walked · D-2 wire-witnessed · D-3 walked whole on production.
**Polls are done.** Sequencing beyond this seal is the founder's.
