# TDW — CE SUCCESSION NOTE VII (Chief Engineer chair handoff)

**Written:** 2026-07-21 (~02:00 IST), at the close of the SEND-(#200) resolution session, for the eighth chair.
**Authorship disclosure (belt-and-suspenders):** the seventh chair (an Opus session) bled context mid-tenure and banked into a handoff ZIP (`tdw_send200_handoff.zip`, chat transport — PROBLEM_STATEMENT + EVIDENCE_MANIFEST + 18 screenshots) rather than a note. This note is authored by the successor investigation session (Fable) at the founder's direction, closing the seventh tenure's ledger AND recording its own resolution night. Every repo claim below was re-derived by command at origin; every console/Graph claim is founder-hand, witnessed live in the session by the founder who will hand you this note. Trust the repo over this note; trust this note over any memory.
**Verify-first law applies to this note itself.** origin/main tip at writing = `6b42f65`. FINDINGS_LOG tip = **CE-37** (Note VI bank) — everything below CE-37 is NOT yet in the log (see §5.1, the first debt). Re-fetch origin and confirm before acting; a stale clone is a named defect class (CE-22).

## 1. THE ROLE (unchanged; read Notes I and VI §1)
CE authors specs/kickoffs, rules with evidence at origin, never executes. Executors can fetch/clone/build/bench and must disclose real output — never claim an un-run by-command result. Founder runs migrations/SQL/smokes/**console acts** and holds copy veto. Two chairs, never collapsed. Model tiering: **Fable both chairs** (Note I §4; the seventh tenure's Opus context-bleed is the second confirmation of the rule).

## 2. THE SEVENTH TENURE'S REPO WORK (verify at `6b42f65`)
- `e84a13f` — `docs/TEMPLATES.md` §7: submission-ready AUTHENTICATION filing specs, 5 OTP keys (discharges Note VI §5.1).
- `907814b` — **shared Meta receiver / GATE-1**: one callback, per-change PNID fork on the marketing ingress (Option B); `metaInbound.js` routes by `metadata.phone_number_id` vs the three `*_PHONE_NUMBER_ID` envs; fork bench 31/31; sealed benches byte-stable.
- `abbcfe4` — **F-05.9**: signup OTP off dead Twilio — `ensureAuthIdentity` create-or-heal on the Meta verify path (couple+vendor `verify-otp`); signup bench 10/10; `mintSession` 0-line diff; pwa side sealed at `dreamos-pwa 165e17a`.
- `6b42f65` — F-05.9 executor handover (docs-only): `docs/specs/TDW_05_F059_OTP_HANDOVER.md`.
These builds are real and stand; the CE seal ENTRIES for them are the log debt in §5.1.

## 3. THE SEND-(#200) RESOLUTION — ROOT CAUSE AND CURE (2026-07-20/21 night; lives ONLY here + chat)
**Symptom:** every template send from the production numbers returned `(#200) no permission to send on behalf of this WABA` while every Graph read was green and the numbers had prior deliveries.
**Root cause (final, witnessed):** WABA-PROD `1299109268220358` is **Twilio-born**: `ownership_type: CLIENT_OWNED`; its only subscribed apps were three Twilio apps (`7140899846025065`, `713285137508430`, `178145949230870`); its only payment instrument was **Twilio Inc.'s credit line (Default)**; its billing profile **USD-locked** ("location and currency cannot be changed once set") so the founder's INR-only card could not attach. A direct send had **no payer of record** → #200. Management reads never touch billing, hence all-green reads; the sandbox worked because it is App-LIVE's own test WABA. The historic deliveries (17/17, 2/2) rode Twilio's pipe.
**Exonerated en route (do not re-chase):** the token (app_id `1425513376067685` = App-LIVE, SYSTEM_USER, `expires_at: 0`; **bare granular_scopes = NOT asset-restricted** — authority is evaluated live against the system user's assets, so no re-mint is ever needed when assets change); system-user assets; app subscription ([A]: App-LIVE was subscribed to WABA-PROD mid-investigation — real missing wiring, proven insufficient alone); App-DEV `1073940998303309` (not even in the business portfolio; pure name-collision hazard; hygiene-deletion candidate). fbtrace_ids on record: `AjZl40j5k4ouNOnuOmNTnpJ`, `AN1L8WHniv-74Ul1OG4-Rmg`.
**Cure executed (founder-hand, "Path B′" — rebirth, not repair):**
- **WABA-DIRECT `1739793260373677`** ("The Dream Wedding Direct") created directly under business `995204059832918`: born SELF, time zone Asia/Kolkata, **billing INR + MasterCard ····0986 attached at birth** (the once-only currency write, made correctly this time).
- Wiring day-one: App-LIVE subscribed (`subscribed_apps` → success); system user `tdw-whatsapp` (`61592314612760`) Full control.
- **+91 79821 59047 DELETED from WABA-PROD** (the night's one irreversible act, ruled + eyes-open; old PNID `1143702945489388` and its Insights are gone) and re-added to WABA-DIRECT — no OTP challenge (same-business re-add) — **NEW PNID `1197664646766743`**, registered on Cloud API; two-step PIN set (founder holds it off-repo; NEVER write it into the repo).
- **11 templates filed on WABA-DIRECT via API**, bodies byte-identical to `src/lib/templates.js`: all 5 AUTHENTICATION **APPROVED instantly** (`tdw_vendor_login_otp 889453490444121` · `tdw_vendor_reset_otp 2141803752887353` · `tdw_couple_login_otp 1807080077120201` · `tdw_couple_reset_otp 897152556224351` · `tdw_circle_join_otp 1045197921243934`); 6 content templates PENDING at bank (`tdw_marketing_opener` · `tdw_morning_nudge_vendor` · `tdw_morning_nudge_bride` · `tdw_crew_assignment` · `tdw_payment_due` · `tdw_demo_invite`). The 3 `_hx…` Twilio-legacy templates were deliberately NOT refiled (recommendation: they die with WABA-PROD; founder veto pending).
- **CE-36's named live-Meta unknown is CLOSED:** the `sub_type:'url'` auth-button send shape was ACCEPTED by live Meta (wamid returned). No `buildAuthTemplatePayload` flip needed.

## 4. VENDOR CUTOVER — SEALED ON FOUNDER WITNESS (2026-07-21)
Railway (ALL THREE services): `META_WABA_TOKEN` = the fresh system-user token (in Railway + founder's keeping only — never in repo/notes) and `VENDOR_PHONE_NUMBER_ID=1197664646766743`; all three redeployed green from the same tip (this also cleared the three-different-deploy-hashes blocker from the handoff ZIP's evidence).
**Witness chain:** raw template send accepted (`wamid.HBgMOTE4NzU3Nzg4NTUwFQIAERgSNzcwN0FEMjRGNEI4RTQxOUU2AA==`) → production API smokes founder-run from the Codespace: `POST /api/v2/vendor/auth/forgot-pin` AND `/send-otp` for `+918757788550` → **two OTPs delivered to the test handset** as Meta auth templates. Vendor lane is **LIVE on Meta direct** — site → `otpSend.js` → PNID → Meta → handset, end to end. F-05.2 (opt-out) is hereby ACTIVE for the vendor lane. `BRIDE_PHONE_NUMBER_ID` deliberately UNSET (dead lane must not half-wake); `OTP_WA_NUMBER` unset (dormant fallback, per CE-36); `MARKETING_PHONE_NUMBER_ID` untouched (still sandbox — P3's sealed state).
**Note VI §4's cutover model is REVISED by events:** the cutover happened by **WABA rebirth**, not by flipping the number within WABA-PROD; no per-lane webhook re-point was needed (webhook config is app-level on App-LIVE; the [H] subscription carries it). `docs/specs/TDW_05_CUTOVER_CHARTER.md` does not exist in the repo — treat Note VI §4 + this section as the charter record. Rollback-to-Twilio for the vendor lane is DEAD (old PNID deleted; Twilio deactivated) — the fallback story is now `OTP_WA_NUMBER` (would need a live Twilio number) or forward-fix; with 3 test accounts this was ruled acceptable.

## 5. DEBTS AND PROPOSED FINDINGS (the CE's home is the log; number these there)
1. **FINDINGS_LOG is 5 commits + one resolution night behind** (tip CE-37). Owed, as the eighth chair's docs-only ZIP: seal entries for `907814b` (shared receiver) and `abbcfe4` (F-05.9), then the full §3/§4 record (proposed **F-05.10**: Twilio-born-WABA root cause + rebirth cure + vendor cutover seal). Masterplan 05 row likewise.
2. Proposed **F-05.11** (dreamos-pwa): the Forgot-PIN flow navigates back to the landing page instead of the reset screen — server path proven healthy (the smoke used the API directly), so this is purely frontend routing. Needs a pwa-clone triage session.
3. Proposed **F-05.12** (infra hardening): founder's ISP DNS fails to resolve `*.up.railway.app` (ERR_NAME_NOT_RESOLVED; domain verified alive from an independent resolver) — if the founder's ISP trips, vendors' ISPs will too. Durable cure: serve the API from `api.thedreamwedding.in` (CNAME → Railway) so browsers only resolve the product domain. Post-cutover, not urgent.

## 6. STANDING LAWS (deltas only; the rest live in Notes I/VI + the protocol)
- **Token law (new, proven):** the system-user token is app-scoped and asset-evaluated live — adding numbers/WABAs never requires a new token; only assign the asset to `tdw-whatsapp` (+ subscribe App-LIVE for a new WABA). Regenerating tokens cures nothing whose disease is assets or billing.
- **WABA provenance law (new, the night's lesson):** a WABA's birth certificate — ownership_type, subscribed apps, credit line, billing currency — is load-bearing and partly immutable. Diagnose provenance BEFORE permissions. Never accept a partner-born WABA for direct sends; billing currency is a once-only write.
- All Note VI §6 laws stand (W-1 · P-06.T · OTP structural bypass · clobber law · delivery discipline). M2b's gate is unchanged: BOTH lanes live on Meta (vendor ✓ tonight; bride pending).

## 7. IMMEDIATE QUEUE FOR THE EIGHTH CHAIR
1. **Verify at `6b42f65` by command**, then verify §4's live state the cheap way: `GET /1739793260373677/message_templates` (roster + the 6 PENDING statuses) and `GET /1739793260373677/subscribed_apps` (App-LIVE present).
2. **Pay §5.1** — the docs-only ZIP that brings FINDINGS_LOG + masterplan current (CE-38…).
3. **Bride-lane charter**: the +1 is Twilio-held and dead; recommended path is a **fresh Indian number on WABA-DIRECT** (₹-cheap SIM; register exactly as §3; set `BRIDE_PHONE_NUMBER_ID`; repo default `+14787788550` and `TDW_WA_NUMBER_BRIDE` then need reconciling — fold into F-05.7's cleanup). Founder gates sequencing.
4. **Marketing-lane decision**: move `MARKETING_PHONE_NUMBER_ID` off the sandbox onto a WABA-DIRECT number (could share the +91 per P-06.T clause 17 or take its own); run the still-OPEN P3 opener live test once `tdw_marketing_opener` approves.
5. **After bride is live: M2b** (Twilio sunset) — now ALSO covers: unsubscribe the three Twilio apps from WABA-PROD, retire WABA-PROD itself (its 14 templates + the `_hx` legacies die with it, founder veto), release the Twilio +1, delete App-DEV `1073…` (name-collision hygiene). None before the gate.
6. **Then P4** (crons/reminders; `0086_nudge_optout`; F-05.5 straggler pass — templates now file-by-API in minutes, price that in), then the 05 tail, then certification. Then the founder-ruled arc: Block 06 resumption (F-06.13, F6, the zero two-green clock) per the masterplan.
7. Carry-overs unchanged: F-05.3 durable `engines.node>=22` · F-05.7 var reconciliation · Meta media-inbound shim (text-only today) · display name "In review" on the reborn +91 (expected to clear; witnessed sending regardless).

## 8. WHY THIS BANK (honestly)
The seventh chair banked into a ZIP because context bled — the handoff itself said it "bled context chasing symptoms," and its final gift was the correct problem shape (wiring/identity tangle) that this session rode to the root. This session banks at the natural seam: the vendor lane is live and witnessed, the founder is at the end of a long night, and everything unrecorded now lives in this note. What HELD tonight: derive-or-declare (every Graph fact founder-hand, every repo fact by-command), evidence-over-narrative (the kickoff's own hypothesis was half-wrong and was corrected twice — the subscription was necessary-not-sufficient; the target_ids gate was withdrawn when [D] falsified it), and the guardrail on irreversible moves (exactly one was made, ruled, and it was the cure). The repo is truth; seat, verify, pay the log debt, and take the bride lane home.
