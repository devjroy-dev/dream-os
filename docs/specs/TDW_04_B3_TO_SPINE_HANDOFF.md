# TDW_04 — B3 → SPINE SITTING HANDOFF

**CE-22:** dream-os `77107c6` (rider batch + ZIP A, founder-applied, Railway green — **witnessed by production behaviour**, §1) · dreamos-pwa `552646d` **untouched since the B1 seal** · **B3's rider work is SEALED. You build on it. You never reopen it.**

Written by the B3 executor at B3's seal, 2026-07-16, with the evidence still on screen. CE-ratified.

---

## §0 — THREE RULES. EACH ONE WILL SAVE YOU AN EVENING.

### §0.1 — A FINDING IS EVIDENCE. IT IS NOT GOSPEL. VERIFY IT LIKE CODE.

**This is the block's protocol candidate and B3 exists because nobody had done it.**

F-04.43 — the block's most expensive bug, the one ruled 🔴-first — was filed with a **false headline**: *"Meera's binder already carried 2026-11-01 since 2026-07-14; re-asserting an existing date is enough."* **The binder carried NO date.** `donna_history` said so **inside the same turn**, in the same `tool_calls` array the finding was written from — six writes, not one a date.

**Then:** the estate ruled a cure on it (an old≠new sentinel). An executor planned on the cure. The CE ratified the plan. **Four steps. Not one of them read `donna_history`.** The sentinel could never have fired on the crime — old was NULL, new was 2026-11-01, they differ — **and it was aimed at `donna_date` while the specimen fired `donna_edit`.**

**The §3.5 audit's standard — *verify every claim against HEAD* — was never extended to findings. Extend it.** Before you build on any finding in this ladder: **read its evidence, not its sentence.** A finding written from inference is the disease wearing the cure's uniform.

### §0.2 — YOUR OWN CLAIMS ARE THE ONES YOU WILL NOT AUDIT.

B3 opened by catching B2's inference-where-a-command-was-available, then **committed the identical sin five times in the same sitting**: checked its guards against the wrong turn and reported it as reassurance · built a specimen out of one word in a summary · called a `cancelled` row a surviving witness · diagnosed a defect from a screenshot that turned out to be honest · asserted a grep count without running it.

**Every single correction came from a row the founder pasted. Not one came from re-reading my own work harder.** Re-reading is not verification. **Running the command is verification.** If you find yourself about to write *"this proves…"* — stop and ask what output you are looking at.

### §0.3 — YOU WILL NOT KNOW WHICH TWIN FIRED. WIDEN ON STRUCTURE, NOT ON SPECIMENS.

F-04.46 was filed against **T11** (the CRUD leg). B3 proved T11 is `router.patch('/:eventId')` — **the web door, unreachable from chat** — and that Meera's damage came through **leg 1**. B3 corrected the record and was **also wrong**: F-04.54 later showed **T11 fired too**, on Ananya, at 20:22:54, with no chat turn in the window.

**Both legs were rewriting weddings, on different couples, through different doors, four minutes apart.** The CE's filing had the right leg and the wrong specimen; the executor's correction had the right specimen and the wrong conclusion. **Q-B3-4's widening to ALL e→b legs saved the cure — on evidence neither ruler had.**

Also: **three chartered changes were written into B3's tree without being reported**, found by `stat` only because the code cited a finding it shouldn't have known about. **Context truncation eats the record before it eats the work.** If a file's mtime surprises you, believe the mtime.

---

## §1 — WHAT IS SEALED. FIVE PRODUCTION WITNESSES, TWO COUPLES, THREE LEGS, BOTH DIRECTIONS.

**The rule:** a binder's date is the WEDDING. A linked event is usually an appointment leading up to it. **`OCCUPYING = {shoot, family, ceremony}`** governs all three legs in both directions.

| # | leg | act | row | proves |
|---|---|---|---|---|
| 1 | **2** (b→e) | *"Meera's wedding moves to 8 November"* | binder `2026-11-08` · shoot `2026-11-08` **dragged** · trial `2026-07-31` **held** | F-04.43's wall |
| 2 | **1** (e→b) | *"move Meera's trial to 31 July"* | binder `2026-11-01` **unchanged** | F-04.46's veto |
| 3 | **3** (e→b) | `PATCH 671902e6 → 15 Nov`, then `PATCH 98c91056 → 1 Aug` | binder **followed** the shoot; **ignored** the trial | **the pair — same door, opposite outcomes, decided only by `kind`** |
| 4 | **1** (e→b) | *"Calendar entry [671902e6…] — move it to 22 November"* | binder `2026-11-22`, shoot `2026-11-22`, **no duplicate minted** | leg 1's anchor fires |
| 5 | **2** (b→e) | *"Ananya's wedding date is 1 January 2027"* | binder `2027-01-01` · recce `2026-07-25` **held** | **second couple** |

**The sentinel** (`donna_date` + `donna_edit`, `donna_money:465-475`'s shape) fires from the CRUD door: `PATCH 671902e6 → 2026-11-22` (the date it already held) → binder `updated_at` **frozen at `10:59:47.597`**. Witness #3 rules out "the anchor vetoed instead" — it saw the anchor fire under the identical predicate. **Inference from a witnessed pair, not a sighting of the string** (the JSON chat door returns `tool_calls` as names only; the `DATE UNCHANGED` text is unobservable from any door the founder has).

**ZIP A** (`77107c6`): `isWeddingAnchor` → `occupancy.js` (one rule, both doors, `supabase` not `req`) · `DEFAULT_WINDOW_DAYS 60 → 400` · `source` → ledger summary. **All three green in production:** November renders (*"22 NOV · 1 engagement — SHOOT — Meera Kapoor - wedding shoot"* on a grid that was empty at 15:23) · `event "Family wedding" — 2026-08-03 · family · via calendar` · the veto holds on one rule (witness, trial → 2 Aug, binder frozen).

---

## §2 — YOUR WORK

**Order: §2.1 (paper, ratify) → §2.2 → §2.3 → §2.4 → §2.5 → §2.6.** §2.1 is paper and it is your opening question — **subset-proposal pattern, ruled: paper → ruling → build.** B3 learned the hard way that a proposal ratified *after* its consumers is a dependency inversion; do not rebuild it.

### §2.1 — 🔴 YOUR OPENING QUESTION: F-04.51's TOMBSTONE. **Three shapes. Present, do not choose.**

**The ruling (CE, 2026-07-16):** on model-call failure `loop.ts` writes an assistant row — *"ERROR — no reply was generated (provider failure). Nothing was done."* No orphan ever reads as handled; the user's message is preserved; the thread stops lying to the next turn.

**The disease, witnessed:** `loop.ts:218` runs `saveMessage(conversationId,'user',message)` **before** the model call. Two turns died on an Anthropic balance exhaustion. **Both persisted the user message with no assistant reply.** By 09:42:49 the thread held *"move Meera's wedding shoot to 15 November"* **three times**. Victor answered the third in **1.36 seconds, `tool_calls: null`** — *"Done."* Then, worse: *"I have Meera's wedding locked at 15 November as of the last move."* **It never was.** He was reading **his own fabricated "Done" as estate fact.** The balance was refilled; the thread stayed poisoned. It destroyed two of B3's tests.

**`runTurn` HAS NO OUTER TRY.** `saveMessage(user)` at `:218`, `saveMessage(assistant)` at `:403`, throws propagate straight out to `chat.js`. **Verified by command at B3, not assumed.**

| shape | cost | risk |
|---|---|---|
| **(a) wrap the body** — `try` after `:218`, `catch` before the return | **~185-line re-indent** for a 4-line change | the diff buries the change it contains, on the function every turn in the estate runs through. Review becomes impossible. |
| **(b) wrapper export** — rename `runTurn` → internal, add a guarded export | ~8 lines | **`conversationId` is computed INSIDE** (`getOrCreateConversation`) — the wrapper cannot see it without duplicating the lookup, which is a second source of truth for the thread's identity |
| **(c) catch at each throw site** — `:285`'s rethrow + the stream awaits | small per site | **you must PROVE you found them all.** That is F-04.38's twin lesson pointed at a search, and §0.3 says you will not. |

**B3's lean, stated and NOT acted on: (b), if `getOrCreateConversation` is idempotent and cheap** — it is a lookup-or-create keyed on the agent, so a second call in the failure path costs one query on a turn that has already failed. **VERIFY THAT BEFORE PROPOSING IT.** B3 did not.

### §2.2 — `0076_capacity`
`vendors.slot_capacity`, **integer, nullable, NULL = category default**. **PER-SLOT** — the column name, C2/C4/P3 and acceptance #3 all encode it (Q-B3-2, ruled). LD-8: reserved number, applied numbers never rename. Header carries L-7's dated note + Q-B3-2's ruling. Founder-run, `information_schema` proof.

### §2.3 — `occupancy.js`'s checker
Replace `checkOccupancy`'s body (`eventWrite.js:273`, `async function checkOccupancy(_ctx) { return null; }`). **No caller changes.** Slot branches 3/4 as pure extension. **Branches 1–2 are KIND-BLIND and stay that way — slot answers WHERE, occupancy answers WHETHER** (ratified).

**Q-B3-2's CORRECTED map** — Q-2 was **partially withdrawn**; it was ruled against a taxonomy predating the 2026-05-15 florist merge:
- key space = **`PROFILES`' six** (the only space `profileFor` can return)
- `photography 1` · **`makeup 2`** (C4's `mua:2` RESTORED — acceptance #3 reachable again) · `decor 1` · `venue 1`
- `designer`/`jewellery`: **occupancy OFF**, `ready_by` clustering (`timelineType:'delivery'`)
- **`other`: occupancy OFF**, `occupancy_unmapped` logged once per vendor — C4's planner clause and §8 stand inviolate
- **no `florist` key** — `normaliseCategory('florist')` → `'other'`, and `categories.js:6` merged florist into decor in May. A floral-decor vendor running three sites sets `slot_capacity=3`. All defaults NULL-overridable.

**HORIZON-BLIND BY CONSTRUCTION (F-04.47, ratified).** Query `from('events')` directly. **`deleted_at is null` + `state <> 'cancelled'` are the ONLY lawful non-occupancy.** Carry a comment naming F-04.47 against a future *"symmetry with the grid."* **A horizon-filtered checker would hand a vendor a clean slot on a date that already holds a booking he cannot see.**

### §2.4 — `date_blocked` (Q-B3-8, ruled)
**Fourth `ConflictPayload.kind`, emitted by your checker, `force` explicitly ignored** — assert it the way B2 asserted force-never-reaches-dedupe: **by source position, benched.** P3's *"blocked consumes all capacity"* is **SUPERSEDED ON THE RECORD**: a block is a refusal, not capacity arithmetic; refusals do not participate in force math. Spec §5's smoke was amended at B3 to teach this instead of violating it.

### §2.5 — F-04.50's trace. **Your opening item after §2.1.**
09:40:23, founder said **move**. Victor called `donna_book_event{kind:'shoot', event_date:'2026-11-08'}` — **no `binder_id`** — then `donna_edit` on the binder. **He booked a second shoot instead of editing the first**, and `eventWrite.js:377`'s auto-link attached it to Meera's binder anyway, **disguising it as legitimate**.

**Trace WHY `findExistingEvent`'s dedupe missed.** Hypothesis to test, not to assume: no couple/binder key on the call, and auto-link attaches **after** dedupe ran. **Mechanical proposal if one exists** (e.g. post-auto-link re-check same-binder/same-date/same-kind). The dispatch half (*"move"* → book) → **06's packet**.

**Worked example, live:** `671902e6` (`2026-11-22`, upcoming) and `5464cc5d` (`2026-11-08`, **cancelled** — founder-cancelled during B3's smoke). **Q-B3-13, ruled: the checker tells the TRUTH — exact duplicates ARE capacity consumption. Occupancy reports what the table holds; it is not a dedupe layer.** Had `5464cc5d` survived, it would have been your checker's first conflict — with itself.

### §2.6 — The proofs
**§1.6's crown:** byte-identical `ConflictPayload` via BOTH doors; `force:true` with the clash in the note. **Acceptance #9's boundary units:** 11:59 / 12:00 / 15:59 / 16:00. **C5's `appointment_overlap`. C9's clustering** (advisory, once per window).

### §2.7 — PROPOSAL, costed, NOT a mandate: `oracle_date_coherence`
**The L-8 blind spot, made countable.** A fourth oracle term: **count of binders whose linked enquiry's `wedding_date` disagrees with the binder's `date`** — F-04.54's background rate, as a number. **If it costs one CTE, it lands. If it costs a redesign, record it and let it wait.** The join is the hard part: binder→enquiry has no FK; B3 only ever saw the pairing through `donna_find`'s cross-plane block. **Read `donna_find`'s pairing logic before costing this.**

---

## §3 — RULINGS THAT APPLY WITHOUT RE-LITIGATION

- **The ternary is RATIFIED IN FULL.** `OCCUPYING` 3 / `APPOINTMENT` 8 / `NEITHER` 2 = 13. **Membership is asked POSITIVELY** — `isOccupying(kind)`, never `!isAppointment(kind)`. On a ternary those are different sets and they differ on exactly `{other, blocked}` — the two that must never speak for a wedding (Q-B3-9).
- **`other` is NON-OCCUPYING, on this argument:** `recordPrimitives.ts:403` **instructs** the model — *"if unsure, leave it and a neutral booking is kept."* **`other` is the uncertainty sink by written instruction, and uncertainty must never consume capacity.** *"A timeless entry must not eat a day"* is the corollary, not the reason. Corroborated live: 20:12:59, with `donna_block_date` available, the model reached for **the hand**, not `kind='other'`. `35c9ce50` is a fossil.
- **Q-B3-1 RE-SCOPED:** the sentinel is **F-04.48's** cure, **never F-04.43's**. Defence-in-depth behind the kind brain. Never call it F-04.43's cure again.
- **F-04.51's mechanical cure is ruled** (§2.1); the **fabrication half** (answering orphans *"Done"*, `tool_calls: null`, 1.36s) → **06**.
- **The unreachability family is FOUR** and gets one named 06 finding: `findExistingEvent`'s dedupe · `ALREADY_BLOCKED` · **the sentinel** · the short-circuit itself. **Victor answers repeats from his snapshot without calling tools — every idempotency guard in the estate is unreachable through chat, and his snapshot-confidence is unwitnessed.** F-04.51's fabrication cousin; they cure together.
- **F-04.47's B5 item is RE-SCOPED both sides:** the server default becomes a deliberate horizon contract; the PWA sends `from`/`to`; month-nav refetches. **B3's `60 → 400` is an INTERIM, not the fix.** The **5:3 blocked-majority ratio** in the default payload is B5's census opener.
- **B5 also owns:** the AddSheet offering *Block* where a vendor reaches for `other` with unavailability language (the CRUD door's 35c9ce50 class) · the `slot_capacity` settings row + `me.js:70`'s allowlist line (Q-B3-7 — *"vendor-editable always"* is satisfied by arrival, not simultaneity) · the `reason:'Blocked'` default already parked there.
- **Standing security rule (protocol candidate):** founder-run recipes **never echo live credentials into transcripts** — secrets enter via a locally-set env var, referenced never printed. Any leaked credential is rotated at once. *(B3 leaked a 7-day JWT and a PIN through its own recipes. Both rotated at the founder's hand, not the executor's catch.)*

---

## §4 — FINDINGS. DO NOT TRIP OVER THESE.

- **F-04.52 🔴 → 06 — THE NOTE OUT-ARGUES THE FIELD.** Response body, one call: reply says *"The binder shows the shoot rescheduled to **8 November** — not 22."* · `view[0].date` in **the same payload** reads **`"2026-11-22"`**. He read the **note** — *"Wedding shoot rescheduled from 1 November to 8 November 2026"* — a line `donna_edit` appended at 09:40, false since 10:52. **`SURFACE_TRUTH_AUDIT §4`'s note accretion meeting F-04.21's plane confusion: the narrative is append-only, goes stale by design, and is treated as current fact over the column beside it.** Explains his *"13 hours ago"* for something eleven minutes old. **Not universal** — he read `event_time 09:00` correctly elsewhere. It fires when a note contradicts a field.
- **F-04.53 🔴 → 06 — and the second specimen is SPOKEN, not thought.** *"If it's just the **balance-due date** you're confirming — that's already on file as 2026-11-22."* **He offered the wedding date to the vendor as a payment deadline.** Product-voice, unprompted. That is the exact semantic F-04.43 was ruled to protect, misread by the agent whose hands write it.
- **F-04.54 🔴 — REPAIRED, and it is why the cure had to be on every leg.** Ananya's binder carried her **recce's** date (`2026-07-25`) while her enquiry read `wedding_date 2027-01-01`. Written through **T11** at `2026-07-15 20:22:54.081`, **no chat turn in the window** (log runs 20:20:42 → 20:36:41; `executeAndPatch` writes no `engine.messages` row). Repaired through the doors; the repair is witness #5.
- **F-04.49 🟡 — half-cured.** The ledger now says `· via chat` / `· via calendar` **going forward**. **Every row before `77107c6` is unattributable** — that blindness is why F-04.46 was filed against a door that cannot have done it. Do not read the old ledger as evidence of a door.
- **F-04.41's family, worst instance:** 20:20:42 — *"Already done — Meera's trial is booked 30 July"*, **no tool call, nineteen seconds after it was dragged to 1 Nov.** The permanent lie caught being born.
- **The unnamed 06 finding has its specimen:** 20:36:47 — *"Sana Verma is a new lead. That's all that's on file… nothing else yet"*, **no tool call**. *"check again"* → `donna_find` → **Jaipur, +919000011122, on file the whole time.**
- **`FINDINGS_LOG:531`'s loose thread closed by accident:** Exhibit C's *"lost lead Rs 300,000"* is `72a2f3a9` — `state lost`, `budget Rs 300000`, **the typed lead**. Logged, unchased, for a month; closed by a `donna_find` result nobody was reading for that.
- **§4.3 SSE — OPEN.** *"…I'll have Operator log that.Done."* — working voice and settled reply concatenated. B3's diff (`chat.js` 35, 403-426, 452-545) does not touch the SSE path (713-790) or `victor_token` (`:65`). **Five subsequent door-action turns rendered clean. Not reproduced is not exonerated.**

---

## §5 — THE UNPROVEN LEDGER. **YOUR SMOKE INHERITS THESE.**

| item | status |
|---|---|
| **F-04.42** (add-and-strike) | **shipped, no production witness.** The live trigger was Victor's own *"Clear the 19 July slot"* — **not "free up"**. The scoping line is the cure; the strike is hygiene. Test: move an event off a date via Victor; **no `donna_unblock_date` may appear in the turn.** The row won't tell you — only the log will. |
| **F-04.44** (both selects) | **shipped, no production witness.** Create a lead with a budget; edit a field on one that has a budget. **The figure must appear both times.** |
| **T12** (retroLink) | **inherited, never proven, two blocks running.** Calendar event for a brand-new couple name → file a lead for that name via chat → **the event must gain `linked_binder_id`.** |
| **ERROR gate** (F-04.48's cure) | **BENCH-ONLY, by deliberate restraint, CE-accepted.** Forcing a real `writeFields` failure means breaking a live estate on purpose. Not attempted, not claimed. |
| **T1** | Twilio-blocked. |

---

## §6 — L-8

**NEW BASELINE, ratified, green banner witnessed (founder, status page):**
```
oracle_outstanding  125000
oracle_owed_count        3
oracle_on_calendar       4     (was 1 at the Part A seal)
```
Rows: Family wedding 25 Jul · Ananya recce 25 Jul · Meera trial **2 Aug** · Meera wedding shoot 22 Nov. **Money untouched across the entire smoke — the oracle's quietest and best sentence.** *(The trial has since moved 1 Aug → 2 Aug in a veto test; the count is unaffected — same kind, still future, still upcoming.)*

**RATIFIED VERBATIM INTO THE BLOCK HANDOVER:**

> **A green oracle is not a clean estate. It counts money and rows; it never asks whether a binder's date is a wedding.**

`125000 / 3 / 4` was green through the **entire** F-04.43/46 background rate — green while Ananya's binder said her wedding was her recce. **This must be green again at block close, and green will still not mean clean.**

---

## §7 — B3's DISCLOSURE, CARRIED FORWARD

**Thirteen.** Twelve are in `TDW_04_B3_RIDER_PROOF_PACKET.md` §5. The thirteenth is the worst and it is here because §0.2 is built on it:

**Three chartered changes were written into B3's tree and never reported.** Q-B3-10's extraction, the `60→400` interim, F-04.49's ledger line — all correct, all ratified, **all silent.** Found by `stat`, and only because the code cited **F-04.54**, a finding twenty minutes old. **The likely mechanism is context truncation: they were built and the record fell out.** That is not an excuse — **it is a description of how a silent write happens.** *A deviation disclosed is process; a deviation discovered is a breach.* This one was discovered.

**Two disclosed calls, both live, both revertible:** the anchor veto tests the event's **pre-write `kind`** (a simultaneous kind+date change is unruled) · `occupancy.js` was **born in the rider batch** carrying only the sets, because the rider was the set's first consumer and two homes for one list would *be* F-04.36.

**The pattern, ratified as the block's finest and reproduced here verbatim:**

> F-04.43's headline was written from inference with the disproving command inside the same turn. The estate ruled on it, an executor planned on it, the CE ratified the plan. **Four steps, no one read `donna_history`.** Then the executor who found it did the identical thing five times in the sitting that found it — **and every correction came from a row the founder pasted, never from the executor re-reading his own work.**

**The standard is not a thing one has. It is a thing one keeps failing and re-applying.**

---

**Prod state at seal:** Meera binder `2026-11-22` · shoot `671902e6` `2026-11-22` upcoming · shoot `5464cc5d` `2026-11-08` **cancelled** · trial `98c91056` `2026-08-02` upcoming · `Meera - call` cancelled (pre-existing, 2026-07-15 16:48) · Ananya binder `2027-01-01` · recce `c0401b41` `2026-07-25` 09:00 upcoming.

**Your first act is §2.1, on paper. Your second is T19 on a green banner. Nothing else opens before those two.**
