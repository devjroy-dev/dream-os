# TDW_04_LEDGER_AND_CALENDAR_FINAL (v2, consolidated 2026-07-15) — CRUDs Completion, Surface Truth, and the Calendar
**Supersedes:** TDW_04 v1 (absorbed whole as Part B) and TDW_03's unexecuted phases (absorbed as Part A). TDW_03 is CLOSED at P2 (PROVEN pending founder smoke); its handover + FROST-style residue notes bind Part A.
**Why consolidated (founder-ruled):** CRUDs and Calendar share the same disease class (surface truth, plane declaration, read-your-writes); one serial session replaces three parallel lanes. After this block: 04.5 → 05 → … serially.
**Repos:** dreamos-pwa (Part A primary) + dream-os (Part B primary, Part A read-paths) · **Doctrine:** TDW_BUILD_PROTOCOL.md; BRIDE-standard audit sitting first (§3.5).

## 0. READ FIRST
docs/SURFACE_TRUTH_AUDIT.md (committed with this spec — THE evidence base; its S/M/R numbering is cited as ST-*) · docs/TDW_03 handovers + FINDINGS_LOG (F1/F2, the Slice Door, prefill-not-fire grammar, SliceScreen tenancy law) · docs/TDW_02_BLOCK_HANDOVER (wire contract, recordCompleteness amender rights, POST-verb truth) · TDW_04 v1 spec text (Part B's content, in docs/specs) · the 02-HOTFIX-2 handover (lands before Part A3; its snapshot/activity changes are Part A's read-path ground truth) · live handlers per protocol §6 — verify every route named below.

## 1. LOCKED (founder + CE rulings binding this block)
| # | Ruling |
|---|---|
| L-1 | ST-1: lane declarations on EVERY record surface — one provenance line under each title, house voice: cabinet "Everything you've filed — your working binders + calendar" (replaces "Everything kept"); Leads "Enquiries pipeline"; Clients/Invoices/Expenses "From your binders"; Events "Your calendar". The cabinet column "Booked" renames to its truth ("On the calendar"). No surface may claim totality it doesn't have |
| L-2 | ST-5: the Leads swipe-delete is DEFANGED — it uses the real soft-delete door (removes the snapshot line); "Mark lost" becomes a deliberate, separate action with its own confirm. The booked/LOST manufacture line (M3) dies |
| L-3 | ST-2: the cross-chip universalizes — cabinet binder cards, Events rows (via linked_binder_id), Invoices rows; tappable → jump to twin; each list discloses its blindness once ("Some entries may also exist as enquiries — phones connect them") |
| L-4 | ST-4: read-your-writes on the chat screen — GreetingLine/Ledger/CommandBar money+invoice counts repoint to the binder-derived numbers (the /today handler already computes them); stale public.invoices rows stop being counted or get settled; "New Enquiries" stays typed and says so |
| L-5 | 03's inherited laws: prefill-not-fire primer grammar · SliceScreen tenancy (migrate-comments honored as slices rebuild) · F2's cure = optimistic-write + undo in Part A2 · the P1 remount judged under live navigation in Part A2 |
| L-6 | Part B absorbs TDW_04 v1 VERBATIM (phases → B1–B6) + Addendum B (imports + Snap-a-Date → B7–B8): eventWrite one-writer, slots, capacity, occupancy, date-aware Victor, day sheet, heat grid, conflict verdicts, ICS/CSV with mandatory preview + batch undo, photo-to-events with confirm-before-write. v1's rulings tables carry whole |
| L-7 | Migration ladder truth: 0074 was consumed (scope_org_id drop). Part B's two reservations shift to **0075 + 0076** with this dated note; downstream unapplied reservations (04.5 onward) shift at their own audit sittings per LD-8 (unapplied reservations move freely with a note; applied numbers never rename) |
| L-8 | The cross-plane census harness (TDW_03_CROSSPLANE_CENSUS.md) re-runs at this block's close per its standing law |
| L-9 | 02-HOTFIX-2 (engine lane, runs in the existing hotfix session, NOT this one): ST-3a rebuild reads engine.records · ST-3b twin-annotated snapshot lines · ST-3d door writes logged + activity window widened for record mutations · ST-3e the empty money_entries read gated · ST-6 same-value guards on the money stamps. This block's Part A3 verifies against its results |

## 2. PROPOSED — AWAITING FOUNDER RULING
| # | Proposal |
|---|---|
| P-a | ST-7, the cabinet's role: CE recommends list pages = canonical lane-labelled surfaces; the cabinet demotes to a chat-adjacent glance ("what you've filed with Victor"), cards deep-linking to slices. Alternative: single person-page (deferred as TDW_16's presentation precursor). Founder's word before Part A4 ships the cabinet strings |

## 3. PART A — CRUDs completion + surface truth (dreamos-pwa; four sittings)
**A1 — Wishbone + lanes (ST-1/L-1):** the WishboneSheet goes live (missing-cell chips gain their tap; complete_inline PATCH for leads / POST for binders per the wire truth; tell_victor prefill per grammar) · every lane declaration + rename lands · both themes, tsc, executor browser pass, founder smoke.
**A2 — Swipes, bulk, optimistic honesty (L-2/L-5):** the approved swipe table per slice with the defanged delete + separate Mark-lost · bulk-select + BulkBar · optimistic-write + 30s undo (F2's cure) on every mutation · the remount judged under live slice→slice navigation, verdict recorded.
**A3 — Mastheads + read-your-writes (ST-4/L-4, after HOTFIX-2 lands):** slice mastheads (pipeline value, outstanding, month figures per v1's card) · the chat-screen repoint · phantom-invoice settlement or exclusion, proven against the Invoices page in one screenshot · ST-2 chips universalized (L-3).
**A4 — AddSheet, splash, cabinet verdict, sweep:** draft-first AddSheet with All-details expander · the cold-open splash per the 03 addendum (portfolio-first) · ST-7 per the founder's P-a ruling · Part-A acceptance sweep + the census re-run staged for block close.

## 4. PART B — the calendar (dream-os + pwa; six-to-eight sittings)
B1–B6 = TDW_04 v1's P1–P6 verbatim (read that spec as this section's text; its READ FIRST, guardrails, and acceptance apply). B7–B8 = Addendum B verbatim (ICS/CSV import; Snap-a-Date). Two amendments only: migrations renumber per L-7 (0075_events_slots, 0076_capacity — headers carry the dated note), and B-phase Victor context work verifies against HOTFIX-2's snapshot changes rather than assuming v1's pre-hotfix shape.

## 5. ACCEPTANCE (block)
Part A: every surface carries its lane line (screenshot set) · M3 unreproducible (swipe-delete leaves no lost line in the snapshot — SQL-verified) · cross-chips on all four surfaces + blindness lines · chat masthead figures match the Invoices page to the rupee · wishbone round-trips both verbs · optimistic undo restores in-place · both themes throughout. Part B: v1's acceptance + Addendum B's, under the new numbers. Block: the census harness re-run shows no NEW twin mechanisms (M1/M2 remain until 16 by design; M3 dead) · masterplan flips 03 CLOSED-AT-P2 + 04 DONE · handover names 04.5's inheritance.

## 6. SESSION LAW
ONE session, serial sittings, audit-first per §3.5 (its packet includes verifying every ST file:line claim against HEAD — the audit is evidence, not gospel). HOTFIX-2 is the only other live lane and closes before A3. No parallel block sessions until this closes (founder's consolidation ruling).
