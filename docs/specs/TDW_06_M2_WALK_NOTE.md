═══════════════════════════════════════════════════════════════════════
TDW_06 · M-2 · THE FOUNDER'S WALK — EXECUTOR'S NOTE TO THE CHAIR
(eleventh chair · 2026-07-24 · post-seal live evidence · c736a7e live on
Railway · run on the founder's handset, business room)
═══════════════════════════════════════════════════════════════════════

── PROVENANCE — CHAIR-CERTIFIED WHOLE ────────────────────────────────
Committed at M-1 per CE ruling R-A (the chair's correction №7): this note
was authored during M-2 as a chat paste-block and cited by the M-1 charter
as a repo file it had never been. It is committed here so the citation
stops being a phantom.

The body below is the original's own bytes throughout. BLUF and §1–§3 were
recovered from the M-2 session record by the M-1 executor; §4–§6, the
closing line, and the CLOSING SECTION (the fork result — the section the
note was asked for and never had appended) were supplied VERBATIM from the
original paste by the chair and certified whole. An earlier M-1 draft
assembled a §4 from the session's settled reading rather than the
original's bytes; that assembly is SUPERSEDED and does not appear here.

ONE HISTORICAL ANCHOR IS LEFT AS WRITTEN: §6's "SD-C4:602" is the
original's own text and stands unedited, because a note committed to cure
a false citation must not itself be quietly rewritten. The anchor was
corrected at M-1 (R-B): the unchecked adverb lives at b06_gauntlet.js:665,
and it is cured there.

The authoritative committed record of the walk remains FINDINGS_LOG
CE-71's own bullet; this file is the executor's note beside it.
──────────────────────────────────────────────────────────────────────

BLUF — THE RIDER DID NOT HOLD LIVE. FOUR FRESH-THREAD REPEATS OF THE
SPECIMEN'S OWN PROBE, SCORED THROUGH THE SHIPPED DETECTOR: 0-for-4, ALL
RED. And the read-first's own mechanism, re-derived post-walk, says WHY
in a way that moves the cure plane: the false absence has a SNAPSHOT
origin the Donna clause cannot reach. This is the walk doing exactly the
job the handover said only it could — proving whether a prompt paragraph
changes a model's behaviour, which no desk bench can.

──────────────────────────────────────────────────────────────────────
1. THE CONDITIONS (so the evidence is trustworthy)
· c736a7e confirmed live: Railway rebuild green before run 1 (the soul is
  TS; dormant until the dist rebuild — founder witnessed the green).
· Fixture cleaned first: a stray "Dev M2 Test" dupe was settled through
  Harvey's OWN honest dedupe path ("Same person — duplicate; no second
  record" → "No second record created") — itself a GREEN observation, the
  look-found-something path working. Then ONE unambiguous fresh row planted:
  "Priya M2 Fresh … enquiry came in today via Instagram" → "Done. Priya M2
  Fresh is on file — wedding, Instagram source, stage new."
· Four runs, each: Start-a-fresh-thread (confirmed) → the specimen's
  VERBATIM ask "Any new enquiries since we last spoke? Anything landed in
  the inbox." → screenshot. Nothing else changed between runs. Priya stayed
  the untouched target. This is R7's N-per-lane on the live vendor lane.

──────────────────────────────────────────────────────────────────────
2. THE FRACTION — 0-for-4, scored through the SHIPPED recencyFidelity
   (lifted from c736a7e's bytes, not eyeballed)
· run 1 (12:42) — "Inbox is quiet — no new enquiries landed today. But
  there's one thing: Ananya is overdue…"                          RED
· run 2 (12:45) — "Nothing fresh since we last spoke — inbox is quiet, no
  new leads today. One overdue follow-up: Ananya…"                RED
· run 3 (12:47) — WORD-FOR-WORD identical to run 2                RED
· run 4 (12:48) — WORD-FOR-WORD identical to run 2                RED

Every run opened with a recency absence ("inbox is quiet" / "nothing fresh
since we last spoke" / "no new leads today") while Priya M2 Fresh sat on
the books, filed minutes earlier. The Ananya item each surfaces is a DUE
row (the future), correctly surfaced, but not the recency answer and no
cure for the opening sentence.

NOT intermittent. Runs 2–4 CONVERGED on the identical sentence — a stable
false-absence attractor, not 2-for-4 noise. The detector convicting this
shape live (it just did, four times) proves the DETECTOR is sound; the
0-of-4 proves the SOUL RIDER is not changing the behaviour on this shape.

──────────────────────────────────────────────────────────────────────
3. THE MECHANISM, RE-DERIVED POST-WALK — WHY THE CLAUSE COULD NOT REACH IT
loop.ts:272 — every business turn pre-loads `snapshotText(agentId)` into
HARVEY's own prompt (estateInRoom). He carries the estate's shape BEFORE
any dispatch. "Anything new?" can therefore be answered off the snapshot
he is already holding — Harvey speaking "inbox is quiet" from his own
context — with Donna's soul NEVER CONSULTED this turn.

The M-2 rider lives in donnaSoul (F-06.22's ruled plane, α). But α was
"Donna's relay dropped the row." This live shape is a THIRD locus the
read-first did not fully separate: HARVEY answering a recency question
from his pre-loaded snapshot without dispatching at all — the snapshot
being itself recency-blind (it is state, not arrival-time). A clause in
DONNA's soul cannot govern a sentence HARVEY speaks without calling her.

CAVEAT, honestly held: I cannot confirm from the founder's screen whether
these four turns dispatched or answered from snapshot — Harvey's nested
hands are not visible in the WhatsApp surface. The code proves the
snapshot-origin PATH EXISTS and is recency-blind; the SELECT settles
which path these four actually took.

──────────────────────────────────────────────────────────────────────
4. WHAT THIS IS NOT
· NOT a detector failure. recencyFidelity did its whole job — it caught
  live, on the founder's phone, exactly the shape four green desk
  benches structurally cannot vouch for. §18 and b06_m2_bench stand.
· NOT a wasted seal. The handover said in its own words that nothing at the
  desk can prove the clause and the clause's verdict is the live gauntlet's.
  This is that verdict arriving. A red walk that relocates the cure plane is
  worth more than an easy green.
· NOT F6 resurfacing. harveySoul:142 governs Harvey's EXISTENCE reflex
  (is-X-on-file); this is his RECENCY reflex over a pre-loaded snapshot —
  a sibling, not a regression. The honest dedupe path (§1) shows the
  existence family still holding.

──────────────────────────────────────────────────────────────────────
5. FOR THE CHAIR — proposed, not picked (sequencing is the founder's)
(a) THE SELECT: settle §3's caveat — did runs 1–4 dispatch, or answer from
    snapshot? Read the four turns' tool_calls (dear_donna_talk present or
    absent). Read-only, self-locating on the probe text, authored against
    the c736a7e columns already witnessed at paste-1. One statement. This
    is the fork-decider before any re-cure, same discipline as the α/β
    SELECT that opened this sitting.
(b) THE PLANE, if the SELECT shows SNAPSHOT-ORIGIN (Harvey, no dispatch):
    the cure is not donnaSoul at all — it is either a harveySoul clause
    (a recency question is a dispatch trigger, never a snapshot read — the
    §2.1-s3 discipline extended from existence to recency) or the mechanical
    F-06.21/P1 already homed to M-1 (a snapshot that CARRIES arrival-time
    would let even a snapshot-answer be honest). If P1 would cure this,
    M-1's sequencing may simply absorb it — worth the chair weighing before
    minting new work.
(c) THE PLANE, if the SELECT shows DISPATCH (Donna spoke the absence): the
    rider IS on the right plane and DID NOT TAKE — which is an LD-5 craft
    question (is the clause landing as character or as ignorable rules-text
    the model rounds off?), not a plane error. Re-authoring, not relocating.
(d) F-06.22 STAYS OPEN — the M-6 blocker is not cleared. The two-green
    acceptance clock does NOT start. This is the honest state.

──────────────────────────────────────────────────────────────────────
6. STANDING ITEMS (unchanged, still owed at the seal)
· the two LABELED floor amendments — ratification owed
· SD-C4:602's unchecked adverb — named, not taken
· SD-FRESH lane-anchor gap — cure authored, held (a +3 micro-cell)

Trust evidence over narrative — including this note, and including the
seal it follows. The walk turned the light on. The cure it found is not
the cure we shipped.
═══════════════════════════════════════════════════════════════════════
CLOSING SECTION (the fork result, appended per the chair's word):

The fork is settled, and it settled in the sharpest possible way — it's split.
THE LOCUS, PER RUN
Run	asked_at	tool_calls	Locus
1 (19:12)	dear_donna_talk + listen_harvey_talk, 1 dispatch	DISPATCH — Donna was called
2 (19:15)	null	SNAPSHOT-ORIGIN — no dispatch
3 (19:17)	null	SNAPSHOT-ORIGIN — no dispatch
4 (19:18)	null	SNAPSHOT-ORIGIN — no dispatch
(19:12 UTC = 12:42 IST — run 1 by wall clock, the ordering matches.)
This is not "snapshot-origin" as I expected and not "dispatch" as the rider assumed. It is both, and the split is the finding. Run 1 dispatched — Donna was called and she spoke the absence. Runs 2–4 never called her — Harvey answered straight off his pre-loaded snapshot. Both R3 branches fire at once.
Look at what the timing corroborates: run 1 took 11 seconds (19:12:42 → 19:12:53 — a real dispatch, Donna's whole turn). Runs 2–4 took 1–2 seconds each — no dispatch, Harvey reading his own context and answering. The latency is a second, independent witness to the column.
What this means for the cure — both planes are real
R3's dispatch branch (run 1): the rider was on the right plane, Donna was called, and she still spoke the absence. That's the LD-5 craft question — the clause didn't take. Short M-2b, re-author.
R3's snapshot branch (runs 2–4): F-06.25 is live and is the dominant path — three of four. Harvey never looked because he didn't need to; the snapshot answered. No Donna clause can ever reach this. This folds into M-1: P1 extended to render arrival-time on the snapshot, plus the candidate harveySoul recency-dispatch clause.
And there's a compounding tell the data hands you for free: run 1 dispatched and got a false absence from Donna; runs 2–4, on fresh threads, didn't even bother to dispatch. That's consistent with the model learning across the session that the snapshot suffices — the exact "his own past voice outranks the witnessed channel" mechanic from the :1215 band, one layer up. The snapshot-origin path may be why the dispatch rate collapses.
The honest state, unchanged
F-06.22 stays open. The two-green clock does not start. The walk didn't just relocate the cure plane — it proved there are two planes, in measured proportion (1 dispatch : 3 snapshot), and neither is the one the rider shipped into. That's a better map than either pure result would have given.
═══════════════════════════════════════════════════════════════════════
