# TDW_09 · QUEUE ADDENDUM — TWO ITEMS RE-HOMED FROM TDW_14 D-4
(authored by CE-33, 2026-08-14, on the founder's word. Terminology, so
no seat ever wonders: Row 9 = Block 9 = TDW_09 — the agent/soul arc.
This addendum is a DELTA: the standing queue at
TDW_CE_SUCCESSION_NOTE_32.md §3② is unchanged; these two ADD to it.
The Row 9 chair derives everything below at its own tips before
building — this doc locates the work, it is not evidence.)

## ITEM 1 · G-5 CLOSES HERE — the straggler tool's retirement notice
`create_task` (brideTools.js) is the last live-described task tool
while its four siblings carry "DEPRECATED — use *_event instead."
Its cure is ONE DESCRIPTION BYTE: deprecate toward
`create_event kind='task'`, symmetric with the siblings. The executor
stays live (never strand a caller mid-retirement). Tool descriptions
are model-facing text — this cuts under Row 9's W-1 lift, which is
why CE-33 moved it out of D-4 (R-31.2: brideTools.js/brideEngine.js
are Row 9's files; a mechanical edit days before the soul re-author
is the collision R-31.2 exists to prevent).

## ITEM 2 · THE WHATSAPP ASSIGNMENT ARM
`create_event`/`update_event` gain the OPTIONAL assignee param
(schema + executor) writing `events.assigned_circle_member_id` —
the column TDW_14 D-4 ships at 0125. Two constraints inherited whole
from D-4's rulings, non-negotiable at build:
  · the write routes through `coupleEventWrite.js` — the couple
    plane's ONE writer (TDW_05 ARC M6). Extend, never fork.
  · `events.assigned_member_ids` (vendor crew, team_members
    id-space) is UNTOUCHABLE — the never-both cell in D-4's bench
    is the guard; Row 9's work must keep it green.
Until this item lands, the bride assigns on the bloom only — the
labeled D-4 acceptance departure this item repays.

## SEQUENCING
Both items ride Row 9's normal read-first → ruling → build chain,
under its charter's W-1 lift, at whatever position its chair prices —
this addendum adds queue items, never order. The circle send-site
delivery (assignment nudge + completion notification + F-14.7's cure,
travelling together per R-D3.6's shape) remains OUTSIDE both this
addendum and D-4 — population-gated, its own day.
