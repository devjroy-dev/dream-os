# CE-42 · 4b-3b · R6 · THE SUNDAY BRIEF, WIRED (dream-os + one pwa flip) — HANDOVER · seat R6

1. **Shipped (dream-os).** `src/lib/vendor/sundayBrief.js` — the one writer of `0165`'s `public.instagram_briefs` and the one reader the door and the job share: the week ruled (b) (Monday 00:00 → Saturday 23:59:59 IST through `istDayWindowUtc`, the label still the calendar week) · every state the shell draws answered as a CODE (`pending` before any row when the plane is off · `notconnected` / `connect` / `expired`, the three the chair kept as states · `error` from the row, its reason never travelling · `stale` by comparison AT THE DOOR, never by a job · `empty` from `media_count` · `live`; under-100 and the arrows are PAYLOAD shapes, `new_followers: null` and `prev: null`, which is how the shell already reads them) · "Check again" ruled (b) (generate now, throttled 10 minutes on `generated_at`) · the Sunday job, the plane read FIRST so a dark plane costs zero network for anybody. `igOAuth.js` gains the Instagram-Login insights readers on `graph.instagram.com` (`fetchAccountInsights` `reach,follows_and_unfollows,saves,shares` · `period=day` · `metric_type=total_value` · `since`/`until`; `fetchMediaInsights` asking the wire's `saved` and answering the stored `saves`; `fetchFollowersCount` on `/me?fields=followers_count`; `probeInsightsScope`), the incremental authorize (ruling 13(b): a signed state FLAVOUR, `IG_SCOPE` untouched for every other vendor), and `listInstagramMedia` MOVED IN (F-42.174). `igConnection.js` gains `tokenForCall` (F-42.177, lifted out of the router), `markInsightsGranted`, `listInsightsConnections`. `ig.js` mints the flavour on `?scope=insights`, PROVES the grant with one account read before storing it, and returns the insights flavour to `pwaPaths.vendorPath('posts')` (new key, F-42.192's precedent). `capabilities.js` gains `PERM_INSIGHTS` — the first `perm.*` constant, landing with its two readers (ruling 14(b)). `capabilitiesSweep.js` gains `probeInsights` (ruling 15(a)): `admin_config.ig_probe_vendor` → DEV440 → her own token → `graph.instagram.com`, never `graph.facebook.com` (F-42.164 cured). `postCards.js` gains kind `brief` and `briefCardUrl` (share card ruled (a)). `posts.js` gains `GET /sunday` and `POST /sunday/refresh`. `cron.js` gains `'0 7 * * 0'` in `Asia/Kolkata`. Migration `0165` (2 statements).

2. **Shipped (pwa, the flip).** `SUNDAY_PREVIEW = false` in the same edit that wired the door. `sunday.ts` gains `SundayDoor` and `SundayActions`; `routes.ts` gains the three addresses; `SundaySection` takes `actions` and its controls become real — Connect Instagram is a PRE-MINTED `<a href>` (portfolio/screen.tsx:470–540's law: no `await` between her finger and the navigation, re-minted every 8 minutes while the tab is visible, since the server state lives 10), the best-post row opens its `permalink`, Share this opens the card, Share/Download are the cards section's own handlers over the signed brief card, Check again POSTs refresh. **Zero chrome changed** (R-42.14): same classes, same bytes, same order — b76's inventory is the contract, and without `actions` every CTA still toasts `COPY.launchingSoon`, which is how b76 still renders all eleven states.

**Cut 2 (the founder's floor on cut 1 read one delta).** `b68_introductions_bench` §7 — `4a adds NO perm.* constant — 4b owns those with their readers` — pinned `!Object.values(K).some(v => v.startsWith('perm.'))`, a clock spelling of a rule whose own label says 4b lands them. Ruling 14(b) is that landing, so the cell convicted the delivery it was written to permit. AMENDED BY LABEL (R-41.121) to the roster law at `capabilities.js:266–268`: every `perm.*` constant on the roster is named by at least one file under `src/` besides the roster itself, plus a second cell holding 4a's own arm (`introductions.js`) to naming none. Both ways: green at the untouched tip (zero perm keys, vacuous) and at this tree (PERM_INSIGHTS read by `sundayBrief.js` and `capabilitiesSweep.js`); RED under a readerless perm constant — `PERM_PUBLISH` added to the roster reddens it, which is the thing 4a was guarding. Cut 1 (`r6-4b3b-sunday-os-c5de470.zip`) is superseded, not retired: it applied cleanly and 0165 was run from its blocks and is NOT re-run. Cut 2 is 18 paths — cut 1's 17 plus `scripts/b68_introductions_bench.js`.

3. **Proven / not proven.** b77 39/39 with **17 production mutations, 16 RED by name** (manifest §6). **M6 does not redden and that is declared, not hidden**: removing the door's `insights_granted_at` guard still answers `connect` with zero Meta calls, because the arm's own guard answers the same code — the state is unreachable-by-two-paths, not unguarded. b76 amended by label (R-41.121) and **19/19**; `tsc --noEmit` clean. Three clock-spelled pins amended by label: `b73` 5.3 and `b75` 7.3 (both pinned `=== 3` doors; the meaning is EVERY door guarded, and the pair must match and never shrink) and `b07_p4a` §12.3/12.4 (the callback slice ended at `async function tokenForCall`, which left the router at F-42.177; it now ends where the next door begins). b07_p4a 110/110, b73 24/24, b75 19/19. The benches that read a touched module exit identically at untouched `c5de470` and here. **NOT proven:** the full floor (the seat's window cannot hold it — it runs in the founder's verify block) · `npm run build` (this box cannot reach fonts.googleapis.com, so `next/font` fails and the run exits 1 with no error naming a touched file; the founder's build is the gate, as it was at 4b-3a) · the walk · one real Meta insights response. `PUBLIC_SCHEMA.md` regen is owed at 0165 (and still at 0164).

4. **Next.** The founder's walk (card §7) · whatever the walk teaches about DEV440's tester-role token — under 100 followers is the likely first frame, which is S3 and is drawn · a later packet for the WhatsApp pointer (fork 12 kept (a): PWA only this block) · `best_time` stays HELD until `online_followers` is witnessed (F-42.164's second half; it is not in Meta's Instagram-Login metrics table at all).

5. **Base.** dream-os `c5de470c004b12d68b98fcdca2455feceee2ac76` (4b-2's own tip, re-derived at the cut). dreamos-pwa `a9b5e0cd6ec58f2725a500cb6902c7973d30cc24` (R7's 4c-3a shell over 4b-3a's `948176dc`). Collision derivation: R7's 4c-3a touched none of this packet's five pwa paths (each is the same blob at `948176dc` and `a9b5e0cd`), so the flip lands on the shell exactly as it was benched.

---

## 6 · THE MUTATION MANIFEST (b77, both ways)

Each line is one edit to PRODUCTION source; the cells named go RED and return GREEN when it is reverted.

| | file | mutation | RED |
|---|---|---|---|
| M1 | igOAuth.js | `ACCOUNT_METRICS` asks `impressions` (Meta's deprecated name) instead of `follows_and_unfollows` | 1.2 · 1.4 · 5.2 |
| M2 | igOAuth.js | the media read asks `saves` (the stored name) instead of the wire's `saved` | 1.3 |
| M3 | igOAuth.js | `authorizeUrl` widens the scope for everyone, not just the flavour | 2.2 |
| M4 | igOAuth.js | `probeInsightsScope` reads a permission refusal as granted | 1.6 · 7.3 |
| M5 | sundayBrief.js | the door reads a row before the plane | 3.2 |
| M6 | sundayBrief.js | the door drops the `insights_granted_at` guard | **NONE — declared above** |
| M7 | sundayBrief.js | the window runs through Sunday instead of Saturday 23:59:59 | 5.1 |
| M8 | sundayBrief.js | `stale` decided by age alone, not by the week | 4.5 |
| M9 | sundayBrief.js | the Check-again throttle dropped | 5.7 |
| M10 | sundayBrief.js | the job reads Meta with the plane off | 6.1 |
| M11 | sundayBrief.js | the token is appended to an error row's reason | 5.4 |
| M12 | capabilitiesSweep.js | every `perm.*` row routed to the insights probe | 7.1 |
| M13 | capabilitiesSweep.js | the evidence says "app mode read" | 7.2 |
| M14 | postCards.js | the brief card's base is not colorized (her photo shows through) | 8.1 · 8.3 |
| M15 | cron.js | the job fires Monday instead of Sunday | 6.3 |
| M16 | ig.js | the grant is stored without the proving read | 2.3 |
| M17 | igConnection.js | the job lists connections without the grant | 6.2 · 9.3 |
| M18 | capabilities.js | a second `perm.*` constant on the roster with no reader (`PERM_PUBLISH`) | b68 §7 |

## 7 · THE FOUNDER CARD (R-42.4)

**Truths only his device witnesses:** the consent screen, and the numbers.

1. **Run `0165`, one statement per paste block, in the Supabase editor.** Block 1 creates `public.instagram_briefs`; block 2 adds `vendor_ig_connections.insights_granted_at`. Then the verify block (commented at the foot of the file, uncomment it to run): expect **2 rows** — `table 1`, `column 1`.
2. **Apply the dream-os ZIP (cut 2),** then verify, then push. The chain, verbatim: `unzip -o FILE.zip && cp -r deploy/. . && rm -rf deploy FILE.zip`
3. **Apply the pwa ZIP** the same way, on the pwa workspace. `npm run build` is the gate here (the repo script carries `--webpack`); it did not run on the seat's box.
4. **Railway green** on dream-os after the push (the cron registers at boot; the log line to look for is nothing — a Sunday job says nothing until Sunday).
5. **The four dev-mode steps.** (i) Confirm DEV440's number is a **tester on app `1425513376067685`**. (ii) In Posts & ads → Sunday, tap **Connect Instagram** — long-press → "Open in New Tab" if the Instagram app claims the tap (the standing note). (iii) The consent screen should now list **insights** beside the basic permission; Allow. You land back on **/vendor/posts**, not the portfolio. (iv) **Switchboard → Check now**: the sweep reads `perm.instagram_business_manage_insights` on DEV440's own token and should move it `pending → approved`, with an evidence line naming DEV440 and saying `app mode unread`. Then **flip it On** — that tap is the R-41.8 tap **on your walk**, not on a grant from Meta; the probe cannot tell a tester's 200 from an approved one (F-42.175).
6. **The brief.** Sunday 07:00 IST writes it. Before Sunday, **Check again** generates it now (once per 10 minutes). Expect the real numbers for 7–13 September. **If DEV440 is under 100 followers, "New followers" will carry "Instagram shows this after 100 followers." and that is correct** — Meta withholds the metric, and that is S3 from the frames.
7. **The share card.** Tap **Share this** → the brief as a 9:16 card (Graphite plate, the four numbers in gold, your page address). Share and Download behave as the cards section's do. **This is the one artefact no bench can witness**: the transformation string is pinned, the pixels are yours to see. If the plate is not solid, say so and the colorize line is the single site to change.
