# CE-42 · 4b-2 · R6 · BROADCASTS + THE ROLE LINE (dream-os) — HANDOVER · seat R6
1. **Shipped:** `src/lib/vendor/broadcasts.js` — the one writer for 0164: the ruled list (clients ∪ wedding couple phones ∪ consent phones ∪ booked leads, last-ten de-dup, source carried) · the fee as whole paise by BigInt (1 → 102, 6 → 612) from 0163 · a dark attempt writes nothing · referral once per IST year (0164's index; 23505 read as already_this_year) · stopped numbers filed `refused_stopped` · the inbound arm (F-42.171: STOP → stopped_at, never falls through, zero prospects writes; any other text stored on the row, replies APPENDED, ruling (b)). Hooks: `prospects.js` (the branch above STOP, after the introduction branch; the consent guard gains `!broadcastMatched`) · `relayStatus.js` (the ninth home) · `posts.js` GET/POST `/broadcast` (refusals as codes; `dark` never carries a key) · `CAPABILITY_KEYS` COUPLE_/REFERRAL_BROADCAST · registry entries `couple_broadcast` (approved), `referral_broadcast` (approved — cut 2, on the founder's WhatsApp Manager capture of 2026-09-10 14:45 IST reading Active; TEMPLATES.md row 17 still says In review, seat G2's to update), `introduction_reply` (draft), bodies sha-equal to TEMPLATES.md rows 15–17 · F-42.172 ruled: `postCards.js` ROLE_LINE, raw mehendi/mehndi/henna first, else the normalised category, else "By {business_name}". Migrations `0163` (1 statement) and `0164` (6).
2. **On the glass (with the pwa half):** Posts & ads → Broadcast shows her past couples, the fee upper bound, both messages; Send works once the founder turns `template.tdw_couple_broadcast` on at the Switchboard; referral stays dark until the sweep reads row 17 Active (Switchboard → Check now) and the founder turns `template.tdw_referral_broadcast` on.
3. **Proven / not proven:** b75 19/19, 16 production mutations each red by name · b73 24/24 (role cells 2.4/2.6/2.7 red under their mutations) · b64 149/149 + b64_mutations 6/6 (F-42.201 cured; the pair LEAVES the named base: R7's 25 → 23, rebuilt onto 728cd1a's file) · b66 amended by label, 78 → 79, chair-ratified (R-41.121) · the 47 benches that read a touched module exit identically at untouched 728cd1a and here, except the b64 pair (red → green) and b75 (new). 0163/0164 proven on PostgreSQL 16; the six paste blocks produce a schema dump identical to the committed file. NOT proven: the full floor (the seat's window cannot hold it — it runs in the founder's verify block), the walk, a real send. PUBLIC_SCHEMA.md regen is owed at 0164. The 4b-1 handover's F-42.201/b73 note rides this ZIP; the micro `r6-4b1-os-handover-aa1f5ec.zip` is retired unapplied.
4. **Next:** the pwa half (Broadcast section, formatRs paise arm) · 4b-3 the Sunday brief (dark; "Launching soon." on tap while pending) · a later packet shows her the stored replies.
**Cut 2:** the first cut (`r6-4b2-broadcasts-os-728cd1a.zip`) is retired unapplied; the only change is `referral_broadcast` pending → approved. The founder's 0164 verify read 4 of 5 indexes — statement 3 (`uq_broadcasts_referral_year`) re-handed alone before the ZIP.
**Cut 3 (chair-ruled (a)):** the founder's floor on cut 2 read one delta — `b40_victor_sitting_bench` §26 "…ZERO DDL — no migration is added by this rider" (V-2 rider r2, `7160b5f`) pinned the clock spelling `/^016[3-9]/` in `db/migrations`; 0163/0164 tripped it while its meaning held. Amended by label (R-41.121) to the meaning — no migration names `expense_facts`; both ways: green with 0163/0164, red with a probe migration adding an `expense_facts` column. Cut 2 (`r6-4b2-broadcasts-os-728cd1a-cut2.zip`) retired; the SQL was run from cut 2's blocks and is not re-run.
5. **Base:** dream-os `728cd1aac8aa0dac40b4d617ab7f52a0afa7fc7a` (R7's 4c-1). Collision derivation: R7 touched none of this packet's source paths (every modified path is the same blob at aa1f5ec and 728cd1a); `scripts/floor-base.txt` rebuilt onto R7's.
