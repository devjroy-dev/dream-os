# repo: dream-os @ cd71986 (base) · dreamos-pwa untouched · code · TDW_CE45_LCV15_LSP_4.zip
# TDW · CE-45 · SEAT LCV-15 · LSP_4 · F-05.56's ISLAND AND ITS ORPHANS, DELETED (L4-a; 7.6 ruled (i)) · 2026-09-24 IST

Line numbers derived by command at cd71986; re-derive before citing. Trust evidence over narrative, including this file.

## 1 · WHAT GOES
src/agent/engine.js (1847 → 737 lines): F-05.56's DEFUSED ISLAND (:752 to :1845, the box, handleOnboarding, executeTool and every tool case in it,
commit_event_proposals among them), callerless since ARC M5; the ten requires only it read (systemPrompt, onboarding, tools, invoiceMessage, waNumbers,
invoicePdf, format, clients, whatsapp, vendor/snapshot) and WA_MUTATING_TOOLS. A three-line note at site.
NAMED-PATH DELETIONS (A-45.1): src/agent/tools.js, src/agent/systemPrompt.js, src/agent/classifier.js (no caller since ARC M5), and
src/lib/vendor/replyToCouple.js (ruled (i): its one requirer was the island's executeTool, so this cut made it an orphan).
Every dropped library keeps a live requirer elsewhere (invoiceMessage, waNumbers, invoicePdf, format, whatsapp, vendor/snapshot; lib/clients.js through
lib/vendor/contracts.js :239). replyToCouple.js is still NAMED, in comments only, at engine.js :68, relayToCouple.js :4, :283, :398 and
coupleWaWindow.js :37: history, kept (relayToCouple.js is byte-pinned by b113).

## 2 · WHAT STAYS
runCoupleAgenticTurn (the couple lane, Eliza), byte-identical to cd71986 over its whole 28,912-character body, the lone export; relayAttributionPrefix,
markRelayProvenance, mergeSameRole identical; the seven requires the couple lane reads. src/agent/onboarding.js KEPT callerless (R2; b45 pins behaviour in it).

## 3 · THE BENCHES (A-45.2)
b08_p5_unblock, BASE-RED, NOW GREEN (ruled (i)): 4.4 retired (site 2 lived in the island); 2.3 re-aimed to the one lane left AND read on code. Its base red
was only two COMMENTS citing the one date home (engine.js :46, :65): e-121, with its lesson, a floor-base line is read on code, comments stripped, before
it is trusted. floor-base.txt 21 → 20; b115 pins the removal. Its dead mutation arm dropped.
b49 2.1: the refusal cured, re-derived stronger (no island, no exclusion; every line of engine.js counts).
b06_m0 4.7: a couple-lane live point re-aimed; its `iterations:` had matched only inside the island's text (a hollow green, exposed by the deletion).
b66 14.2b: engine.js's one money line reads witnessLine.rupees, the wire's one money home (R2-B).
b05_arc_m5 11 → 7 (2.1-2.3, 4.2 retired; 1.1 re-aimed; 3.1 writers 3 → 2). b05_f0532 9 → 8 (2.4). b84 129 → 128 (7.7). b06_relay_hand 75 → 74 (7.6).
b47 1.4 re-derived stronger. b55's seal cell re-aimed (its island slice would have read the file's last character: a hollow green in waiting).

## 4 · RUNG b115 (25) AND e-122
The island absent with a grep control; the couple lane byte-identical by hash, the hash pinned to cover the whole turn; the requires; the four paths absent and
unrequired; onboarding.js present and callerless; floor-base at 20; the couple lane DRIVEN (b08_p5_unblock run whole, its §1.1 through runCoupleAgenticTurn);
the money functions; two mutations. 16/25 at cd71986.
e-122: the seat's first "byte-identical" read of runCoupleAgenticTurn used a first-brace body reader, which on a destructured parameter list compares the
PARAMETERS alone. b115's own mutation failed to redden and exposed it; the reader now skips the parameter list. Every earlier rung's pins were re-checked with
the new reader and were whole (none of those functions destructures its parameters).

## 5 · THE PROOF
DIFFERENTIAL in series on cd71986, 18 benches (any bench naming engine.js or walking src/agent, the deleted paths, floor-base.txt, the amended, b115): exits
changed for b08_p5_unblock (ruled) and b115; six output changes attributed; the five re-aimed benches byte-identical to base. FLOOR: "FLOOR = NAMED BASE, no
delta (refusals, not in base: 3)" against floor-base 20.

## 6 · THE WALK AND ITS RECORD
The card: one couple-lane message from the test couple's number (9625759924) to DEV440's line, answered by Eliza; rows 1, 5 and 8 then NO on the app lane.
The record: owed at the walk.
---
A PROSE INSTRUCTION IS NOT A MECHANISM.
