# repo: dream-os @ 713340a622acc2120de5689b97e6bceada38d684
# TDW · CE-43 · SEAT LC-2 · PACKET 1 · HANDOVER (the dream-os packet) · 2026-09-17

Cut on dream-os `713340a622acc2120de5689b97e6bceada38d684`, re-derived at origin with `git fetch` at the moment of cutting (unmoved since charter). The dreamos-pwa packet cuts after this one is at origin, on its own re-derived tip (R-38.16). Migrations 0167 and 0168 are the chair's allocation (C-43.8). Findings used by the sitting so far: F-43.46 to F-43.73 (F-43.56 unused; F-43.59 is the chair's). The chair lands them (clobber law).

## §1 · What shipped

| File | What |
|---|---|
| `db/migrations/0167_leads_binder_link.sql` | `public.leads.binder_id uuid`, no foreign key; `uq_leads_binder_id` UNIQUE WHERE binder_id IS NOT NULL (F1: deleted leads included). |
| `db/migrations/0168_vendor_packages.sql` | `public.vendor_packages`; `public.lead_packages`; `invoices.lead_package_id` and `contracts.lead_package_id` (SET NULL); five indexes incl. `uq_vendor_packages_default`, `uq_vendor_packages_seed` (F-43.70), `uq_lead_packages_live`, `uq_invoices_lead_package`. Nine statements. |
| `db/seeds/package_seeds.source.txt` | The founder-vetoed seed block (R-43.10), sections 1 to 11 with the block's header, byte for byte. sha256 `91c21e187ccb5016898c0a8efa886060c3ee4c537f517aa498e4a49ee8d94be1`, pinned in b81 §1.0. The FIELD MAP that followed section 11 is not seed data; it is LC-3's record and is carried in §7 below. |
| `db/seeds/package_seeds.json` | Derived from the source by `tools/lc2_seed_json.js` through the one parser. 11 categories in the taxonomy's order, 33 options. |
| `src/lib/vendor/packageSeedParse.js` | The one parser. Ruled defaults (F-43.54), delivery basis from each section's own "Delivery date" line (F-43.51), names in sentence case (C-43.15) with `source_name` carrying the vetoed bytes. |
| `src/lib/vendor/packageSeeds.js` | `ensureSeeded(supabase, vendor)` (F-43.50 arm (b)): once per vendor, ever; conditioned on `uq_vendor_packages_seed` (the 23505 branch). |
| `src/api/vendor/packages.js` | `GET /api/v2/vendor/packages`: seeds on the first read, fail-soft, lists live rows in vetoed order, default marked not moved. |
| `src/api/vendor/core.js` | One mount line: `/packages`. |
| `src/lib/vendor/leadSerializer.js` | `binder_id` joins `LEADS_COLUMN_CENSUS`, dispositioned present in the schema and on no wire (F-43.73, ruled YES, the `wedding_id` precedent). `WITHHELD_FIELDS` unchanged. |
| `docs/db/PUBLIC_SCHEMA.md` | Regenerated by the PAIR at ladder `0168` (the regen owed since 0154). |
| `scripts/b81_lc2_p1_seed_bench.js` | Rung b81 (chair-allocated). |
| `scripts/floor-manifest-lc2-p1.txt` | The declared dirt for `run-floor.sh --delivery`. |

**The ruled defaults, as they render** (C-43.15 sentence case): planning · Partial planning; designer · One outfit made to order; photography · Photographs and film; makeup and hairstylist · Bride, every function with a trial; jewellery · Rental sets, every function; decor · Main functions; venue_catering · Venue and catering, one function; content_creator · Wedding day with same-day reels; other · One function; performer · none.

**Delivery bases:** photography `days` 45; content_creator `days` 14; designer and jewellery `handover`; the other seven `on_the_day`.

## §2 · Rulings and veto carried (recorded here as ordered)

- **Founder veto, 2026-09-17, on the chair's lean:** YES to every string in the read-first §6 as written, with two changes. Q2: in the quote body only, the middle line drops "(optional)" and reads `30% one month before the first function · Rs {b} · {date}`; the ruled bytes with "(optional)" stay everywhere the vendor sees the schedule (P9, A5, V5). F-43.69 closed. P10: the third delivery option reads "Handover date". Everything else verbatim: P1 to P13, A1 to A13, C1 to C5, Q1, D1 to D4, V1 to V12.
- **C-43.15 (chair, under the founder's "go with your lean"):** names render in sentence case; P3 reads "1 package" for one. Presentation of the vetoed bytes, not copy. **Reported at the cut:** "first letter capital, rest as written" changes nothing on an all-capitals source, so the rest is lower-cased (the ruling's stated intent), and `DJ` is kept as an acronym by a one-word list (`KEEP_UPPER`), derived by reading all 33 names. The chair may overrule in one word.
- **F-43.70: YES.** Statement 3 of 0168.
- **RLS: precedent noted, nothing cured.** No RLS statement on either new table, as on the estate's recent tables (0158, 0161, 0164, 0166); every reader and writer is the service-role server.
- Part 1 and part 2 rulings: F1, F2, F3 (reserve-then-create), F4, F5(a), F6, F7(a) on create and PATCH, F8(a), F9(a), F10, F11, F12, F13(a), F14(a), F15(a), F16, F17, F18, R-43.11. None of them is built in this packet except F1, F2 and F18's room (pwa); the rest are packets 2 to 5.

## §3 · What is proven

**The migrations, before production.** Both parse clean under the real Postgres parser (pglast). On PGlite (Postgres compiled to WASM) both applied twice with no error, and 28 of 28 constraint cells held both ways (single live default; seed key once and still refused after delete; remainder > 0; the days pairing; unknown basis; zero total; line_items not an array; one binder one lead with the first deleted; one live package per lead; snapshot not an object; one invoice per package, a replacement after soft delete allowed; the contract link; package delete). Not a substitute for production; the founder's apply below is.

**The migrations, on production (founder-run 2026-09-17, screenshots):** Steps 1 to 11 each answered `Success. No rows returned`. Step 12 returned exactly the 21 named constraints expected: `contracts_lead_package_id_fkey`, `invoices_lead_package_id_fkey`, eight on `lead_packages` (`_lead_id_fkey`, `_package_id_fkey`, `_pkey`, `_quote_draft_id_fkey`, `_schedule_check`, `_snapshot_check`, `_total_check`, `_vendor_id_fkey`) and eleven on `vendor_packages` (`_days_pairing`, `_delivery_basis_check`, `_delivery_days_check`, `_deposit_pct_check`, `_line_items_check`, `_middle_pct_check`, `_name_check`, `_pkey`, `_remainder_positive`, `_total_check`, `_vendor_id_fkey`). Step 13 returned all six indexes with their `WHERE` clauses as written.

**The regen.** The founder's four exports (s0 to s3) through `format_public_schema.js` and `append_constraints_to_public_schema.js` (arguments `2026-09-17 0168 713340a`). Every guard passed: column snapshot 101 == 101; §1 273, §2 164, §3 359, each rows_returned == rows_expected. 101 tables, 1142 columns (91 at 0154; the ten new tables are those 0158, 0160, 0161, 0164, 0165, 0166 and 0168 create). The diff removes only header lines and count headings rewritten with new figures; no column line is lost. F-43.13 (engine.messages.room) is untouched by this regen, as ruled; the ENGINE doc is not regenerated here.

**`b81_lc2_p1_seed_bench`: 56/56 on the cured tree**, run from `/tmp`.
- §1 the JSON equals a fresh parse; taxonomy order; 33 options; ruled defaults; delivery bases; schedule wording with the handover sentence on designer and jewellery only; line items, names and descriptions verbatim; C-43.15 sentence case with DJ kept.
- §2 `ensureSeeded` over a double that enforces the unique index as the database does (whole batch or nothing), stamps one `created_at` per insert and returns rows unordered.
- §3 the real router's handler.
- §4 the mount. §5 the migration text. §6 column existence against the regenerated doc (R-40.80).
- §7 nine mutations of production code, each turning its named cell RED.

**Both ways.** On a clean base worktree at `713340a` with the bench copied in: 1 passed, 55 failed. The one green is §1.3 (the estate taxonomy equals the bench's pin), a true fact at base. Before the regen landed, the cured tree read 51/56 with exactly §6's five cells red.

**Bench defects of my own, caught and fixed before cut:** per-category cells passed vacuously on an empty set at base (now gated on eleven categories); the bench threw instead of reddening on files absent at base; the first double handed rows back in insert order with distinct stamps, which hid a missing sort (M7 did not bite until the double was made honest).

**Sealed and adjacent benches (the readers of `core.js`), base and cured identical in this container:** b43 35/35 · b68 154/154 · b78 22/22 · b07_p1 75/75 · b07_p4b 75/76 (in `floor-base.txt`) · b46 25/29 (the same four container-side mutation cells on both trees) · b59_mutations RED on both (in `floor-base.txt`).

**Syntax.** `node --check` clean on every touched `.js`.

**Floor.** Base, founder-run 2026-09-17: `FLOOR = NAMED BASE, no delta (refusals, not in base: 3)`. At the cut it rides the verify line under `--delivery scripts/floor-manifest-lc2-p1.txt --check`, founder-run, compared by SET (R-38.19). Not claimed here (F-43.25).

## §4 · Drift and reports

- **D4 as written was wrong (c-43.12, F-43.46):** the Clients Add sheet writes `public.clients`, not a binder.
- **Kickoff paths (c-43.13, F-43.52):** the provision door is `src/api/vendor/auth.js` (POST `/provision`), and onboarding is `src/api/vendor/onboarding.js`, `src/agent/onboarding.js` and `src/api/admin/vendors.js`.
- **D7's premise killed (F-43.53):** `public.leads` carries no couple link.
- **The walk count:** the kickoff's "eleven seeds" is one category's options on the vendor's account; DEV440 (photography) receives three.
- **C-43.15's literal reading:** reported in §2.
- **F-43.72 · the r2 floor delta, half one (the seat's own defect).** The r2 verify ran b81 green and the floor to completion, then printed `FLOOR DELTA`: `bOB_taxonomy_bench` RED. Its cell 6.1 found `src/lib/vendor/packageSeedParse.js` spelling all eleven category tokens without importing the canonical list, the private-taxonomy class F-OB.3 closed. Cured in r3: the section-to-category map is derived by position from `VENDOR_CATEGORIES` (`src/agent/categories.js`), the defaults are keyed by section title, and a length mismatch throws at load. The seed JSON is byte-identical before and after (sha256 prefix `0ae9f3af09b686a3`). bOB: base GREEN, r2 RED, r3 GREEN. b81 M1 re-aimed at the new line (still bites).
- **F-43.73 · the r2 floor delta, half two (the regen's red, ruled).** `b36_leadgate_a_bench` RED on leg A: the regenerated `PUBLIC_SCHEMA.md` shows `leads.binder_id`, which `LEADS_COLUMN_CENSUS` had not classified. The bench's own law is that the cure is a ruling. RULED YES by the chair at the r3 cut: present in the schema, on no wire, the wedding_id disposition (no door selects the column; legs B1, B2, C and D stayed green). b36: base 95/95, r2 red, r3 95/95. The other readers of `leadSerializer.js` are unmoved: b37 and b39 green on both trees; b51 red on both, in `floor-base.txt`.
- **Why two reds reached the founder.** This seat's container kills any command at 300 s (F-43.25), so the seat ran only the readers of the files it changed, and neither bench reads those files: bOB scans every file under `src/`, and b36 reads the regenerated doc. The rehearsal now names that gap rather than implying a floor it did not run.
- **The refusal count.** The r2 floor printed four environment refusals (`b5_wa_door_smoke` joined the three the base carries); the runner does not count refusals in the delta, and it is an environment refusal like the other three.
- **F-43.71 · the r1 cut's manifest (the seat's own defect).** `TDW_CE43_LC2_P1_dream-os_713340a.zip` declared the two seed files but not the `db/seeds/` directory line that `git status --porcelain` prints for a new untracked folder. The founder's verify ran b81 green (56/56), then `run-floor.sh --delivery` refused with `dirt OUTSIDE the declared manifest: db/seeds/`, and he stopped before the git line, as the D-10 STOP required. The estate's precedent was on the shelf (`scripts/floor-manifest-ce42-4b1-os.txt` lists `tools/card_fonts/` and its files); I did not read it before cutting, and my rehearsal ran the bench but not the floor's dirt comparison. Re-cut as `_r2` (R-40.54) with the directory line added; the rehearsal now runs the runner's own comparison on an applied clone. The declared directory is hashed as ABSENT on both sides by the runner (it hashes files only); the two files inside it are hashed by name as before, and their contents are pinned independently by b81 §1.0 (source sha256) and §1.1 (JSON parity).

## §4b · The re-cut record (R-40.82, R-40.65)

**The three cuts.** r1 `TDW_CE43_LC2_P1_dream-os_713340a.zip` (retracted, F-43.71); r2 `…_r2.zip` (retracted, F-43.72 and F-43.73); r3 `…_r3.zip`, this packet. The founder applied r1 and r2 and committed neither; origin never moved.

**Collision derivation (R-40.82), at the moment of cutting r3.**
- Origin `main` = `713340a622acc2120de5689b97e6bceada38d684` after `git fetch`; `git log 713340a..origin/main` is empty. No commit has landed since the charter, so no file in this manifest can collide with a newer origin version.
- `src/lib/vendor/leadSerializer.js`, the one path new to the manifest at r3, was last changed at origin by `2227cc6` (2026-09-06). r3's copy is that file plus one hunk of eight added lines inside `LEADS_COLUMN_CENSUS` and nothing else (`git diff --stat`: 8 insertions, 0 deletions).
- r3 against r2, by sha256: changed are `src/lib/vendor/packageSeedParse.js` (F-43.72), `scripts/b81_lc2_p1_seed_bench.js` (M1 re-aimed), `scripts/floor-manifest-lc2-p1.txt` (gains `leadSerializer.js`), this handover, and the new `src/lib/vendor/leadSerializer.js`. The other nine files are byte-identical to r2, including `db/seeds/package_seeds.json` (sha256 prefix `0ae9f3af09b686a3`), the migrations and the regenerated `PUBLIC_SCHEMA.md`.

**The restore before r3 (R-40.65).** The founder's tree held r2 applied and uncommitted. `git status --porcelain` showed exactly r2's twelve manifest paths and nothing else, so the entire dirt was one retracted deliverable: no hunk of the founder's own work shared a file with it, and none needed separating. This is the case R-40.65 permits whole-file reversal for. The two modified tracked files were restored to `713340a`, and the ten new untracked paths were removed by name. The restore ended on an empty `git status --porcelain`, witnessed before the guard ran.

**The 0167/0168 statements and the regen ran once, on 2026-09-17, and stand.** None of the three cuts re-ran them.

## §5 · Founder acts

Done: Steps 1 to 17 (0167, 0168, both verifies, the four exports). Nothing further in this repo before the pwa packet. The walk (card P1) runs after both packets are applied and both deploys are witnessed; it is in the pwa handover.

## §6 · What packet 2 picks up

- **Standing, from the chair's r3 ruling:** every packet's pre-cut note names the benches the seat could NOT run in its container, so the founder's verify floor is their declared witness, not a surprise.

- The Packages writes (add, edit, delete, rename, set default).
- Attach to a lead with edits.
- The schedule computed per C-43.2 to C-43.4 and F10.
- The remaining vetoed bytes P7 to P10, P12, A3 to A9.
- Rung b82.
- The remainder helper is packet 3's, on create and PATCH (F7(a)).

## §7 · The FIELD MAP (carried verbatim for LC-3; not seed data)

As received in the founder's PKG-COPY block, 2026-09-17, under "FIELD MAP · package to contract v4 · founder's answer: YES (chair, R-43.10)". Name → G.1 service name / D.1 service level and the quote title. Description → nothing in the contract. Line items → 4.1 and Annexes A, B, C, D, F, G as listed there. Total → 4.1. Deposit share → 4.3. Schedule → 4.4 rows two and three. Delivery date → 4.4 row three and G.3. One wording, both places, as ruled. The two disagreements (Photography's album against 2.2; Designer and Jewellery against 7.2, F-43.37(b)) and F-43.37 (a) to (g) are LC-3's. The block itself is in the chat record of this sitting.

Sequencing beyond this sitting is the founder's.
