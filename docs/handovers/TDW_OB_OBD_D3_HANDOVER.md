# TDW · ARC OB · CHARTER OB-D · D-3 HANDOVER
# THE BYTES LAND · THE DOOR LEARNS ALL SIX FACTS · THE ENDPOINT STOPS LYING

**Derived at `7fb9465`** (TIP-AT-CHARTER, re-derived fetch-first at this
sitting's opening; zero drift from the chair's figure).
Head-guard `"name": "dream-os-backend"` ✓ · `npm ci` exit 0 · `npm run build`
exit 0 · sibling-full clone (`dreamos-pwa @ 60b4317` beside, READ-ONLY).

Delivered under CE-32's ruling on the D-3 read-first. **Nothing pushed.** ZIP to
the chair, then the founder's terminal.

---

## 1 · WHAT SHIPPED

| # | file | act |
|---|---|---|
| 1 | `src/lib/onboardingGate.js` | the two founder-vetoed redirect bytes land; the copy lock survives them |
| 2 | `src/lib/onboardingPredicate.js` | ruling ⓵ — `business_name` joins as the SIXTH mandatory field; the shared service-area edge validator |
| 3 | `src/api/vendor/onboarding.js` | F-OB.2's cure — the endpoint validates through the one predicate home |
| 4 | `scripts/bOB_d2_onboarding_gate_bench.js` | 40 → **75 cells**; cell 6.4's gap CLOSED |
| 5 | `docs/handovers/TDW_OB_OBD_D3_CENSUS_SELECT.sql` | the founder's six-field census, read-only, zero placeholders |
| 6 | this note | — |

**NOT shipped, deliberately:** `me.js` is BYTE-UNTOUCHED (the ⓷ removal is HELD
— OB-P is unseated); no DDL (nothing needed a column that does not exist); no
W-1 prompt line (§7 below is prose awaiting veto); no gate arming (R-OB.9's
conditions are unmet and arming was never chartered).

---

## 2 · THE TWO BYTES — VETOED, FROZEN, PINNED

Founder relay, 2026-08-12, superseding every D-2 §3 draft (B1–B3, V1–V3 are
historical record; **none is resident anywhere in this tree** — cell 3.3c
asserts it against code with comments stripped):

> **BRIDE** 「 Hi! Before we start planning, head over to thedreamwedding.in, sign in and fill in the details about your wedding. 」

> **VENDOR** 「 Hi! Before I can start working for you, I need your business details. Head over to thedreamwedding.in and set up your profile. 」

**ONE HOST, both lanes**, by founder ruling. Pinned character-for-character by
cells 3.3 and 3.3b: an edited comma reddens the bench. **APPROVED-COPY-CARRIES-
ITS-HASH — copy may not ride a refactor.**

**THE STANDING RE-VETO CONDITION, in my own words and recorded at the site it
binds** (`onboardingGate.js`, above the constants): *if OB-P mounts the form on
a subpath, these two bytes go back to the founder with the path in them before
any amendment. A byte carries a path only on a fresh veto — never on an
executor's inference that a deeper link would be more helpful.* The bytes name a
host today because a host is what the founder approved; the day the form lives
at `/vendor/start`, the sentence is his to re-approve, not mine to extend.

### The two API sentences — vetoed at the D-3 relay

Both are responses OB-P renders, both frozen at the byte, both pinned by cells
(7.19, 7.20) whose mutation is a single character:

> **INCOMPLETE** 「 A few details are still needed before your profile is live. 」

> **CATEGORY** 「 That isn't one of our categories. Please pick one from the list. 」

**The category sentence NAMES NO CATEGORY, by founder ruling, and that is a
design decision rather than a style one.** The enumerating draft was refused
because the estate's taxonomy is under active revision (§9 ⓹): copy that lists
its own options must be re-vetoed every time the options move, and vetoed copy
with an expiry date is copy that eventually ships stale. This sentence outlives
every future taxonomy.

**The list still reaches the caller — as DATA.** The 400 carries
`allowed: [...]`, read from the taxonomy module at the moment of refusal, beside
`code:'CATEGORY_UNKNOWN'`. Same division the incomplete refusal already runs on:
**the human words are frozen, the machine list is live.** OB-P's picker can build
itself from `allowed[]` rather than hardcoding a second copy — which is F-04.36's
mirrored-map drift avoided before the mirror exists. Cell 7.21 reddens if a later
sitting folds the list back into the sentence; 7.22 reddens if the endpoint
restates the list instead of reading it.

**BYTE NOTE:** the apostrophe in *isn't* is ASCII `U+0027`. Recorded because
APPROVED-COPY-CARRIES-ITS-HASH freezes at the byte and an editor's smart-quote
substitution is precisely the silent edit that law exists to catch.

**THE COPY LOCK DID NOT RETIRE WHEN THE BYTES ARRIVED.** It now guards the next
sitting — the one that nulls a byte, or trims one to whitespace, and would
otherwise hand a stranger an empty message as her first impression of this
estate. Cell 6.3 proves it still works by deriving a byte-nulled copy of the
production module and driving it (see §4).

---

## 3 · THE FOUR FORKS, AS RULED

**(a) Cell 6.4 — CLOSED.** The third fixture is a `user` whose `name` is a
throwing getter: the resident byte lets the copy lock pass, the predicate's
first touch detonates *inside* the gate's `try`, and an anti-vacuity flag proves
the getter actually fired. Deleting the try/catch reddens 6.4 alone.

**(b) 400-with-`missing[]`.** `400 { ok:false, error, code:'INCOMPLETE',
missing:[…] }` · `200 okRes({ …, onboarding_state:'complete' })` only on
predicate-complete. Written as a raw `res.json` because the contract carries a
third field and `errRes`'s shape was not widened for one caller.

**(c) `open_to_travel` — STOP-WRITING.** 0122's own stamp reads 「 Do not write
from new code 」 and this body is new code on that path. The column stays
readable estate-wide and is untouched here.
**DISCLOSED:** 0122 §5's reader census names `src/api/vendor/onboarding.js:51 :71`
as live readers. **Those two are now retired by this diff.** The migration is
NOT edited (LD-8, append-only) — the retirement is recorded here, which is where
a census correction belongs once its migration has shipped.

**(d) Category — TOKEN-VALIDATED, require-in-place.** `src/agent/categories.js`,
16 locked tokens. An unknown category is a 400 naming them; a valid one is
written verbatim. **The cross-folder require is declared, not accidental**, and
is commented at its own site as expected to be re-pointed (never deleted) when
the taxonomy-re-homing micro the chair filed to the convenience shelf lands.

**Starting price — MANDATORY, as ruled.** `rate_min`, integer, strictly `> 0`.
`"80,000"` is understood; `0` and blanks resolve to *absent* and come back as
`missing:['starting_price']` rather than as a third sentence saying the same
thing. `stated_rate`'s prose path is additive and untouched.

**ONE SHAPE I CHOSE AND AM DECLARING RATHER THAN BURYING — THE REFUSAL IS
ATOMIC.** An incomplete submission writes **nothing**: not the valid fields, not
a partial row, not a half-marker. The alternative (save what came, withhold the
stamp) makes "what did she actually tell us?" unanswerable from the row alone,
because the answer would depend on how many times a form was half-filled. Cell
7.3 proves zero writes on refusal. **A form that wants to save drafts is asking
for a different door, and OB-P inherits that question rather than discovering
this answer by accident.**

---

## 4 · BOTH-WAYS PROOF — every mutation RUN against production code

Bench run **bare**, exit code as the second independent method. Cured tree:
**total 75 · run 75 · skipped 0 · 75 passed, 0 failed · VERDICT GREEN · exit 0.**
Tree restored after every mutation; `git status` clean but for the shipped files.

| cell | production mutation | result |
|---|---|---|
| 1.12 | predicate: delete the `business_name` push | RED, exit 1 |
| 1.14 | `VENDOR_FIELDS`: drop `'business_name'` | RED, exit 1 |
| 1.15 | `VENDOR_FIELDS`: add a seventh field the predicate never checks | RED, exit 1 |
| 2.12 | edit one character of a service-area sentence | RED, exit 1 |
| 3.3 | one character of `BRIDE_REDIRECT_BYTE` | RED, exit 1 |
| 3.3b | one character of `VENDOR_REDIRECT_BYTE` | RED, exit 1 |
| 3.3c | point the vendor byte at `thedreamai.in` | RED, exit 1 |
| 3.3d | delete the copy lock | RED, exit 1 |
| 6.3 | delete the copy lock (fixture is DERIVED from production, so it loses it too) | RED, exit 1 |
| **6.4** | **delete the try/catch** | **RED, exit 1 — THE D-2 GAP, PROVEN** |
| 6.6 | return the bride byte on the vendor lane | RED, exit 1 |
| 7.2 | delete the `if (!verdict.complete)` refusal | RED, exit 1 |
| 7.3 | hoist a write above the predicate check | RED, exit 1 |
| 7.6 | drop `onboarding_state` from the update | RED, exit 1 |
| 7.8 | delete the taxonomy membership check | RED, exit 1 |
| 7.10 | delete the `validateServiceAreaPair` call | RED, exit 1 |
| 7.11 | restore `open_to_travel` to the update | RED, exit 1 |
| 7.12 | drop the `: vendor.category` candidate fallback | RED, exit 1 |
| 7.17 | re-derive completeness locally instead of requiring the home | RED, exit 1 |
| 7.18 | `coerceRateMin` `n > 0` → `n >= 0` | RED, exit 1 |
| 7.19 | one character of the INCOMPLETE sentence | RED, exit 1 |
| 7.20 | one character of the CATEGORY sentence | RED, exit 1 |
| 7.21 | fold the token list back into the CATEGORY sentence | RED, exit 1 |
| 7.22 | hardcode `allowed[]` instead of reading the taxonomy | RED, exit 1 |
| 7.9 | narrow the membership check to a hardcoded subset | RED, exit 1 |

---

## 5 · THREE BENCH DEFECTS, SELF-CAUGHT BY RUNNING, ON THE RECORD

**C-1 · cell 3.3c reddened on my own comment.** The cell asserted no
`thedreamai.in` anywhere in the gate; the file's history comment names the
retired host as history, and the cell ate it. **A check a comment can absorb is
a check on the comment.** Cured by stripping comment lines — the standing bench
law, earned here rather than inherited. Prose may discuss the old host forever;
a *literal* is what reddens.

**C-2 · cell 7.16 counted a token instead of asking its question.** It asserted
`onboarding_state` appears exactly once outside comments and reddened at 2 — the
write and the 200's honest echo of it. R-OB.8 forbids **reading** the marker to
decide anything; it says nothing about writing it or reporting it back. Rewritten
to look for the shapes a *read* would take (`vendor.onboarding_state`, `===`,
`if(…)`, `select(…)`). *Counting a token is not asking the question.*

**C-3 · a mutation found nothing, and that absence was the finding.** Flipping
`coerceRateMin`'s `n > 0` to `n >= 0` reddened **no cell**: on a virgin row the
predicate refuses 0 either way, so a different guard was satisfying the test.
The difference is only visible on a **returning** vendor, where `>= 0` turns an
empty numeric box into a real `0` that **overwrites the price she saved last
week**. Cell **7.18** was written to that hazard and the mutation now bites.
*A guard whose only cell is satisfied by a different guard is an unproven guard.*

**Also recorded:** cells **1.7, 1.8, 1.9, 1.10** and **3.3** and **6.3** are
LABELLED AMENDMENTS to D-2 cells, each one making its assertion **stricter**
(1.9's count moves 5 → 6; 3.3 moves from null-assert to byte-pin; 6.3 keeps its
question and rebuilds its fixture). None was loosened to keep a green.
**C-4 · cell 7.9 was written for a byte the founder then retired.** It asserted
the refusal SENTENCE named all 16 tokens — a right answer under the enumerating
draft and no answer at all under B2. Amended to its durable half, in a stronger
behavioural form: it now drives ONE SUBMISSION PER TOKEN off the taxonomy itself
and reddens if the endpoint refuses any category the estate defines. *The ruling
that retires a byte owns the cell that read it* — RETIRE-WITH-THE-READER,
applied to a bench.

**Ratify-or-revert.** The bench's cell count grows 40 → 75 by design, and the
`GAP 6.4` line is gone from the summary because the gap is gone.

---

## 6 · THE FLOOR — sealed list re-run bare at this tip, sibling-full

`bOB_d2` **75/75** (was 40/40 + GAP — the chartered growth) · `b09_d3_structural`
30·27·3-skipped · `b09` media 27/27 · meter (tools/bench) 38/38 · tier 81 ·
billing 52 · selfserve 30 · micro 23 · forkc 113 · relay_hand 126 ·
bride_arrival 103 · relay_foundations 42 · f0613 40 · oow 74 · b07_p5 **136
SIBLING-FULL** · prospect 13/36/10. **ELDERS, pinned and unmoved:** b06_meter
28/29 · f0555 22/23 · f0772 158/159 · b05_p4 46/48 · combined_cap 36/37
(§6.4 LAWFUL-STALE, F-05.77's fourth). **No figure moved but bOB_d2's, which
moved by charter.**

---

## 7 · THE W-1 VETO SHEET (R-OB.10) — PROSE ONLY, ZERO BYTES IN THIS ZIP

W-1 is **SHUT** for this sitting's code. Nothing below ships until it is vetoed,
and then only in the delivery *after* its veto. Current line beside proposed
line, both quoted from the tree at `7fb9465`:

**① `src/agent/systemPrompt.js:416`** — the vendor prompt's travel sentence,
built from the stale boolean.
- CURRENT (mechanism): the prompt reads `open_to_travel` to say whether the
  vendor travels.
- PROPOSED: read `service_area`/`service_cities` instead, so the prompt can say
  *worldwide* — which the boolean cannot express at all (0122 §5: two values
  cannot carry three states). A vendor who selected Worldwide in the form is
  currently described to couples as based in her city.
- **Exact replacement sentence: NOT DRAFTED HERE**, deliberately — it is
  model-voiced copy and drafting it inside a delivery is how an unvetoed byte
  gets normalised. It comes to the founder as a byte, alone, in its own veto.

**② `src/agent/coupleSystemPrompt.js:80`** — the same sentence on the couple
side (`travelsText`), same mechanism, same proposal, same refusal to draft.

**③ The vendor-facing onboarding nudge** the arc will eventually want (the
sentence a vendor sees when the form sends her back to finish). **Not drafted.**
It is OB-P's surface and its byte is the founder's.

Both prompt sites are named in 0122's reader census as `W-1, D-3 under ruling
⑤'s lift`. **The lift is not exercised here**: R-OB.10 says prompt lines land
only in the delivery AFTER their veto, and no veto has been taken on them.

---

## 8 · WHAT THE FOUNDER DOES — IN ORDER

**STEP 1 — RUN THE CENSUS FIRST** (`docs/handovers/TDW_OB_OBD_D3_CENSUS_SELECT.sql`,
Supabase SQL editor, three read-only SELECTs, nothing to fill in). **Paste all
three result sets back.** *The walk card is authored FROM those rows and is
deliberately NOT in this note* — fixture-state law: a card written before the
rows is discovery wearing a card's clothes. Result set 2 is the one to read
closely; it measures how many rows say `complete` today while failing the
six-field predicate. **A non-zero answer there is the finding, not a surprise** —
the cure stops the count growing; it rewrites no history.

**STEP 2 — the walk, once the rows are in.** The gate is DARK, so it is
API-and-rows, not handset: the endpoint exercised on the test vendor
(**9888294440**) for `missing[]` honesty and the `'complete'` write gating.
Authored on your pasted rows, next message.

**DASHBOARD ACTS: NONE.** No env var, no Railway setting, no Meta change, no
flag flip. `onboarding.gate_enabled` stays `false`; **nothing in this delivery
changes what either lane does today.**

**OB-P'S INHERITANCE — the truths only a form can witness**, named rather than
smuggled into a card: that the six fields render as six boxes keyed on
`VENDOR_FIELDS`' strings · that the three service-area labels (SET A) map 1:1 to
the tokens · that a 400's `missing[]` re-opens exactly the right boxes · that
the category picker offers the 16 tokens and no free-text escape · that the
redirect byte's host resolves to a form that actually answers. **None of these
is provable from this repo, and R-OB.9's arming waits on the last one.**

---

## 9 · CARRIED TO THE CHAIR

**⓵ The atomic-refusal shape is declared, not ruled** (§3, last paragraph). If
OB-P needs draft-saving, that is a second door and a fresh charter — not a
loosening of this one.

**⓶ 0122's reader census is now one line stale** by this diff's own design
(§3(c)). Recorded here because the migration is immutable. The remaining live
readers of `open_to_travel` are `me.js` (eight sites, HELD — OB-P unseated),
both W-1 prompt sites (§7), and `agent/onboarding.js` (retires under ruling ③).
**The arc's completion cell — zero live readers at arc close — has three
survivors to go, and they are all already someone's charter.**

**⓷ The taxonomy's home.** `src/agent/categories.js` is load-bearing for
`categoryProfiles`, `categoryFraming` and `occupancy`, and now for this
endpoint — while the flow whose folder it sits in retires under ruling ③. The
chair filed the move to the convenience shelf; this note records that the
endpoint's require is **expected to be re-pointed, never deleted**, so the micro
does not read as a chance to drop it.

**⓸ Both API sentences are VETOED AND RESIDENT** (§2). A2 and B2 were the
founder's picks at the relay; both are pinned at the byte and both mutations
bite. **No unvetoed copy ships in this ZIP.**

**⓹ THE TAXONOMY REPLACEMENT — FOUNDER-RULED, RECORDED HERE, NOT BUILT.**
The founder ruled a new vendor category list at the D-3 relay, 2026-08-12, and
ruled that it lands as a CE note rather than in this package. Carried verbatim,
in his own words and order:

> **Event Planner · Designer · Photography · Make up · Jewellery · decor ·
> venue · performer · others**

— together with: *"in the onboarding, the only options in the drop box for
category should be the ones I have written."*

**NOTHING IN THIS DELIVERY ACTS ON IT.** The endpoint validates against
`src/agent/categories.js` AS IT STANDS (16 tokens, file byte-untouched), which
refuses nothing a vendor could legitimately send today and pre-empts no part of
the ruling. What follows is the derivation the next charter needs, so it is not
re-derived from scratch:

**(a) IT IS LOAD-BEARING, NOT A MENU.** Derived at `7fb9465`:
`categoryProfiles` defines profiles for exactly `makeup · photography ·
designer · jewellery · decor · venue`; `occupancy.CATEGORY_CAPACITY =
{ photography:1, makeup:2, decor:1, venue:1 }`; `occupancy.RULED_OFF =
Set(['planning'])`. Consumers: `me.js` capacityFacts · `vendor-engine/chat.js`
(two call sites) · `coupleSystemPrompt` · `agent/onboarding.js`'s extractor.

**(b) 「 Event Planner 」 IS TOKEN `planning`, AND `planning` IS OCCUPANCY-OFF
BY A STANDING RULING** until 04.5 (C4, crew math). The new list's first entry
lands on the one token the estate has already ruled a special case.

**(c) FIVE TOKENS HAVE NO HOME in the nine:** `catering`, `videography`,
`mehendi`, `transport`, `invitations`. **Catering is the loud one** — a wedding
estate whose form cannot say caterer. `performer` appears to absorb `music_dj`,
`music_live` and `choreography`, but that is an INFERENCE and was not acted on.

**(d) LABELS OR TOKENS — the question the list does not settle.** 「 Event
Planner 」 and 「 Make up 」 are display labels; `planning` and `makeup` are
machine tokens. 0122 already ruled this boundary for service area (copy never
enters DDL; labels live in the PWA and map 1:1). If the nine are LABELS over
existing tokens, it is a PWA mapping and zero migration. If they are NEW TOKENS,
every live row on an old one needs a backfill — a migration with its own sitting.

**(e) THE DROP BOX DOES NOT EXIST.** `dreamos-pwa`'s
`app/vendor/onboarding/page.tsx` declares `category` as state (`:53`) and lists
it in the submit dependencies (`:83`) and **never renders a control for it and
never sends it**. There is no picker with the wrong options; there is no picker.
That repo is out of this charter's radius and the surface is OB-P's — the ruling
rides his charter, and this endpoint is the first thing in the estate that will
accept a category from a form.

**(f) SEQUENCE IT AFTER THE CENSUS.** Result set 3 shows which live vendors sit
on a category about to stop being offered. Authoring the replacement before
those rows are in hand is the fixture-state law's own failure mode.

---

## 10 · APPLY

Head-guard first: `head -2 package.json` must read `"name": "dream-os-backend"`.

The apply block, the verify line with its D-10 STOP, and the git line as its own
paste-block travel in the delivery message, not in this file — a runnable block
inside a committed document is a block that gets run out of order.

**Nothing in this delivery changes observable behaviour on either lane today:
the gate is dark, and the endpoint's cure refuses only submissions that were
already incomplete when it accepted them.**
