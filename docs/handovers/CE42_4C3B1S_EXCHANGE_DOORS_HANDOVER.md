CE-42 · 4c-3b-1s · R7 G5.3 THE INFLUENCER EXCHANGE — THE WRITER AND THE DOORS (dream-os, base 0b7dfc9) · [CE-42 SEAT R7]

1 · SHIPPED. `src/lib/vendor/exchange.js` — the ONE WRITER: role, browse (three predicates — creators, opted-in, active — and never herself), the reach view, the send with its shape guard, the state machine as a TABLE (`TRANSITIONS`), and the two lists in one row shape. `src/api/vendor/exchange.js` — ten doors, no PostgREST chain, one status per refusal code, the four verbs posted as VERBS with empty bodies. `me.js` — the opt-in on ALLOWED_FIELDS and BOOLEAN_FIELDS, `=== true` on both the GET shape and the PATCH echo (the FALSE-default coercion, not its neighbour's). `core.js` — one mount beside referrals. `0166` as the ladder record with statement 4 LIFTED.

2 · THE MACHINE. `sent → accepted | declined` (creator) · `sent → withdrawn` · `accepted → completed` (sender). EVERY transition is one guarded UPDATE — `.eq('state', rule.from)` and `.eq(<my side>, actor.id)` in the same statement that sets the new state. Two racing taps: the second matches ZERO ROWS and is told NOT_IN_STATE. A stranger holding a real request id gets the same answer as someone whose row already moved, deliberately — telling those apart is a way to confirm someone else's request exists. Not a lead, not an event: b78 4.5 asserts the plane writes `exchange_requests` and nothing else.

3 · THE CONTRACT WAS HONOURED. 4c-3b-1p shipped `lib/vendor/api/exchange.ts` calling itself "a contract offered, not a read of a door that exists". These ten paths and payloads are the answer to it, compared field by field at this cut: same paths, same `{role, opted_in}`, same `CreatorRow`/`ReachView`/`RequestRow` keys, same empty verb bodies. The pwa's flag can flip with no edit to the client — subject to §5 below.

4 · PROVEN / NOT. `b78` **22 GREEN, 0 RED**, with **21 mutations against production source, each proven RED and reverted** (role collapsed · the window at ten years · the opt-in predicate dropped · the follower count made the sort key · `select('*')` off vendors · a creator through the sender doors · the money guard removed · any ask count · the creator re-check dropped · the state named on insert · the state guard dropped · complete allowed from sent · the side check removed · the ownership clause dropped · the inbox reading the sender side · withdrawn hidden from her · a money column in 0166 · the writer touching leads · the door building a query · the opt-in leaving BOOLEAN_FIELDS · the router unmounted). `b75` 19/19, `b73` 24/24.

   ⚠ THREE HOLLOW GREENS CAUGHT BY THE BOTH-WAYS RUN, ALL RECORDED BECAUSE ALL THREE NEARLY SHIPPED.
     (a) 2.3 stayed green with `CREATOR_COLS = '*'` — the double's `select()` ignores its argument, so the cell was asserting on a chain that never reads it. It now asserts the constant, and says in its own comment that a double cannot catch this class.
     (b) 4.2's first mutation set `rule.from = undefined`, which made `.eq('state', undefined)` match zero rows — the cell passed for the WRONG REASON. Replaced with an edge that actually widens the machine (`complete` from `sent`).
     (c) 6.1's money grep used `\bfee\b`, which does not match `fee_paise` (`_` is a word character), and `Rs\s` under `/i` matched "ALTE**RS** " and "doo**RS** " in the file's own prose. Stems unanchored on the right, the rupee word-bounded and case-sensitive, SQL comments stripped first (the strip-first law), and the whole `validateRequestBody` guard block cut before the sweep rather than just its header.

   NOT PROVEN: `npm run build` (per `tools/preflight.sh`, owed before any floor number). No device. No live PostgREST — the double is a double, and the walk is the proof that outranks it.

5 · OPEN — TWO THINGS GATE THE PWA FLAG FLIP, NEITHER MINE TO CLOSE.
   (a) **`withdrawn: 'Withdrawn'` IS STILL UNVETOED** (4c-3b-1p §4a). The state exists in 0166, this writer produces it, and the pwa cannot draw it until the founder vetoes or replaces the byte. THE FLAG MUST NOT FLIP FIRST.
   (b) **The reach card's post tiles have no door.** Still fixture-only; the card omits the section when a row carries none. Serve them from R6's `/posts/cards`, or drop them — a ruling.

6 · FILED FOR THE CHAIR.
   (a) **b77 cell 9.2, AMENDED BY LABEL — one line, a neighbour's bench.** It asserted «no 0166 exists», a LADDER-TAIL claim: true and useful at R6's cut, but red for the next migration anyone lands, whoever lands it. It now asserts what it meant — that its file is 0165, and that any 0166 present is the ALLOCATED one. Proven still red when the rung is double-claimed. `b77` back to 39/39.
   (b) **`b68_introductions_bench.js` THROWS, at this cut AND at the untouched base** — `Cannot find module src/engine/dist/core/tools/introduce.js`. A build artifact absent from the repo; not this sitting's, not cured here.
   (c) **F-42.206 (workspace integrity) STANDS.** None of the four unattributed artefacts reached origin — checked by `git cat-file -e origin/main:<path>` for each at `0b7dfc9`, all absent. This sitting was built in a FRESH CLONE with the old workspace moved aside untouched; not one byte was carried across, and every file here was authored from the read-first and the rulings. The event itself is still unexplained.

7 · 4c-3b-2 (VERIFICATION) — MY CALL, NAMED: IT FOLLOWS, IT DOES NOT RIDE THIS PACKET. Blob-checking `0b7dfc9` turned up the reason. R6 shipped four insights readers — `fetchAccountInsights`, `fetchMediaInsights`, `fetchFollowersCount`, `probeInsightsScope` — and **not one of them reads audience demographics**. The reach snapshot needs audience by city · age · gender, which is `follower_demographics` with a `breakdown` parameter, and that reader DOES NOT EXIST at the tip. So 4c-3b-2 is not a wiring job on an existing key: it is a NEW Meta reader in `igOAuth.js`, a file R6 cut hours ago and holds the pen on. Two seats in that file the same day is the collision the estate's session law exists to prevent. It also needs the founder's insights grant on the creator's account, which the walk cannot supply until the second test vendor exists. Recommend: 4c-3b-2 as its own packet, after the walk, with the chair naming who writes the demographics reader. Until it lands EVERY card reads Pending — which is exactly what the vetoed banner already tells her.

8 · NEXT. `0166` statements 1–3 are ALREADY RUN (founder-hand, 2026-09-10, verify green ×3 — witnessed on his device). Statement 4 (the three indexes) is NEW with this ZIP and runs AFTER it. Then Railway green, the founder's second test vendor of category `content_creator` on a clean number, and the walk: DEV440 sends → the creator accepts → DEV440 marks completed.
