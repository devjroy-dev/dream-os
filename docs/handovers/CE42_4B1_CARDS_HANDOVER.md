# CE-42 · 4b-1 · R6 · THE CARDS ARM + DOOR (dream-os) — HANDOVER · seat R6
1. **Shipped:** `src/lib/vendor/postCards.js` (last gallery → vetoed caption → three signed Cloudinary URLs, Post 1:1 / Status 9:16 / Story 9:16, Graphite ink held to the pwa's theme.ts by b73 §4) · `src/api/vendor/posts.js` `GET /cards` (404 no_gallery · 409 not_live · 409 no_address · 500 not_configured) mounted `/posts` in `core.js` · `tools/upload_card_fonts.js` + `tools/card_fonts/` (the two estate faces, OFL) · bench `b73` 22/22, 15 production mutations each red by name.
2. **On the glass:** DEV440's Posts & ads room shows a card from `wedding` (the newest page with a photo; the three newer pages have none), its caption, Download/Share/Copy — once the fonts are uploaded (card step 1). Without them Cloudinary renders the text in its fallback face.
3. **Not proven:** the render itself (Cloudinary, device), the fonts on the card, CORS on Download — the founder's walk. The full floor did not finish in the seat's window; it runs in the verify block. Readings for the chair: consent checked on the newest-with-photos page, no fallback · F-42.172 "Photographed by" only for photography · typographic ’ on NOT_LIVE · photo id read from `secure_url`.
4. **Next:** 4b-2 broadcast with 0163 (fee + GST + ig_probe_vendor rows) and 0164 (broadcasts, broadcast_recipients + stopped_at, receipt arm, partial UNIQUEs, the inbound arm above STOP — F-42.171) · formatRs paise arm (F-42.170).
5. **Base:** dream-os `d27144fb2639a904637c2f0060ec2403f1fbc267` — RE-PINNED from `548944a` at the cut: origin moved by G2-C r2 (docs only: `docs/TDW_CE42_G2C_HANDOVER.md`, `docs/TEMPLATES.md`, two `docs/filings/` files). Collision derivation: the one modified path, `src/api/vendor/core.js`, is blob `b697bcbb30` at both; every other path is absent at both. No migration.
