# TDW CE SUCCESSION NOTE 14 — the fourteenth chair banks (2026-07-29, at the founder's word, post-block-close)

## §1 · YOUR SEATING (fifteenth chair — this section governs it)
Fresh-clone `devjroy-dev/dream-os` + `devjroy-dev/dreamos-pwa`; fetch AND checkout; **`npm ci` BEFORE `npm run build`** (a clean clone builds RED under the container's global tsc — TS5107; the pinned tree builds green; the fourteenth proved it at its own seating). Read this note WHOLE, then the protocol (§7, §9, §10 twice, §11 twice), masterplan rows 06 (the closed arc, CE-97→CE-113 tail) and 07 end-to-end with its spec set (`docs/specs/` TDW_07 + the CE-62 IG-import addendum), FINDINGS_LOG CE-104→CE-113 entry-whole. Chair-run the floor; match §3 below at your own hand; a discrepancy is a FINDING, not a bounce. **Expect this note stale at issue — the last three were, and every seating proved it. Trust evidence over narrative, including this note.**

## §2 · THE ESTATE AT BANKING
**TDW_06 IS CLOSED (CE-112)** under the founder's CE-110 terms: Evening Eight red on SD-REL alone — **the residual in ink: the relay echo on RECEIPTLESS paths (create/graduate authors no `refused` carrier; B-3's boundary is the update path's two sites; F-06.83's board item)**, architecture-indifferent, proven both hands one run. **PRODUCTION IS OPEN** under the standing instruments: the interceptor (costume-only; retry-the-actor; the imperative retry on the nine soul stems) behind ONE kill variable (`WIRE_GUARD_STAGE2=off` on Railway) · the ONE-FALSE-INTERCEPTION tripwire (a halt, then a ruling) · the WEEKLY PRECISION READ (§4) · the REPORT word + chip + `glitch-report` route. **The E-1 split STANDS: Victor anthropic-haiku (cached static prefix) · Donna deepseek** — both flips REFUSED at CE-112 (the residual is architecture-indifferent; the split is ~43% cheaper). Three lanes live on Meta direct (vendor `+917982159047` · bride `+917011788380` · marketing `+918810531764`). **Standing test account: `9888294440`** → vendor `23165e38-6510-4639-ab6a-9f35bab93742` · agent `d02c7a9a-8622-4543-aa40-13d0911faf9b` (business) — founder-resolved by SELECT at CE-109; the `prestige`/`entry` cross-plane tier mismatch is flagged there (meter-check before any walk). **THE FOUNDER'S SEQUENTIAL RULING (CE-113, verbatim in the log): Block 07 onward, SEQUENTIALLY.** 06.5 stays parked; the Block 05 remnants DEFER (M2b dormant at zero cost; F-05.19 self-cures with engagement; one delivered-opener witness closes it); the old 16-first order is superseded.

## §3 · THE FLOOR AT THE FOURTEENTH'S OWN HAND (docs tip `59545b6`, code `e4051c5`)
dream-os: build GREEN (post-`npm ci`) · selftest **386/386** · forkc_wireguard **113/113** · f0613_relay **40/40** · m0 50 · m1 45 · m2 43 · m3 37 · m4 33 · m4b 24 · m4c 20 · m4d 16 · f0658 20 · f0667 16 · f0681 17 · f0692 23 · advisor 16 · advisor_route 16 · 0081 12 · sonnet 13 · donna_cache 16 · b0461_p6 25 · b6_floors 47 · b6_s1 24 · b6_sitting2 22 · door_rider 15 · f0550 31 · arc_m2 27 · arc_m4 18 · arc_m5 11 · **meter 28/29 KNOWN RED (F-06.41), carried loudly**. dreamos-pwa (`19b2580`): installed-tree `tsc --noEmit` ZERO (an UNINSTALLED tree reds ~768 lines — the artifact class, twice disclosed) · `tdw06_m3_report_chip.proof.mjs` 41/41 · `tdw06_f06133_drawer.proof.mjs` green. Known residue, named: nine `e7_card*` files at the dream-os root, ZERO readers (provenance strays, left at the founder's apply economy).

## §4 · YOUR FIRST DUTIES
**(1) THE WEEKLY READ is the standing cadence** — the founder runs these two blocks Mondays and pastes the rows; rule them like a measurement round (every `costume` row checked against delivery; `imperative_retry` rows counted; REPORT filings matched; **WATCH №1 specifically: the advisor-limb false-arm on redirect phrasing — "…and it's filed" conditionals; a live false interception is the tripwire's subject, disarm-then-rule**):
```sql
SELECT f.created_at, f.claim, f.severity, f.truth_status, f.evidence_ref, r.scenario, r.run_type
FROM engine.evals_findings f LEFT JOIN engine.evals_runs r ON r.id = f.run_id
WHERE f.created_at > now() - interval '7 days' ORDER BY f.created_at DESC;
```
```sql
SELECT created_at, scenario, run_type, verdict, cost_inr_total FROM engine.evals_runs
WHERE created_at > now() - interval '7 days'
  AND (scenario LIKE 'imperative_retry%' OR scenario LIKE 'wire_guard%') ORDER BY created_at DESC;
```
**(2) OPEN BLOCK 07 (DISCOVER)** — read row 07 + its spec set whole, then author its opening §10 kickoff at the benchmark's thickness (**re-read protocol §10 immediately before authoring EVERY kickoff — the fourteenth's correction №7 is what long context does to a chair**). Executor: Opus-LE default (CE-49; Opus 5 is the current Opus). **(3) Loose ends, classify-only, gating nothing:** the Evening Seven SD-REL tee block (founder pastes whenever; the receiptless-path class almost certainly covers it) · Evening Seven's Card Three founder-run (pends his hand, its block is committed) · the post-close shelf as CE-112 enumerates it.

## §5 · LAWS THAT OUTLIVE THE BLOCK (by name; full text in the protocol + log)
Verify-never-trust · report-never-adapt (§0.2) · read-first · both-ways by mutating production code + the F-06.111 vacuous-`every` grep · one home per mechanism · W-1 · the clobber law · D-10 · conditional-withheld · the apply chain AS BYTES + the git line its OWN paste-block · **the token-grade fixture law** (sweep per-token, both repos, before any SELECT) · **the tee law** (every gauntlet/batch run tees to a file) · the measurement-read law (transcript beside SELECT) · the one-block batch form (`sleep 10` + `tee`; the SELECT its own block) · the Nirali pre-flight pattern · the secrets law (TWO key incidents this block — keys at the terminal, never in chat, rotate-at-once) · **the Codespace path: `/workspaces/dream-os` IS the tree — never `cd dream-os`** · the corrections convention (forward, in place, never rewrite) · seat-naming · findings minted by the chair alone.

## §6 · THE FOURTEENTH'S OWN FAILURE MODES — expect them in yourself
FIFTEEN chair corrections this tenure, all in-band. The classes, so you catch them early: **kickoff format drift in long context** (№7 — the §10 re-read law above) · **vacuous probes on stale paths** (№2: `git diff` no-ops silently on absent paths — witness paths before trusting an empty diff) · **ruling against underived surfaces** (№6: Fork A vacated — never class-match against a table whose WRITERS you haven't read; №9: never cite a witness that doesn't exist) · **hollow docs claims** (№15: an entry said a correction rode a ZIP that didn't carry it — grep the ZIP's own bytes before claiming its contents) · **minting at the wrong grade** (№11: fixtures sweep at TOKEN grade, the instrument's grade, not the record's) · charter cite drift (№12–14: re-derive every `file:line` at the tip you're ruling at). The executors caught most of these before harm — **the two-chair split is the integrity mechanism; let it convict you and say thank you.**
