# M-LEADGATE-RECUT · SEAT B′ + F-16.25 FOLD — EXECUTOR HANDOVER

**Seat:** LE · **Repos:** `dream-os` (ZIP 1) then `dreamos-pwa` (ZIP 2), strict order · **Bases at delivery, re-derived fetch-first at the moment of cutting (CE-22):** `dream-os d05a07a` · `dreamos-pwa 1db813b` · **Bases at authoring:** `dream-os 0dc5a27` · `dreamos-pwa 3252a04`, both fetch-first at fresh clones, both trees clean at first motion · **Rulings executed:** R-37.21 · R-37.22 → R-37.28 · **Delivered under:** R-37.30 · **Corrections raised by this seat:** c-B′.1 · c-B′.2 · c-B′.3 · c-B′.4

## 0 · PROVENANCE — TWO HANDS, NAMED (R-37.30)

**This build was authored by the B′ session that SEVERED BEFORE DELIVERY. Its work was FOUND in the successor's workspace, HELD BYTE-UNTOUCHED, VERIFIED GREEN BY COMMAND, and delivered by that successor under R-37.30 — adopt-and-repair with attribution.** Nobody's name absorbs another seat's work on a money surface, and nobody's work travels unattributed.

**§11's severed-session sentence, said rather than tidied away:** the tree carried the predecessor's unbanked work. One sentence converts a security event into a handover, and under R-37.30 that sentence is required, not optional.

**HOW IT WAS CAUGHT — credit to the instrument, not to a reading.** The successor's first motions derived BOTH trees clean at `0dc5a27` / `3252a04` by command. The found files appeared afterward, during the read-only ladder pass, in two waves. **`scripts/run-floor.sh`'s clean-tree refusal is what surfaced them** — LESSON 4's *"a floor is measured on a clean tree or not at all"* — printing all eight paths. Four of them had already been read during the ladder pass without the change being noticed. The refusal was the guardrail; a human reading was not.

**THE BASES MOVED MID-SITTING** with no git action from the successor: `dream-os 0dc5a27 → d05a07a`, `dreamos-pwa 3252a04 → 1db813b`. Both are forward (`git merge-base --is-ancestor` YES). **ZERO OVERLAP with either B′ file set, derived by command** — `d05a07a` touched `auth.js` / `provisionRole.js` / `onboardingPredicate.js` and their benches, `1db813b` touched the landing page and one bench. So the move is a **hash correction, not a rework**: the work applies cleanly at the new tips and every gate below was re-run there.

**THE SUCCESSOR'S VERIFICATION, run at the moved tips — the whole basis on which this adopts:** manifest matches the actual dirt exactly (8 paths, the A-sitting's one-path-short tuition honoured) · `node --check` clean on all six `.js` · engine gate `rc=0` · `b36_leadgate_a_bench` **95/95** · `b37_f1625_band_floor_bench` **35/35** · `tdw37_leadgate_b_slot` **37/37** · floor `--delivery --check` **NAMED BASE, no delta**, declared files unmoved by hash before and after.

**Relay guard:** clean, both repos. Nothing beyond either tip.

**ZIP ORDER IS LOAD-BEARING, NOT ADMINISTRATIVE.** The door must accept `budget_floor` before the sheet posts it. Applied in reverse, every top-band enquiry between the two deploys posts a field nothing reads.

---

## 1 · WHAT SHIPPED

**ZIP 1 — `dream-os` (8 files)**

| File | State | What |
|---|---|---|
| `src/lib/discover/enquiryFields.js` | modified | `bandFloor` — the floor's parse, its own function for its own fact. `bandAnswered` — the ''-vs-absent discriminator, corroboration only. |
| `src/api/couple/enquire.js` | modified | `budget_floor` accepted, parsed, written to `budget_min`. The ZIP-order window announces itself. Demo path deliberately untouched (R-37.26). |
| `src/api/vendor/leads.js` | modified | `budget_min` joins `dataSelect` **and** the mapper — both halves, per F-04.10. |
| `src/lib/vendor/leadSerializer.js` | modified | `budget_min` dispositioned **PRESENT** in both censuses, amended by label. |
| `scripts/b36_leadgate_a_bench.js` | amended by label | 94 → **95**, one cell: the disposition proven, not merely declared. |
| `scripts/b37_f1625_band_floor_bench.js` | **NEW** | **35** cells — the parsers, the door's wiring, the wire, the mark, the demo exclusion. |
| `scripts/floor-manifest-m-leadgate-recut-b.txt` | **NEW** | This delivery's declared dirt. |
| `docs/TDW_M_LEADGATE_RECUT_B_HANDOVER.md` | **NEW** | This file. |

**ZIP 2 — `dreamos-pwa` (8 files)**

| File | State | What |
|---|---|---|
| `components/vendor/slices/SliceShell.tsx` | modified | **R-37.22 the swipe suppression** (first in the ZIP) + **R-37.24 the connect slot** at the leads detail footer. |
| `components/vendor/slices/SliceRow.tsx` | modified | `Row` gains `redacted` and `budgetMin`. The clients-only contact block untouched. |
| `app/vendor/list/[slice]/leads.tsx` | modified | `redacted` off the wire · `leadBudget` reads the pair · **R-37.28** the coalesced masthead with its named semantic. |
| `lib/frost/budgetBands.ts` | modified | **R-37.25** the explicit `floor` field + `openBandLabelFor`. Not one vetoed label byte moved. |
| `components/frost/EnquirySheet.tsx` | modified | posts `budget_floor` from the same array the value came from. |
| `lib/vendor/types/vendor.ts` | modified | `Lead` gains `budget_min` and `redacted`. |
| `scripts/tdw09_vendor_census.json` | regenerated | a +26 line shift, see §5 ③. |
| `scripts/tdw37_leadgate_b_slot.proof.mjs` | **NEW** | **37** cells. |

**No migration, no DDL, no founder SQL beyond the walk's own flip.** `0130` remains next-free. Plane read: `public` only. **W-1 clean** — no soul, prompt or engine file is touched in either repo.

---

## 2 · THE INCIDENT, CLOSED — R-37.22

`SliceShell.tsx`'s leads left-swipe read `row.phone ? Call : Mark lost`. Seat A′'s recut withholds `phone` from a basic vendor's wire, so **on `0dc5a27`'s deploy the left swipe on every basic vendor's leads rows stopped being `Call` and became `Mark lost`** — a different verb in a remembered position, on the paid product, with no ruling behind it.

**The exposure window, stated at its true weight:** live from `0dc5a27`'s deploy until ZIP 2 deploys. **Gated behind the standing confirm** (`setMarkLostConfirm(true)`), and `Mark lost` is itself undoable — so nothing was destroyed silently and no lead was lost without two deliberate acts. The defect was the gesture changing identity underneath a thumb, not a silent write. Ruled at exactly that grade; over-grading is how c-36.8 happened.

**The cure is suppression, not substitution.** On a redacted row the left side renders **nothing**. Both refused arms stay written down at the site: routing the gesture to the upsell was refused because a gesture must never open a sales surface, and keeping `Mark lost` was refused because per-tier gesture semantics *is* the muscle-memory trap. `Mark lost` stays reachable through the sheet's own control.

**Keyed on `redacted`, never on `!row.phone`** (R-37.23). A lead that never had a number is not a withheld one, and its swipe should keep working — there is a cell for exactly that row shape.

---

## 3 · THE CORRECTION THIS SEAT OWES ITSELF — c-B′.1

The kickoff's §2(a), NOTE 36 §3, **and my own Seat A′ handover** all said the contact buttons at `SliceRow.tsx:293/:303` would become tier-true automatically. They will not, because:

```tsx
{slice === 'clients' && row.phone && (
```

**Clients only. Never leads, at any tier, before or after the recut.** Three chain links from one ungrounded grep, and one of them is mine — written at Seat A′, committed at `0dc5a27`, and cited as this sitting's charter. I took NOTE 36's sentence and did not run the command.

Lead contact actually lives in **three** places, derived: the detail-sheet footer (`SliceShell`, the real home — where the slot now sits), the left-swipe (§2 above), and `SliceRow`'s clients-only block. That derivation is what put the slot in the right place; without it R-37.24 would have shipped an upsell where nothing had ever been.

---

## 4 · PROOF

**`b37_f1625_band_floor_bench` 35/35** · **`b36_leadgate_a_bench` 95/95** · **`tdw37_leadgate_b_slot` 37/37**.

**Non-vacuity by TWENTY-SEVEN mutations of production code** — twelve on `dream-os`, fifteen on `dreamos-pwa` — each biting only its own cure, every tree restored byte-exact and re-verified at zero reds after each. Highlights: the swipe reverted to the phone ternary bites 4 · the slot keyed on phone-absence bites 2 · the founder byte paraphrased bites 1 · a vetoed band label edited bites 2 · the sheet posting `''` for silence bites 1 · the masthead dropping the floor bites 1 · the `Unknown` tripwire defeated bites 4.

**§1 of the pwa bench executes the shipped swipe expression rather than reproducing it**, and reproduces the **pre-cure** expression beside it, asserted to still produce `Mark lost` on a redacted row. That return *is* the disease; the proof fails if it comes back.

**FLOORS.**
- **`dream-os`: NAMED BASE, no delta** — measured **last**, in `--delivery` mode against this delivery's own manifest, on the exact tree in ZIP 1. Engine gate RC=0. `node --check` clean on all touched `.js`.
- **`dreamos-pwa`: NAMED BASE SEVEN with ONE declared exception** — `tdw_f0774_vacuity_probe` reds, and its own output says why: *"STOP — the tree is dirty. This probe writes to production source and restores it; on a dirty tree it cannot prove the restore was clean. Nothing was touched."* NOTE 36 §5 names this exactly — a mid-apply red from it is the instrument working. **It closes on the founder's tree after apply-and-commit.** Every other bench matches base by name.

**TYPE FLOOR: DECLARED, NEVER CLAIMED.** This container cannot reach Google Fonts, so `next build` cannot run here. `npx tsc --noEmit` over the whole tree returns **zero errors**, which is a type read and *not* the floor. The chair witnesses the floor on the delivered tree per NOTE 36 §2.2.

### THE TYPE CENSUS CAUGHT A BYTE I WOULD NOT HAVE SQUINTED AT

The slot's sentence first shipped at `fontSize: 15`. `tdw09_type` named it at the byte — `SliceShell.tsx:916 15px` — failing three cells: below the T-1 body floor, and a tenth declared size against nine rungs. Cured to **16**, which is both the floor and the row's own detail-line rung, so the slot's sentence now matches the italic line directly above it. **A census found a font size in a sentence nobody would have measured.**

---

## 5 · DECLARED DEVIATIONS AND GAPS — ratify or revert

**① THE ZIP-ORDER WINDOW IS REAL AND ANNOUNCES ITSELF.** Between ZIP 1's apply and ZIP 2's deploy, the door accepts a floor the sheet does not yet post. A top-band enquiry in that window behaves exactly as it did before this sitting — `budget_min` null, card reads `Rs —` — and the door emits a `console.warn` naming the window by name. It is a **warn, never a refusal**: an enquiry is never lost to this, and a cell extracts the block and proves there is no `throw`, `return` or `res.status` inside it. The line closes itself the moment ZIP 2 lands. **The walk must be run after BOTH deploy.**

**② `bandAnswered` IS BUILT AND DELIBERATELY NOT LOAD-BEARING.** The door now reads the ''-vs-absent distinction instead of discarding it, and a cell asserts the top-band inference is **not** riding on it — the floor is the carrier. Building on `''` alone would put the band table's meaning in two repos. It exists so a future reader does not rediscover the distinction as news.

**③ `scripts/tdw09_vendor_census.json` IS REGENERATED, NOT HAND-EDITED.** One cited site moved, `SliceShell.tsx:745 → :771`, a **+26** shift matching the swipe comment block exactly (arithmetic checked, and `:771` verified to land on a real 9px `F.label` site). It ships because without it the founder's tree would carry a census pointing at a line that has moved. **It is also the PATH-OVER-RANGE disease in a data file** — a census that pins line numbers regenerates on every insertion above them. Named, not chased.

**④ F-08.105 / R-37.26 — THE DEMO PLANE IS EXCLUDED BY RULING.** `demo_leads` has no `budget_min`, so parity costs a migration this sitting attests zero of. Three cells assert the exclusion stays deliberate, including one that reds if `demo_leads` ever *gains* the column — a later seat is forced to read the ruling rather than discover the gap.

**⑤ THE LIST-ROW SLOT ARM IS FILED, UNTAKEN.** R-37.24 refused it as new chrome wanting its own founder word. A cell asserts no slot appeared on `SliceRow`, so the refusal cannot erode quietly.

**⑥ `Unknown` STAYS, WITH A TRIPWIRE (R-37.27).** For a lead whose name the estate genuinely never captured, `Unknown` is **true** — agent-created nameless leads exist by construction (`createLead` writes `name: name || null`). The A-cut's crime was rendering it *over a name the estate held*. Four cells now prove a wire carrying a name never renders it, including on a redacted wire. No new vendor-facing byte; the word itself is on the founder's veto shelf whenever he next tastes copy.

**⑦ A DERIVATION WORTH KEEPING, found by mutation.** `bandCeiling`'s opening `if (s === '') return null` **is not the load-bearing guard** — `Number('')` is 0 and the closing `n > 0` refuses it independently; deleting the empty-string line bit nothing. A future tidy-up removing `n > 0` "because the '' case is handled above" would ship the Rs 0 lead. Both are now pinned.

---

## 6 · THE SMOKE CARD

**Run only after BOTH deploy** — ZIP 1 on Railway, ZIP 2 on Vercel. Fixture state from the founder's own pasted rows: vendor `Dev Roy Photography` / `23165e38-…3742`, **already `basic`**, so no flip is needed to enter the walk.

1. **Both deploys green.** Evidence: the two banners. Nothing below means anything otherwise.
2. **Open a lead's detail sheet.** Evidence: screenshot. **Expect the slot** — 「Upgrade to Essential tier or above to connect with your lead.」 with 「See plans」 beneath it, where WhatsApp and Call sit for a paying vendor.
3. **Tap `See plans`.** Evidence: screenshot. Expect `/vendor/billing`.
4. **Swipe LEFT on a leads row.** Evidence: screenshot or a plain sentence. **Expect nothing to happen** — no `Call`, and critically **no `Mark lost`**. This is the incident's own witness.
5. **Enquire as a couple choosing `Rs 10,00,000+`.** Evidence: screenshot of the new lead. Expect the Budget row to read **`Rs 10,00,000+`**, not `Rs —`, and the pipeline masthead to move by 10,00,000.
6. **Flip to a paying tier, reload, flip back.** Evidence: two screenshots. Expect WhatsApp + Call to return and the slot to vanish, then the reverse. The flip SQL travels **with the card, conditional-withheld**, never beside the build.

**Steps with no thumb-path, named:** none this sitting — every ruling above is walkable, which is unusual and worth saying out loud.

**Walk B (the OOW template leg) is UNFIRED, not held — corrected on the chair's word.** Its hold condition was *"until the doors are gate-true,"* and **that condition was MET at `0dc5a27`**, the A′ seal. It has been dischargeable ever since. It is not fired by any step here because it waits on the founder's word, sensibly after this delivery lands so the OOW leg fires onto a coherent surface — which is a **sequencing choice, not an unmet gate**. The distinction is recorded so the record does not drift into reading a discharged condition as an open one.

---

## 7 · WHAT THE NEXT SITTING PICKS UP

1. The walk, then the deviations at §5 ①③ ratified or reverted.
2. **The list-row slot arm** (§5 ⑤), if the founder wants it.
3. **F-08.105** — the demo plane's floor and its own `Unknown` and contact bytes, migration-gated, so the planes diverge deliberately or not at all.
4. **The name-column contact finding** from Seat A′ — a phone in a *granted* column defeats a column-keyed withheld set. Four `public.leads` INSERT sites, none validating `name`. Still unruled.
5. **F-SW.10** and the routing-token render in the conversation block, both filed, both out of radius.

**Copy inventory: exactly one new vendor-facing sentence and one CTA**, both the founder's, both shipped character-exact. The band label reuses `budgetBands.ts`'s existing vetoed byte. Zero persona names in product chrome.
