CE-42 · 4a · R9-J1 INTRODUCTIONS (dream-os, base 09317d6) · [CE-42 LE-E2]

1 · WHAT SHIPPED. `0161` public.introductions (applied by the founder ahead of this
    ZIP; blocks 6-8, the three `comment on` statements, run beside it).
    `src/lib/vendor/introductions.js` — the one writer, the dark arm, E3, chipState.
    `templates.js` gains `introduction`, body proven byte-for-byte against the filed
    string (F-42.36 — the one template J1 sends had no registry entry).
    `capabilities.js` gains TDW_INTRODUCTION and ASSIST_FORWARD_ALERT.
    `victorLines.js` gains FIVE founder-vetoed bytes, their hashes, and the
    load-time self-check the file has claimed since CE-40 and never had (F-42.66).
    `relayStatus.js` gains the eighth home, so 0161's wamid and its router arm ship
    together (R-40.110). b68 is new; b40 (+5) and b64 (+3) move with it.

2 · WHAT IT DOES ON THE GLASS. Nothing yet — the plane is DARK. The Switchboard row
    `template.tdw_introduction` reads `approved`; the founder's tap moves it to `on`
    and the arm wakes. Off, the row is filed `dark`, Railway logs
    `NOT SENT: template.tdw_introduction is off on the switchboard`, and no packet
    reaches Meta. On, DEV440 asks on WhatsApp, is asked one question, is shown the
    filled body, names the recipient, and the handset receives the page button at
    https://thedreamwedding.in/v/DEV440. A second introduction to the same number is
    refused in words and by index — Introductions go once.

3 · WHAT IS NOT PROVEN. No live send, no database — b68 drives a double that applies
    0161's status CHECK and its partial UNIQUE, and a double is not Postgres. THE
    FULL FLOOR DID NOT RUN: 180 benches exceeded the seat container's per-command
    limit, so NO FLOOR NUMBER IS CLAIMED HERE and the founder's run is the floor
    of record (R-38.19). It runs as `--delivery scripts/floor-manifest-ce42-4a.txt`,
    NOT `--check`: an applied, uncommitted delivery tree is dirty BY CONSTRUCTION
    and `--check` refuses any dirty tree. [F-14.16] is that exception and the
    manifest is its input, so the manifest's file table is MACHINE-READ. Six benches were run by hand and are
    named in scripts/floor-manifest-ce42-4a.txt with twelve mutations, each red.

4 · WHAT IS OWED. The pwa cut — `switchboardCopy.ts:91` and the Leads source map —
    held for after D5a by ruling. R9 still owns the per-vendor STOP register and the
    per-vendor daily cap (F-42.40, F-42.41): declared, not built. The recipient
    handset for the walk is the founder's to name, and whichever number he uses is
    burned for DEV440 permanently by uq_introductions_vendor_recipient.

5 · THE HASH. Base 09317d6bceb168afa23bac01ab7fa088f18a0b14. b68 99/99 · b40 113/113
    (was 108/108, +5 disclosed) · b64 134/134 (was 131/131, +3 disclosed) ·
    b67_r8 exit 0 · b20_a2 165/165 · b66 77/77. First cut pinned fd9d0da4 and was
    bounced: base drift, and b67 collided with R8-1's bench (R-40.86). r2 was
    re-cut as r3: its verify line asked for --check on a tree it had just dirtied,
    and its manifest carried no file table for --delivery to read (e-3, e-4).

── RIDER (same ZIP family, applied on top of r3; one commit for both) ────────

6 · WHAT THE RIDER SHIPS. F-42.67 — `b55_g2_reviews_bench`'s count cell re-pinned on
    its guarantee. It read `if (buttons !== 2)` while the comment directly above it
    said "FULLY DERIVED… an appended entry never reds this cell for the wrong reason";
    the prose was the intention and the code was a census. Seat D's F-41.123 and
    F-41.114 took it to four and it has been red in nobody's delta ever since. 4a's
    entry took it to five, which is how it became this seat's: shipping a green packet
    that makes a red redder is absorption. c-42.12 — the chair's line on
    `scripts/floor-base.txt`, which gains `b05_f0550_ping_drain_bench`, 23 → 24 named.
    That bench is F-42.68, FILED AND UNTOUCHED; its owner is whoever holds the F-05.50
    drain (Block 09). The manifest grows to thirteen declared paths.

7 · F-42.69, FILED, NOT CURED, AND IT BOUNDS THE WALK. There is NO INBOUND ARM.
    `createLead` with `source='introduction'` is written nowhere on the tree —
    `grep -rn "'introduction'" src/` returns only this packet's own registry key and the
    router arm's reason string. 4a is the OUTBOUND half of R-41.11 and nothing more. So
    the walk can witness the flip, the question, the draft, the E3 refusal, the send,
    the receipt on the handset and the button's destination — but a reply from that
    handset lands on the marketing line and becomes NO LEAD. Step 7 of the founder card
    ("/vendor/leads shows an introduction lead") CANNOT HAPPEN AT THIS CUT. The card
    ends at the receipt until the inbound arm is chartered.

8 · FLOOR AT THE RIDER. b68 99/99 · b40 113/113 · b64 134/134 · b55 50 GREEN 0 RED
    (was 49/1). M13: a plain entry grown an undeclared header still reds the re-pinned
    cell, so it is stronger than the counts it replaced. The full floor is still the
    founder's `--delivery scripts/floor-manifest-ce42-4a.txt --check`; with c-42.12's
    line in the base it must read FLOOR = NAMED BASE, no delta.
