# CE-42 · SEAT G2 · G2-B · THE FILINGS DEBT — HANDOVER
base `aaa238e` · **docs only** · no `src/`, no `db/`, no migration, no bench

Six filed items, none of which were on the tree. All six are now on it.

1. **F-42.129** — `TEMPLATES.md` gains **§1a**: both filed URL-button bases store a literal
   `{{1}}` and append the suffix rather than substituting it, witnessed twice on the founder's
   handset; the two pwa bends recorded as landed at `a96e2e2` with `file:line`; and the rule for
   every future filing — **enter the base URL only; Meta appends its own `{{1}}`; the sample field
   takes the suffix alone.** Each of the three URL-button rows (10a, 11, 13) now records its base
   as Meta holds it. No template edited (R-42.2).
2. **F-42.44** — row 13's heading said *"the vendor line for the J1 walk"* and contradicted
   R-41.11. Corrected to **marketing**; the registry always said so (`templates.js:948`).
3. **F-42.93** — recorded in row 13's new **Registry witness**, with **two** sites to cure, not
   one: `src/lib/vendor/introductions.js:58` and `src/lib/victorLines.js:186` (`:171` at the
   charter tip; the file moved under V-2/J1-IN r2 and the citation is re-derived). Three further
   `four`s in those two files are **correct** and are named in the entry so r2 does not over-cure
   them. Code left to J1-IN r2.
4. **F-42.96** — `tdw_introduction_reply` body ruled and recorded as row 15 in §2 and §3, marked
   **not filed**, Meta ID owed. Founder's steps below.
5. **Succession note** — `docs/TDW_CE_SUCCESSION_NOTE_22.md` renamed to
   `docs/CE_SUCCESSION_CE41_TO_CE42.md`; §4's roadmap row letters corrected with a dated line.
6. **§3 hygiene** — the tracker table was three fragments; it is one table, rows 1–15.

## THE RENAME NEEDS THE FOUNDER'S HAND (read before applying)

A `deploy/` ZIP can only add and overwrite. **It cannot delete.** The new file rides the ZIP; the
old path must be removed by command, and the `git rm` is in the git block for that reason. If it
is skipped, the estate carries **two** copies of CE-41's note and the collision this sitting cured
comes straight back.

**Pointers re-checked at `aaa238e`.** No file anywhere in either repo cites the old path as a
string. The two committed references to "NOTE_22" —
`docs/specs/TDW_CE_SUCCESSION_NOTE_23.md:6` and `docs/FINDINGS_LOG.md:3991` — both mean the
twenty-second chair's own note at `docs/specs/TDW_CE_SUCCESSION_NOTE_22.md`, which keeps its name.
Nothing to fix, nothing declared.

## WHY THE ROW LETTERS WERE WRONG (so it is not re-derived)

`docs/specs/TDW_CE41_ROADMAP.md` §2: row **F** = R8 · row **G** = G2 s2 · row **H** = R6 · row
**I** = R7 s3 · row **J** = R9. The note's §4 item **3** *is* G2 s2 — the true row G — so item 2
was handed item 3's letter and the rest shifted by one.

## FOUNDER — FILE `tdw_introduction_reply` (R-42.4)

**A NEW template. Not an edit of anything.** Not urgent; do it whenever.

1. WhatsApp Manager → **The Dream Wedding Direct** (`1739793260373677`) → **Message templates** →
   **Create template**.
2. Category: **Utility**. Name: `tdw_introduction_reply`. Language: **English** (`en`).
3. Header: **none**. Footer: **none**. Buttons: **none**.
4. Body — paste exactly this one line, nothing before or after it:

```
Hi, {{1}} replied to the introduction you sent after meeting at {{2}}. The reply is saved as a lead. Open your Leads on The Dream Wedding to read it.
```

5. Sample values, when Manager asks for them: `{{1}}` = `Anjali Mehta` · `{{2}}` = `the Bhatia sangeet`.
6. Check the preview reads: *Hi, Anjali Mehta replied to the introduction you sent after meeting
   at the Bhatia sangeet. The reply is saved as a lead. Open your Leads on The Dream Wedding to
   read it.* If a word differs, stop and say so — the body is hash-carried in `TEMPLATES.md`.
7. **Submit**.
8. When it clears, send back two things: the **Meta template ID** and the **category Meta gave
   it** (it may come back Marketing — that is Meta's call, R-40.58, and is recorded, not argued).
   Both go into `TEMPLATES.md` §2 row 15 and §3 row 15 in a one-line follow-up.

## OPEN / NEXT

- **Meta ID for `tdw_introduction_reply`** — owed by the founder, one-line docs follow-up.
- **F-42.129 stays open**: `/r/` was found and cured at D5a; `/v/`, the same base shape filed the
  same week at the same Manager, went unchecked until R8. The finding is the gap between them.
- **F-42.93's code cure** — both sites, `introductions.js:58` and `victorLines.js:186`, to
  **J1-IN r2**. Leave `introductions.js:59`, `introductions.js:46` and `victorLines.js:80` alone:
  the first counts the reused relaySeat bytes correctly, and the other two quote the chair's own
  W-1 ruling, which said four and was true when it was made.
- **Both strips retire together** at Meta's next edit window, each with its own family
  (`route.ts:127`, `metaPlaceholder.ts:47-51`).
- **Still carried, not this sitting:** F-41.120 retention table · the R-41.130 transfer paragraph
  (09-16).
