# CE-42 · SEAT V-2 → NEXT SEAT · HANDOVER (base 2fadf83)

1. **F-42.98 cured by ARM B** in `moneyGrounded` — a spoken figure not held by equality is admitted only when it is an exact sum of ≥2 of that block's own row figures, the grand total never an addend, candidates rendered FORWARD through `witnessLine.rupees` and compared as strings (no rupee parser exists in this tree — F-42.114, and none was written). Bound: sizes 2–4 to 20 rows, 2–3 to 40, no admission above 40. The 00:52:18 production bytes acquit; Rs 55,000, Rs 32,000, a product, the total-as-addend, and the founder's paid and cancelled rows all still convict.

2. **F-42.97 cured by a fact block**, `src/lib/vendor/expenseFacts.js`, on A1's seam — door-built, `estateInRoom`-gated, LAST in the dynamic tail after `moneyBlock`, no bracket, no label, fail-closed on its own vetoed byte `EXPENSE_UNREADABLE` (named, not numbered; line 2 stays vacated). Its reader is `readRecentExpenses` in `expenses.js` — last 30 days, 40 rows, window folded in JS because `expense_date` is NULLABLE and a SQL-side window would silently drop rows logged without one. **Dates are handles in both spellings**, because the 00:52:38 specimen spoke only ONE figure and that figure was TRUE: the whole lie was the date.

3. **F-42.123: equality merges across blocks, arithmetic does not.** Each block's rows are their own ARM B pool. A merged pool, driven against the founder's real rows, admitted Rs 55,000 (the invented-figure cell), Rs 45,000 (a cancelled invoice), Rs 18,000 and Rs 12,000 (paid invoices) — 76 of 91 admitted sums existed on neither plane. `MB3` is that mutation and it falls.

4. **The WhatsApp lane was not in the read ladder and it is where the disease was witnessed** (c-42.24). `src/lib/vendorInbound.js` builds its own `moneyFacts` and calls the guard twice — the turn and the Fork D retry. Four seams wired there; wiring only the two PWA doors would have shipped a cure that never reached the handset.

5. **F-42.131 cured in the same sitting** (chair's lift, by arm). `MONEY_STATE_RE` was entirely INVOICE vocabulary, so an expense-shaped reply never reached the money limb and the expense fence could never fire — a backstop that reported itself as present. Widened with `spent · spend(s) · expense(s) · went out · paid out · cost(s)`, each **bounded to within 40 characters of an `Rs`, in either order**, exactly as `invoice` is bounded to a state word. No bare `paid` — the invoice plane owns that word. `§23` benches both directions, including the two sentences an unbounded list would have dragged in ("I spent a while on the edit", "the shoot cost her a week of prep").

**A GAP IN F-42.131'S REACH, DECLARED.** The literal 00:52:38 bytes read `Rs 5,000 **out** on 10 September` — bare `out`, which is not in the ruled vocabulary and is far too common to add. So the production specimen still does not reach the limb; the chair's own phrasing (`went out`) does. **The BLOCK is what cures that specimen** — Victor is handed the row and no longer has silence to fill — and the fence catches the phrasings around it. Stated so the next seat does not read `§23` as covering bytes it does not cover.

**Still open, not this sitting's.** F-40.2's 00:53:25, pinned by `§22`, deferred to Block 09. F-42.120 (no clock time on the money planes) is the soul sitting's.

---

## THE RUNNER CHARTER INHERITS THESE (Block 09)

**F-42.124, specimen 2.** `scripts/run-floor.sh` run under an outer `timeout`. SIGTERM lands on a bench that mutates production source, its `finally` never reaches the restore, and `src/engine/src/core/tools/recordPrimitives.ts` was left carrying `Rs ${v}` where `Rs ${inr(v)}` belongs — **the rupee formatter itself, silently unformatted, on production source.** Caught by the floor's own out-of-manifest guard on the next run, not by any bench. Identical in kind to the specimen that cost a clone.

**c-42.25 (chair's) / e-3 (mine).** A floor was backgrounded and its liveness read from `pgrep -f run-floor.sh` — **a pattern that matches the command doing the grepping.** Three consecutive reports said RUNNING while the process had been dead for forty-three minutes. Protocol §11 already carried this as law (`No seat backgrounds a floor; pgrep -f self-matches`, R-40.63, F-40.128) and this seat re-committed it anyway. A liveness check whose pattern can match its own command line is not a check.

---

## THIS SEAT'S OWN ERRORS

- **e-1** — the echo cell asserted "≥ 3 of `FRAME_BYTES` appear in the readable block", which the UNREADABLE path's bytes can never satisfy. A count that tolerates its own misses is F-06.111's shape. Re-aimed: each byte asserted against the block that renders it, plus a negative that no frame byte is written down outside its module.
- **e-2** — `MB2`'s probe carried Rs 1,62,000, derived from the DEV440 rows two sections above, against a bench whose own FIXTURE totals Rs 1,10,000. It could not fall for any reason the mutation controlled. Re-derived from THIS bench's fixture (60,000 + 1,10,000). A probe derived from the wrong fixture is vacuous no matter how sound the mutation.
- **e-3** — the false heartbeat, above.
- **e-4** — reported to the chair that MB4/MB5's probe-depth reason was "stated in their comments". **It was not in the file** — it existed only in the build script that generated the edit. A claim about what a file says, made without reading the file. Now written into the bench above `MB4`.
