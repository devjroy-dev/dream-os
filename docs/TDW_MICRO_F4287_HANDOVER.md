# TDW · MICRO F-42.87 + F-42.21 CURES 1–2 — HANDOVER

**Base `e23ab4d`** (re-pinned from `7615cf3`; every source blob verified identical across the whole span, including the half the kickoff did not name). Manifest: six files + this manifest.
`b40_victor_sitting_bench` **129/129 → 154/154**, ten mutations biting. `b06_forkc` 113/113 after two labeled amendments. Floor run **pre-commit** on a declared-dirt tree (F-14.16).

1. **F-42.87 arm (a) — the `i` flag defeated the comment above it.** The lifter promised *"case-SENSITIVE on the name, so the verb itself can never be lifted"* and carried `/i`, which makes `[A-Z][a-z]+` match any word. On the founder's live line it lifted `for`, `the`, `here`, `dor`, `saying`, `as`, `Send`. The verb list keeps its case-insensitivity; the NAME capture is now genuinely case-sensitive, a short closed-class list fences what shape alone cannot, and the "first capitalised token anywhere" fallback is gone — it bought nothing a named recipient does not already give. The lifter returns `null`, never `''`, because arm (b) reads that distinction and the empty string could not carry it.

2. **F-42.87 arm (b) — the door needs standing to speak.** `handleStage`'s refusal now fires only when a recipient was **named** and merely lacks a phone; an unresolved lift returns null and the turn goes back to the model whose answer was already right. `doorStage` declines a null lift with its own reason, so the decline stays observable (F-06.171). `ambiguous_recipient` stays on the speaking side deliberately: several candidates means a name **was** given, and refusing to choose among them is the wrong-bride guard's outer wall.

3. **F-42.21 cure 1 — the fence was built against the ruling's example, not the column.** R-40.2 wrote `invoice /05`; the column holds `TDW/DEV440/05`. Both forms are read now, and a stored number grounds an extracted handle when it equals it or ends with it. The **store is the authority** and prose is checked against it, never the reverse. **Cure 2 —** Fork D skips a class with an empty acquittal set: retrying a capability the lane cannot hold produces the same lie twice and ships F3 over R-40.2 line 1, which is what the founder read at 23:28:15.

4. **Both ways.** `M9` an unresolved lift speaks again → the 23:42:52 draft is destroyed. `M10` the fence reads only the tail → every money answer citing an invoice convicts. `M11` Fork D retries a hand-less class → F3 lands over line 1. Plus: real names still lift (`Priya`, `Kunal`, `Rohan`, `Sarah`, a phone), a named-but-phoneless recipient still gets the refusal, F-06.159's specimen still convicts, a wrong rupee and a wrong invoice still convict.

5. **Errors owned — three, all mine, all found by driving.** (i) The first lifter cut returned null for **`Send a message to Kunal`** — the earliest verb hit is `message to`, `to` fails the case test, and I gave up instead of trying the next; found by driving a sentence that must *work*, not the ones that were broken. (ii) The second cut used `matchAll(verb + name)`, which **consumed** `message to` whole so the verb actually preceding the name was already eaten — now it scans verb *positions*. (iii) The phone branch returned a trailing space the character class ate. Plus the mutation-probe interpolation slip (`' + scratch + '` for `${scratch}`, then nested backticks) — the third time this sitting; the pattern is mine and it is in the record.

## FLOOR
`b08_p5_oow_relay_bench` is **inherited** — proven red at clean `7615cf3` with none of this work, byte-identical bench and subject. Not this packet's.

**F-42.89** — `b08_p5_oow_relay_bench` is red at clean `7615cf3` and at `876cef2`, byte-identical bench and subject, and is **not in the floor base**, so `--delivery` passes it silently. Its failing cells are the out-of-window relay send path — the same lane this packet cures, one door over. Filed to Block 09 with F-40.8. **Not this packet's, not folded in, and named here so it cannot be read later as something this micro broke.**
