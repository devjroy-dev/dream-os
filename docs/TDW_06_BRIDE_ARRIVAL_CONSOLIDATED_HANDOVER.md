# TDW_06 · THE BRIDE'S ARRIVAL — CONSOLIDATED HANDOVER

**Written at the close of the sitting that made her arrival whole.**
**Base tip for rider 4: `a2439d3`. Chain: `9f98dfa → e8a89ba → dc7d8bf → a2439d3 → this.`**

**R-29.34 APPLIES TO THIS DOCUMENT.** Every claim names where it can be
re-derived. Nothing here is a memory. **Trust evidence over narrative, including
this document.**

---

## §1 · WHAT THE SITTING WAS

One coherent pass over everything that happens from the moment a bride's inbound
hits the vendor lane. Chartered on five findings; **closed on fourteen**, because
the walks kept telling the truth about what the benches could not see.

**Two production walks. Two brides. Both founder-witnessed.**

---

## §2 · TRUE STATE OF EVERY PATH AT RIDER 4

| path | state | re-derive by |
|---|---|---|
| doorbell-aware routing | ✅ **live, twice, two brides** | walk 9 + 10 `[routing:doorbell] … — no question asked` |
| her inbound on file | ✅ **live** | `conversations` `last_inbound_at` moved on both brides |
| window production at arrival | ✅ live | the auto-send fired, which the predicate gates |
| auto-send end to end | ✅ **live** | walk 10 `auto_sent sent draft=b6eb26b9` |
| the model silenced on an acted arrival | ✅ **live** | walk 10 `model_silent — the door's deed stands alone` |
| ③ to the vendor's handset | ✅ live | walk 10 `Sent to +918595986978.` |
| **№14 delivered receipt** | ✅ **live — first in the estate's history** | walk 10 `delivered_receipt` ×2 |
| №15 read receipt | ⚠️ **unwitnessed** — Meta sent no `read` for the relayed sid | walk 10 log |
| vendor handset resolution | ✅ live | `vendor_handset_resolved vendor=… user=…` |
| the staging-turn block (A6) | ✅ **live** | walk 10 7:01pm screen — one quotation, the door's |
| replace-never-append (A5) | ⚠️ **BENCH-GRADE ONLY** | see §5 |
| the door's confirm stamp | ⚠️ **bench-grade** — shipped in rider 4, unwalked | |

---

## §3 · THE FOURTEEN, AND HOW EACH WAS FOUND

**Chartered five:** `.175` A6 · `.176` A5 · `.177` A1 · `.178` A2/A3 · `.170` (arms
retired unread by ruling).

**Found by the seat at the read-first:** `.179` — her inbound dropped on the
ambiguous branch. **The chair minted A7 on it.** Confirmed live when
`last_inbound_at` moved off a three-day-old value.

**Found by walk nine:** `.180` (`vendors` has no `phone`; every relay byte to the
vendor's handset mute **since seating**) · `.181` (two benches, one sealed, whose
fixtures asserted that column) · `.182` (the bride lane narrating a completed
relay as future — **the founder, reading his own handset**).

**Found by walk ten:** `.183` (the guard acquitting a wire claim on a lead write —
the estate's own eval row convicted it) · `.184` (A5's gate was the guard arming,
not the ruling) · `.185` (a founder-struck phrase still shipping a day later) ·
`.186` (the bride addressed by her number twice) · `.187` (phone-as-name in
`leads.name` — **restated after the founder corrected the seat's reading**) ·
`.188` (an approval gate decided by a display string) · `.189` (「 She has it. 」
walking a vocabulary both defence layers shared).

**Found by rider 4's own probe battery:** the contraction gap — 「 She's got it 」
walked because the limb required a space before the apostrophe. Cured in the same
limb. **A second survivor is declared, not hidden:** 「 It's with her 」 walks a
*neighbouring* limb this ruling does not cover — **F-06.190, proposed**, asserted
as a known gap by cell `A14.5b` so it can neither be forgotten nor silently
closed.

---

## §4 · THE LAW THIS SITTING PAID FOR, THREE TIMES IN THREE COATS

**WHEN THE MACHINERY ACTED, THE MODEL DOES NOT NARRATE THE ACT.**

- `.182` ruled it on the **bride lane**: an arrival that delivered a relay skips
  the couple turn entirely — no tokens, no assistant row, no costume to patch.
- `.184` ruled a claim-tested replacement on the **vendor lane**.
- `.189 (α)` ruled that replacement **unconditional**, because the claim test was
  a vocabulary question and the vocabulary missed.

**And the sentence the chair took into the band:** *depth over a shared predicate
is depth in implementation and not in evidence.* The two layers now differ in
**kind** — one structural, one lexical — which is what depth meant all along.

**AN APPROVAL GATE IS DECIDED BY A DISPLAY STRING** (`.188`) is its sibling: the
door's act is a row, the predicate read a sentence, and **this sitting moved the
gate while fixing copy**. The stamp ends that class. Its derived wall — two admin
cost readers filtering `sent_by='agent'` — is cured **in the same delivery**,
because a stamp that silently under-counts spend would be the finding's own
disease in a second coat.

---

## §5 · DECLARED UNWITNESSED — THE LEDGER, OWED TO WALK ELEVEN

1. **A5 / `.189(α)`** — no acted vendor turn has been walked since the rule became
   unconditional. **Now trivially checkable:** any staging turn should show the
   door's frame **alone**, with no Victor prose above it. Five minutes.
2. **E3-prime's PLAIN lane, for a nameless bride** — walk ten approved by
   **naming**, per `.188`. The stamp should now make a bare 「 yes 」 work; unwalked.
3. **№15** — Meta sent no `read` for the relayed sid. Not a defect; unwitnessed.
4. **THE BUTTON PATH** — walk eight's own failure path, still unproven. **A
   doorbell cannot ring while a window is open.** Earliest: **2026-08-12 12:34 UTC**
   (Priya) or **14:19 UTC** (`+918595986978`). *The seat proposed this test minutes
   after walk ten with both windows open; the founder caught it.*
5. **The confirm stamp** — shipped in rider 4, unwalked.

---

## §6 · THE SEAT'S CONDUCT RECORD, BOTH DIRECTIONS

**Self-caught and disclosed at discovery:** an ordinal mistaken for an identity ·
a read-first span too narrow · a bench stripper that deleted its own cells'
landmarks · a store double speaking a contract its subjects don't · a
fixture-shaped cell of my own · a ZIP verified and never handed over · a verify
line that ran benches before the gate.

**Founder-caught, four times, and each one mattered more than the last:**
`.182`'s false sentence · the `.187` misreading, corrected against protocol §4's
write-first doctrine · the button test proposed inside an open window · **and the
one that started walk nine — asking where the ZIP was.**

**Amendments across the sitting: seventeen**, every one labelled, count-preserved,
ratify-or-revert.

---

## §7 · QUEUED, CHARTERED, NOT STARTED

- **`.187`** — the write guard in `donnaLead.ts`, the row-4fc3f1d9 correction, and
  the data census. Engine plane; its own micro.
- **№16** — the expiry notice, founder-ruled 「 a 」. Depends on `.180`'s one home.
- **`.143` limb 1** — sid-less notification inserts. **Two nights of dated
  evidence** now (`matched=0 — NO ROW CARRIES THIS SID`, eleven firings). Outside
  radius, on the shelf.
- **`.190`** — the neighbouring contraction gap, declared by cell.
- **`PUBLIC_SCHEMA.md` regeneration** for `pending_couple_drafts`.
- **The toolchain pin** — `"typescript": "^5.6.0"` is a range; 6.x makes the
  engine gate hard-fail (proven in this seat's container, day one).

---

## §8 · THE ARC, PLAINLY

Eight walks to deliver one message. Two more to prove the whole path on a second
bride — routed without a question, delivered without a word, receipted on the
vendor's own handset.

**Every failure this sitting found was on the safe side.** Nothing unearned ever
reached a bride. The store told the truth on every single turn, including the two
where the prose lied — and both times the machinery's own record is what convicted
the sentence.

**The mechanical layer was already honest. This sitting was the sentences catching
up with it.**
