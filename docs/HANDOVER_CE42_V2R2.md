# CE-42 · SEAT V-2 · RIDER r2 → NEXT SEAT (base 567ab21)

1. **F-42.132 arm (1), as amended by c-42.27 — the trigger is the figure, the FENCE is not.** `moneyClaim` now opens on a spoken rupee figure as well as on the verb list. `MONEY_STATE_RE` keys on `owes/unpaid/outstanding/invoice` plus F-42.131's expense verbs, and **none of them is the word a man reaches for reading his own book back: he says LOGGED.** The verb list stays as a second door — it still catches "nothing outstanding", which speaks no figure at all. One reader (`extractAmounts`) serves both the trigger and the fence, so they cannot disagree about what a figure is.

2. **SHAPE 1 (c-42.27) — the fence rules only on the planes it has books for.** Arm (1)'s first cut let the figure both open the limb and expose the turn to a fence holding two books. Counted off `engine.messages`: of 51 assistant replies carrying a rupee figure in thirty days, **33 carry no invoice word** — and reading them they are relay drafts quoting the vendor's own fee, lead budgets, and `engine.records` figures. All true, none groundable; a two-book fence would have convicted every one. So the limb is three-way: **(a)** a money-state sentence is fenced as before; **(b)** a stative report carrying a figure grounds or convicts; **(c)** neither → `kind='unfenced'`, recorded with `spoken_figures`, never a specimen, never an F3 line. **The third door is reachable only when the figure is the ONLY reason the turn arrived** — no presence claim, no narration, no jot, no relay claim, no act claim — because the first cut let it swallow the 00:52:38 specimen's live `presence_claim` conviction. A new class must never quietly retire an old verdict; `MC7` is that mutation.

3. **F-42.132 arm (2) — the fact-grounded escape reaches the expense plane.** A **stative report** says a row exists; an **act claim** says Victor did something this turn. The second is still false by construction and still convicts. Four load-bearing clauses: `stativeDone && !participleDone && !victorClass && spokenFigures.length > 0`, and the fence must actually pass. The 00:52:38 specimen — a true Rs 5,000 on an invented 10 September — still convicts on the date.

4. **F-42.134 — the fence can see shorthand.** `10k → 10000`, `1.5L → 150000`, `2Cr → 20000000`, folded into the same list `extractAmounts` already returns, grounding against raw digits the block already pushes. No new handle form anywhere. **This arm grounds the number and never the style** — the register breach is F-42.133's and belongs to the soul sitting under W-1. A fence that convicted on spelling would do the soul's job badly and destroy true answers doing it.

5. **F-42.135 (e-5, mine) — the expense block's arrival rides the record.** `expense_facts` beside `money_facts` in the same jsonb, carrying `present`/`readable`/`rows`/**`truncated`**, absent-shape `present:false` rather than a missing key. Zero DDL. Before this, `jsonb_object_keys` over the 03:31 rows returned fifteen keys and none of them said whether the block reached him.

6. **THE LESSON THIS RIDER PAID FOR, AND IT IS THE SEAT'S.** r1's bench was green on every cell and the walk found the defect in ninety seconds, because **every cell used phrasings the seat composed** — the chair's verbs, never Victor's. e-4. Every cell in `§24`–`§26` is driven off `engine.evals_runs` production bytes. Two probes in this rider were caught mid-run for the same class of error: a "wrong figure" cell used Rs 7,000, which is a genuine expense subtotal (5000+2000); and MC3 first probed a bare "Done.", which `moneyClaim` already guards, so the clause it mutates could not be the thing that fell. **A mutation that cannot fall for the reason it names is a hollow green** — both recorded in the bench beside their cells rather than quietly fixed.

**Open, not this rider's.** F-42.133 (register breach on the wire) and F-42.120 (no clock times) → soul sitting, W-1. F-40.2's 00:53:25 → Block 09, pinned at `§22`. F-42.124's two specimens and c-42.25 → the runner charter, in `docs/HANDOVER_CE42_V2.md`.

**F-42.146 — WHAT THE UNFENCED CLASS IS FOR.** It exists so the weekly read can count how often Victor speaks figures the estate cannot ground, and whether those planes want books of their own. Three planes showed up in the thirty-day read and none has one: the **relay draft's body** (the vendor's own quoted fee, which is in `pendingRelay` and is not a handle), **lead budgets** (`public.leads`), and **`engine.records`** money columns. That is a later charter, not this rider's.

**A GAP IN (b)'s REACH, DECLARED.** The 00:52:41 sibling of the specimen — 「 Already logged. Rs 5,000 out on 10 September … filed 04:58 this morning. 」 — is the SAME invented date, and it classifies **null at the base** and **unfenced here**. It is not a money-state sentence, and `stativeDone` does not fire on 「 Already logged. 」, so (b) never sees it. `unfenced` is strictly more visibility than the null it replaces, but the lie is still not convicted, and one phrasing of it (「 I have that on file already 」, a presence claim) convicts while its sibling does not. Not cured here and not this seat's to widen: the stative predicate is the soul-adjacent surface the chair has kept narrow all sitting.

**A derivation that was owed and IS NOW MADE.** The count above is why arm (1) was amended before it shipped. It was measured against `engine.messages` rather than `engine.evals_runs` — the first SELECT this seat proposed used `evals_runs`, which only holds turns the classifier already ruled on, so the population arm (1) would newly admit was invisible in it **by construction** and the reassuring 29/5/24 was measuring the wrong set.
