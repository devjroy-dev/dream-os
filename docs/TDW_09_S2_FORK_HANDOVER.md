# TDW_09 · UX BLUEPRINT · STAGE 2 OPENING ZIP (FAST-TRACKED: F-09.20 FORK PAPER + CE-193) — EXECUTOR HANDOVER

**Base:** dream-os `e0ff7d9` · dreamos-pwa `e3210b5` (chair's R-X6–R-X9 note the founder tip moved to `d5eda29` with the stage 1 apply; this ZIP's docs are append/new-file only and apply cleanly on it — nothing here collides with any file that moved).
**Under:** R-X9 (stage 2 released; CE-193 rides this ZIP) · AD-X3/AD-X4 (F-09.20 + the fast-tracked fork paper) · AD-X5 noted (the Opus L-B hold rides this paper's ruling).
**This ZIP: two docs + one chair-authored ledger band. Zero code bytes, zero pwa bytes, zero soul/lens/prompt/engine bytes — W-1 held. Zero copy bytes ship (the paper proposes; the veto rides the cure).**

## 1 · WHAT SHIPPED
| Path | What |
|---|---|
| `docs/specs/TDW_09_S2_SIGNUP_FLOW_FORK.md` | the F-09.20 fork paper — machinery derived whole (four witnesses, three of them the estate's own comments), full footprint with load-bearing check, arms (a)/(b)/(c) with admin + L-B effects each, recommendation (a) with evidence |
| `docs/FINDINGS_LOG.md` | **APPEND ONLY** — the chair-authored CE-193 band, carried verbatim per R-X9; append-only proven: first 1,083,062 bytes byte-identical to origin's (`cmp` clean), 4 lines appended |
| `docs/TDW_09_S2_FORK_HANDOVER.md` | this file |

## 2 · THE PAPER'S HEADLINE DERIVATIONS (each verified by command at my tip)
The acquisition path is already open (landing `:436`'s own F-05.9 comment; `:679`/`:689` → codeless `invite_phone`) · the `invite_code` screen has **zero setters** — dead scenery, so `/invite/validate` has zero reachable frontend callers · `src/api/register.js:4` declares the gate replaced and itself has **zero pwa callers** (the open door was built twice, wired once) · real curation lives at `src/api/admin/discover.js:52` (`discover_eligible` / `discover_request_state`) — the feed is gated, so a door gate duplicates a working control · `circle_members`/crew tokens never touch `invite_codes` — the retirement cannot reach those doors · `waitlist_signups` + `invite_codes` rows are history; every arm leaves both tables standing, any drop is founder SQL under the destructive-DB law.

## 3 · DISCLOSURES
**D-1 ·** The load-bearing check on the three test accounts is an inference from architecture (consume-path superseded pre-dating the accounts' mint era + register.js callerless), not a row-level witness — the paper says so and the cure's own verification re-derives caller sets at its tip before any deletion (R-X6's discipline, chair-instructed). **D-2 ·** The "~20 strings" figure is the chair's, confirmed at the same order by my census (60 raw request/invite hits, ~20 rendered strings after state-names and handlers are excluded); the cure ZIP will carry the exact string-by-string veto table. **D-3 ·** Arm (b)'s cost statement ("pending state + gating sweep + admissions queue") is shape-level, not a build estimate; if (b) or (c) rules, its own read-first derives the sweep radius.

## 4 · FLOOR
Docs + ledger append only; dream-os `npm run build` exit 0 at this tree (no-regression witness). The append-only proof is the delivery's real gate and it is stated in §1 with its byte count.

## 5 · WHAT IS NOT DONE / WHAT GATES WHAT
The founder's one-word arm ruling — **it gates the Opus L-B cure (AD-X5) and this sitting's admin/backend retirement ZIPs**. Stage 2's remaining order (heuristic + states + touch) proceeds next on this sitting's side regardless. The R-X8 Notes re-home cure (F-09.18 arm (a) + the F-06.6 class) — chartered, not in this ZIP; its read-first verifies Notes' full inbound set at its tip per the ruling.

Sequencing beyond this sitting is the founder's.
