# TDW · MICRO F-42.21 / F-42.22 — HANDOVER

**Base `f3e9ff0`** (re-pinned twice: `082939a` -> `b3b5995` -> `f3e9ff0`; see §RE-PIN). Manifest: four files + this manifest. Floor run **pre-commit** on a declared-dirt tree (F-14.16), `--delivery scripts/floor-manifest-ce42-victor-f4221.txt`.
`b40_victor_sitting_bench` **113/113 → 129/129** (+16 cells; the chair's estimate was +11, the true count is +16 and is derived below). Floor **= NAMED BASE, no delta**, exit 0, run by SET on a committed tree at the new base of 24.

1. **F-42.21(a) · R-42.3.** A first-person future transmission (`'ll` / `will` / `going to`) in a reply that names an input as missing and asks for it is an **offer, not a claim**, and is not self-marking. Predicate reads the **whole reply**; sits at **module scope beside `RELAY_CLAIM_RE`**, not in the ladder. It lifts **both** transmission families (`RELAY_CLAIM_RE` *and* `leadSendClaim`) — lifting one shipped the identical interception. It **cannot lift a completion**: the future spans are excised and the **shipped** regex re-tested; anything still matching turns the exemption off.
2. **F-42.21(b) · the seat's own half.** `BARE_INTENT_RE` fired on a bare `reaching` — the particle was optional, so 「 …confirm I'm **reaching the right person** 」 read as a transmission. Each particle now required with its own verb (`reach out` / `get in touch` / `follow up`); the four plainly-transmissive verbs require a recipient preposition. `writing the draft` and `getting the file` no longer fire.
3. **F-42.22.** `moneyOnly` computed **once** and read by both the class ladder and the kind limb, so a fact-grounded turn is filed under `money` and cannot scatter into `booking`. No behavioural change beyond the filing: `moneyOnly` entails `!claimsAct`, so the money limb returns before `classWitnessHands` is consulted.
4. **Both ways.** `M5` (drop R-42.3) → the 18:28:36 false interception returns · `M6` (restore the bare gerund) → half (b) returns · `M7` (booking ahead of money) → the kind and class disagree again · `M1d` re-aimed at the line R-42.3 rewrote. F-06.150's bare promise, F-06.159's 「 Message to Priya is live 」 and a completion beside an offer all still convict; a real `donna_relay_send` still acquits.
5. **Errors owned — five, all mine.** **B-4** invented a fixture carrying `action_claim`. **B-5** used a control sentence carrying a mutation verb, so it filed `date` not `booking`. **B-6** pinned one paragraph of a four-paragraph reply, so `M7` applied and changed nothing and the cell would have gone green on a cure it never exercised — caught only by running the mutation by hand when it refused to red. **Siting:** `FUTURE_TRANSMISSION_RE` carries `whatsapp` as a transmission verb and I declared it inside `wireGuardClassify`, reddening `b06_forkc §6.11`'s seat-branching census; **the cell was right and the siting was wrong** — the vocabulary moved to module scope and the cell was not amended. **F-42.66, filed against me by 4a:** `victorLines.js` carried a comment claiming `assertLineHashes()` ran at module load since CE-40 and it never did — my bench called it, which is not the same thing, and the comment asserted a guarantee the file did not have. Cured by 4a; recorded here as mine.

## RE-PIN (chair's method, executed)

`git rev-parse origin/main:<path>` for every manifest path. **Two of five moved at the first re-pin**, both by `c1c17db` (4a); at the second re-pin (`b3b5995` -> `f3e9ff0`, 4a's door rider `d6cd660` + D5b) **all four manifest blobs are byte-identical** — the rider touched `vendorInbound.js`, `donna.ts`, `introduce.ts` and `introductionSeat.js`, none of them mine, and that was derived by `git rev-parse` rather than read off the commit message: `scripts/b40_victor_sitting_bench.js` and `src/lib/victorLines.js`. `chat.js`, `wireGuardVictor.js` and `moneyFacts.js` are **blob-identical** `082939a..b3b5995`, so their edits carried across unchanged; the bench's edits were **re-applied onto 4a's content**, never overwritten.

**c-42.15 (chair, one correction).** The kickoff names `src/lib/vendor/victorLines.js`. That path **does not exist**; the file is `src/lib/victorLines.js`. Derived by `git rev-parse` at the tip.

**`victorLines.js` is NOT in this manifest.** It moved, this micro does not touch it, and shipping it would carry 4a's F-42.66 cure back out through a packet that has no business holding it.

**The count.** 113 + 16 = 129, not the estimated 124: `§14` adds eleven cells and `§15` three, plus `M5`/`M6`/`M7` register two more assertions than the estimate assumed. The sets union cleanly — no cell was displaced and none was renumbered.

**The two "inherited reds" are gone at the new base, verified not carried.** `b05_f0550` was added to the base by `c-42.12`; `b55` was re-pinned by 4a's rider (`F-42.67` — the button census had stood in for its guarantee and had been red since `F-41.123`). Floor is exit 0 with four refusals, all of them the absent `dreamos-pwa` sibling.

## RE-CUT (bounce, three things — all three are mine to own)

**Apply chain.** I shipped `cp -r deploy/*`, the retired spelling that silently drops dotfiles. The chain is bytes: `unzip -o FILE.zip && cp -r deploy/. . && rm -rf deploy FILE.zip`. This packet carries no dotfile, so nothing was lost — but the law is about the spelling, not this packet's luck, and a chain a founder pastes from muscle memory is exactly where a retired spelling survives.

**Head-guard.** Block 1 opened straight on `tools/base_guard.sh` with no repo assertion above it. The guard line is `grep -q '"name": "dream-os-backend"' package.json || { echo "WRONG REPO — STOP"; exit 1; }` and it goes first, because a base_guard run in the wrong clone answers a question nobody asked.

**Floor siting.** I moved the floor after the commit to cure executor error №7 (running it on a tree its own apply had dirtied) and, in doing so, missed that the tree ships an instrument for exactly this: `--delivery <manifest>` (F-14.16). The floor belongs **in the verify block, pre-commit**, on a declared-dirt tree, with the git line last and its own block. Every other packet tonight ran it that way; I invented a sequence instead of reading the runner's own flags.
