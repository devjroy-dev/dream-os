# CE-41 · SEAT D · D3b (dream-os) — the couple is told · HANDOVER

**Cut at** dream-os `b66b99b3d1293708d6a046f8538c6b2d9932865f`, **re-pinned to `b72d855e0e9577664e453076aedb948d60931c6b`** — seat B's B4-9a (R-41.132/R-41.135 recorded in the policy read) landed between the cut and the apply.

**The base guard caught it and stopped the chain**, which is the second time today it has caught seat B landing a docs commit in the same window. **Collision derivation (R-40.82):** `git diff --name-only b66b99b..b72d855` is **one file** — `docs/filings/META_MESSAGING_POLICY_READ.md`. None of this packet's six was touched. Clean fast-forward, no carry.

Migration **`0158`**.

## 1 · Its own table, and why not a second wamid

`0150` anticipated this in its own comment: *"this row will one day carry the couple-facing 'we found you' send too… two sends on one row need two named homes."* The forward's wamid belongs to the message sent **to the vendor or the outsider**; this one belongs to the message sent **to her about it**. Same event, two directions — and one column cannot hold both without a send silently overwriting the other's receipt. R-40.110's partial UNIQUE would then be actively harmful: it would make the collision look like a correctly-matched row.

**One event, one row**, keyed on `forward_id` with a UNIQUE — structural, not a convention. A couple whose photography reaches three vendors hears three times, once each.

## 2 · The column and the arm shipped together

`lead_alerts` (F-40.177), `referral_alerts` (F-40.190) and `payment_reminders` (F-40.229) each shipped a `wamid` **with no router arm**, and every receipt fell to the orphan line until someone noticed — **three times**. This is the first table to get both in one packet, and a cell asserts the pair.

The eighth home is **appended, never reordered**, and is the seventh's shape with one table name changed — because `0158` reuses `0148`'s receipt vocabulary rather than inventing a second dialect.

## 3 · The arm

Rides the **bride** line and `coupleContact` — the one home for her number. **Gated on its own register key per kind**, writes a dark row when shut. The row is written **before** the send, so a death mid-flight leaves something the boot reconciler can see: F-41.81's lesson applied at birth rather than after a walk found two rows stranded.

Both bodies take the trade as a **bare noun**, so F-41.80 cannot repeat. **No phone of the vendor's or the outsider's rides her body** — the outsider arm carries an Instagram handle, which is public.

## 4 · Two defects this packet nearly shipped

**The notice was first placed on `forwardToProspect`'s trailing return — which is unreachable.** Every branch of the try/catch above returns, so the outsider's *successful* send would silently never have told her. Moved into the success branch, and **the dead tail now throws** rather than returning a shape that looks fine and told no one.

**The arm first read `request.__item_category`** — a field invented on a row that does not have it. The item is passed now.

## 5 · Four cells moved, three of them counts

*"three gated sendWa sites"* and *"three `const out = await sendWaFn`"* — the writer legitimately has four arms. The second was **re-cut to a property rather than a bigger number**: every `sendWaFn` is captured and checked, no bare await. **Fourth counting-cell specimen this sitting.** A count says how many; it never says whether each one is bound.

*"a vendor with no users.phone → nothing sent"* and *"key ON → ONE send"* both **meant** one thing and **said** another — they were the same statement until the couple got her own notice on her own key and her own line. Scoped to their own templates, and the outsider one still asserts **exactly one** `assist_lead_outside`, which is the real guarantee.

## 6 · `found_vendor` is approved, because a reading moved it

The nightly sweep read Meta at `2026-09-09 02:27Z` — `APPROVED · UTILITY · id 3160852754105015`; F-41.114's re-filed button cleared review. The word moved **because a reading moved it**, which is the only thing that may, and the entry records which reading.

## 7 · The founder's steps

1. Apply, verify, push. 2. **Run `0158`.** 3. **Both bride keys stay off** — the arms write dark rows and send nothing until you flip them, and neither has been walked.

**Range F-41.105–F-41.107 unspent.**
