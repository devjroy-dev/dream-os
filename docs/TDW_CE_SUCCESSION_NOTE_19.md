# TDW — CE SUCCESSION NOTE 19

Banked 2026-08-03 by the nineteenth chair. Tips at banking: dream-os `f46bd75` · dreamos-pwa `6e6e805`. Your fetch derives the true state.

## §1 · SEATING

Fresh-clone both repos, fetch AND checkout. `npm ci` BEFORE `npm run build` on dream-os — BUILD-ENGINE-FIRST is inked law (CE-119); a floor against an absent dist is a claim about your container. Read this note whole, **§6 twice**. Then the protocol (§7, §9, §10 twice, §11 twice), the Block 08 spec whole, masterplan row 08, and FINDINGS_LOG CE-117/118 plus this tenure's entry, entry-whole.

Expect staleness. **Origin and the LE's record outrank this note wherever they differ.**

## §2 · THE FOUNDER'S STANDING RULINGS — THESE OUTRANK ANY DOC, INCLUDING THIS ONE

- **Sequencing, copy veto and pushes are absolute.** He runs every migration and every push.
- **A founder instruction outranks a CE ruling.** When the LE acts on his word outside a charter, the correct chair response is to rule it AUTHORIZED and file the drift — not to litigate the scope. This happened once this tenure (the nav-legibility packet) and the LE handled it correctly: it named the twin, moved both homes, declared the absence of a bench, and refused to fold a live vendor nav into a demo shell because nobody ruled that.
- **Copy is frozen AT THE BYTE, not the glyph (CE-117), and the veto is RECORDED BEFORE BUILD.** An inference that he would approve is not an approval. This tenure caught the LE writing "copy is founder-locked" when no word had come back. Hold that line; it is the whole content of the veto.
- **P2 (self-serve claim) is DEFERRED by founder word**, not by any ruling.
- **Destructive DB actions require an export first.** He asked to delete eleven demo vendors this tenure; the chair derived that `demo_leads_demo_vendor_id_fkey` is `ON DELETE CASCADE` and ten lead rows carrying real `bride_name`/`bride_phone` would have gone with them. He read the cost and ruled **「 if they are not wasting time then let them stay 」**. Nothing was deleted. **Name the cost; do not withhold the act.**

## §3 · THE FLOOR AT BANKING — MEASURED AT MY OWN HAND, NOT INHERITED

Run under the **F-07.46 INTERIM PROTOCOL**: clean tree before, strictly sequential, a kill means `git checkout -- .` first, counts read directly. Selftest flag: `--rig-selftest`.

**FIVE BENCHES ARE SIBLING-DEPENDENT AND EVERY ONE STATES BOTH SIDES.** NOTE_18 named four and published a lone-side number for the fifth without saying so; that cost this chair a finding. Do not repeat it.

| bench | PAIRED | lone |
|---|---|---|
| `b07_p1` | 75 | 71 |
| `b07_p5` | 136 | 114 |
| `b07_f0774_stripper` | 20/20 | 19/19 + 1 NAMED SKIP |
| `b08_p1_lifecycle` | 106 | 105 + 1 NAMED SKIP |
| `b08_p4_factory` | 83 | 83 (sibling-INDEPENDENT — stated so it is not assumed either way) |

**dream-os** — selftest **386/386** · sweep **101 scripts, EIGHT non-zero**, and they are exactly: `b05_f0555` 22/23 (F-07.11) · `b06_meter` 28/29 (F-06.41) · `b5b_movementb` rc=2 at `:225` (F-07.114) · `b07_f0772_circle_auth` 158/159 (F-08.13) · `b07_p4b_body` 75/76 (F-08.14) · plus **three credential-gated live rigs that are not benches** (`b06_gauntlet` bare rc=2, `b5_wa_door_smoke` rc=3, `test-shape` rc=1). Byte-stable: `b08_p3` 61 · `b07_p6` 29 · `b07_f0784` 59.

**dreamos-pwa** — `rm -rf .next` then `npx --no-install tsc --noEmit`, **TRUE EXIT ZERO**. **26 `.mjs` proofs, all rc=0.** `tdw08_p4_factory` 45 · `tdw08_p3_landing` 88 · `tdw07_p4b_body` 133 · `tdw07_f0760_claim` 82 · `tdw07_p1_discover` 43 · `tdw07_p6_fold` 68.

**SEVEN `scripts/*.proof.ts` runners exist, are NOT on this list, and were not run by me.**

Two of these numbers moved between the LE's last packet and this banking (`b08_p4_factory` 68 → 83, `tdw08_p4_factory` 36 → 45, post-cure). **Re-derive; do not inherit.**

## §4 · WHAT YOU INHERIT MID-FLIGHT

1. **P4 IS WALKED AND RULED. This packet is its seal.** Verify it at origin by your own command before describing P4 as sealed to anyone.
2. **THE NEXT SITTING IS UNCHOSEN AND IT IS THE FOUNDER'S.** On the board: **P5** (the Closer — demo block in the sales agent's dynamic context, relay batching verified against the new lifecycle, `demo_invite` sends moved onto the lifecycle) · **P6** (deletion queue executes, truth audit, acceptance sweep) · **the console sitting** (F-08.42 + the two invite predicates + the create form's unmarked required fields) · the two `middleware.ts` items · **F-08.35 after Block 08 closes**. This chair recommended the console sitting over P5 and did not get a ruling. Put it to him once; do not re-litigate it.
3. **BEFORE ANY P5 KICKOFF, DERIVE §5's STRATEGIC LINE YOURSELF.** Nine of twelve original demo rows carry no phone; the invite machine reaches three and today reaches none. P5 builds sending machinery on a population near zero. **That is a founder conversation, not a chair ruling.**

## §5 · THE OPEN LEDGER — LOADED

I inherited one described as empty and it was not the truth. This one is not empty.

**F-08.42** the admin `.fade-up` transform creates a containing block trapping **thirty-six `position:fixed` elements across forty-one surfaces** (toast, scrim, sheet), plus the React equal-value `setToast` bail · **F-08.44** `rate_display` is free text with **no gate** — typed money reaches the demo card and the public landing verbatim; `formatRs` governs money the estate computes and nothing governs money a human types · **F-08.43** heroless demo rows did not come through the console create path · **F-08.41** the IG ingestion contract does not exist; neither built nor struck, its missing pieces enumerated · **F-08.37** "25/day" is a per-run batch size, not a daily meter · **F-08.38** stale in-comment cites, one species, nine sites, corrected on contact only · **F-08.33** `PUBLIC_SCHEMA.md` eight migrations stale · **F-08.30** the fourth duplicate type · **F-08.32** `about` reaches the mirror unfiltered (celled) · **F-08.34** the constant wearing a column's clothes (celled) · **F-08.35** the couple cannot edit budget or functions after onboarding, chartered for its own sitting after Block 08 closes · **F-08.22** plus an undiagnosed 503 on `/admin`, filed as an observation, **not merged, needs a repro** · the two invite predicates and the create form's required fields (chartered with F-08.42) · `middleware.ts` swallows `/manifest.json` and `/sw.js` on the demodiscover host and `:47` still rewrites to a `/demo/not-found` that does not exist — one file, founder-sequenced · the two-homes nav drift, corrected together and deliberately NOT folded · P6 holds the orphaned Cloudinary assets · F-08.9 · F-08.11 · F-08.12 · F-08.19 · F-08.23 · F-08.2 · F-08.3.

**G-6 IS CLOSED** — iOS and Android, Instagram in-app browser, founder-witnessed 2026-08-03, the gold CTA holding at the bottom on both.

## §6 · KICKOFF DOCTRINE — AMENDED AT MY OWN EXPENSE

My predecessor wrote that clauses (b)–(f) of his §6 were each a mistake he made. **So are these.** I produced fifteen numbered corrections and four unnumbered class errors in one tenure, and the executor caught most of them.

**(a) A KICKOFF IS A DERIVATION.** Every `file:line` in it is run by YOU, at the tip you name, before it ships. I asserted `shapeDemoRow` at `:362` having run no command for it — worse than shape-reading, because nothing produced the number at all.

**(b) A GREP HIT IS NOT A DECLARATION.** I cited `demodiscover:26 DiscoverVendor`; the declaration is `:20` and it is named `DemoVendor`. `:26` was a field line inside it.

**(c) ARITHMETIC OVER A `sed` WINDOW IS NOT A NUMBERED READ.** I put `rate_display` at `:385` by counting from a table header. It is `:388`.

**(d) READ PAST YOUR OWN CITE.** I ruled a code-siting fork from `shapeVendor.js:12-19`. The paragraph refusing that ruling is `:29-33` — ten lines down, **in the file whose entire purpose is stopping a number from drifting**, and it exists because a previous sitting wrote it to prevent exactly what I ruled. The LE verified my citation and stopped at the same boundary. Both clauses of the INDEPENDENT-METHOD LAW were born here.

**(e) DERIVE FROM THE LADDER, NOT THE DOC.** I minted F-08.31 saying `demo_leads` had thirteen columns, read from `PUBLIC_SCHEMA.md`, in a ruling that had just re-derived that doc's staleness. `0106:69` adds a fourteenth.

**(f) A CENSUS OF ROWS IS NOT A MECHANISM.** I wrote *"no forbidden byte reaches any vendor- or couple-facing surface"* from twelve founder-pasted rows and put it in a charter as a derivation. There was no mechanism. F-08.32 exists because the LE said so.

**(g) DO NOT ORDER A CELL THAT ASSERTS A NEGATIVE.** I ordered *"no path re-validates an existing row."* The LE returned a mechanism instead — `_write` throws by name on `photos` — which is provable both ways by mutating the filter. **An absence-cell passes honestly while something one layer up breaks it. That is how G-6 broke.**

**(h) CE-115 CLAUSE 2 BROKE THREE TIMES IN ONE BLOCK.** A `ThemeProvider` above a tree; a `transform` on a wrapper trapping thirty-six fixed children; a computed `${endpoint}` a syntax census cannot see. **Ask what the ancestor DOES TO the descendant, not only what wraps it.** My own orphan grep returned zero for three live routes for this reason.

**(i) YOUR OWN TOOL CAN FAIL SILENTLY.** `grep -c $'\u2013'` returned 0 against a real en dash because the shell did not expand it. I nearly filed against a correct executor. **A check whose failure mode is a silent zero is not a check.**

**(j) DO NOT REPEAT A SPEC'S ASSUMPTION AS A DERIVATION.** I told the founder to test the Instagram in-app browser because a device matrix said so, without deriving that every system-sent link travels by WhatsApp. Then I patched it with a WhatsApp in-app browser that does not exist. **Two wrong answers in two messages, both from not opening the file.**

**(k) THE LE IS THE INTEGRITY MECHANISM, NOT A SUBORDINATE.** This one produced three §0.2 reports — two of which dissolved a chair ruling before a byte — five corrections to me, two self-corrections filed unprompted, three findings minted from counting nobody asked for, and **three sentences that became standing law**. Credit corrections louder than rulings. **An LE who stops before a byte on a defect he was not chartered to find is doing the job exactly.**

**(l) THE FOUNDER'S EYE IS AN INSTRUMENT AND THE BENCH IS NOT A SUBSTITUTE.** Five P3 defects were unfindable from any container: a tease that read as a list, a contact bar that read as a loading spinner then as an elongated hyphen, navy rooms, a tag-cloud chip strip with an orphaned widow row, and a 1400px slab on a link that travels by WhatsApp and lands on laptops. **Author walk cards that put a screen in front of him; do not treat a green floor as a walked sitting.**
