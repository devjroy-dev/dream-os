# TDW_09 · UX BLUEPRINT · STAGE 3 — THE FOUR FORK PAPERS — EXECUTOR HANDOVER

**Bases:** dreamos-pwa `e3210b5` · dream-os docs tree. **Under:** the founder's 「 go 」 + R-X17's stated order · F-1(c) (metric tables + two-screenshot smoke) · F-4(a) standing (mocks as self-contained HTML in `docs/mocks/`).
**This ZIP: two papers + four mocks + this handover. Zero code/pwa/copy bytes; W-1 held. Every proposed label in every mock is marked as riding the veto.**

## 1 · WHAT SHIPPED
`docs/specs/TDW_09_S3_INTERACTION_FORK.md` (the founder's verbatim questions → machinery derived → three models, rec: Model 1) · `docs/specs/TDW_09_S3_MUSE_MODES_BASELINE.md` (Muse cure rec arm (a) uniform-ratio grid · R-U17 tabs-vs-chip distinction · F-09.16 metric tables + ~2px computed delta at the worst site + the two-screenshot smoke) · `docs/mocks/tdw09_{interaction_models,muse_render,mode_controls,baseline_cure}.html` — each opens from the repo, names its one question and failing observation in its own header comment, current-vs-proposed side by side, sources cited by path.

## 2 · FINDINGS MINTED — F-09.24 · F-09.25 (the chair enters)
**.24** `fmtINR`'s ₹ reaches VENDORS through the Hub's Your-Books sheet (`Cabinet.tsx:63`/`:65`, home `lib/vendor/cabinet.ts`) — the 13th money home, missed by F-09.6's twelve-formatter census; register law on a live vendor surface. **.25** the Cabinet skin persists via `localStorage` (`:277`/`:303`) against the house guardrail — while `VictorModeChip.tsx:7` obeys it by name two components away. Both cures fold naturally: .24 into the money consolidation (Paper 3 §4), .25 into whichever interaction model rules (Models 1/3 re-home the switcher; Model 2 deletes it). **Next free: F-09.26.**

## 3 · DISCLOSURES
**D-1 ·** Font metrics are `@capsizecss/metrics`' published tables (derived by command from the allowlisted npm registry), not the rendered fonts — the container cannot fetch them (adjudicated env limit). The computed deltas are a model; the founder's two screenshots are the settling witness, which is F-1(c)'s own design. **D-2 ·** The mocks use system font fallbacks for the same reason and say so in their headers; they demonstrate geometry and mechanism, not final dress. **D-3 ·** Model recommendations are single recommendations with the runner-up's trigger named (Model 2 if skins prove unused; Muse arm (b) if the founder wants the ragged look) — the fork is the founder's, not mine. **D-4 ·** The Muse reflow simulation uses synthetic delayed loads; the mechanism (no reserved box → shift) is source-derived and the simulation only makes it visible.

## 4 · FLOOR
Both papers + four mocks are docs-only; dream-os build unaffected (exit 0 stands at this tree). Mocks hand-opened and visually checked in this container's renderer-less limit by structure review only — stated, not overclaimed; they are static HTML with no external fetches and no storage APIs.

## 5 · WHAT IS NOT DONE / GATES
The founder's rulings: interaction model (1/2/3) · Muse arm (a/b) · mode-control labels (veto) · the baseline cure's adoption ride (canon Row primitive → the Opus landing cure). His two-screenshot smoke — before the baseline cure is ruled, per F-1(c). **Next in the stated order: the theme strategy paper (AD-X2), then the prioritized cure queue.**

Sequencing beyond this sitting is the founder's.
