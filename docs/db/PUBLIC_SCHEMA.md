# docs/db/PUBLIC_SCHEMA.md — the `public` schema, WITNESSED PROD SNAPSHOT

**Snapshot taken:** 2026-07-16, founder-run in the Supabase SQL editor, output handed back verbatim. **57 tables, 641 columns.**
**Project / role:** `nvzkbagqxbysoeszxent` / `main` (PRODUCTION), role `postgres` — **the same editor session witnessed in the `0076` `information_schema` proof earlier the same day. The executor did not see this run's editor chrome and names that rather than assert it.**
**Generated by:** `db/queries/public_schema_dump.sql`. **Regenerate on demand by re-running that file.**
**NEVER HAND-EDIT.** A hand-edited snapshot is prose again, and prose is what this file exists to kill.

**THE GUARD PASSED.** The dump's self-computing `tables_expected` read **57**; the result carried **57** rows. Equal ⇒ the editor's row cap did not truncate this snapshot (F-04.29's disease, made self-detecting — the engine twin hardcodes "confirm 25 rows"; this one cannot, because public's table count is the fact it exists to establish).

**WHY THIS FILE EXISTS — F-04.57.** `ENGINE_SCHEMA.md` covers the `engine` plane only. Until this file, the `public` plane — which owns `vendors`, `events`, `leads`, `invoices` — had **no witnessed column list anywhere**: `BASELINE.md` carries counts without names, and `SCHEMA.md`'s ladder header is stale at 0064. The masterplan's *"blind-SQL class dead structurally"* was **half-true**, and that half is how `0076` came to be written against `public.vendors` with no witnessed list. *Founder-run SQL is written ONLY against witnessed column lists — never against prose.* This is the other half.

**⚠ SCOPE — WHAT THIS FILE DOES *NOT* CARRY, NAMED SO THE SILENCE IS NOT MISREAD.** `information_schema.columns` yields column **name, type, nullability and default** — and nothing else. **CHECK constraints, indexes, foreign keys, triggers and RLS policies are ABSENT from this snapshot.** So `events.kind`'s thirteen-value CHECK, `events.slot`'s CHECK, and `0075`'s UNIQUE partial index on `(vendor_id, event_date) where kind='blocked'` are **real and invisible here.** This file answers *"what columns exist, of what type"* — the question that made `vendor_activity_log.detail` and `agent_snapshot.rebuilt_at` into founder-run SQL. It does not answer *"what values are legal."* **BASE TABLEs only; views are excluded, exactly as the engine twin excludes them.**

**Ordinal gaps are not errors.** A hole is a dropped column's fingerprint — e.g. `vendors` runs 1–34 then jumps to 43 (eight dropped columns), and `events` skips 13.

---

## public.admin_activity_log  ·  7 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. admin_email text NOT NULL
3. action text NOT NULL
4. target_type text
5. target_id uuid
6. metadata jsonb NOT NULL default '{}'::jsonb
7. created_at timestamp with time zone NOT NULL default now()
```

## public.admin_config  ·  4 columns

```
1. key text NOT NULL
2. value text NOT NULL
3. description text
4. updated_at timestamp with time zone NOT NULL default now()
```

## public.bride_pages  ·  9 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. couple_id uuid NOT NULL
3. user_id uuid NOT NULL
4. entry_date date NOT NULL default CURRENT_DATE
5. mood text NOT NULL
6. mood_color text NOT NULL
7. body text NOT NULL
8. created_at timestamp with time zone NOT NULL default now()
9. updated_at timestamp with time zone NOT NULL default now()
```

## public.circle_activity  ·  13 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. couple_id uuid NOT NULL
3. actor_user_id uuid
4. actor_name text NOT NULL
5. actor_role text NOT NULL
6. activity_type text NOT NULL
7. subject_type text
8. subject_id uuid
9. payload jsonb NOT NULL default '{}'::jsonb
10. surfaced_to_bride boolean NOT NULL default false
11. surfaced_at timestamp with time zone
12. created_at timestamp with time zone NOT NULL default now()
13. session_id uuid
```

## public.circle_members  ·  13 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. couple_id uuid NOT NULL
3. invitee_name text NOT NULL
4. invitee_phone text
5. role text NOT NULL
6. invite_token text NOT NULL
7. status text NOT NULL default 'pending'::text
8. sticky_couple_until timestamp with time zone
9. invited_at timestamp with time zone NOT NULL default now()
10. joined_at timestamp with time zone
11. created_at timestamp with time zone NOT NULL default now()
12. updated_at timestamp with time zone NOT NULL default now()
13. expires_at timestamp with time zone
```

## public.circle_sessions  ·  10 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. couple_id uuid NOT NULL
3. circle_member_id uuid NOT NULL
4. started_at timestamp with time zone NOT NULL default now()
5. last_activity_at timestamp with time zone NOT NULL default now()
6. summarized_to_bride boolean NOT NULL default false
7. summarized_at timestamp with time zone
8. summary_message_id uuid
9. created_at timestamp with time zone NOT NULL default now()
10. updated_at timestamp with time zone NOT NULL default now()
```

## public.clients  ·  12 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. user_id uuid
4. name text NOT NULL
5. phone text
6. email text
7. source text NOT NULL default 'lead_promotion'::text
8. referrer_name text
9. notes text
10. created_at timestamp with time zone NOT NULL default now()
11. updated_at timestamp with time zone NOT NULL default now()
13. deleted_at timestamp with time zone
```

## public.collab_posts  ·  14 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. requirement_type text NOT NULL
4. event_date date NOT NULL
5. city text NOT NULL
6. open_to_other_cities boolean NOT NULL default false
7. budget_inr integer
8. payment_period text
9. event_type text
10. details text
11. state text NOT NULL default 'open'::text
12. expires_at timestamp with time zone NOT NULL default (now() + '30 days'::interval)
13. created_at timestamp with time zone NOT NULL default now()
14. updated_at timestamp with time zone NOT NULL default now()
```

## public.collab_responses  ·  8 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. post_id uuid NOT NULL
3. responder_vendor_id uuid NOT NULL
4. state text NOT NULL default 'interested'::text
5. poster_notified_at timestamp with time zone
6. contact_shared_at timestamp with time zone
7. created_at timestamp with time zone NOT NULL default now()
8. updated_at timestamp with time zone NOT NULL default now()
```

## public.contracts  ·  15 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. client_id uuid
4. lead_id uuid
5. invoice_id uuid
6. title text NOT NULL
7. storage_path text
8. file_size integer
9. mime_type text default 'application/pdf'::text
10. notes text
11. state text NOT NULL default 'draft'::text
12. sent_at timestamp with time zone
13. signed_at timestamp with time zone
14. created_at timestamp with time zone NOT NULL default now()
15. updated_at timestamp with time zone NOT NULL default now()
```

## public.conversations  ·  11 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. vendor_id uuid
3. counterparty_user_id uuid
4. counterparty_phone text
5. kind text NOT NULL
6. state text NOT NULL default 'new'::text
7. mode text NOT NULL default 'draft'::text
8. last_message_at timestamp with time zone
9. created_at timestamp with time zone NOT NULL default now()
10. updated_at timestamp with time zone NOT NULL default now()
11. couple_id uuid
```

## public.couple_bookings  ·  14 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. couple_id uuid NOT NULL
3. vendor_name text NOT NULL
4. vendor_id uuid
5. category text NOT NULL
6. amount_total integer
7. amount_advance integer
8. amount_paid integer NOT NULL default 0
9. balance_due_date date
10. state text NOT NULL default 'booked'::text
11. notes text
12. created_at timestamp with time zone NOT NULL default now()
13. updated_at timestamp with time zone NOT NULL default now()
14. contact_phone text
```

## public.couple_enquiries  ·  9 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. couple_id uuid NOT NULL
3. vendor_id uuid NOT NULL
4. vendor_name text
5. vendor_category text
6. vendor_city text
7. routing_handle text
8. vendor_lead_id uuid
9. created_at timestamp with time zone NOT NULL default now()
```

## public.couple_receipts  ·  11 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. couple_id uuid NOT NULL
3. booking_id uuid
4. amount integer
5. vendor_name text
6. description text
7. receipt_date date
8. image_url text
9. tags ARRAY NOT NULL default ARRAY[]::text[]
10. created_at timestamp with time zone NOT NULL default now()
11. label text
```

## public.couple_state  ·  5 columns

```
1. couple_id uuid NOT NULL
2. summary text
3. vendor_shortlist jsonb NOT NULL default '[]'::jsonb
4. taste_notes text
5. updated_at timestamp with time zone NOT NULL default now()
```

## public.couple_tasks  ·  9 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. couple_id uuid NOT NULL
3. title text NOT NULL
4. status text NOT NULL default 'pending'::text
6. due_date date
7. event_name text
8. notes text
9. created_at timestamp with time zone NOT NULL default now()
10. updated_at timestamp with time zone NOT NULL default now()
```

## public.couples  ·  21 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. user_id uuid NOT NULL
3. partner_name text
4. wedding_date date
5. wedding_city text
6. budget_total integer
7. events_planned jsonb
8. planning_state text default 'browsing'::text
9. created_at timestamp with time zone NOT NULL default now()
10. updated_at timestamp with time zone NOT NULL default now()
11. onboarding_state text default 'new'::text
12. nudge_sent_at timestamp with time zone
13. pin_hash text
14. pin_failed_attempts integer NOT NULL default 0
15. pin_locked_until timestamp with time zone
16. taste_quiz_done boolean NOT NULL default false
17. aesthetic_tags jsonb NOT NULL default '[]'::jsonb
18. tier text NOT NULL default 'basic'::text
19. function_count integer
20. wedding_days integer
21. functions text
```

## public.couture_appointments  ·  13 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. couple_id uuid
4. appointment_at timestamp with time zone NOT NULL
5. duration_minutes integer NOT NULL default 60
6. fee_inr integer NOT NULL
7. state text NOT NULL default 'booked'::text
8. razorpay_order_id text
9. paid_at timestamp with time zone
10. vendor_payout_inr integer
11. notes text
12. created_at timestamp with time zone NOT NULL default now()
13. updated_at timestamp with time zone NOT NULL default now()
```

## public.couture_availability  ·  8 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. slot_at timestamp with time zone NOT NULL
4. duration_minutes integer NOT NULL default 60
5. fee_inr integer NOT NULL
6. state text NOT NULL default 'open'::text
7. booked_by_appointment_id uuid
8. created_at timestamp with time zone NOT NULL default now()
```

## public.demo_claim_requests  ·  7 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. ig_handle text NOT NULL
3. vendor_name text
4. phone text NOT NULL
5. claimed_at timestamp with time zone NOT NULL default now()
6. contacted boolean NOT NULL default false
7. notes text
```

## public.demo_leads  ·  13 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. demo_vendor_id uuid NOT NULL
3. demo_vendor_handle text NOT NULL
4. bride_name text NOT NULL
5. bride_phone text NOT NULL
6. bride_ig_handle text
7. bride_email text
8. bride_wedding_date date
9. bride_wedding_city text
10. otp_verified boolean NOT NULL default false
11. notified_vendor boolean NOT NULL default false
12. admin_notified boolean NOT NULL default false
13. created_at timestamp with time zone NOT NULL default now()
```

## public.demo_vendors  ·  14 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. ig_handle text NOT NULL
3. display_name text NOT NULL
4. category text NOT NULL
5. city text NOT NULL
6. whatsapp_phone text
7. about text
8. rate_display text
9. photos jsonb NOT NULL default '[]'::jsonb
10. active boolean NOT NULL default true
11. created_at timestamp with time zone NOT NULL default now()
12. created_by text NOT NULL default 'admin'::text
13. discover_eligible boolean NOT NULL default false
14. discover_eligible_at timestamp with time zone
```

## public.discover_heroes  ·  8 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. image_url text NOT NULL
3. cloudinary_public_id text
4. caption text
5. display_order integer NOT NULL default 0
6. active boolean NOT NULL default true
7. created_at timestamp with time zone NOT NULL default now()
8. updated_at timestamp with time zone NOT NULL default now()
```

## public.enquiry_taps  ·  4 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. handle text NOT NULL
3. source text
4. tapped_at timestamp with time zone NOT NULL default now()
```

## public.events  ·  16 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. vendor_id uuid
3. title text NOT NULL
4. event_date date NOT NULL
5. event_time time without time zone
6. kind text NOT NULL
7. linked_lead_id uuid
8. state text NOT NULL default 'upcoming'::text
9. notes text
10. created_at timestamp with time zone NOT NULL default now()
11. updated_at timestamp with time zone NOT NULL default now()
12. couple_id uuid
14. deleted_at timestamp with time zone
15. linked_binder_id uuid
16. slot text
17. ready_by date
```

## public.expenses  ·  12 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. vendor_id uuid NOT NULL
3. amount integer NOT NULL
4. category text NOT NULL
5. description text
6. expense_date date default CURRENT_DATE
7. client_name text
8. linked_lead_id uuid
9. notes text
10. created_at timestamp with time zone NOT NULL default now()
11. updated_at timestamp with time zone NOT NULL default now()
13. deleted_at timestamp with time zone
```

## public.exploring_photos  ·  8 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. image_url text NOT NULL
3. cloudinary_public_id text
4. caption text
5. display_order integer NOT NULL default 0
6. active boolean NOT NULL default true
7. created_at timestamp with time zone NOT NULL default now()
8. updated_at timestamp with time zone NOT NULL default now()
```

## public.hot_dates  ·  8 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. date date NOT NULL
3. note text
4. region text default 'All India'::text
5. created_at timestamp with time zone NOT NULL default now()
6. label text
7. intensity text NOT NULL default 'high'::text
8. active boolean NOT NULL default true
```

## public.image_throttle_log  ·  5 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. phone text NOT NULL
3. engine text NOT NULL
4. created_at timestamp with time zone NOT NULL default now()
5. rejection_sent boolean NOT NULL default false
```

## public.invite_codes  ·  9 columns

```
1. code text NOT NULL
2. kind text NOT NULL
3. tier text
4. notes text
5. created_at timestamp with time zone NOT NULL default now()
6. created_by text
7. consumed_at timestamp with time zone
8. consumed_by_user_id uuid
9. intended_phone text
```

## public.invoices  ·  21 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. vendor_id uuid NOT NULL
3. lead_id uuid
4. invoice_number text NOT NULL
5. client_name text NOT NULL
6. client_phone text
7. description text
8. amount_total integer NOT NULL
9. amount_advance integer
10. amount_paid integer NOT NULL default 0
11. due_date date
12. state text NOT NULL default 'unpaid'::text
13. pdf_url text
14. notes text
15. created_at timestamp with time zone NOT NULL default now()
16. updated_at timestamp with time zone NOT NULL default now()
17. client_id uuid
19. last_payment_at timestamp with time zone
20. deleted_at timestamp with time zone
21. has_schedule boolean NOT NULL default false
22. binder_id uuid
```

## public.landing_slides  ·  8 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. image_url text NOT NULL
3. cloudinary_public_id text
4. caption text
5. display_order integer NOT NULL default 0
6. active boolean NOT NULL default true
7. created_at timestamp with time zone NOT NULL default now()
8. updated_at timestamp with time zone NOT NULL default now()
```

## public.leads  ·  27 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. vendor_id uuid NOT NULL
3. name text
4. phone text
5. email text
6. wedding_date date
7. wedding_city text
8. event_types ARRAY
9. budget_min integer
10. budget_max integer
11. source text default 'whatsapp'::text
12. referrer_name text
13. state text NOT NULL default 'new'::text
14. raw_message text
15. notes text
16. created_at timestamp with time zone NOT NULL default now()
17. updated_at timestamp with time zone NOT NULL default now()
18. client_id uuid
20. deleted_at timestamp with time zone
21. vendor_summary text
22. intent_summary text
23. intent_summary_at timestamp with time zone
24. wedding_date_precision text
25. function_count integer
26. wedding_days integer
27. functions text
28. draft_meta jsonb
```

## public.messages  ·  17 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. conversation_id uuid NOT NULL
3. direction text NOT NULL
4. channel text NOT NULL
5. body text
6. media_url text
7. sent_by text NOT NULL
8. tool_calls jsonb
9. tool_results jsonb
10. twilio_sid text
11. created_at timestamp with time zone NOT NULL default now()
12. delivery_status text
13. model text
14. input_tokens integer
15. output_tokens integer
16. cost_usd numeric
17. cost_inr numeric
```

## public.muse_pool  ·  8 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. image_url text NOT NULL
3. cloudinary_public_id text
4. caption text
5. aesthetic_tags jsonb NOT NULL default '[]'::jsonb
6. active boolean NOT NULL default true
7. sort_order integer NOT NULL default 0
8. created_at timestamp with time zone NOT NULL default now()
```

## public.muse_saves  ·  16 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. couple_id uuid NOT NULL
3. save_number integer NOT NULL
4. source_type text NOT NULL
5. source_url text
6. image_url text
7. vendor_id uuid
8. caption text
9. aesthetic_tags jsonb NOT NULL default '[]'::jsonb
10. vision_raw jsonb
11. saved_by_user_id uuid NOT NULL
12. saved_by_role text NOT NULL
13. created_at timestamp with time zone NOT NULL default now()
14. updated_at timestamp with time zone NOT NULL default now()
15. circle_comment_count integer NOT NULL default 0
16. surface text NOT NULL default 'muse'::text
```

## public.notes  ·  7 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. vendor_id uuid
3. conversation_id uuid
4. content text NOT NULL
5. tags ARRAY
6. created_at timestamp with time zone NOT NULL default now()
7. couple_id uuid
```

## public.otp_sessions  ·  5 columns

```
1. phone text NOT NULL
2. otp_hash text NOT NULL
3. purpose text NOT NULL
4. expires_at timestamp with time zone NOT NULL
5. created_at timestamp with time zone NOT NULL default now()
```

## public.owner_notes  ·  5 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. body text NOT NULL
4. binder_id uuid
5. created_at timestamp with time zone NOT NULL default now()
```

## public.payment_schedules  ·  13 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. invoice_id uuid NOT NULL
3. vendor_id uuid NOT NULL
4. milestone_label text NOT NULL
5. pct numeric NOT NULL
6. amount_due integer NOT NULL
7. due_date date
8. state text NOT NULL default 'pending'::text
9. paid_at timestamp with time zone
10. paid_amount integer
11. ordinal integer NOT NULL
12. created_at timestamp with time zone NOT NULL default now()
13. updated_at timestamp with time zone NOT NULL default now()
```

## public.pending_event_proposals  ·  8 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. proposals jsonb NOT NULL
4. source_image_url text
5. caption text
6. created_at timestamp with time zone NOT NULL default now()
7. resolved_at timestamp with time zone
8. resolution text
```

## public.pending_lead_pings  ·  9 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. lead_id uuid NOT NULL
4. lead_name text
5. bride_message text
6. intent_summary text
7. source text NOT NULL
8. created_at timestamp with time zone NOT NULL default now()
9. acknowledged_at timestamp with time zone
```

## public.spotlight  ·  9 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid
3. image_url text NOT NULL
4. cloudinary_public_id text
5. caption text
6. week_label text
7. active boolean NOT NULL default true
8. sort_order integer NOT NULL default 0
9. created_at timestamp with time zone NOT NULL default now()
```

## public.taste_quiz_images  ·  8 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. image_url text NOT NULL
3. cloudinary_public_id text
4. caption text
5. aesthetic_tags jsonb NOT NULL default '[]'::jsonb
6. active boolean NOT NULL default true
7. sort_order integer NOT NULL default 0
8. created_at timestamp with time zone NOT NULL default now()
```

## public.tds_ledger  ·  18 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. invoice_id uuid
4. client_id uuid
5. client_name text NOT NULL
6. client_pan text
7. client_tan text
8. gross_amount integer NOT NULL
9. tds_rate numeric NOT NULL
10. tds_amount integer NOT NULL
11. net_received integer NOT NULL
12. section text
13. deduction_date date NOT NULL
14. financial_year text NOT NULL
15. certificate_no text
16. notes text
17. created_at timestamp with time zone NOT NULL default now()
18. updated_at timestamp with time zone NOT NULL default now()
```

## public.team_members  ·  11 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. name text NOT NULL
4. role text
5. phone text
6. daily_rate_inr integer
7. notes text
8. active boolean NOT NULL default true
9. deleted_at timestamp with time zone
10. created_at timestamp with time zone NOT NULL default now()
11. updated_at timestamp with time zone NOT NULL default now()
```

## public.team_messages  ·  7 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. body text NOT NULL
4. pinned boolean NOT NULL default false
5. sent_to_count integer
6. linked_event_id uuid
7. created_at timestamp with time zone NOT NULL default now()
```

## public.team_payments  ·  13 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. team_member_id uuid NOT NULL
4. linked_event_id uuid
5. linked_task_id uuid
6. description text
7. amount_inr integer NOT NULL
8. state text NOT NULL default 'owed'::text
9. paid_at timestamp with time zone
10. paid_via text
11. notes text
12. created_at timestamp with time zone NOT NULL default now()
13. updated_at timestamp with time zone NOT NULL default now()
```

## public.team_tasks  ·  13 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. assigned_to_member_id uuid
4. linked_event_id uuid
5. title text NOT NULL
6. description text
7. due_date date
8. priority text NOT NULL default 'normal'::text
9. state text NOT NULL default 'open'::text
10. completed_at timestamp with time zone
11. deleted_at timestamp with time zone
12. created_at timestamp with time zone NOT NULL default now()
13. updated_at timestamp with time zone NOT NULL default now()
```

## public.users  ·  9 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. phone text NOT NULL
3. name text
4. created_at timestamp with time zone NOT NULL default now()
5. updated_at timestamp with time zone NOT NULL default now()
6. email text
7. pending_routing_context jsonb
8. pronouns text
9. auth_user_id uuid
```

## public.vendor_activity_log  ·  8 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. surface text NOT NULL
4. action text NOT NULL
5. summary text NOT NULL
6. entity_type text
7. entity_id uuid
8. created_at timestamp with time zone NOT NULL default now()
```

## public.vendor_discover_requests  ·  7 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. state text NOT NULL
4. reason text
5. decided_by_admin text
6. decided_at timestamp with time zone
7. created_at timestamp with time zone NOT NULL default now()
```

## public.vendor_featured_submissions  ·  18 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. slot_kind text NOT NULL
4. hero_image_id uuid
5. caption text
6. proposed_start_date date
7. proposed_end_date date
8. fee_inr integer NOT NULL
9. razorpay_order_id text
10. paid_at timestamp with time zone
11. state text NOT NULL default 'submitted'::text
12. scheduled_start timestamp with time zone
13. scheduled_end timestamp with time zone
14. rejection_reason text
15. decided_by_admin text
16. decided_at timestamp with time zone
17. created_at timestamp with time zone NOT NULL default now()
18. updated_at timestamp with time zone NOT NULL default now()
```

## public.vendor_portfolio  ·  13 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. vendor_id uuid NOT NULL
3. image_url text NOT NULL
4. caption text
5. is_hero boolean NOT NULL default false
6. in_carousel boolean NOT NULL default true
7. aesthetic_tags jsonb NOT NULL default '[]'::jsonb
8. approval_state text NOT NULL default 'pending'::text
10. created_at timestamp with time zone NOT NULL default now()
11. updated_at timestamp with time zone NOT NULL default now()
12. reviewed_by_admin text
13. reviewed_at timestamp with time zone
14. rejection_reason text
```

## public.vendor_state  ·  7 columns

```
1. vendor_id uuid NOT NULL
2. summary text
3. pricing_policy jsonb default '{}'::jsonb
4. recent_notes jsonb default '[]'::jsonb
5. open_threads integer default 0
6. pending_actions integer default 0
7. updated_at timestamp with time zone NOT NULL default now()
```

## public.vendors  ·  37 columns

```
1. id uuid NOT NULL default uuid_generate_v4()
2. user_id uuid NOT NULL
3. business_name text
4. category text
5. vertical text NOT NULL default 'wedding'::text
6. city text
7. upi_id text
8. gstin text
9. status text NOT NULL default 'active'::text
10. tier text NOT NULL default 'trial'::text
11. founding_cohort boolean NOT NULL default false
12. created_at timestamp with time zone NOT NULL default now()
13. updated_at timestamp with time zone NOT NULL default now()
14. onboarding_state text
15. routing_handle text
16. instagram_handle text
17. open_to_travel boolean default false
18. travel_notes text
19. briefing_enabled boolean NOT NULL default true
20. invoice_prefix text
21. invoice_counter integer NOT NULL default 0
22. style_notes text
23. pin_hash text
24. pin_failed_attempts integer NOT NULL default 0
25. pin_locked_until timestamp with time zone
26. aesthetic_tags jsonb NOT NULL default '[]'::jsonb
27. rate_min integer
28. rate_max integer
29. discover_preview boolean NOT NULL default false
30. discover_eligible boolean NOT NULL default false
31. discover_request_state text
32. couture_eligible boolean NOT NULL default false
33. featured_eligible boolean NOT NULL default false
34. about text
43. base_fee_min integer
44. base_fee_max integer
45. slot_capacity integer
```

## public.waitlist_signups  ·  9 columns

```
1. id uuid NOT NULL default gen_random_uuid()
2. kind text NOT NULL
3. name text NOT NULL
4. phone text NOT NULL
5. instagram_handle text NOT NULL
6. status text NOT NULL default 'new'::text
7. notes text
8. created_at timestamp with time zone NOT NULL default now()
9. updated_at timestamp with time zone NOT NULL default now()
```
