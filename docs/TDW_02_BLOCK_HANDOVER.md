# TDW_02 BLOCK HANDOVER — the Engine block, closed 2026-07-14

Seven phases proven on production under CE-22 discipline. Amendments One + Two and all
in-thread rulings (CE-1..CE-23, P7 gate) executed. TDW_03 inherits:

## Exports for TDW_03's read-first table
- **The wire contract** — SCHEMA.md appendix: lead `draft` wishbone (single-primer-per-row,
  03 may amend per-field), records `missing_cells` (+POST-verb draft), turn `view`
  (= READ-TIME snapshot, not end-state — G4's lesson), P6 `operator_action`
  {summary, record_ref, undo?} (writes/errors only, P7-b), P5 `meta`.
- **recordCompleteness.js** — 03 is the NAMED AMENDER (CE-16).
- **Merge preview** — F4 fold: precise-truth-wins falls out of fill-blanks-only;
  couple-name drift documented (CE-19c). **F7** corrections affordance: chat-path
  corrections to filled typed fields have no door BY DESIGN (G7 materialized it —
  binder note landed, typed column untouched); wishbone/PATCH is the correction surface.
- **Manifest cabinet engine** — BUILT, UNMOUNTED; inline shape canonical (D6).

## The P7 verdict + routing (founder-ruled)
DeepSeek PASSED the gauntlet GLM failed. Matrix: signature = Victor haiku / Donna
deepseek (role-split shipped) · harvest = deepseek (F11 retired GLM) · trial anthropic ·
prestige anthropic. Cost: DeepSeek implicit prefix-cache proven (48.8k first turn ->
3.4-10.9k after; ₹1.09-1.85/turn Haiku-priced-OVER-stated) vs anthropic ₹0.49-9.59.
Provisionality: binding for the current prompt era; 06's lens re-bench is
re-confirmation, not re-litigation.

## Open items with owners
- **Block 06**: Donna cache debt (₹9.59 anthropic ceiling), F6 in-flight lock, F9
  invented-date lens (repair still pending the real Ruchi shoot date — record holds a
  fabricated 2025-04-01 actively confusing turns), F10 probe-style divergence
  (Donna-on-deepseek improvises availability probes), snapshot labeling nuance,
  **usage-schema limitation: Donna's model never hits the ledger** (one model column
  per turn = Victor's) — 06's telemetry item.
- **F11 date-shape residue**: the letters-guard catches junk strings; a PLAUSIBLE
  fabricated date passes format guards — harvest prompt discipline + chips are the
  countermeasure (06 + product).
- **24h provider_downgrade glance** owed post-flip. **F9 writes-read** still owed.
- Cross-plane state echo: Meera's binder 'booked' vs lead 'new' (03/06 affordance).

## The ledger
FINDINGS_LOG.md: P5-a, F1-F11, corrections. UNIT_ECONOMICS.md: baselines + both bench
verdicts. Protocol: two proof-evidence laws promoted (CE verbatim).
