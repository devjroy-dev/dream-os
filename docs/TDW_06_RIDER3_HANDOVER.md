# TDW_06 · RIDER ZIP 3 HANDOVER — THE SENTENCES MATCH THE STORE

**Base tip: `dc7d8bf`, re-derived fetch-first on a fresh clone. Six files.**
**Zero pwa bytes. Zero migrations. One founder-struck phrase executed; no new copy.**

Cures **F-06.183 · .184 · .185 · .186** — all four found by walk ten, two of them
by the founder reading his own handset.

---

## §1 · WHAT SHIPPED

**F-06.183 — THE WITNESS IS CLASS-SCOPED (§0.2 into `chat.js`, granted).**
One line: the census feeding the `witnessed_hand` limb is now filtered through
`isDeedOfClass(h.name, deedClass)`. The class machinery existed at **both** ends
of this file — `deedClass` computed at the verdict site, `RELAY_DEED_RE` tested
in the predicate — and nothing joined them. A **lead write acquitted a claim
about a WhatsApp message**, on production, at 13:32:30, and the estate's own eval
row records it. Vocabularies untouched. **The filter can only shrink the
acquitting set**, so this can convict where the estate passed and can never pass
where it convicted — the cheap direction, deliberately.

The same join fixes the other classes: a filed lead can no longer acquit an
unblock either, which is the confusion `isDeedOfClass`'s own comment describes.

**F-06.184 — THE DOOR'S INDEPENDENT SECOND LINE.** A5 shipped gated on the wire
guard arming. Walk ten proved that is not the chair's ruling: the guard passed,
so nothing armed, so the founder read 「 Done. Message is out 」 directly above
the door's own 「 I haven't sent anything. 」 The door now tests `RELAY_CLAIM_RE`
on the model's prose **itself** — it already holds a store-derived relay outcome,
and the predicate is pure. **This layer depends on no ladder verdict and would
have prevented that screen with .183 unfixed.** Both layers cell independently,
each proven to fire with the other mutated dead.

**F-06.185 — THE STRIKE, EXECUTED; RETIRED-COPY-LEAVES-THE-TREE.** Two survivors
cured: ①'s `showBlock` now opens 「 Here is the draft: 」, and
`doorbellLine_RETIRED_v1` — a founder-**retired** byte still carrying the
founder-**struck** phrase — is gone from the tree, chair-verified reader-less
first. `templates.js:92` is a comment and out of strike scope, recorded not
cured. **The execution census ships as a cell**, so a ruled copy act can never
again half-execute.

**F-06.186 — A NUMBER IS NOT A NAME.** `recipientLabel` renders the nameless form
when the name's digits are the recipient's phone. Digit comparison with
suffix-matching in both directions (F-06.154: the column is not normalized), and
**a name containing any letter is always a name** so 「 Priya (+91…) 」 is never
swallowed. Render-only: the row is not rewritten, because a display fix that
silently edits data is two changes wearing one name.

---

## §2 · PROOF

| bench | result |
|---|---|
| **`b06_bride_arrival_bench`** | **89/89** (71 + A10's 6 + A11's 5 + A12's 7) · **77/89** uncured — all 12 reds in the new blocks |
| `b06_relay_hand_bench` | 126/126 (2 labelled amendments) |
| `b06_forkc_wireguard_bench` | 113/113 (4 labelled amendments) |
| foundations · f0613 | 42/42 · 40/40 |
| tier · billing · micro · selfserve · combined_cap | 81 · 52 · 23 · 30 · 37 |
| meter · f0555 · f0772 | 28/29 · 22/23 · 158/159 — chartered elder reds |
| `npm run build` (tsc 5.9.3 via `npm ci`) | **EXIT 0** |

**A10.1 is the named red specimen, verbatim:** walk ten's exact bytes tested
against the shipped `RELAY_CLAIM_RE`. **A11.5 is defence-in-depth asserted rather
than asserted-about:** the guard layer is mutated dead and the independent layer
must still be present.

---

## §3 · LABELLED AMENDMENTS — RATIFY-OR-REVERT, COUNTS PRESERVED

8. **`relay_hand` §2.2** — asserted the frame *declares itself* verbatim in words.
   The founder struck that phrase; the cell was pinning an un-executed ruling in
   place. Property (delimited artefact, not speech) untouched, and the strike's
   own enforcement now sits beside it.
9. **`relay_hand` §7.7** — the vetoed-byte needle carries the executed strike.
10. **`forkc` §5.8d** — 4→5 `replyText` writers, re-authored stricter: writer 5
    must carry its flag on the next statement, so an unguarded fifth still reds.
11–13. **`forkc` §6.11, §11.6, §14.10** — **comments are not code.** These three
    asserted vocabulary-**negatives** over the raw file, so this rider's own
    decision comments tripped them: 「 WhatsApp message 」 read as a seat branch,
    「 defence in depth 」 read as a retry counter. The estate promoted this exact
    law after five cells in one sitting read a comment as code; these three had
    never been brought under it. Each property untouched — only the text each
    reads is now the code.

**Running total for this sitting: thirteen.**

---

## §4 · F-06.186 LAYER ② — THE WRITER CENSUS, METHOD AND BLIND SPOT DECLARED

**Method:** every `insert`/`update` naming `leads.name` under `src/`, each read
for whether a phone can reach that column. **Blind spot:** SQL run by hand, and
any writer that assembles the field indirectly.

| site | verdict |
|---|---|
| `src/agent/engine.js:857` | `name: input.name \|\| null` — passes through |
| `src/lib/vendor/leads.js:53` | `name: name \|\| null` — passes through |
| `src/engine/src/core/tools/donnaLead.ts:157` | body passed through |
| `src/agent/engine.js:510` · `harvest.js:419` · `intentExtractor.js:165` | no name coercion |

**NO CODE WRITER COERCES A PHONE INTO `name`.** The value arrived as a **model
tool input** — Donna, holding no name, supplying the number. The cure is a
tool-input validation or a soul matter, both **out of this radius** (W-1). Per
the ruling: **a finding with its census attached, its own micro.** Proposed as
**F-06.187**, not minted by the seat.

**One live fact I could not verify:** the chair derived that the lead row for
`+918595986978` holds the phone in `leads.name`. I hold no database. The render
guard is correct either way, but the row itself is founder-witnessed or nothing.

---

## §5 · DECLARED

- **A5 and A6:** A6 is now **production-green** (walk ten, 7:01pm — one
  quotation, the door's). A5 remains unwitnessed live; walk ten halted before an
  approve. **Not claimed.**
- **The honest decline is banked:** `no-stage (no_body_composed)` when Victor
  asked which December wedding rather than composing. Nothing fabricated.
- **`b6eb26b9` stands** — `staged`, untouched, expires 2026-08-12 13:31. If this
  rider outruns that clock, walk ten re-stages fresh and ⑥'s moment is witnessed
  as a bonus.
- **F-06.143 limb 1** remains open and loud, outside radius, on the shelf.
