# CE-41 · SEAT D · D3c2b (dream-os) — the redirect, the cap, the public door · HANDOVER

**Cut at** dream-os `3f323817cbdc332f07571d6790c46d354983d3cd`. **No migration.**

## 1 · F-41.128 — the door the chair reads, and what it refuses

`GET /api/v2/public/enquiry/:token`. **Unauthenticated. Anyone with the link.**

**Returns exactly four fields:** category, city, month, budget **band**.

**Never returns:** her phone (roadmap §7's standing refusal — this is the last place it could leave and the first place it would be noticed) · her name · the **day** of the wedding · the budget **figure** · any other item on the request · any id of any kind.

**The token is not a secret and is not treated as one.** `enq-<8 hex>` is guessable, which is exactly why nothing behind the door is private: those four fields are what already goes to a stranger in the WhatsApp template. **The token names an enquiry; it authorises nothing.**

**It refuses to confirm what it cannot show.** Missing, closed, malformed and ambiguous-prefix all answer identically, so a prober cannot learn which enquiries exist by trying tokens.

**Driven, not read** — all four paths run against a double whose fixture row *carries* phone and name. Good prefix returns four fields with neither.

## 2 · b20_a2's one-home cell caught my architecture

`.from('assistance_*')` may appear in **exactly two files** — the writer and `relayStatus`. **My first cut of the public door made a third.**

The law was right and the door was wrong: the **query** moved into the writer as `publicEnquiry`, and the door became a **shape** — it decides what a stranger may see, the writer decides what is read. **A sealed bench refusing an architecture is the cheapest review in the estate**, and it fired before this packet ever left.

## 3 · F-41.151 — refuse with a redirect

A number was three things at once today: a vendor's user phone, a vendor with 57 leads, and an outsider prospect with a forward against it. **`ALREADY_HAS` guards a different question**, so nothing stopped it.

**`vendors` has no phone column** — the lookup is phone → `users` → `vendors`, the join `alertVendorOfForward` walks in reverse.

**Its first cut pulled every user row into memory** and matched in JS — a full scan on the path whose ordinary answer is *"not a vendor"*. `findCoupleIdByLastTen` already had the shape one function up: filter in Postgres, take two, and **refuse to guess between country-code twins**. A redirect naming the wrong handle is worse than no redirect.

**The refusal carries the vendor** so the queue can offer the TDW forward. A sentence that says *no* without saying *here instead* costs a second search.

## 4 · F-41.100 — the cap finally asks

`§6.2` ruled three per category and **nothing ever checked it**: `FANOUT_DEFAULT` was read only for display while `bumpForwarded` incremented unconditionally. The walk showed `6 of 3`. **A number the code never keeps reads as a rule and isn't one.**

**Warn and confirm, not a wall** — the override was always part of the ruling. It refuses once, names the count, and `confirm: true` passes. **It gates on the item, above both arms**: the cap counts vendors who have seen this category, and a TDW vendor counts as much as an outsider.

## 5 · Three cells of mine were wrong before they were right

`8.3` matched the **function definition** of `consentEvidences` and read the gate as misplaced when it wasn't — F-41.107's family. `10.1` read the door for a select that had moved. `10.6` **counted** occurrences of `NOTHING`, and the count moved when the query did — re-cut to the property across both homes, per R-41.138.

## 6 · The founder's steps

1. Apply, verify, push. **No SQL.**
2. **The walk:** forward Sarah's Photography item (at `3 of 3`) to anything — expect *"This is 4 of 3 for a photographer. Forward anyway?"* Retry to pass it. Then forward to **`8757788550`** — expect *"This number is already on TDW as @DROY550."* Then open `thedreamwedding.in/e/enq-<first 8 hex of an item id>` in a private window: four fields, **no phone, no name, no date, no figure**.

**Range F-41.153–F-41.156 unspent** (`.151` and `.152` recorded by the chair).
