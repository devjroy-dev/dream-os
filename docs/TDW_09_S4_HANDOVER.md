# TDW_09 · BLUEPRINT · STAGE 4 — THE THEME STRATEGY — EXECUTOR HANDOVER
**Under:** AD-X2 whole (chair direction as starting position) · R-U16 by pointer · R-X24 (baseline amendment rides this ZIP). **Docs + one mock; zero code bytes; W-1 held; every proposed value/word veto-marked.**
**Shipped:** `docs/specs/TDW_09_S4_THEME_STRATEGY.md` (full inventory both apps with measured floors per role · verdict table: Espresso RETAIN · Paper RETAIN · Navy DISCARD with named deletion radius · Wine RETAIN+one-α · Sky MODIFY→Warm Porcelain with pre-solved AA values · v2 MODIFY · globals MODIFY-FOLD · demo RETAIN-WITH-VENDOR · NEW none, cap met · system-follow as a feature, server-persisted) · `docs/mocks/tdw09_bride_themes.html` (the MODIFY veto carrier: reconstructed card, three columns, measured ratios printed on the render) · the R-X24 amendment appended to the baseline paper.
**Findings — F-09.26** (frost home-mode via localStorage, `tokens.ts:348/:362` — the .25 class, bride lane) · **F-09.27** (sanctuary's 31 inline `dark?` ternaries with `#1A0810` ∉ tokens — the theme-literal family live; cure rides the re-solve). **Next free: F-09.28.**
**Disclosures:** ratios are this sitting's computed derivation (WCAG over composited values, sources cited per row) — reproducible, and the mock prints them; the founder's-walk specimen 「 white and navy filter 」 is SETTLED as Sky & Ivory (a theme, not a filter) by the getV2Tokens census; the settled answer changes the cure exactly as AD-X2 anticipated (re-solve, don't hunt an overlay).
**Gates:** founder vetoes Warm Porcelain's values/words from the mock; the Navy deletion + the re-solve are cure-queue items, not this ZIP's. **Next and last: the prioritized cure queue.**
Sequencing beyond this sitting is the founder's.
