# TDW · pwa — COLOUR SEAT → CE · NOTE 01

**LE (colour seat) to CE · 2026-09-07**
**Derived at `3f413bc6f85843ddbdf9421b685c91b8e71a6b1f` (`dreamos-pwa`, `origin/main`), fresh
clone, clean tree. Every claim below was produced by command at origin; none is recalled.**

---

## 1 · WHAT LANDED

`3f413bc6` — **F-40.263's micro**, the founder's push, base `f05016079ded9b571959ac28b063364d5af68517`.
Three files, 311 insertions, 16 deletions:

| path | |
|---|---|
| `components/vendor/Toast.tsx` | cured; blob `9c9c4b05` (was `9ca948f6`) |
| `scripts/b58_f40263_toast_surface_bench.js` | new, 229 lines, inside `run-floor.sh`'s glob |
| `scripts/floor-manifest-ce40-f40263.txt` | new |

**Verified on a clean clone at the landed tip, not on the delivery tree:**
`b58` GREEN **21/21**. Floor **32** (28 RED · 4 REFUSED) — byte-identical to the set at pristine
`f050160`. The micro landed without moving the floor.

**What it cured.** `Toast.tsx` now reads `--atelier-sheet-bg` for ground and `--atelier-ink` for
message ink in every kind, both modes. The Chalk error variant went **1.53:1 → 18.96:1**. The
five shell rooms plus `NotesBody` and `SettingsScreen` no longer ship an unreadable refusal
sentence. The `useT()` read retired with the literals, so this file is out of F-40.262 rather
than on one side of it.

**Owed and NOT claimed:** the founder's walk on the glass. Force a failure in portfolio,
couture, contracts, tds or calendar in both modes and read the toast. `b58` is a source bench;
a green there is not a green on the deploy. `tools/wl_audit.mjs` is the served-bytes gate and
has not been run against this cure.

---

## 2 · THE CENSUS'S STANDING CORRECTIONS

Three, already taken by the chair at c-40.56, restated so a successor does not re-derive them:

1. **Wine Night reads nowhere under `/vendor/**`.** One crossing from `lib/frost/**` exists and
   it is `istDayKey`, a date function (`SliceRow.tsx:16`).
2. **The kickoff's `lib/vendor/tokens.ts:399` was a mis-cite.** That file is 42 lines. Wine
   Night is `lib/frost/tokens.ts:88`.
3. **The oxblood was the shell's own authored literal**, not a bleed — `WlToast.tsx:94`.

A fourth, minted at the cut and worth carrying: **`ToastKind` is two-way**
(`hooks/vendor/useToast.ts:7`, `'success' | 'error'`). The kickoff named three variants. There
is no `warn` to paint and R-40.129 ④ keeps it that way.

---

## 3 · THE PALETTE PACKET — SCOPE, RE-DERIVED AT `3f413bc`

**162 rows · 39 colours · 31 files.** Not 155: G3.1 s2's `your-website` room brought seven in
after the count was last taken.

| | |
|---|---|
| shell call-site literals | 311 |
| of which LEAK (outside the seven-ink law) | **162** |
| of which restated-token literals (right colour, wrong home) | 149 |

The three heaviest files are `TipsCarousel.tsx` (23), `FilingChip.tsx` (13), and
`portfolio/screen.tsx` / `MessageBubble.tsx` (11 each). `Toast.tsx` is now **zero**, which is
the micro's six rows leaving the table.

### The reds that remain, after the micro

| hex | rows | note |
|---|---|---|
| `#7A3828` | 21 | the terracotta — ChatThread, Header, TipsCarousel, MessageBubble |
| `#E07B5C` | 14 | **the retired light `--role-critical`**, 2.66:1 on Chalk, under the 3:1 bar |
| `#E07070` | 9 | the danger hairline (was 10; `Toast.tsx`'s left with the micro) |
| `#B42828` · `#7A1A1A` | 4 · 2 | NotesBody, CalendarBlockSheet, CalendarDaySheet |
| `#4A1616` | 1 | `WlToast.tsx:94` — the founder's original specimen, still live |
| `#A12B2B` | 1 | `ListRow.tsx:33` |
| `#B4453C` | 1 | `wedding-pages/page.tsx:806` — see the phantom token, §4 |

`#5A1414` and `#3C1414` are **gone**; they were `Toast.tsx`'s alone.

---

## 4 · TWO FORKS OPEN — RULINGS OWED BEFORE THE PACKET CUTS

**① The four Google brand hexes.** `app/vendor/(shell)/your-website/screen.tsx:194-197` carries
`#4285F4`, `#34A853`, `#FBBC05`, `#EA4335` — the Google wordmark, drawn in the SEO card that
arrived with G3.1 s2. R-40.129 ③ rules `#25D366` onto the WhatsApp brand mark only and says
nothing about these. They are the same class and I read them as covered by the same principle,
but the radius of a rule is derived before the ruling names it (R-40.64) and the chair has not
named these. **I will not extend ③ by analogy without a word.**

**② `--atelier-danger` is a phantom.** It is read at exactly one site
(`wedding-pages/page.tsx:806`, `var(--atelier-danger,#B4453C)`) and **defined nowhere in the
tree** — that grep is the entire census, one hit, the read itself. So the fallback is
unconditional. Under ① the cure is `--role-critical` and the phantom is **deleted, not minted**;
naming it because a later reader will see a `var()` and assume a token exists behind it.

---

## 5 · THE STANDING REDS AT THIS TIP — NONE OF THEM THE COLOUR SEAT'S

Derived at `3f413bc` on a clean clone. **28 RED · 4 REFUSED**, unmoved from pristine `f050160`.

| bench | cell | owner |
|---|---|---|
| `b40_worklist_shell_bench` | **C50** — a real person named in a vendor-facing byte, `contracts/screen.tsx`; belongs in `lib/worklist/copy.ts` (F-39.6) | G3.2 s3's screen |
| `b40_worklist_shell_bench` | **C102** — straight apostrophes at `wedding-pages/page.tsx:840` and `:841` (R-40.57 / R-40.19) | F-40.239's byte |
| `b42_g11_wedding_pages_bench` | one red, the room-string cell | inherited |
| `tdw_f3942_census_guard` | **9/10**, `§1.3 totals match (scanner 29/29, retired 227/228)` | the stripper census went stale again |

Two counts a successor will otherwise re-derive:

- **`b42` reads 171/172 in a pwa-only seat, not 175/176.** The four missing cells print
  `REFUSED — dream-os sibling absent`; they are the cross-repo mirror checks. Both numbers are
  correct, in different seats. Do not treat the difference as a regression.
- **`tdw_f3942_census_guard` has now flipped twice**: RED at `bfa3197e`, GREEN at `2baa86cd`
  when F-40.269 regenerated the census, RED again at `f050160` when G3.1 s2 moved file
  membership. It reddens on membership drift by construction, so it will red on the palette
  packet too unless the census is regenerated as the chair's act in the same cut.
- **`run-floor.sh --check` reports a 4-entry delta above its named base.** All four
  (`b40`, `b42`, `tdw09_hotfix`, `tdw37_leadgate_b_slot`) were confirmed present at a pristine
  worktree. **The base manifest is stale, not the delivery.** Flagged rather than absorbed —
  `run-floor.sh:469` says a base entry nobody can account for is how a real regression gets
  absorbed.

---

## 6 · WHAT THE SEAT IS HOLDING

Ready to cut on the chair's word, against `3f413bc`:

1. **The 162 rows**, by the proposed-token column ratified at R-40.129 ⑤.
2. **The seven-ink census cell** — reds on any colour literal under `app/vendor/**`,
   `components/vendor/**`, `components/worklist/**`, `lib/worklist/**` outside the two
   token-definition files, and on any `--wine|--vibe|--oxblood|--bride|V2_WINE` identifier or
   any `lib/frost/**` import beyond the named `istDayKey` allowance. It must exclude `&#\d+;` —
   `AiDock.tsx:59` is `&#8593;`, an arrow, and a cell that reddens on HTML entities gets
   silenced rather than obeyed. Pinned to **162**, both-ways: RED at `3f413bc`, GREEN at cured.
3. **The contrast cell** — every toast variant ≥ 4.5:1 in both modes, values parsed off
   `lib/worklist/theme.ts` rather than transcribed, mutations that pin either ink or ground
   going RED. `b58` §5 is the working precedent; the new cell generalises it to `WlToast` too.

**Sequencing risk to name:** the packet touches 31 files across the shell, including
`SliceShell.tsx`, `portfolio/screen.tsx` and `calendar/screen.tsx`. Any seat working those in
parallel will collide. The chair holds sole sequencing authority; the seat is asking for the
lane to be clear rather than assuming it.

---

**Derived at `3f413bc6f85843ddbdf9421b685c91b8e71a6b1f`, `origin/main`, `dreamos-pwa`,
2026-09-07. Transport only — `CE_PACKET/`, not `deploy/`. Nothing in this packet is meant to be
applied to a tree.**
