# CE-41 · SEAT D · D3c2a (dream-os) — the link, and two corrections · HANDOVER

**Cut at** dream-os `3135d3184b4262bca37008c6a710e39dba4a1d15`. **No migration. No new Railway variable.**

## ⚠ 0 · This is part of D3c2, and I am saying which part

**Cut and proven:** the `wa.me` link builder, F-41.107's cure, F-41.149's comment.

**Not cut:** F-41.128's public enquiry read, the fan-out confirm, the pwa frame, and the inbound branch that matches the token and answers with the enquiry.

I stopped rather than push four more surfaces through on a thinning margin at the end of a long sitting. **The remaining work is a full packet, not a remainder**, and it deserves a fresh read of the two doors it touches.

## 1 · The link

`https://wa.me/918810531764?text=Send%20me%20the%20enquiry%20enq-5b253fb2` — driven, byte-identical to the chair's.

**The token is `enq-` + the id's first eight hex**, the same vocabulary as R-41.119's button suffix. One prefix family, so a reader who has seen `enq-` on `/r/` knows what this is without being told twice. It is in the **text**, not only the URL, because a `wa.me` link carries no metadata through — the only thing that survives her tapping it is what she sends.

**The number is derived, never typed.** `MARKETING_WHATSAPP_NUMBER` holds the E.164 in Twilio dress and **`normalizeTo` already strips `whatsapp:` and `+`** — so the chair's *"sibling, or the same function if it already accepts that form"* resolved to the second, verified by running it on both dresses. A cell asserts the digits appear nowhere in the file.

**No number or no token → `null`, never a half-built URL.** A `wa.me` link with no number opens WhatsApp on nothing and looks like the estate working.

## 2 · F-41.149 — and my correction repeated the error

The comment called `MARKETING_WHATSAPP_NUMBER` a Meta phone-number-id. It is not: it is the E.164 in Twilio dress, and `MARKETING_PHONE_NUMBER_ID` is the id. **The two were transposed, and the sentence read as an argument for the transport it was explaining.**

**My first correction quoted the sentence it was correcting**, so the wrong claim was still in the file and `7.1` caught it. Reworded to describe rather than reproduce.

**A wrong comment on a working value is the hardest kind to notice** — `normalizeTo` folds the Twilio form, so the value worked everywhere while the label lied.

## 3 · F-41.107 cured, and the cure guards itself

The cell compared the consent arm to the **first** `openProspectConversation`. There are three; the START arm opens one. **It passed for the wrong reason and would have passed if the arm moved above that call.** Anchored now on the slice beginning at the consent arm, and `4.4` asserts more than one such call still exists — so the cure cannot quietly become vacuous.

## 4 · The founder's steps

1. Apply, verify, push. **No SQL, no Railway change.**
2. **Nothing changes on the glass** — the builder is exported and nothing calls it yet. The queue surface and the inbound token branch are the next packet.

**Range F-41.150–F-41.156 unspent** (F-41.149 spent here).
