# CE-41 · SEAT D · D3c3 (dream-os) — the fourth walker, two words, one money format

**Cut at** dream-os `0f43228bf56e48bb43c9da15393755ab425128b3`. **No migration.**

## 1 · F-41.125 — and what this packet cannot prove

Meta enforces Messaging Policy §7 at the **account**: a WABA can be restricted, its quality tier cut, its sending suspended — and `account_update` is the only notice. **Nothing received it.** The founder's first sign would have been sends failing for a reason no log named.

**The walker is `extractTemplateStatusUpdates` with one word changed.** Four walkers now cross one `entry[].changes[]` envelope split only by `ch.field`; a fourth with its own traversal would be a fourth dialect for one shape.

**⚠ Its first real execution is your webhook, not my bench.** Meta's payload cannot be sent from this container, so every claim about its shape comes from the siblings and Meta's docs — **the same position that produced F-41.153's two wrong fixes.** The cells therefore assert the **traversal**, which is the siblings' and is testable, and nothing about Meta's inner field names beyond `event`.

**The receiver logs the whole value on purpose.** Every other receiver logs named fields because it knows the shape. This one doesn't — **the raw payload is the evidence for whoever reads the next sitting.** Once witnessed, that line should narrow.

**And it does not act.** A restriction is your decision. Flipping keys on a webhook whose shape has never executed here would take the plane down on a guess, and a transient restriction would leave the switchboard dark with nobody knowing why.

## 2 · F-41.154 — six literals, two facts

The walk forced this: both live requests had `wedding_date` null, so the outsider's message read *"a couple planning a wedding in India needs a photographer"* with fallbacks doing all the work — and **four sites each held their own copy of the date word.**

`"an upcoming"` reads correctly in the template's own sentence. The old word was written for a different slot and never re-read when the slot moved — **F-41.80's shape**: literal and value composing into nonsense.

**The founder's own notify keeps its words** — `date_words` is a *full date* in a different template read by a different person. Asserted, so the next sitting doesn't "finish the job" and break row 9.

## 3 · F-41.75 — and the require that wasn't there

Three files, five L-format sites, zero left in code. `formatRs` gives Indian grouping and no glyph; `Rs 1.5L` rounds `1,49,000` and `1,51,000` to the same unusable figure.

**`briefing.js` had no `require` at all.** My first cut replaced the format and left `formatRs` undefined — **it would have thrown on the 8am cron and at no bench.** Caught by reading the header, not by running.

## 4 · An error of mine that was not an error in the code

I read `detail.js`'s range as missing its separator and nearly "fixed" it. **The separator is an en-dash, and my own inspection pipeline strips non-ASCII** — the character vanished in the display, not in the file. The false comment never landed only because an assert happened to abort the write.

**A tool that silently drops bytes is a tool that invents findings.** When the claim is about a character, read bytes.

## 5 · The founder's steps

1. Apply, verify, push. **No SQL.**
2. **Subscribe `account_update` on the WABA** in Meta's dashboard — the receiver is live but Meta sends nothing until the field is subscribed.
3. Nothing else changes on the glass. The Switchboard restrictions row is the pwa half.

**Range F-41.155–F-41.156 unspent.**
