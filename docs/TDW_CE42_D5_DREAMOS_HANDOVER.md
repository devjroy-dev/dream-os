# CE-42 · SEAT D2 · D5 (dream-os) — the public mount, the city gate, the trade in English

**Cut at** `c1c17db50917582bc9ee24da6a0487a0365fa074` — re-pinned from `09317d6` after 4a. No migration. `b20_a2` **180/180** (base 165), `b66` 77/77, `b64_template_slots` **134/134** (4a moved it from 131), `b64_mutations` 6/6, `b07_f0776` 65/65, `b07_f0784` 59/59, and 4a's own `b68` 99/99 and `b40` re-run green beside them — every bench that reads a changed file, run by command at this tip.

**What shipped:** `POST /api/v2/couple/assistance/public` — ruling (ii), inside `requireCoupleAuth`, `origin` chosen by the route and never read from the body; both doors run one handler, so the session read, the profile coalesce and the response shape have one home. **F-42.55** — A2's reserved second router is **deleted**, not uncommented: its condition arrived (D4's `ensureCoupleRow`) but the ruling went the other way, and a body-phone door with no session was an open spam door. **F-42.58** — an admin-typed request with no city refuses the outsider forward (*"Add a city before forwarding this outside TDW."*), so `{{3}}`'s `India` fallback is never spent on a blank column; `:676` untouched, month untouched, no template edit (R-42.2). **F-42.57** — `src/api/public/enquiry.js` reads `categoryNoun` from the writer's one noun home, so `/e/` says *venue and caterer* where it said `venue_catering`; R-40.88.

**Scope named, not widened:** the gate is `origin='admin'` only, as ruled. A cityless **bride** or **public** request still forwards and still sends *"in India"* — **F-42.63**, filed at the gate in-comment. It is deliberate: an admin-typed request is the founder's own keystrokes and he can refile it, while a bride's has no remedy until R-41.95 gives the queue a way to add a city, and a gate with no door out is worse than the sentence it prevents. **F-42.58 closes on this gate.**

**Re-pin, derived not assumed:** all five of this packet's source blobs are byte-identical at `09317d6` and `c1c17db` — 4a touched `templates.js`, `victorLines.js`, `capabilities.js`, `introductions.js`, `relayStatus.js`, four benches, `0161` and `floor-base.txt`, and none of them is in this manifest. The whole-file copy reverts nothing. **Not proven:** the full floor. `scripts/run-floor.sh --check` could not complete in the seat's container — the warming pass alone exceeds the per-command wall — so the floor line rides the founder's verify block per R-38.19 rather than being quoted here. Dirt declared at `scripts/floor-manifest-ce42-d5.txt` (F-14.16). **Next:** the pwa half — `/plan`, the sheet extraction, `lib/auth/otpSignup.ts`, the layout branch, the sitemap.
