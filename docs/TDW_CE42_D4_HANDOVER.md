# CE-42 · SEAT D · D4 (dream-os) — every request gets a couple

**Cut at** `e101b7e7a1deee96b9a9232a6e788732a0275673`. No migration. `b66` 77/77 · `b20_a2` 165/165.

**What it does:** an admin-filed request whose phone matches no `users` row now creates the account and attaches it, so her found-notice reaches a real couple and her reply is recognised on the bride lane instead of dying at the invite gate. A country-code twin refuses the request rather than guessing. `enquiry_ref` moves to the eight-hex form the public door parses (R-42.1).

**Not a third writer:** `ensureCoupleRow` already existed and already had two callers; D4 is the third. The ruling's extraction was done long ago.

**Not proven:** nothing — all three paths are driven. **Filed not cured:** F-42.2, the onboarding gate will lock these couples out the day it arms; named at the create site.

**Copy for veto — the twin refusal, the only user-facing byte:** *"Two accounts share those ten digits. Resolve them before filing this request."*
