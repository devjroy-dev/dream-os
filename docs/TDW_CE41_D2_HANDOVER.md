# CE-41 · SEAT D · D2 (dream-os) — the template plane · HANDOVER

**Cut by** LE-D **at** dream-os `0fb3ee234818eecc0146fec391272d6e85e791c7` (seat B's B1f), derived fetch-first.

**Collision derivation (R-40.82).** `git diff --stat 4fe3a66..0fb3ee2` across `src/lib/templates.js`, `src/lib/couple/assistance.js`, `src/lib/capabilities.js`, `scripts/b64_*`, `scripts/b20_a2_*`, `db/migrations/` is **empty**. Seat B shipped docs only. Ancestry confirmed.

---

## 0 · SCOPE — this is D2's template plane, not all of D2

D2's charter is the amend door, the public intake door, the two bride arms and mehendi. **Mehendi closed** (the founder ruled it stays under `other`). **The two bride arms and R-41.118's v2 rename are here.** The **amend door, the public intake door, per-item status, decline and `attachCoupleByPhone` are not** — they are doors and a second migration, and putting them in the same packet as a live template rename would have made one apply carry two unrelated risks.

**The reason to cut this half first is that v1 is live and failing.** Every outsider join alert on the tree right now rides a MARKETING template against a per-user cap that refused it four times in one night. This packet is what stops that. **Chair's call whether the doors are D2b or D3**; I hold them ready either way.

## 1 · R-41.118 — the outsider alert is v2, and it is Utility

`tdw_assist_lead_outside` was Marketing filed as Utility. R-41.30 chose *"accept, no appeal"* to protect the Utility lane — and what actually refused it was the **per-user marketing cap**: `131049`, **four times on one handset in one night** (`assistance_forwards`, 2026-09-08 19:29 → 23:45). The honest Utility shape existed all along — **an enquiry notice, not an invitation** — and the re-file found it. R-41.30's record gains that resolution.

| | |
|---|---|
| registry `name` | `tdw_assist_lead_outside_v2` |
| `category` | `UTILITY` — **the `line` stays `marketing`** |
| body | byte-for-byte §2 row 10a (the founder's Manager preview, 07:42 IST) |
| Meta id | `2544506315978894` |

**Category and PNID are independent**, and the packet says so where it matters: a Utility template still rides the marketing number, so its receipts still land on `marketingIndex`'s arm — the one F-41.60 gave `applyStatusEvent`. The `sent → delivered` path witnessed on the walk carries over unchanged.

**The five slots did not move**, so `b64` reads v1 and v2 against the same shape, and **`{{4}}` is still a bare noun** — `b64` §3 confirms no doubled article for any of the eleven categories.

## 2 · R-41.4(b)/(c) — the two bride arms, derived from the filing

Both entries are **row 11 and row 12's blockquotes, byte for byte**. Neither was authored beside its filing: row 10's whole lesson, in seat B's words, was *an entry written independently of its filing* — internally consistent for a day, garbled on the handset.

- **`assist_found_vendor`** — `['name','category_noun','month_year','vendor_name']`, bride line, Utility. **`status: 'pending'`**, because it is **In review at Meta**, not Active: F-41.114 re-filed its button and the edit is being assessed. The honest word, not a guess; the sweep moves it when the Manager reads Active. An F-06.85 note forces the next sitting to re-read before touching that word.
- **`assist_found_outside`** — `['name','month_year','category_noun','ig_handle']`, bride line, Utility, **no button**. She replies on the thread and the founder arranges it by hand, which is why no vendor page can be linked for someone who has not joined.

**All three bodies supply the article** (`needs a {{4}}`, `matched a {{2}}`, `matched to a {{3}}`), so all three take `categoryNoun`. `b64` §3 covers each **the moment it exists**, because it finds the trade slot by shape rather than by a pinned name. That near-miss has now paid for itself three times.

## 3 · Migration `0155` — three keys seeded, v1 retired

Ladder tail derived by command immediately before allocation → `0154`. Number chair-allocated, never claimed (LD-8). **Not run by this seat.**

**`status: 'approved'` is Meta's word; `'on'` is the founder's.** Seeding approved arms nothing — every arm reads shut until he flips it, the same posture `0151` took. `assist_found_vendor` seeds **`pending`**, because the row must not claim a state Meta has not given.

**v1's key is set `off`, not deleted.** The row is the only durable record of what the estate sent before 2026-09-09 and why it stopped; deleting it would take the evidence with the key. The update is idempotent and narrow (`and status <> 'off'`), so it cannot resurrect a key the founder has since turned off himself.

Both CHECK values are **witnessed against `0149` lines 23–24** and named in the file, not recalled.

## 4 · The partition moved in both directions at once

Registering the two bride arms takes them **off** `NOT_YET_REGISTERED` and **into** the compared set in the same cut — §4.q reds otherwise. The rename adds a **third state the bench had no name for**: `tdw_assist_lead_outside` now has a §2 heading (row 10, kept as the v1 record) and no registry entry. It is **not "not yet registered" — it is retired**, and those are not the same claim. It is listed with its reason rather than deleted from the bench's sight, so **a retired name quietly reappearing in the registry reds** (`5.5`).

## 5 · c-41.61 and one more bench, awaiting a number

**`b20_a2` ×3 (c-41.61)** — the key literal, the Meta id, and *"the registry entry is Marketing"*. Re-cut to the **witnessed v2 facts, not relaxed**: `UTILITY` is asserted by name, so a silent slide back reds. The cell now asserts category **and** line together, because the pair is the claim.

**`b61_switchboard_bench` ×1 — seat C's, and it needs its own number.** It read `TDW_ASSIST_LEAD_OUTSIDE === 'template.tdw_assist_lead_outside'` — a **transcription of the constant's value**, so it broke the day the template legitimately versioned. That is seat G's §8.1 class (a mirror carrying its own copy of the thing it mirrors). **Re-cut to derive:** the constant must exist, be a `template.` key, and agree with what the registry holds for `assist_lead_outside`. That is the contract seat A's cell was defending; the literal never was.

It is now **green on both trees**, which is correct for a contract cell — so non-vacuity is proved by two hand mutations instead, both in the manifest, each convicting with its own sentence.

## 6 · Four anchors died on this seat's own edits

`b64_mutations` M1 (a comment block moved between the two halves of a joined anchor), M4 (**aimed at a name R-41.118 retired**, so its cell no longer fires for it — unobservable rather than uncaught), `b61` §4, and `b20_a2` ×3. All re-derived; `PRE_CURE` struck rather than left dead.

**Seat G's §5 named this exact class one packet ago** — *"foreseeable, and not foreseen"* — and this seat lost `b62_mutations` M6 to it under c-41.43. The harness reporting `matched 0 times` rather than passing is the only reason it is survivable, and that is a property worth defending in every mutation file the estate has.

## 7 · The founder's steps

1. **`0155` is his to run**, after the apply and before flipping anything.
2. **Nothing changes behaviour until he flips a card.** All three keys read shut on arrival.
3. **The v2 walk:** flip `template.tdw_assist_lead_outside_v2` on, forward one outsider, and read the handset. The body should be the enquiry notice — *"you have a new enquiry through The Dream Wedding…"* — with **Visit website**. `131049` cannot arise on a Utility send; if it does, that is a finding against the filing, not against this packet.
4. **`assist_found_vendor` stays shut** until the Manager reads Active.

## 8 · Open, carried

**F-41.63's wire half** — the brother's handset, still the reading. **F-41.98** — R-41.107's pwa half, resolved by the chair (the Advisor page mounts its own instance with `room="advisor"`); it is the pwa packet's first byte. **The doors** — amend, public intake, per-item status, decline, `attachCoupleByPhone` — held. **The `/e/[id]` page and the `enq-` branch** (R-41.119) ride the pwa packet, cut together with the send's wiring.

**Range: F-41.99–F-41.107 unspent.** No finding was filed by this packet.
