# TDW · THE VICTOR SITTING — EXECUTOR HANDOVER

**Base:** `dream-os @ 2ad4637` (re-derived at origin, fetch-first, at the moment of cutting).
**Delta:** 14 files, 1,757+ / 79−. Four new files, eight production files touched, five benches labeled-amended, one bench new.
**Floor:** `FLOOR = NAMED BASE, no delta` — run by SET on a committed tree (c-P72.11), `npm run build` green first, four refusals named and not in base (`b06_gauntlet`, `b5_wa_door_smoke`, `bf1_bride_tool_fidelity_bench`, `test-shape` — the sibling repo is not cloned in this container; they REFUSE by clone layout, not by tree).
**New bench:** `scripts/b40_victor_sitting_bench.js` — **108/108**, seven production mutations biting.

This document rides the ZIP. It is not a CE-numbered entry and it does not touch `FINDINGS_LOG.md` or the masterplan.

---

## 1 · WHAT SHIPPED, BY RULING

| Ruling | What landed |
|---|---|
| **F-A = A1 · R-VS.2** | `readOutstanding` + `OUTSTANDING_STATES` move to `src/lib/vendor/invoices.js` — the writer home becomes the reader home. `src/api/vendor/money.js` imports both and **declares neither**. `src/lib/vendor/moneyFacts.js` builds the door-side fact block; `loop.ts` gains `moneyFacts?: string` and `moneyBlock`, the fourth door-built sibling, last in the dynamic tail. |
| **R-VS.6 (a)** | Fence 1: a money sentence acquits only where every figure and invoice handle it speaks is in the block's own set, by equality. Fence 2: `money_facts {present, readable, rows}` rides `evals_runs.transcript` beside `hand_census` — **zero DDL**, additive into the existing jsonb (`ENGINE_SCHEMA.md:215`), so the ruling's conditional column never fires and no migration is owed. |
| **R-VS.9** | Fence 1 enforces amounts and invoice handles; **client names are not enforced**, chair-ruled as the seat reported. |
| **R-VS.7 · B-i** | The eligibility gate learns three families it never had. Six strings that returned `null` at `c841082` now classify. |
| **R-VS.7 · B-ii (as amended by R-VS.12)** | `expense` is a structural class with an **empty acquittal set**, never `records` — this cures F-40.7 for that arm. `lead_send` is **retired**. |
| **R-VS.7 · B-iii (as amended by R-VS.13)** | The door authors line 1 for `expense` and nothing else. `relaySeat.js` keeps its own founder-vetoed relay bytes. |
| **R-VS.11 / .12 / .13** | Victor's line to a lead is the existing relay pair. `TRANSMISSION_CLAIM_RE` feeds `relay`, not a rival class. R-40.2 line 2 vacated, no replacement minted. |
| **R-VS.14** | `leadSendClaim` joins `isClaimSentence` and `markerIn` — a claimed transmission is self-marking. |
| **R-VS.4 = D1** | The advisor word is refused on the WhatsApp lane before `applyModeFlip` is reached; the row is never written. `MODE_FLIP_LINES.advisor` retired with its last reader. |
| **R-VS.10** | The fact block's frame carries no bracket, no label and no house word; `§12`'s echo cell proves the frame's bytes never reach the vendor. |
| **R-40.2** | Three constants in `src/lib/victorLines.js`, hash-carried, self-checking at load. |

---

## 2 · DISCLOSURE — SEVEN ERRORS, OWNED

**Executor №1 · the SELECT that could not answer its own question.** Read-first 2's founder SELECT omitted `hand_census` and `stage2_delivery` — the two fields that answer what the vendor actually received. Cost one round trip.

**Executor №2 · escalating before deriving.** I raised the 2026-09-01 17:45 interception as a probable false interception and a possible CE-107 STOP **before deriving whether its content was true**. The events read refuted me: the rundown invented a 4-September block and omitted a 3-September recce. The guard was right; I withdrew it. Reach-don't-derive, named by the estate's own word.

**№3 · the completion word wearing an object.** `Logged Rs 5,000 for the assistant, today.` returned `null` — `BARE_COMPLETION_RE` required the fragment to be nothing but the word. Cured with `LEADING_COMPLETION_RE`. Found by driving, not by reading.

**№4 · the cure's own refusal convicted.** `MONEY_STATE_RE` matched *"what's outstanding"* inside R-40.2 line 3, and an unreadable block made the equality fence false — line 3 would have been replaced by a glitch line. A false interception on a founder-vetoed byte, the direction CE-107 forbids.

**№5 · and that cure's first version did not work.** The exemption ran against `eligible`, which is `sentences.join('\n')`, so a two-sentence constant could never match by `indexOf`. Both sides are whitespace-collapsed now. A cure that is not driven is a cure that is asserted.

**№6 · a fence built across a lane boundary that does not exist.** I closed a "cross-lane hole" — a claimed send to a lead landing in the bride lane's `relay` class — reasoning from R-VS.3's premise that no door existed. The premise was vacated (c-40.6). **The guard was working.** The fence is reversed by reversing the edit (`claimedIntent` is byte-identical to its pre-fix form); the vocabulary stays, re-homed. My census was correct and my inference from it was not: I read `relayCouple.ts`'s tool descriptions and did not open `relayToCouple.js` or CE-212 before concluding the door was absent.

**Bench cell defects B-1 / B-2 / B-3.** B-1: a bounded `[\s\S]{0,1400}` window that was a guess — the comment block above the guard is longer, so the cell REDded against correct code; re-aimed to index ordering. B-2: a slice running to end-of-file read `module.exports.MODE_FLIP_LINES` as a *read* by the PATCH door; bounded to the handler body. B-3: an anchor `router.patch('/'` that matched my own comment quoting the route; anchored to a line start.

**One design fact, recorded rather than smoothed.** The eligibility gate is widened by **two** mechanisms (`if (victorClass) claimsAct = true` and `&& !moneyClaim`), so a single mutation could only ever prove half of B-i. Split into `M1a`/`M1b`/`M1c`/`M1d`. A cell that cannot fall for the reason it names is a hollow green.

---

## 3 · LABELED FLOOR AMENDMENTS — five, count-preserved, RATIFY-OR-REVERT

Every one is F-38.27's class: a cell pinned to a spelling a ruled cure lawfully changed.

- **`b47_money_crossing_bench` §5.5 / §5.6** — asserted the derivation lived inline in `money.js`; R-VS.2 moved it. The subjects (one positive list, never a negation; server-side derivation) are unchanged and now follow the value to its home. 21→23 cells, both green.
- **`b06_forkc_wireguard_bench` §5.3 / §5.8b** — the guard seams gained an optional trailing `ctx`; the old anchors ended on `)`. Subject untouched: one function, both doors, and the count still decides.
- **`b06_forkc_wireguard_bench` §5.9** — RED because a **comment** in the classifier explaining the fence used the word "glitch". **This is F-06.55 exactly** — the law announcing the floor convicting the floor. Comments are stripped; the cell reads executable bytes. Instrument fixed, artefact not papered.
- **`b06_relay_hand_bench` §9.13** and **`b05_f0550_ping_drain_bench` §2.2** — the dynamic tail gained a fourth sibling; `moneyBlock` seeded empty exactly as `calBlock`/`actBlock`/`relayBlock` already are. Both cells' own comments name this pattern.

**`b06_bride_arrival_bench` A12.2 needed no exemption.** It went RED on F-06.185's struck phrase "word for word", carried by R-40.2 line 2 and by one byte of my own. I removed mine and refused to touch the census or the vetoed line; R-VS.13 then vacated line 2 and the cell is green with the strike intact estate-wide.

---

## 4 · THE FOUNDER CARD

Fixture state derived by SELECT before the card ships, never recalled. DEV440 `+919888294440`, WhatsApp, business mode.

| # | Founder does | Seat reads as evidence |
|---|---|---|
| 1 | `who owes me money` | The reply names Priya Nair and Rs 60,000. Evidence: the `evals_runs` row for the turn — `kind: fact_grounded`, `money_facts.present: true`, `rows` matching the SELECT. |
| 2 | `paid the assistant 5000 today` | The vetoed line 1, no "Done", **zero tool calls** in `donna_history`. Evidence: `kind: costume`, `deed_class: expense`, `what_shipped.delivered` = line 1 exactly, and **no glitch word and no REPORT word**. |
| 3 | `message Kunal that we're available Nov 22` | The draft shown verbatim, an explicit yes/no naming the recipient, **no wire event**. Evidence: a `pending_couple_drafts` row staged; no send row. |
| 4 | `yes` | **One wire event: the doorbell.** Evidence: the doorbell's send row and its `sid`, and the **absence** of any free-form `sent_by: 'vendor_relay'` row. |
| 5 | a fresh thread's first turn | Business mode, derived from the row. Evidence: `engine.agents.victor_mode` unchanged at `business`. |
| 5b | `advisor mode` on WhatsApp | The vetoed line 5. Evidence: `victor_mode` **still `business`** — the row was never written. |

**Step 4's derivation, by command.** Kunal Dhillon is a lead on DEV440 with **zero conversation rows** → `no_conversation` → `windowVerdict` (`relayToCouple.js:72–75`) returns `closed` → `relaySeat.js:878` arms the three-armed fork. The phone is bare `9811077563`; `asPhone` (`relayToCouple.js:266–272`) turns a bare 10-digit into `+91` + the number, so the door dials **`+919811077563`**. Not malformed; the doorbell will send.

> ⚠ **STEP 4 SENDS A REAL WHATSAPP TEMPLATE TO `+919811077563`.** That number belongs to a real person unless it is a fixture you control. **Do not walk step 4 until you have said so.** If it is not yours, say the word and the card swaps steps 3–4 to a lead you own; steps 1, 2, 5 and 5b are unaffected and can be walked now.

---

## 5 · WHAT THE NEXT SEAT PICKS UP

- **F-40.6** cured here (B-i's money arm + R-VS.6). **F-40.7** part-cured — the `expense` arm only; `isDeedOfClass`'s `return !isDateDeed` catch-all remains, **Block 09's by number**.
- **F-40.1** (`buildVendorSnapshot`, zero call sites) rides the island's retirement. **F-40.5** (the island's live-shaped `log_expense`) stays dead by law; its deletion rides the same sitting.
- **F-40.2** (`donnaFind` renders `engine.records` money) — not cured here. B1's derivation showed it is not on the fact block's seam. Handed to Block 09 with the `engine.records` money-vocabulary retirement.
- **F-40.9** (the 2026-09-01 snapshot-origin rundown) — Block 09's soul/analytics sitting, with the guard's catch credited.
- **F-40.4** (the PWA Business·Advisor chip) — the pwa repo, Block 09.
- **The engine plane's own money model** (`recordsView.ts:16` carries `amount`/`amount_received`/`amount_pending`/`payment_status`) is untouched and still empty. This sitting stopped Victor answering from it; retiring it is Block 09's.
- **Two local measurement commits** existed in the LE container so the floor could run on a committed tree. They are collapsed into this ZIP. **LE never pushed.**
