CE-42 · 4c-1 · R7 G5.2 THE SHOOT BOARD (dream-os, base aa1f5ec0b142b20f41b09667555b97b54cfa5340 — re-pinned from d27144f after R6 4b-1, zero shared paths) · [CE-42 SEAT R7]

1 · SHIPPED. `src/lib/vendor/cityMatch.js` — the declared twin of dreamos-pwa `lib/vendor/cityMatch.ts` (ruling §4(a); CITIES, the alias body and `matchCity`'s body byte-identical, b4c1 §1 reads both repos) plus dream-os's own `sameCity`. `src/lib/vendor/collabKinds.js` — the shoot kind's one home (event_type editorial|brand_shoot, ruling 2(a)), shoot expiry at the end of its IST day (1(a)), and `expireCollabPosts`, the nightly sweep moved out of `src/cron.js` with the event-date close added (every kind). `src/api/vendor/collab.js` — `GET /requirement-types` (4(b), F-42.184: the eleven + the shoot event types), `?kind=collab|shoot` on /feed and /my-posts (3(ii); absent = collab, anything else 400), the city leg through `sameCity` (F-42.187), shoot inserts set `expires_at`. F-42.192 (ruled §3): `pwaPaths.yourWebsite`, and `solutions/google.js` RETURN_PATH reads it — c-42.37's corrected cite.

2 · ON THE GLASS. Nothing until the pwa ZIP lands; the Collab room keeps working on the old doors (no `kind` = collab). Server-side at deploy: the Google return lands in Your website & SEO, not the storefront; a Delhi vendor now sees Delhi NCR posts. RADIUS OF THE SWEEP, named per ruling 1(a): ONE row — 5f047847 (DEV440, decor, dated 2026-09-04, open) closes at the first 03:15 IST run after deploy, and the Collab feed is then EMPTY ESTATE-WIDE (fixture 8: it is the only open post) until the walk posts.

3 · PROVEN / NOT. `scripts/b4c1_shoot_board_bench.js` 30 PASS · 0 FAIL, driving the real router; ten production mutations, each named in its cell, each turned its cell RED and was reversed by edit. Floor, in the seat container by batch at aa1f5ec (the founder's `--delivery` run is the floor of record): tip → cured by SET, exactly `RED: b45_precutover_seat_bench` leaves (cell E's one hit was google.js:43) and nothing enters; the named base moves 24 → 25 in this delivery under c-42.44: b45 leaves (this cure), the b64 pair enters (F-42.201, pre-existing, owner R6 4b-2). NOT proven: Postgres (a double), a Railway cron run, any glass.

4 · OPEN. F-42.201: `b64_template_slots_bench` + `b64_mutations` RED at 548944a and d27144f (TEMPLATES.md §2 rows tdw_introduction_reply, tdw_couple_broadcast, tdw_referral_broadcast with no templates.js entry) — entered into the base here by c-42.44; they leave it in R6's 4b-2. b4c1 exits 3 (REFUSED) without ../dreamos-pwa beside dream-os. F-04.112's named-not-cured case is LEFT NAMED: this cure widens the city leg, not the first-look window's category-blind audience. F-42.189 (the demo collab page's sixteen) is Block 08's.

5 · NEXT. The pwa ZIP @ a96e2e23 re-pinned at its cut: the one exported form with a kind prop, the eleven from the door, the Shoots block in Referrals & partners, the role note, full-month dates, the twin named in `cityMatch.ts`'s header. The fixture-7 (d) card is authored from the founder's roster SELECT rows, after both deploys.
