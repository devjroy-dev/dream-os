CE-42 · 4a PACKET 3a · THE THREE INTRODUCTION DOORS (dream-os, base b37e0e0) · [CE-42 LE-E2]

1 · WHY THIS PACKET EXISTS — F-42.82. 4a shipped the arm; the door rider gave it a
    WhatsApp seat; R-42.8 moved the ask to a screen — and `git grep -rn
    "introductions" -- src/api/` returned NOTHING. The library had no HTTP surface,
    so the screen the chair ruled had no address to call. e-5's shape one layer up.

2 · WHAT SHIPPED. `src/api/vendor/introductions.js`, mounted on the vendor router:
    GET / (her rows, newest first, the recipient's LAST FOUR and never the number —
    a list is not a phonebook), POST / (stage → 201 with the filled body and the
    page url), POST /:id/send (E3 on the server). Every refusal is the ARM'S,
    forwarded with a distinct code: 400 missing_slot carrying the founder-vetoed ask
    byte · 409 already_introduced carrying INTRO_ALREADY_SENT · 503 dark carrying
    cap.reason()'s own sentence · 409 name_mismatch. The doors decide nothing.

3 · TWO RULINGS INSIDE THE CUT. The DARK GATE IS READ AT STAGE, not at send: she
    must learn the plane is off before she is shown a message and asked to approve
    it. The staged row SURVIVES a 503 — it is a fact she authored and the walk's
    dark half needs it. And the 201 carries the estate's OWN envelope (payload
    spread at the top level, src/lib/response.js:2-4); the first cut wrapped it in
    `data:` and would have handed the screen a shape no other vendor door uses.

4 · WHAT IS NOT PROVEN. No live HTTP walk — the bench drives the real express
    router against a double, and a double is not Postgres. §11.0 asserts auth
    against the SHIPPED SOURCE, because the harness stubs the middlewares and a
    cell proving auth through its own stub proves nothing. Still owed: F-42.69, no
    inbound arm, so a reply becomes no lead; packet 3b, the screen; and a vetoed
    byte for the seat's no_staged_row moment.

5 · THE HASH. Base b37e0e0b39948d4ca16d3afdaa575df750677a93 — cut at f3e9ff0 and
    RE-PINNED after the Victor micro; both modified paths re-derived blob-identical
    at my own tip (core.js 1644706f, b68 dd7f432b), so nothing was rebuilt. npm run build exit 0 ·
    b68 153/153 (was 125/125, +28) · b06_forkc 113/113 · b40 129/129 (moved by the micro, not by this packet) · b64 134/134 ·
    b20_a2 184/184. Six door mutations; M22's first run was VACUOUS (the sed never
    edited the file) and is re-run and red at 152/153 — named in the manifest.
