# CE-41 · SEAT D · D3c3b (dream-os) — the line D3c3 was pushed without

**Cut at** dream-os `e0ffee4ab71c1d2b2902e3741d664993d281c582`. No migration.

D3c3 was pushed from the ZIP I cut **before** folding the link in; everything else landed. This is `wa_link` on every queue item — built server-side because the marketing number is a server env the pwa cannot read.

**The pwa's link line hides without it.** Apply before D3c pwa.

`b66` 69/69.
