# CE-42 · SEAT V-2 · RIDER r3 → NEXT SEAT (base 7160b5f)

1. **F-42.147 — the date check left the verb.** On the record at 04:44:25, `Yes. Rs 5,000 to the assistant — logged 1 September as a payment to Swati.` classified **`unfenced`**. The answer was true and it shipped — but through the third door, **which does not run the fence, so the date was never checked.** Had he said *10 September*, the 00:52:38 lie would have shipped with the guard standing aside. The cause was a vocabulary race: `stativeDone` fires on "logged as assistant payment" and not on "logged 1 September as a payment". The check now rides the trigger, so which door a true sentence takes no longer depends on word order.

2. **What arms it is the FIGURE, not the block's presence (c-42.30).** The first cut armed on `expenseFacts` being present — and derived at the tree, **that is every vendor turn**: `fetchExpenseFacts` runs unconditionally at both PWA doors and `buildExpenseFacts` unconditionally in the WA lane. Built that way, a relay draft quoting "Rs 80k by 13 August" and a lead board reading "Rs 4,50,000 budget, filed August 5th" both convicted on real dates — the 33 rows again. **One spoken figure held by the expense handles** means the sentence is about an expense row; then its dates are the expense book's business. `MD1` is that mutation and it falls.

3. **F-42.151 — a phone number is not a rupee figure.** Seen in `spoken_figures` at 04:44:48: `["918595986978","80,000","42,000"]`. Twelve digits, so the bare branch took the vendor's own lead phone as money. Noise in the third door; **a false conviction in the money-state door**, because no phone is a held amount — `Priya Nair owes Rs 42,000 — reach her on +918595986978` is true and would have been destroyed. Two discriminators already in the text: a `+` immediately before the run, or ten-plus digits with no grouping (the estate renders every figure through `rupees()`, which groups above three). Grouped long figures and the Rs-prefixed branch are untouched.

4. **F-42.152 — DECLARED GAP, BENCHED AS ONE.** A claim built **entirely** of unheld material — "Rs 7,200 on 3 September", no held figure to arm the check — lands `unfenced`. Closing it by convicting any unheld figure beside a date would convict the relay draft, which is the 33 rows. The hard case is the next guard sitting's, with F-42.147's own sibling class. **The cell for it asserts `unfenced` and will go RED when it is cured — that is the point of it.**

5. **THE PATTERN ACROSS r1, r2 AND r3, AND IT IS THE SEAT'S.** Every one of these was found by the founder's walk within minutes of a green bench, and every one was a phrasing the seat had not driven: r1 shipped a fence unreachable by the word "logged"; r2's `§24` cell used the 03:31:35 phrasing and the walk changed the words the next morning; r3's first cut of F-42.147 armed on a presence test that is always true. **A bench written from bytes the seat chose measures the seat, not the estate.** The cells here are driven off `engine.messages` and `engine.evals_runs` production rows; the standing correction is that a cure to a phrasing-sensitive surface must be benched against a *class* of phrasings, and the estate has no rig for that yet. That is worth a charter.

**Open:** F-42.152 and F-42.147's "Already logged" sibling class → next guard sitting. F-42.133 (register breach, "10k" on the wire) and F-42.120 (clock times) → soul sitting, W-1. F-40.2's 00:53:25 → Block 09. F-42.146's three ungrounded planes — relay draft body, lead budgets, `engine.records` — → later charter. F-42.124's two specimens and c-42.25 → runner charter.
