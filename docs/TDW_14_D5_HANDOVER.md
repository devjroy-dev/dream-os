# TDW_14 · D-5 · C-8 — THE WELCOME · HANDOVER
(LE seat, 2026-08-14. Bases: dream-os `1441c82` · pwa `ac226a2`,
both fetched at my own fresh sibling clones. Executor git read-only
per R-33.7 — nothing here is a landing claim; the founder's push
output is.)

D-5 is Block 14's last door. Three pieces: the manifest, the success
step, the welcome copy. Two ZIPs. DDL none. W-1 shut — no soul byte,
no prompt byte, no new send.

---

## 1 · THE ARITHMETIC — 62 raw bytes → 34 sheet lines → 9 server homes

The inventory opened at **62 bytes a member can read from tap to
coplanner**. Two principles cut it before a line was written:

**One byte per CONDITION, not per call site.** The sheet organised by
meaning, so five sentences for "the link doesn't resolve" became one
line recommending one byte and naming the five homes that collapse
into it.

**Reachability gates the polish (R-33.6, applied to copy).** For each
server string the census asked whether a member can reach it through
the real surface. Eight could not: the client gates phone length at
`page.tsx:93`, fires `/accept` only on six filled digits at `:143`,
and `/set-pin` only on four at `:179`; and `/set-pin`'s user, member
and couple lookups run against a row `/accept` created seconds
earlier. Those eight collapsed to one member-safe default with the
engineer detail relocated to `console.error`.

| stage | count |
|---|---|
| raw bytes in the radius | 62 |
| sheet lines put to the founder | 34 |
| of those, expected-zeros stated on the record | 5 |
| of those, minted fresh (success step + manifest) | 9 |
| server strings before | 32 |
| server bytes after | 9 |
| server homes before → after | 24 → 9 |

**ONE RECONCILIATION, OWNED.** The 62 was one short. See §3.

---

## 2 · THE PER-SITE `{Bride}` DERIVATION — resolving to ZERO sites

The chair's templating rule: `{Bride}` may appear in a server byte
only where the row is derived to resolve at that site. Derived per
site, at the tip:

| condition | site | is her name in hand? |
|---|---|---|
| ㉖ link doesn't resolve | `join.js:92` malformed token | **no** — no row at all |
| ㉖ | `:106` no member row | **no** |
| ㉖ | `:142`, `:152` send-otp | **no** |
| ㉖ | `:204` accept, malformed | **no** |
| ㉙ expired | `:115` validate | **no** — the `couples` query sits at `:118`, AFTER this guard |
| ㉙ | `:156` send-otp | **no** — this route never queries `couples` at all; `couple_id` is in hand, the name is not |
| ㉙ | `:248` accept | **no** — fires off `claimErr.hint` with no row resolved |

**Zero name-bearing refusal sites.** ㉙ therefore takes ㉖'s nameless
shape, as does every other server byte. Speaking her name at any of
these would mean adding a `couples` hop to a failure path — a second
resolution for cosmetics, refused on the same derive-before-adding
principle that kept the welcome screen's own greeting free of a new
fetch. **The reason lives in the file** (`join.js`, the COPY block's
header), not only in this document.

The bride's name DOES appear where the data is already there: the
welcome line ② and the arrival line ㉑ both ride the `/validate`
response already in state, and the manifest rides the session the
coplanner layout already holds. No fetch was added anywhere in D-5.

---

## 3 · THE HOME CONSOLIDATION — 24 → 9, and one miss owned

| # | condition | homes before | after |
|---|---|---|---|
| ㉖ | the link doesn't resolve | 5 | 1 |
| ㉗ | already claimed | 4 | 1 |
| ㉘ | removed from the circle | 2 | 1 |
| ㉙ | expired | 3 | 1 |
| ㉚ | one number, one circle | **1 already** | 1 (untouched) |
| ㉛ | no code / expired / purpose mismatch | 3 | 1 |
| ㉜ | wrong code | 1 | 1 |
| ㉝ | the code didn't send | 2 | 1 |
| ㉞ | our fault | 4 | 1 |
| ㉟ | the unreachable class | 8 | 1 |
| | **totals** | **33 sites / 24 wordings** | **9 wordings** |

㉚ is the standard the other eight were written to: the founder's byte
of 2026-08-02, which always had one home and two callers, and which
D-5 does not touch. `b14_d5_copy_bench.js` §1.10 exists to prove that.

### THE MISS, AND ITS GENEALOGY

`'Could not send code. Try again.'` stood at **two** sites in the
pre-D-5 client: once as a dead `d.error ||` fallback and once as
`sendOtp`'s live network-failure catch. **The copy inventory counted
the string once** and folded both under the dead-fallback line, so the
live catch never reached the veto sheet.

That is the one-condition-many-wordings disease D-5 exists to cure,
surviving client-side inside a census fold — the same shape as the
server disease, produced by the census rather than by the code.

Caught by `tdw14_d5_welcome.proof.mjs` §6.2, not by re-reading. Ruled
2026-08-14: folded into ⑯, which is client-side-failure-before-the-
server-speaks and therefore the catch's own class. ⑯ now has **one
home and three callers**. Giving it ㉝'s warmer server sentence was
refused on arithmetic — that twins one string across two repos, the
exact duplicate the one-home law prevents, for a marginal gain on a
rare network blip. No new byte was minted; the founder had already
approved this one.

**Corrected inventory: 63 raw bytes, 34 sheet lines, 9 server homes.**

---

## 4 · THE FLOORS, AND ONE DECLARED GAP

**dream-os** — own runner, warm-up discarded, `--delivery` with a
declared manifest (F-14.16), both paths declared and verified unmoved
after the run:

> `21 RED` — `b05_arc_m4_bench` · `b05_arc_m6_bench` ·
> `b05_f0555_media_dedupe_bench` · `b05_p4_crons_bench` ·
> `b06_gauntlet` · `b06_meter_bench` · `b07_f0772_circle_auth_bench` ·
> `b07_f0791_guard_stack_bench` · `b07_p4b_body_bench` · `b07_p5_bench`
> · `b08_p1_lifecycle_bench` · `b08_p5_oow_relay_bench` ·
> `b08_p5_unblock_bench` · `b10_p1_search_bench` · `b10_p2_bridge_bench`
> · `b10_p3_mint_deck_bench` · `b5_wa_door_smoke` · `b5b_movementb_bench`
> · `bf1_bride_tool_fidelity_bench` · `tdw10_combined_cap_bench` ·
> `test-shape`

**Identical to base by name. Zero delta.** `run-floor.sh:196`
enumerates `ls scripts/*.js`, so `b14_d5_copy_bench.js` ran INSIDE
this floor and ran green — it is not a bench standing outside the
floor claiming its own pass.

**pwa** — own runner: the five base reds unchanged by name, plus
`tdw_f0774_vacuity_probe`, which reds on any delivery tree. That is
**F-13.15 — the refusal working, named not cured.** Not a delta.

**BENCHES**
- `b14_d5_copy_bench.js` — **38/38**, four mutations reddening, source
  restored and sha256-verified.
- `tdw14_d5_welcome.proof.mjs` — **66/66**, four mutations reddening
  across three files, each restore sha-verified.

Both mutation legs target PRODUCTION CODE and each target is unique on
the final tree. The first cut of the dream-os bench had its own
R-33.4 guard refuse two targets as non-unique — because the
consolidation is precisely what made those lines identical. The guard
catching its own author is recorded here rather than quietly fixed.

### THE DECLARED GAP — `npm run build`, UNRUN

`app/layout.tsx:2` imports four faces through `next/font/google`.
`fonts.googleapis.com` is not on the executor container's egress
allowlist, and is not on the chair's either — so **neither seat can
close this gap**, and claiming it green would be hollow.

`npx tsc --noEmit` is CLEAN on the delivery tree and is the real gate
both seats can run. **The build is declared, not claimed, and closes
at apply** — the founder's STOP-on-red verify paste runs on the one
machine with the egress.

---

## 5 · THE WALK CARD

To be walked on the founder's own handsets after his push and the
chair's origin pass. Second phone must be a number **not already
active in any circle** — ㉚ refuses it otherwise, correctly.

| # | step | what must be true |
|---|---|---|
| 1 | Mint a real invite from the bride line | link arrives on WhatsApp |
| 2 | Tap it on the second phone | ① "One moment." |
| 3 | The welcome | ② "{Bride} invited you to her wedding circle, {Name}." — HER name, not the house's |
| 4 | Phone → ⑤ "Send code" | ③ reads "Enter your phone number." and does not repeat ② |
| 5 | **⑧'s claim witnessed** | screen says "On WhatsApp, to +91 …" **and the code actually arrives in WhatsApp, not SMS** |
| 6 | PIN → ⑪ ⑫ | "…each time you open the circle" · "Setting up your circle…" |
| 7 | **⑰–㉑ — the step that did not exist** | "You're in." · "Welcome to {Bride}'s wedding circle." · the install line **matching the handset** (iOS: Share → Add to Home Screen; Android: menu → Install) |
| 8 | Perform the gesture | icon appears |
| 9 | **㉓ on glass** | the icon reads **"{Bride first name}'s Circle"** — this is C-8's whole point |
| 10 | Tap the icon | opens standalone into the coplanner, no browser chrome |
| 11 | **One refusal, live** | re-tap the spent link → ㉗ "You've already joined. Sign in with your PIN." |

**Step 9 is the one that cannot be faked by a bench.** Everything else
has a cell; the home-screen label is rendered by the operating system
from a manifest fetch we do not control, which is exactly why the
chair refused a client-minted Blob at tranche 1.

If step 7's install line shows on a desktop browser, that is a RED —
E5 rules an undetectable platform gets **no** teaching line, arrival
and CTA only.

---

## 6 · WHAT DID NOT MOVE, ON PURPOSE

- **The dressing.** Founder's 「skip it」, 2026-08-14. The join screen
  keeps its `/api/v2/landing-slides` call untouched. **F-14.18 stays
  OPEN WHOLE**, carrying the nine-caller census, and rides Row 13's
  audience work (0097's column + endpoint filter + admin picker).
  `tdw14_d5_welcome.proof.mjs` §8 guards this ruling so a future seat
  "tidying" the call meets a cell, not a comment.
- **㉚**, at its 2026-08-02 veto.
- **The invite message** (E1) — §5, no new sends.
- **The house masthead** (E2).
- **The `circle_session` consumer set** — held at FOUR by construction.
  The manifest handler reads a query parameter and never names the key,
  which is why identity rides the href rather than a header.
- **One raw hex**, labelled: the pre-load fallback ground has no token
  in the circle vocabulary, and minting one would be a second token
  home — the geometry this delivery removes.

## 7 · FILED FOR THE BAND

- **F-13.16** — `public/couple-manifest.json` committed, referenced by
  nothing. Outside D-5's radius.
- **F-13.17** — `FALLBACK_SLIDES`, one constant, three byte-identical
  homes in the couple auth lane. Outside D-5's radius.
- **F-14.18** — the member join screen serves the vendor gate's
  unscoped catalogue. Open whole, founder-skipped, Row 13's.
- **Schema witness debt** — `PUBLIC_SCHEMA.md` sits at ladder 0123
  against a ladder at 0125. Two migrations deep. Founder's leisure.
- Four handover documents state this join page as `435 lines`; it was
  454 before D-5. Prose, not cells — labelled, not obeyed.
