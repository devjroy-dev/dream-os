# TDW_06/07 · THE OOW COMPLETION — HANDOVER

**Written at the close of the sitting that mapped both out-of-window templates.**
**Base tip: `dd48506` (the CE-215 seal), fetch-first at seating, tree clean.**

**R-29.34 APPLIES TO THIS DOCUMENT.** Every claim names where it can be
re-derived. Nothing here is a memory. **Trust evidence over narrative, including
this document.**

---

## §1 · WHAT SHIPPED

**M1 — THE ENQUIRY ALERT ASKS THE WINDOW FIRST AND CARRIES HER WORDS.**
`src/lib/vendor/enquiryAlert.js` rewritten whole. `vendorWindowOpen` decides
before any attempt; in-window takes the free-form arm exactly as before;
out-of-window sends `tdw_enquiry_brief_vendor`, whose `{{3}}` is **her own
sentence** — scrubbed, newline-collapsed, truncated at 400 with an ellipsis. The
131047 catch, `isWindowClosed` and `WINDOW_CLOSED_CODE` are **retired whole**.

**M2 — THE OOW FORK GAINS ITS CONTENT LEG.** `sendContentTemplate` +
`contentFits` in `relayToCouple.js`; the fork in `relaySeat.js` asks the content
arm **first**, the doorbell second, byte ④ third. A fitting draft rides
`tdw_enquiry_reply_couple` with `{{3}} === body` byte-exact, the draft stamps
`sent` with `content:<wamid>`, ③ speaks, and her thread holds the bytes she
received under `sent_by='vendor_relay'` so №14 can fire.

**M3 — №16's CRON.** `relayExpirySweep` in `relaySeat.js`, reader
`doorbellExpiredUnanswered` in `coupleDrafts.js`, scheduled `'5 * * * *'`
Asia/Kolkata in `src/cron.js` beside its siblings.

**M4 — THE SID AT THE ALERT PATH'S OWN SITE.** The brief writes its own
`vendor_self` row carrying the wamid, `[enquiry_brief] <meta name>`, the
`ringDoorbell` precedent exact.

**FLOOR AT DELIVERY** — chair-run at this tip, after `npm run build`:

| bench | count |
|---|---|
| `b0607_oow_completion_bench` | **74/74 (new)** |
| `b06_bride_arrival_bench` | 103/103 |
| `b06_relay_hand_bench` | 126/126 |
| `b06_relay_foundations_bench` | 42/42 |
| `b06_f0613_relay_bench` | 40/40 |
| `b06_forkc_wireguard_bench` | 113/113 |
| `b07_p5_bench` | **114/114 — was 113/114 at `dd48506`** |
| tier 81 · billing 53 · micro 23 · selfserve 30 · combined_cap 37 | unmoved |
| `npm run build` (= `build:engine`, strict TRUE) | **EXIT 0** |

---

## §2 · THE THREE CHARTER CORRECTIONS, AND THE ONE THAT CONVICTED THE SEAT

**① §0.2(a) — `{{3}}` had no raw material.** The alert door never received her
sentence. Granted; three spans in `vendorInbound.js` carry `brideMessage`, the
same variable already handed to `scrubModelFrame` on the adjacent line.

**② §0.2(b) — M4 had no row.** The notification row is written by `engine.js`,
sid-less, on a read-only surface. Arm 3 ruled: the brief writes its own.
**The in-window leg's sid gap remains open and is the hygiene micro's.**

**③ §0.2(d) — FORK 5's second limb could not execute as worded.**
`enquiry_alert_vendor` has a **second live reader** at
`src/api/couple/enquire.js` (the PWA enquiry path's F-07.40 fallback) and a
sealed cell at `b07_p5_bench` §7.6. Only the `OOW_REGISTRY` entry retired; the
`templates.js` entry stands.

**④ CHAIR CORRECTION №14 WAS ITSELF OVER-STATED, AND THE SEAT WITHDRAWS IT IN
PART.** The read-first was filed before the read ladder was complete, disclosed
as such. Completing it found `FINDINGS_LOG.md:4272` (CE-212 §②): *two vendor
notifications took a wamid and then reported `status=failed` out-of-window on
2026-08-08.* That is the 200-then-webhook witness the seat asserted did not
exist. **What stands:** the eleven `matched=0` receipts prove sid-loss and
nothing about window state — the charter cited the wrong witness. **What falls:**
"the catch's unreachability is genuinely unwitnessed." It is witnessed, and
F-06.140's own register entry says so in its last clause. The seat quoted a
finding's headline without reading its evidence — the §0.1 family, committed
against the register that names it. **Zero bytes moved; Fork 5 is strengthened.**

---

## §3 · FINDINGS PROPOSED

**F-06.191 — A DISCLOSED GAP OUTLIVED ITS OWN CLOSURE AND THE TREE KEPT
ASSERTING IT.** `enquiryAlert.js`'s R-R5 block (written 2026-08-05) read *"No
live witness exists for which path fires on THIS lane."* CE-212 §② produced that
witness on 2026-08-08 and the sentence never moved — an F-06.85 conditional
outliving its condition, load-bearing enough that a fresh seat quoted it to the
chair as current truth and moved a ruling with it. **Retired free with the catch;
the class is the finding.**

**F-06.192 — A PRODUCTION MUTATION THAT DOES NOT STRIP COMMENTS CAN BE ABSORBED
BY PROSE.** `b06_bride_arrival_bench` §A1.12 mutates a `.like(...)` literal in
`coupleDrafts.js`; `String.replace` defaces only the **first** occurrence. This
sitting's sweep reader shadowed it, and then the comment written to *explain* the
collision shadowed it again by quoting the anchor. The estate already holds
*"strip comment lines before asserting about code"* for ASSERTIONS; this proves
the same law binds MUTATIONS. **Half-cured:** the new file's own
`underMutation` (`b0607_oow_completion_bench.js`) is comment-blind, and
`DOORBELL_REASON_PREFIX` keeps the sealed anchor unique. **The other half —
teaching `underMutation` in the sealed benches to strip comments — is the
successor's, and it is the half that retires the class.**

**F-07.40's INVENTORY CELL WAS RED AT `dd48506`, BEFORE THIS SITTING.**
Re-derived by stashing the diff: 113/114 both ways. `b07_p5_bench` §7.8 asserted
*"exactly one approved vendor-line template"* and had drifted past
`vendor_welcome`, `enquiry_update_couple` and three AUTHENTICATION keys — five
lawful additions, every one of them reddening a cell that was counting a state.
Repaired here under *"the sitting that moves a subject owns the benches that read
it."* **It now ENUMERATES.** `b07_p5_bench` is not on any charter floor, which is
why five additions went unnoticed; **it should be added to one.**

---

## §4 · DECLARED, RATIFY-OR-REVERT

1. **THE UNDETERMINED-WINDOW ARM AT THE ALERT DOOR.** The charter ruled a binary;
   `vendorWindowOpen` speaks three verdicts. The door treats `open !== true` as
   out-of-window because its fallback is an **approved UTILITY template, lawful
   in both window states** — unlike `relayToCouple`, whose arm is a free-form
   send Meta rejects outright on a shut window. Named as an inference in-file
   with its one-line revert. **`relaySeat`'s fork is unchanged and still refuses
   on undetermined (⑤).**
2. **§0.2(c) — `coupleArrival.js` touched, out of the stated radius.** One
   function, `doorbellRouteFor`, gains the content-send fallback. Fork 3(a) has
   no other executable siting. Deleting the block restores prior behaviour.
3. **`b06_relay_hand_bench` §12/§13 — six cells repointed** from `closedWorld()`
   to `bellWorld()` (an unfitting body). Four were red; **three (§12.3, §12.4,
   §12.5) were GREEN while asserting against the content send under names that
   say doorbell.** Count preserved 126→126; the doorbell is now additionally
   proven reachable *beneath* the new arm.
4. **§13.1 renamed TWO→THREE contracts.** `sendWa`'s template path is a
   composite — free-form vocabulary outside, template vocabulary at `.result` —
   and `enquiryAlert` is its first caller needing the id. Reading `out.sid` there
   would have been walk seven, one lane over.
5. **The sweep's silent arm** — `expired_after_reply:<sid>`, derived. A row can
   be doorbelled-and-expired while she *did* write; №16 must not tell a vendor
   she ignored him when she did not.

---

## §5 · WALK TWELVE — AUTHORED **FROM THE FOUNDER'S FIXTURE PASTE**, NOT BEFORE

**THE ARMING FLIP IS AN EXPLICIT FOUNDER STEP.**
`vendor.enquiry_alert_oow_enabled` ships `false` and `readLaneFlag` fails closed
on an absent key. **Without the flip, LEG 2 shows nothing and reads as a build
failure.** Push is not speak (F-08.56).

**LEG 1 (M2)** — an OOW bride, a fitting approved draft → the content template
lands on her handset, his words inside Meta's frame, no doorbell → ③ then №14 on
his handset → her reply routes clean, no question.
**LEG 2 (M1)** — a fresh enquiry against an OOW vendor → the brief **with her
details**, the first full-content enquiry alert in the estate's history.
**LEG 3 (M3)** — a doorbell left unanswered past expiry → №16.
**LEG 3 IS CLOCK-DEPENDENT AND MAY TRAIL DELIVERY BY UP TO 24h. Declared, never
rushed.** The sweep runs hourly at :05 IST, so a notice can trail its expiry by
up to 59 minutes by design.

The five fixture SELECTs are in the delivery cover. **Run them first.**

---

## §6 · WHAT THE NEXT SITTING PICKS UP

- **THE HYGIENE MICRO** — F-06.187's write guard + **F-06.143 limb 1 at
  `engine.js:624-630`** (the in-window alert leg's sid-less row, still open).
  Engine radius, fresh seat. **Named in `enquiryAlert.js` at its own mechanism.**
- **F-06.192's other half** — comment-blind `underMutation` in the sealed benches.
- **`leads.intent_summary` as `{{3}}`** — chartered as a note, engine radius.
  Reachable only inside the engine; does not travel on the turn's return object.
- **`b07_p5_bench` onto a charter floor** — five lawful additions went unseen.
- **An alert-receipt class** — the brief's sid is on file under `sent_by='system'`
  deliberately, so `relayReceipt` stays silent rather than telling a vendor his
  own number received his own alert. Not foreclosed.
- **`PUBLIC_SCHEMA.md` regeneration** for `pending_couple_drafts` (still absent;
  this sitting's SQL provenance rests on 0117/0118's DDL).
- **F-09.164/.169/.170 · F-10.105/.107 · F-06.13 · F6** — untouched here.

**No migration was needed. 0119 remains next-free.**

---

## §7 · THE SEAT'S CONDUCT RECORD, BOTH DIRECTIONS

**Self-caught and disclosed at discovery:** a read-first filed before the read
ladder finished (disclosed as it was done, and it cost a partly-wrong chair
correction) · a sweep reader that shadowed a sealed cell's mutation anchor · the
comment written to explain that collision shadowing it a second time · three
`relay_hand` cells left green over a path they do not name · a marker row
carrying the registry key where the doorbell precedent carries the Meta name.

**Chair-caught:** a described deliverable handed over as a report with no ZIP —
the same defect the bride's-arrival handover records as founder-caught at walk
nine. **A deliverable that is described is not delivered.**

**Every failure this sitting found was on the safe side. Nothing unearned
reached a bride or a vendor. The window-first doctrine and the fit test both
refuse toward silence.**

**Trust evidence over narrative. Including this document.**
