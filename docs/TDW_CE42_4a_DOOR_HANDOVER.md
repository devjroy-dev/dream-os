CE-42 · 4a DOOR RIDER · R9-J1 (dream-os, base b3b5995) · [CE-42 LE-E2]

1 · WHY THIS RIDER EXISTS — e-5, MINE. 4a shipped the arm, 0161, the router arm and
    99 green cells, and NOTHING THAT COULD REACH ANY OF IT. On 2026-09-10 DEV440 asked
    Victor to send his page to a number he had met and the ask went to `donna_lead`,
    because `donna_lead` was the only hand in the bag that could take a number and a
    note. I checked forty ways whether the arm was correct and never once whether
    anything called it. c-42.13 is the chair's half; this is mine.

2 · WHAT SHIPPED. `src/engine/src/core/tools/introduce.ts` — the two hands, pure,
    `{ display }` only, relayCouple's shape exactly, with the chair's pronoun-free
    descriptions vetoed 2026-09-10. `donna.ts` — import, `...INTRODUCTION_TOOLS` in the
    bag, and a SEPARATE branch beside the relay one (a widened set would make one family
    answer for two motions to two kinds of recipient). `introductionSeat.js` — the
    door's half, relaySeat's shape. `vendorInbound.js` — the call at the pre-delivery
    seam in its own try, and the door line replacing the model's prose on an acted turn.

3 · WHAT IT DOES ON THE GLASS. The plane is still DARK behind
    `template.tdw_introduction`. With it On: DEV440 asks, Victor reaches
    `donna_introduction_stage`, a missing slot answers with its founder-vetoed ask, the
    filled body is shown in relaySeat's own SHOW frame, a bare "yes" is refused by E3,
    and a naming affirmative sends the page button to `thedreamwedding.in/v/DEV440`.
    Off, the row is filed `dark` and Railway logs NOT SENT.

4 · WHAT IS NOT PROVEN, AND WHAT IS OWED. NO BENCH CAN PROVE THE MODEL PICKS THE TOOL —
    only the handset can, which is why row J1 closes on the walk. The full floor did not
    run here; the founder's `--delivery` run is the record. Still owed: F-42.69, there is
    no INBOUND arm, so a reply becomes no lead and the walk still ends at the receipt;
    the pwa cut (switchboardCopy.ts:91, the Leads source map) held for after D5a; and a
    vetoed byte for the seat's `no_staged_row` moment, which today says nothing at all.

5 · THE HASH. Base b3b59956b3d8aaadedc42c8016a5ccad4ca0434e — cut at c1c17db and
    RE-PINNED after D5 by blob hash, path by path: all four modified files unmoved
    (b06_forkc 0b1c2bc5, b68 c5945b59, donna.ts 03578e33, vendorInbound 50f70866), so
    nothing was rebuilt and only the base line changed. Benches RE-RUN at b3b5995, not
    carried. npm run build exit 0 ·
    b68 125/125 (was 99/99, +26) · b06_forkc 113/113 (§5.8d's census 5 -> 6, named not
    loosened) · b06_relay exit 0 · b40 113/113 · b64 134/134 · b55 exit 0 · b20_a2 180/180 (D5's own,
    unmoved by this rider). Five door mutations, each red at the c1c17db cut.
