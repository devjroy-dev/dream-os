# TDW · CE-42 · R8-1 — the demand pulse gets a source (dream-os @ `fd9d0da45d4020699a033047eb2821f223fe7de9`)

*Re-cut from the `3fa603a4` packet under R-40.54. `3fa603a4` is an ancestor of this tip (`git merge-base --is-ancestor`, verified) and the only path between them is `docs/filings/GOOGLE_API_DOMAIN_AND_SEARCH_CONSOLE.md`. The three delivered files are byte-identical to the first cut; this base line is the one byte that moved, and R-38.15 is why it had to.*

1. **Shipped:** migration `0160` (`public.date_checks` + `date_checks_vendor_checked_idx` + `hot_dates.source`), the sole writer in `src/api/public/availability.js` placed after the last gate, and bench `b67` (27 cells).
2. **Proven:** `b67` GREEN 27/27 and `b58` GREEN 33/33, both re-run at this tip. `b67` RED under six production-code mutations — call deleted (7 red), call moved above the R-40.78 gate (2), above the R-40.77 consent gate (3), `try/catch` dropped (1), an `ip` added to the payload (1), the index reverted to `(vendor_id, date)` (1).
3. **Rulings built:** O-1 one call site after `:168` — a resolved vendor past every gate · O-2 `(vendor_id, checked_at desc)`, no predicate · O-3 awaited, caught, never touches the status code or the word · O-4 no CHECK on `source` · R-40.118 stated in `0160`'s header, past dates served, off-TDW unknowable.
4. **Filed:** F-42.47 `describeDate` has no falsy return (`occupancy.js` :1110/:1128/:1136/:1137/:1148/:1158/:1161/:1172), so `availability.js`'s `if (!out)` is unreachable and O-1's `:171` clause has no live effect · F-42.48 `verify_failed` wears `occupancy:'off'` and is answered as a 404 miss, defeating the door's own `:146-150` law for every failure inside the checker — a stranger-facing change, carried to R8-2's read-first with the pulse sentence, not cured here · F-42.51 the pulse's rows are checks, not people. Each is pinned by a cell that reddens on its cure.
5. **Floor:** re-derived at this cut, not quoted. The base's 23 are all present and unchanged, the SET is identical to the `3fa603a4` run (no bench changed places), and neither `b67` nor `b58` is in the failing set. This container names 61 because of 38 precondition failures — `.env` absent, `SUPABASE_URL` unset, sibling present at `cc109bd7` with its `node_modules` absent (F-38.34's named liar). **`FLOOR = 23` is claimed for the base set only; the whole floor was not re-derived here** and the founder's single-invocation `run-floor.sh --delivery <manifest> --check` is the record.
