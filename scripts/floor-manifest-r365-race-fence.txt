# scripts/floor-manifest-r365-race-fence.txt
# R-36.5 · THE RACE FENCE — this delivery's own file table, declared for
# [F-14.16]'s --delivery mode. One byte of dirt outside this set and the runner
# refuses; the set is tolerated, the contents are hashed and must not move.

# ── the cures ────────────────────────────────────────────────────────────────
src/api/middleware/agentBridge.js
src/engine/src/core/signup.ts
src/lib/vendorInbound.js

# ── the arbiter (founder-run; travels in the ZIP as the ladder record) ───────
db/migrations/0129_agents_user_id_unique.sql

# ── the bench + this manifest + the handover ─────────────────────────────────
scripts/b05_f0583_race_bench.js
scripts/floor-manifest-r365-race-fence.txt
docs/specs/TDW_R365_RACE_FENCE_HANDOVER.md
