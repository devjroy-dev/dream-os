# scripts/floor-manifest-g12-spine.txt
# TDW_19 · BLOCK 19 · G1.2 — F-40.64 CLOSED, AND IT WAS TWO FINDINGS.
#
# F-14.16: a dirty file outside this list is a STOP until accounted for.
#
# ⚠ F-40.64 WAS CARRIED AS ONE INHERITED RED AND IT WAS TWO, ONE OF THEM MINE.
#   · §2.2/§2.3 — `src/lib/vendor/weddings.js` read `public.engagements`
#     directly. The G1.1c seat's, red since that arc, correctly filed inherited.
#   · §1.11 — the OUT_OF_ORDER register no longer holds 0090's row. THAT WAS ME:
#     item 0's PAIR regen (R-G12.1, banked `d91ec6e`) emptied it, because
#     removing the row IS the cure for F-40.58 — the formatter renders the record
#     verbatim into the header, so a regen alone would re-render the same denial
#     forever. This red joined LATER and was carried in every floor since as
#     though it were the same inherited one. It was not.
# Named so no future reader is told a two-cause delta was a single finding.

# ── THE PLANE'S ONE READER  [§2.2/§2.3] ─────────────────────────────────────
# `coupleIdForLead` moves the spine read into the home the bench guards. Those
# cells WALK THE SOURCE TREE rather than read a list of consumers someone wrote
# down, because a list someone wrote down is the defect (R-31.1's habit).
#
# The `vendor_id` scope moves ACROSS WITH THE QUERY rather than being left for
# the next caller to re-derive: it is load-bearing, not defensive — the fixture
# couple holds three engagements, two of them `photography`, so a match on lead
# alone is ambiguous on real data today. F-40.60's declared miss travels with it,
# ruled DORMANT by R-G12.14 after the census returned booking_only = 0.
src/lib/engagements.js
src/lib/vendor/weddings.js

# ── THE SEALED CELL, AMENDED  [§1.11, ratify-or-revert] ─────────────────────
# WAS: `register.length === 1 && register[0].number === 90`, under "0090's row
# stands, ITS DEBT UNPAID". The debt is paid: the snapshot now describes
# `public.engagements` at :510 with eleven columns, so the row that existed to
# say "the snapshot denies this table" describes nothing.
# THE PROPERTY IS UNCHANGED and asserted more strictly — 0127 still takes no
# register row, which the bench's own ledger at :89-91 states was always the
# subject ("1.11 watches the REGISTER, not the ADD"). What moves is the expected
# CONTENTS: from one paid debt to none outstanding.
# Both ways, run not assumed: amended bench against UNCURED code reds on exactly
# 2.2 and 2.3 (36/38); cured, 38/38; restore byte-identical by sha256.
scripts/b16_p1_engagements_bench.js

# ── THIS MANIFEST ───────────────────────────────────────────────────────────
scripts/floor-manifest-g12-spine.txt
