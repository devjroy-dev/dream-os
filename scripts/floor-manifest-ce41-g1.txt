CE-41 · SEAT G · G1 (dream-os) · FLOOR MANIFEST
base: b5052382b7bfee93ba4277bafd50fc1b941a4427   (RE-PINNED from c52e92a)
      Seat F's F1b landed mid-sitting. Re-derived by command
      (`git fetch -q origin && git rev-parse origin/main`, 2026-09-09), the
      worktree re-pinned, the three overlapping files resolved by hand against
      F1b's own diff, and the whole floor + the uncured measurement re-run here.

⚠ THE MIGRATION IS 0154, NOT 0155 — DERIVED, NOT TAKEN ON INSTRUCTION.
      The re-pin note allocated 0155 on the expectation that F1b took 0154.
      `git ls-tree --name-only b505238 db/migrations/` puts the ladder tail at
      0153_wa_vendor_route_seed.sql: F1b shipped NO migration (its diff is seven
      files, none under db/). `OUT_OF_ORDER.json`'s register is empty and no 0154
      exists anywhere in the tree. LD-8 is that the number is derived at the cut
      and never claimed from memory, and that applies to a number arriving in a
      kickoff exactly as it applies to one arriving in a seat's own head. Filed
      as a chair correction rather than silently obeyed: shipping 0155 would
      leave a permanent hole at 0154 that every later reader must derive.
run:  the seat's container, node v22, after `npm install` + `npm run build:engine`
      (the build is now a stated precondition — ruling 3 of the re-pin note)

FLOOR = NAMED BASE, no delta.

b65_g1_wa_advisor_off_bench     GREEN   29/29    NEW (this packet)
b65_mutations                   GREEN   21/21    NEW (this packet)
b63_f1_model_routes_bench       GREEN   57/57    was 57/57 at F1b — one fixture re-sited (c-41.49)
b63_mutations                   GREEN   30/30    was 30/30 at F1b — M14 anchor re-derived (c-41.46)
b61_switchboard_bench           exit 0
b64_template_slots_bench        GREEN 108/108
b64_mutations                   GREEN   7/7
b20_a2_assistance_bench         GREEN 126/126
b62_mutations                   GREEN  18/18
b62_g34_s2_bench                GREEN  42/42

COUNT MOVEMENTS, DISCLOSED NOT ABSORBED:
  b65             — -> 29     (new bench, seven sections)
  b65_mutations   — -> 21     (new harness)
  b63_mutations  30 -> 30     (no movement; M14's anchor re-derived — see below)
  b63            57 -> 57     (no movement; one fixture re-sited — c-41.49, below)
  every other count unchanged. F1b's own movements (b63 40->57, its mutations
  20->30) are seat F's and are inherited here, not this seat's.

c-41.46 · b63_mutations M14, ANCHOR RE-DERIVED NOT RETIRED:
  R-41.104 gives `vendorLanes` a per-surface advisor arm, so the single line M14
  pinned (`return [...CANON_TIERS, 'advisor'].map(`) no longer exists. Left alone
  the mutation reported `??  target matched 0 times` — a DEAD ANCHOR, which is
  neither a pass nor a proof and is F-41.65's class one bench over. Re-pinned to
  the new `const tiers = …` line, same subject (transcription must red b63's
  derivation cell), re-derived by command against the cured tree. Verified: the
  mutation reds b63's own cell, 20/20 restored.

c-41.49 · b63 §9 — F-41.96's FIXTURE RE-SITED, THE SUBJECT UNTOUCHED:
  F1b's cell 'she gets a transport OBJECT of her own, named anthropic' drove the
  founder's 04:37 state through `surface: 'wa_vendor'` and reached the split via
  the WhatsApp lane's ADVISOR tier — the route read `victor_mode`, got `advisor`,
  missed `model.wa_vendor.advisor` and borrowed the pwa twin the fixture seeds.
  R-41.104 removes that path, so the fixture resolved `wa_vendor.essential` ->
  `pwa_vendor.essential` -> the DEFAULTS matrix, where Victor is anthropic and
  there is no split at all. `donnaTransport` was absent because NO DONNA WAS EVER
  ROUTED, not because F-41.96 regressed.
  Re-sited to `surface: 'pwa_vendor'` — the same row, the same state, the lane
  R-41.104 §4(d) leaves the room on. VERIFIED BOTH WAYS BY COMMAND: the re-sited
  cell greens 57/57 at the UNCURED F1b tree (worktree at b505238, engine rebuilt
  there) as well as at the cured tree, so the correction does not weaken it. The
  sibling cell (essential tier, business room) never used the advisor lane and is
  untouched.

c-41.47 / c-41.48 · the two vacuous cells, numbered at the chair's allocation and
  carried at their own sites in the bench (§1.1 and §3.2). Detail below.

BOTH WAYS, BY COMMAND (worktree at b505238, engine rebuilt there):
  b65 cured    GREEN  29/29
  b65 uncured  RED    17/29
     RED cells: 1.2 · 1.3 · 2.1 · 2.2 · 2.3 · 2.4 · 2.6 · 3.1 · 3.2 · 3.3 · 4.1 · 4.2

  ⚠ §7 IS GREEN AT BOTH TREES, BY CONSTRUCTION. Declared, not absorbed. §7 drives
    `wireGuardClassify` — untouched by this packet — against two hand ledgers, so
    it measures F-41.95's MECHANISM and not the fix. The cure's proof is §2: with
    the door handing the engine `business` at both runTurn sites, the advisory
    ledger of §7.1 can no longer be produced on this lane. Stated in the bench's
    own header so a later reader cannot mistake it for a both-ways cell.

  ⚠ TWO CELLS WERE VACUOUS ON FIRST CUT AND WERE RE-CUT, NOT SHIPPED:
    · §1.1 (c-41.47) asserted `provider === 'deepseek'` for the PWA advisor route. M11
      (retire the advisor tier) PASSED it — with the tier slot gone the lookup
      falls to `model.pwa_vendor.essential`, which is deepseek in the DEFAULTS
      matrix too. Same answer, different question. Re-cut to assert the KEY that
      was asked for, via the double's read log.
    · §3.2 (c-41.48) pinned `= 'business'`. M12 writes `? 'business'` — a ternary arm, the
      obvious way to spell it wrong — and walked past. Re-cut to any literal.
    Both found by the mutation harness, which is what it is for.

MUTATIONS — 21/21, each reddening its NAMED cell:
  M1/M2  the engine reads the row alone · the `??` the wrong way round
  M3     the type widens to admit 'advisor' from a door (F-40.3's shape)
  M4     the STAGE-2 RETRY re-enters the advisory room mid-turn
  M5     the door hands no room at all
  M6     the PWA door starts forcing the room too
  M8     R-41.105's witness line dropped
  M9/M10 the WA route reads the column · asks and discards the answer
  M11    the PWA route loses its advisor tier
  M12/M13/M14  two homes for the rule · waLaneMode grows a switch · the deps bag
  M15/M16 the advisor lane returns to wa_vendor · leaves pwa_vendor
  M17/M18 0154 by pattern · 0154 reaching the app lane
  M19/M20 the advisor word stops being refused · `business` stops being the way home
  M21    the room cured by WRITING the column (F-40.3 under a new number)
  M22    victorLines' load-time hash guard disarmed

  The engine mutations (M1, M2, M3, M8) edit the TypeScript and do NOT rebuild.
  Disclosed in the harness header: the cells they name read the SOURCE, which is
  where the ruled bytes live. §2.3 is the only cell reading `dist` and is named
  by no mutation — it is a regression witness on the compiled artefact.

CONTAINER GAP FROM D0b — CLOSED, AND IT WAS NOT A GAP:
  D0b's manifest declared `b63_f1_model_routes_bench exit 1 on Cannot find module
  '../../engine/dist/core/loop'`. It is `npm run build:engine` not having been
  run in that container. Built here; b63 runs GREEN 40/40 at the base and at the
  cured tree. Nothing to fix in b63 — recording it so the next seat does not
  re-file it as a defect.

MIGRATION: 0154_wa_vendor_advisor_drop.sql. Ladder tail at the re-pinned cut is
  0153 — seat F's 0153, not F1b's, which shipped none. Derived by
  `git ls-tree --name-only b505238 db/migrations/`. See the base note above.

INHERITED RED, NOT THIS SEAT'S, NOT TOUCHED:
  b39_telemetry_bench   REDS 3.1, 3.2, 3.3, 3.4, 5.3        F-41.64, seat C
  b61_mutations         M8 dead anchor (subject woke)        F-41.65, seat C
  Not run in this floor; carried forward from D0b's manifest unchanged.
