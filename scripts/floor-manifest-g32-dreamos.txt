# scripts/floor-manifest-g32-dreamos.txt
# TDW_19 · BLOCK 19 · G3.2 — THE dream-os HALF. The instrument becomes a product:
# 0138's three planes, the pure v3 renderer, the fill/preview/send doors, the
# couple's sign leaf, the date lock, F-40.112's cure, the eighth template dark.
#
# F-14.16: a dirty file outside this list is a STOP until accounted for. Every file
# below was authored or edited by THIS seat in THIS sitting; nothing inherited. The
# tree was clean at 3efa47c before a byte moved, proven by `git status --porcelain |
# wc -l` returning 0 immediately after `git checkout -q origin/main`, and 3efa47c is
# the tip the chair named and this seat re-derived at the cut.
#
# ⚠ THE TIP DID NOT MOVE UNDER THIS SEAT. `git fetch -q origin` was run before the
# read-first, before the mock and before this build; origin/main has read 3efa47c
# throughout. The pwa tip DID move (cb724b6 -> 6d014ea -> 1fe47b0) and none of it is
# in this repo.
#
# 0138_contract_fill_and_sign.sql is FOUNDER-RUN in the Supabase editor BEFORE this
# ZIP is applied; it is listed because it is dirt in this tree, not because the floor
# executes it. No bench in this delivery touches a live database.
#
# ⚠ ONE FILE IS EDITED THAT NO OTHER G3.2 FILE OWNS: src/lib/templates.js. Two
# entries are added (contract_sign, contract_sign_otp) and NOT ONE EXISTING BYTE
# MOVES — proven by `git diff src/lib/templates.js` being additions only.

db/migrations/0138_contract_fill_and_sign.sql
docs/TDW_19_G32_HANDOVER.md
scripts/b56_contract_bench.js
scripts/floor-manifest-g32-dreamos.txt
src/api/router.js
src/api/sign.js
src/api/vendor/contracts.js
src/lib/contractPdf.js
src/lib/templates.js
src/lib/vendor/contractSource.js
src/lib/vendor/contracts.js
src/lib/vendor/dateLock.js

# ── THE FLOOR, DERIVED TWICE ────────────────────────────────────────────────
# Run: every `scripts/b*_*.js`, 90s timeout each, on the clean committed tree with
# the sibling present.
#
#   THIS TREE      GREEN 110  RED 49  TOTAL 159
#   NAMED BASE     GREEN 109  RED 49  TOTAL 158   (3efa47c, this work stashed -u)
#
# DELTA = ZERO. `comm -23` over the two sorted red lists returns nothing. The count
# differs by one file because b56 did not exist at the base; it is GREEN here.
#
# ⚠ THE DELTA WAS NOT ZERO ON THE FIRST DERIVATION, AND THAT IS RECORDED RATHER THAN
# SMOOTHED. Two benches reddened on this work:
#
#   b56_contract_bench      — the seat's own, red on F-40.118 (see below)
#   b53_g11_wedding_pages   — G1.1's, red on the SAME defect through the registry-wide
#                             cell `no registered body opens or closes on a variable`
#
# Both were ONE defect: T1 opened on `{{1}}`, which Meta refuses (F-40.91). The seat
# carried the red loudly rather than papering it, filed **F-40.118**, and the founder
# re-vetoed the body under **R-40.55**. b53 is a sibling's bench and it was right to
# red; c-40.29 is the chair's own note that the veto was given against a rule the
# estate already held a cell for. Both are green now, and the delta is zero.
#
# 49 RED AT BOTH TREES. Inherited, unchanged, and not this delivery's — the same set
# the base carries, file for file.
#
# ── b56, BOTH WAYS ──────────────────────────────────────────────────────────
# 99/99 cells green. SIX PRODUCTION MUTATIONS driven and reverted, each a real edit
# to a shipped file:
#   §1  Math.round -> Math.floor in deriveMoney              → 2 red
#   §2  drop the `signature.verified_at` gate in contractPdf → 2 red
#   §3  isLocked ignores `state !== 'signed'`                → 3 red
#   §4  drop `.is('storage_path', null)` from cleanupDraft   → 4 red   (F-40.112)
#   §6  verifySignCode resets otp_attempts on a wrong try    → 2 red
#   §6b findSigningByToken ignores token expiry              → 4 red
#
# ⚠ TWO CELLS WERE VACUOUS ON THE FIRST DRIVE AND ARE RECORDED, NOT QUIETLY FIXED.
# §6's count cell asserted `=== 1` after ONE wrong answer, which a mutation setting
# `next = 1` satisfied; §6b had no expiry cell at all. Both were caught by DRIVING the
# mutations rather than by reading the assertions, and a second wrong answer plus a
# whole §6b are what now catch them. A cell that cannot go red is a cell that proves
# nothing, and two of these could not until they were driven.

# ── RIDER 1 (2026-09-06, base 390c144) · BOTH TEMPLATES ACTIVE ──────────────
# Three files, all already in the list above. No new file, no schema byte, no door.
#
#   src/lib/templates.js        the eighth Utility's THIRD body (the bytes Meta holds),
#                               status -> 'approved', meta_id 1599338985536926;
#                               contract_sign_otp re-pointed to its own name
#                               (R-G32.9(ii) landed the same night as (i));
#                               the stale AUTHENTICATION flow paragraph replaced
#   src/lib/vendor/contracts.js OTP_TTL_MS 10 -> 5 minutes
#   scripts/b56_contract_bench  §10 re-authored against the post-approval truth
#
# ⚠ THE BODY WENT THROUGH THREE CUTS AND EACH WAS REFUSED BY A DIFFERENT AUTHORITY.
#   1 · opened on {{1}} — refused by META'S RULE (F-40.91), caught by b53, a SIBLING
#       seat's bench, and carried as a loud red until the founder re-vetoed (R-40.55).
#   2 · R-40.55's — refused by META'S PRE-SUBMISSION CLASSIFIER as Authentication, on
#       the tail `and enter the code we send you`. Authentication was not an available
#       answer: that category carries Meta's preset body.
#   3 · the founder's, filed and Active. The code sentence is gone entirely and never
#       belonged — contract_sign_otp sends the code ten minutes later.
#   The founder also struck `and the deposit that holds your dates` on sight. That was
#   v3 clause 4 verbatim, carried so notice and instrument would agree; in a one-line
#   message to someone who has not opened the agreement it explains nothing.
#
# ⚠ FOUR CELLS WERE RE-AUTHORED, NOT DELETED. §10 asserted `status: 'draft'` and
# `isApproved === false`, which was true for about two hours. ONE OF THE TWO GATES HAS
# MOVED and the cells moved with it — the send is still dark, and the FLAG is now where
# that is asserted. Three new cells replace them and all three were driven both ways:
#   point contract_sign_otp back at tdw_vendor_login_otp   -> 3 red
#   OTP_TTL_MS back to 10 minutes                          -> 1 red
#   status back to 'draft'                                 -> 2 red
# b56 99 -> 102.
#
#   THIS TREE      GREEN 110  RED 49  TOTAL 159
#   NAMED BASE     GREEN 110  RED 49  TOTAL 159   (390c144)
#   DELTA          ZERO
#
# CONTRACT_SIGN_SEND_ENABLED remains unset in every environment. Nothing sends.

# ── MICRO 1 (2026-09-06) · derived at dream-os ce32c2e ──────────────────────
# R-G32.17/.18 — promote on pick. THIS IS NOT THE ONE-LINE MICRO THE CHARTER
# EXPECTED, and the difference is the finding: `composeContract` had NO promotion
# call for a `source` value to be added to. It took a `client_id` and nothing
# else, because at the mock this seat never knew it would need one — F-40.140.
#
#   src/lib/vendor/contracts.js   composeContract takes client_id OR {name, phone}
#   src/api/vendor/contracts.js   the door passes them, and `promoted` crosses back
#   scripts/b56_contract_bench.js §6 gains eight cells
#
#   b56    111/111  (102 -> 111), three mutations both ways:
#       client_id back to the argument   -> 2 red
#       source back to lead_promotion    -> 1 red
#       promoted always true             -> 1 red
#   FLOOR  THIS TREE   GREEN 110  RED 50  TOTAL 160
#          CLEAN TIP   GREEN 110  RED 50  TOTAL 160   (ce32c2e, stashed -u)
#          DELTA       ZERO
#   ⚠ 160 not 159, and 50 RED not 49: G1.3 landed `b57_g13_team_bench.js` under
#   this seat and it is RED at both trees. Inherited, named, NOT this delivery's —
#   and the seat's own pwa bench is also called b57, which is a COLLISION ACROSS
#   REPOS worth the chair's eye: `scripts/b57_contracts_wiring_bench.js` (pwa) and
#   `scripts/b57_g13_team_bench.js` (dream-os) share a number. Two repos, so
#   nothing breaks; a reader hearing "b57 is red" would be misled.
#
# ⚠ THE BENCH CAUGHT A REAL BUG IN THIS SEAT'S OWN PATCH ON THE FIRST RUN.
# The insert still read `client_id: clientId` — the ARGUMENT, undefined on the
# promotion path — so every promoted contract would have been written with a NULL
# counterparty and the room would have shown a contract belonging to nobody.
# `client.id` is the resolved row on both paths and is the only correct source.
# Found by a cell, not by review.
#
# ⚠ THE CLONE WAS FIVE COMMITS BEHIND WHEN THIS MICRO WAS FIRST CUT, AND THE
# FIRST ZIP WAS DESTROYED RATHER THAN SHIPPED. `git diff --stat origin/main`
# against a stale clone read as 2,305 DELETIONS — G1.3's 0137, weddingTeam.js,
# weddingCardPdf.js, b57_g13_team_bench.js, the PAIR regen and the chair's docs
# cut, every one of them a file this seat's tree simply predated. The five files
# in the ZIP could not have deleted any of it, but two of them (b56 and this
# manifest) would have been written from a pre-regen tree.
#
# They turned out to be AHEAD, not behind — origin never touched either, and the
# `+25`/`+23` were this seat's own additions read backwards. **The alarm was a
# misread of my own diff.** It is recorded because the check was right even though
# the reading was wrong: R-40.69's derived-at base is a claim that has to be
# re-derived at the cut, and a clone that did not perform the push is not on the
# tip merely because its work is (G5.1's standing lesson, met here).
#
# ⚠ AND ONE STANDING NOTE WAS RETIRED THE MOMENT IT STOPPED BEING TRUE.
# `contractSource.js` carried "OWED a PAIR regen" for 0138's columns. The regen
# landed at 5b3f61f — 79 tables, contract_profiles and contract_signatures
# described, F-40.99 CLOSED — so the note is replaced with a citation. A comment
# that outlives its fact is this estate's most-repeated defect class; leaving a
# paid debt on the page would have been a new instance of it.

# ── MICRO 2 (2026-09-06) · derived at dream-os d4a33a7 ─────────────────────
# F-40.152 — `Preview the PDF` rebuilt on the signed-object arm.
#
#   src/api/vendor/contracts.js     GET bytes -> POST { pdf_url }
#   scripts/b56_contract_bench.js   §10b, eleven cells
#
#   b56    122/122  (111 -> 122), four mutations both ways:
#       send bytes again (the F-40.152 shape) -> 2 red
#       GET instead of POST                   -> 2 red
#       write over the sealed copy            -> 2 red
#       a one-hour preview url                -> 1 red
#
# ⚠ THE DEFECT: the door returned PDF BYTES from behind `authMw` and the room
# opened it with `window.open()`. A NEW TAB CARRIES NO AUTHORIZATION HEADER, so
# every press returned `no_token` and the button had never once worked. NO BENCH
# COULD HAVE SEEN IT — every cell asserted this estate's behaviour and none
# asserted what a browser does with an address. The founder's glass found it.
#
# ⚠ AND THE CURE WAS SIXTY LINES ABOVE IN THE SAME FILE. `getDownloadUrl` returns
# a Supabase signed url precisely so a browser can open a private object without a
# header — which is why `Download` has always worked on this room. Built beside
# the precedent, did not take it.
#
# ⚠ TWO ABSENCE CELLS REDDENED ON THIS SEAT'S OWN COMMENTS before they read
# `code()` instead of `read()`. The door's header EXPLAINS that it must never
# write `.signed.pdf` nor import `generateContractPdf`, and the cells failed on
# the explanation. Third instance this sitting; the lesson is a standing one.
#
# ⚠ THE TIP MOVED BETWEEN THE CUT AND THE RE-DERIVATION, AND THE PACKET WAS
# RE-CUT RATHER THAN RE-STAMPED. ba368ce -> d4a33a7 (G1.3 rider 3, F-40.150).
# Derived by command that it touched b57_g13_team_bench.js, its own floor manifest
# and studio/weddings.js — NOTHING in G3.2 radius — so the work stands. The three
# files were reapplied ON the new tip and the floor re-run there, both ways:
#
#   WORKING TREE   GREEN 110  RED 50  TOTAL 160
#   TIP d4a33a7    GREEN 110  RED 50  TOTAL 160   (stashed -u)
#   DELTA          ZERO
#
# R-40.69 says a base line means derived-at, and the honest way to keep that true
# when a queue moves is to derive again — not to edit the line. The founder asked
# for exactly this before applying, and he was right to.

# ── MICRO 3 (2026-09-06) · derived at dream-os 4b28e87 ─────────────────────
# The preview walk's findings. F-40.159, F-40.160, and R-40.80's first cell.
#
#   src/lib/vendor/contractSource.js   the column, the number's real home, the guard
#   src/lib/contractPdf.js             reads vendor.phone
#   src/api/vendor/contracts.js        the human-named draft path
#   scripts/b56_contract_bench.js      §9b (R-40.80) and §9c, 132 -> 137
#
# ⚠ F-40.159 — `whatsapp_number` WAS NEVER A COLUMN, AND THE FIRST CURE WAS A
# SECOND FABRICATION. `phone` was written in its place from a grep across a line
# range that had run past `public.vendors` into a neighbour's block. **THAT TABLE
# HAS FORTY-NINE COLUMNS AND NOT ONE IS A NUMBER.** R-40.80's cell — written in the
# same hour, for this exact class — reddened on the replacement before it shipped.
# The number lives on `public.users.phone` through `vendors.user_id`, which is where
# vendorHandset.js, auth.js and admin/discover.js have always read it.
#
# ⚠ THE CELL CAUGHT THE SEAT THAT WROTE IT, ON ITS FIRST RUN. That is the whole
# argument for the ruling, and it is recorded because the alternative reading — that
# the seat found its own second mistake by care — would be false.
#
# ── MUTATIONS ──────────────────────────────────────────────────────────────
#   whatsapp_number back in the select            -> 1 red
#   phone back in the select (the first bad cure) -> 1 red
#   the uuid draft path returns                   -> 3 red
#   BOTH vendor guards removed                    -> BENCH CRASH, exit 1
#
# ⚠ **AND THE LAST ONE EXPOSED A DEFECT IN THIS SEAT'S MUTATION HARNESS, NOT IN THE
# CELL.** The harness counts `FAIL` lines. Removing both vendor guards makes
# `vendor` null, so `vendor.user_id` throws and the bench DIES — exit 1, `BENCH
# ERROR TypeError`, and ZERO fail lines. The harness read that as "0 red" and this
# seat nearly recorded a live cell as vacuous.
#
# A crash is a red. Every "0 red" reported in this arc's earlier manifests was read
# the same way and none of them crashed — verified by re-running with exit codes —
# but the harness has been half-blind all sitting and the record says so.
#
#   THIS TREE      GREEN 112  RED 49  TOTAL 161
#   TIP 4b28e87    GREEN 112  RED 49  TOTAL 161
#   DELTA          ZERO
#
# ⚠ HELD FOR RIDER 4, THEN RE-DERIVED — NOT RE-STAMPED. The chair halted this
# packet at base `9988614`; five commits landed under the hold (G3.1 sitting 1 and
# micro1-rev, F-40.146, the chair's docs, G1.3 rider 4). Verified by command that
# NONE touched contractSource.js, contractPdf.js, api/vendor/contracts.js, b56 or
# this manifest — `git diff --stat 9988614 origin/main --` over the five returned
# empty. The files were reapplied ON `4b28e87` and every floor re-run there.
#
# The counts moved with the tree, not with this work: 160 -> 161 (G3.1's b58) and
# 50 -> 49 RED (G3.1's micro1-rev cleared b44 and b55, F-40.169). Both trees agree.
#
# ── R-40.82 · THE COLLISION, CARRIED ────────────────────────────────────────
# `public.contracts` HAS NO NUMBER COLUMN — nothing like `invoices.invoice_number`
# exists, and `composeContract` generates the title as `<client> — wedding
# services`. So two contracts for ONE client produce an IDENTICAL slug, and a
# title-only object path with `upsert: true` would have one draft silently
# overwrite the other's. A vendor previewing contract B would be handed contract
# A's bytes under B's name — the costume class, in storage.
#
# Eight characters of the contract id follow the name. The name leads so the
# download reads as a document; the id follows so it is still one object per
# contract. The tradeoff is stated rather than hidden, and a cell drives it:
# dropping the suffix reds `and it stays unique per contract`.
#
# ── R-40.80's CELL DECLARES WHAT IT READS AGAINST ──────────────────────────
# ⚠ THE SNAPSHOT WENT STALE WITHIN HOURS OF THE RULING. The regen at `5b3f61f`
# states its ladder tip as `0138`; `0140_date_check_switch.sql` has since landed
# with `vendors.date_check_enabled`. A select asking for a post-snapshot column
# would red here while being perfectly correct.
#
# That is not a reason to soften the cell. The snapshot's own header states its tip
# "so this file's staleness is a readable fact, never archaeology", and the cell
# now inherits that discipline: it ASSERTS the tip it is reading against, so a red
# is always one of two things and never a mystery — a fabricated name, or a
# snapshot owed a regen.
#
# ── MUTATIONS, harness reading EXIT CODES ──────────────────────────────────
#   whatsapp_number back in the select              -> exit 1, 1 red
#   phone back in the select (the first bad cure)   -> exit 1, 1 red
#   the uuid draft path returns                     -> exit 1, 3 red
#   drop the id suffix (R-40.82's collision)        -> exit 1, 2 red
#   BOTH vendor guards removed                      -> exit 1, CRASH (a crash is a red)
