# scripts/floor-manifest-ce39-2b2.txt
# BASE 4918275 (main; re-derived fetch-first at the cut — the guard decides).
# Sibling dreamos-pwa bd60ac2 (worklist), and the pwa ZIP applies FIRST.
#
# CE-39 ROAD STEP 2b · 2b-2 · THE COMPANION.
#
# ⚠ WHAT THIS ZIP DOES **NOT** DO, FIRST, BECAUSE IT WAS CHARTERED TO:
#   Nothing is retired. cabinet.js's paid/owed slices and the two /vendor-e GET
#   mounts were ruled retirable on a callers-down sweep that reported ZERO pwa
#   readers. The sweep was a grep and the grep was wrong (F-2b2.3): dropping the
#   two fields from CabinetResponse made tsc name THREE live readers in one run —
#   lib/vendor/derive.ts:100 (moneyBinders composes from cab.paid + cab.owed),
#   app/vendor/list/[slice]/leads.tsx:96 and .../events.tsx:36. The mounts have
#   zero callers and were proven safe by request, both ways, and are HELD anyway:
#   Phase 7 retires the whole vendor-engine twin with its tree in ONE motion.
#
#   base_guard  the two copies made byte-identical — the paragraph claiming they
#               were identical WAS the divergence
#   s-2c.1      b47 1.3's enclosing derivation: the 900-char lookbehind replaced
#               by a brace-depth scope walk. A nearest-preceding declaration is
#               not a scope.
#   b48         NEW, and INVERTED: asserts the four engine money addresses STILL
#               RESOLVE, by request. A guard against accidental retirement.
#
# FILES
tools/base_guard.sh
scripts/b47_money_crossing_bench.js
scripts/b48_engine_mounts_bench.js
scripts/floor-manifest-ce39-2b2.txt
