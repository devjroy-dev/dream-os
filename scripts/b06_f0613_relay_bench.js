#!/usr/bin/env node
// b06_f0613_relay_bench.js — TDW_06 THE DETERMINISTIC SITTING (2026-07-28).
//
// Two cures, one bench, because they were ruled as one act: the SD-WEEK mechanical
// floor (F-06.13's fan-out, CE-25's HOLD founder-REVERSED on Evening Four's
// repetition) and the SD-REL relay-seam complement (her voiced sentence checked
// against her own hand's structured receipt).
//
// IT DRIVES THE SHIPPED FUNCTIONS, NEVER A COPY: every predicate below is required
// out of `src/engine/dist/core/`, so `npm run build` is a precondition and a mutation
// of production code REDS this file. Source pins are used only where the claim IS
// about the source (a law's presence, a W-1 byte-identity, a coverage boundary).
//
// THE LIVE VERDICT IS OUTSTANDING AND IS NOT THIS BENCH'S: no desk cell witnesses a
// model choosing honestly. What these cells witness is that the WIRE no longer offers
// it the choice on the two convicted shapes — which is the whole of what Path A
// bought. EVENING FIVE's SD-WEEK and SD-REL are the only live verdicts these cures
// will ever get, and they are declared here, never claimed.
// ── CE-45 LCV-16 LSP_5 · LABELLED AMENDMENT (A-45.2): THE RETIRED CELLS OF THIS BENCH, AT SITE ─────────────────────
// LSP_5 (the chair's rulings L5-a to L5-e, §7, K6, 25 September 2026) retired the business room from runTurn and deleted Donna's
// turn and the engine modules only it reached. Each row names a cell and why it retires; the cell is replaced at its site by
// __RETIRED (never evaluated). A retired cell prints RETIRED and is NOT counted as a pass. CONTROL: at exit every row must have
// matched exactly ONE reached cell, or the bench exits 1.
const __RETIRE_LSP5 = new Map([["§1.1 THE CONVICTED SHAPE: a sweep-supplied id on an ask that names no record is REFUSED, and the paper is the shipped bytes","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.2 L1 — THE OWNER NAMED THE RECORD (by the name the sweep surfaced): UNTAXED","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.2b L1 — CE-25's PROTECTED CASE, COUNT-BLIND: an owner naming several records is untaxed on every one of them","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.2c L1 — the generous limb: any name-shaped capitalised token past a sentence's first word opens the gate (F-04.27: ambiguity resolves toward the read happening)","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.3 L2 — PROVENANCE (\"how do you know that\"): UNTAXED, the hand's own stated purpose","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.4 L3 — RECONCILIATION BEFORE A WRITE: an ask carrying write intent is UNTAXED (DONNA_STATIC_PREFIX orders her to reconcile before she writes; a gate refusing that read would contradict the estate's own cached law)","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.5 L4 — THE ID CAME FROM A NAMED FIND: UNTAXED, whatever the ask looks like","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.5b L4 OUTRANKS AN EARLIER SWEEP on the same id — once the chain has narrowed by name it has narrowed, and the reverse is never true","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.6 AN ID NO FIND SUPPLIED IS UNTAXED — the gate refuses a chain, never an id","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.7 FAIL-OPEN ON A MISSING ASK: an empty ask can be judged by nothing, so it is never refused","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.8 AN EMPTY binder_id IS NOT THIS GATE'S CASE — the door's own ERROR speaks (the two registers never collide)","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.9 isBreadthFind BOTH WAYS: nothing given is the recents SWEEP; any search term makes it a NAMED find","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.10 F-04.62 — A DELIBERATE REFUSAL IS NEVER A SEARCH FAILURE: the paper opens REFUSED and disowns the failure reading in its first clause; it is not ERROR-prefixed; the hand's two genuine ERROR forms are byte-present and unchanged","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.11 THE PAPER STATES WHAT IT REFUSED, WHY, AND THE LAWFUL PATH (F-06.92's pattern; F-04.27's affordance — a false refusal costs one narrowing step, never a dead end)","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.12 A-3, RULED: THE PLAIN LEG IS ABSENT, NOT DORMANT — historyGate authors no plain clause anywhere, so the paper cannot reach Victor's composer or a vendor by any path","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.13 THE REFUSAL IS RECORDED UNDER ITS OWN HAND NAME, never donna_history — every reader that counts that name means \"a deep read happened\", and a refusal keeping it would leave a hollow RED over a wire that refused correctly","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.14 THE SITING IS THE WORK LOOP (fork A-1(b)) and executeHistory still has exactly ONE caller — the predicate reads the ask, the chain and the ids where they are formed","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§1.15 THE FAIL-OPEN/FAIL-CLOSED CONTRAST IS STATED WHERE BOTH ARE READ: the provenance hold sits at this same seam and fails CLOSED; this gate fails OPEN, and the asymmetry is named in-comment","K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn"],["§3.2 THE DOOR AUTHORS refused FROM THE GUARDS IT HAS ALREADY EVALUATED — one evaluation, two renderings, no second home to drift","K6/L5-c: the refused-authoring door (donnaLead.ts), its coverage law over those sites (now 0), historyGate, Donna's soul and static prefix, and loop.ts's relay dispatch are deleted; §3.1 (the carrier on snapshotTypes, kept) stays"],["§3.3 BOTH SINGLE-MATCH RETURNS CARRY IT, and fail-open: no refusal ⇒ no array ⇒ the relay can never be detected on that hand","K6/L5-c: the refused-authoring door (donnaLead.ts), its coverage law over those sites (now 0), historyGate, Donna's soul and static prefix, and loop.ts's relay dispatch are deleted; §3.1 (the carrier on snapshotTypes, kept) stays"],["§3.4 THE DATE FACT IS PRECISION-HONEST: the structured value comes from humanWeddingDate, the same renderer the sentence uses — a month-precision row can never enter the carrier as a day the estate never had","K6/L5-c: the refused-authoring door (donnaLead.ts), its coverage law over those sites (now 0), historyGate, Donna's soul and static prefix, and loop.ts's relay dispatch are deleted; §3.1 (the carrier on snapshotTypes, kept) stays"],["§3.5 B-3 — THE COVERAGE BOUNDARY IS STATED AS LAW, against `refused` and never conflated with `plain`","K6/L5-c: the refused-authoring door (donnaLead.ts), its coverage law over those sites (now 0), historyGate, Donna's soul and static prefix, and loop.ts's relay dispatch are deleted; §3.1 (the carrier on snapshotTypes, kept) stays"],["§3.5b AND THE BOUNDARY IS TRUE AT THIS COMMIT — derived, not asserted: refused at two sites, plain at three","K6/L5-c: the refused-authoring door (donnaLead.ts), its coverage law over those sites (now 0), historyGate, Donna's soul and static prefix, and loop.ts's relay dispatch are deleted; §3.1 (the carrier on snapshotTypes, kept) stays"],["§3.6 F-06.85 BOTH DIRECTIONS — the seam names the arms it conditions, and the gate names the soul sentence it conditions with the reverse pointer FILED AS OWED (W-1 shut)","K6/L5-c: the refused-authoring door (donnaLead.ts), its coverage law over those sites (now 0), historyGate, Donna's soul and static prefix, and loop.ts's relay dispatch are deleted; §3.1 (the carrier on snapshotTypes, kept) stays"],["§3.7 W-1 HELD SHUT: donnaSoul.ts is byte-free of both cures, and DONNA_STATIC_PREFIX is composed from DONNA_SOUL + the cabinet text untouched — the cache window is not invalidated by this delivery","K6/L5-c: the refused-authoring door (donnaLead.ts), its coverage law over those sites (now 0), historyGate, Donna's soul and static prefix, and loop.ts's relay dispatch are deleted; §3.1 (the carrier on snapshotTypes, kept) stays"],["§3.8 THE DISTIL NEEDS NO READER CHANGE: the annotated text IS the persisted listen_harvey_talk result, so donnaMessages re-teaches the correction with zero reader bytes shipped","K6/L5-c: the refused-authoring door (donnaLead.ts), its coverage law over those sites (now 0), historyGate, Donna's soul and static prefix, and loop.ts's relay dispatch are deleted; §3.1 (the carrier on snapshotTypes, kept) stays"],["§3.9 FORK C IS NOT DOUBLED: a receipt already carried at the seam is not repeated in the composer's payload","K6/L5-c: the refused-authoring door (donnaLead.ts), its coverage law over those sites (now 0), historyGate, Donna's soul and static prefix, and loop.ts's relay dispatch are deleted; §3.1 (the carrier on snapshotTypes, kept) stays"],["§2.11 THE SEAM HAS ONE HOME","L5-b: loop.ts no longer imports or wires the seam (the dispatch that composed Donna's relay is deleted); relaySeam.ts itself is kept whole (L5-e) and its exports stay driven in §2.1 to §2.10"],["§2.12 THE DETECTION IS WIRED","L5-b: loop.ts no longer imports or wires the seam (the dispatch that composed Donna's relay is deleted); relaySeam.ts itself is kept whole (L5-e) and its exports stay driven in §2.1 to §2.10"]]);
const __seenLSP5 = new Map();
function __RETIRED(k) { if (!__RETIRE_LSP5.has(k)) { console.log('  FAIL  ' + k + '  (RETIRED at site but not in the table)'); process.exitCode = 1; return; }
  __seenLSP5.set(k, (__seenLSP5.get(k) || 0) + 1); console.log('  RETIRED  ' + k + '  (' + __RETIRE_LSP5.get(k) + ')'); }
process.on('exit', (code) => { let bad = 0; for (const [k] of __RETIRE_LSP5) if ((__seenLSP5.get(k) || 0) !== 1) { bad++; console.log('  FAIL  retire row ' + k + ' matched ' + (__seenLSP5.get(k) || 0) + ' reached cells (must be exactly 1)'); }
  if (bad) process.exitCode = 1; else if (code !== 0) process.exitCode = code; });

const path = require('path');
const fs = require('fs');
const ROOT = path.resolve(__dirname, '..');
const DIST = path.join(ROOT, 'src/engine/dist/core');

// CE-45 LCV-16 LSP_5 (labelled): historyGate.ts is deleted (K6); this bench now opens only the sources that still exist.
if (!fs.existsSync(path.join(DIST, 'relaySeam.js'))) {
  console.error('DIST MISSING — run `npm run build` first. This bench drives the SHIPPED functions, never a copy.');
  process.exit(3); // F-39.47: a missing build artifact is a REFUSAL (3), never a FAIL
}

const seam = require(path.join(DIST, 'relaySeam.js'));

const SRC = {
  seam: fs.readFileSync(path.join(ROOT, 'src/engine/src/core/relaySeam.ts'), 'utf8'),
  donna: fs.readFileSync(path.join(ROOT, 'src/engine/src/core/donna.ts'), 'utf8'),
  loop: fs.readFileSync(path.join(ROOT, 'src/engine/src/core/loop.ts'), 'utf8'),
  types: fs.readFileSync(path.join(ROOT, 'src/engine/src/core/snapshotTypes.ts'), 'utf8'),
};

let pass = 0, fail = 0;
const T = (name, ok) => { if (ok) { pass++; console.log(`  PASS  ${name}`); } else { fail++; console.log(`  FAIL  ${name}`); } };
const sec = (s) => console.log(`\n${s}`);

// A gate context built the way the work loop builds it: an ask, then a find's yield.
/* CE-45 LCV-16 LSP_5 (A-45.2): the span below was removed and its 18 cells retired at site: K6: historyGate.ts (the breadth gate, its context and refusal paper) is deleted with Donna's turn */
__RETIRED("§1.1 THE CONVICTED SHAPE: a sweep-supplied id on an ask that names no record is REFUSED, and the paper is the shipped bytes");
__RETIRED("§1.2 L1 — THE OWNER NAMED THE RECORD (by the name the sweep surfaced): UNTAXED");
__RETIRED("§1.2b L1 — CE-25's PROTECTED CASE, COUNT-BLIND: an owner naming several records is untaxed on every one of them");
__RETIRED("§1.2c L1 — the generous limb: any name-shaped capitalised token past a sentence's first word opens the gate (F-04.27: ambiguity resolves toward the read happening)");
__RETIRED("§1.3 L2 — PROVENANCE (\"how do you know that\"): UNTAXED, the hand's own stated purpose");
__RETIRED("§1.4 L3 — RECONCILIATION BEFORE A WRITE: an ask carrying write intent is UNTAXED (DONNA_STATIC_PREFIX orders her to reconcile before she writes; a gate refusing that read would contradict the estate's own cached law)");
__RETIRED("§1.5 L4 — THE ID CAME FROM A NAMED FIND: UNTAXED, whatever the ask looks like");
__RETIRED("§1.5b L4 OUTRANKS AN EARLIER SWEEP on the same id — once the chain has narrowed by name it has narrowed, and the reverse is never true");
__RETIRED("§1.6 AN ID NO FIND SUPPLIED IS UNTAXED — the gate refuses a chain, never an id");
__RETIRED("§1.7 FAIL-OPEN ON A MISSING ASK: an empty ask can be judged by nothing, so it is never refused");
__RETIRED("§1.8 AN EMPTY binder_id IS NOT THIS GATE'S CASE — the door's own ERROR speaks (the two registers never collide)");
__RETIRED("§1.9 isBreadthFind BOTH WAYS: nothing given is the recents SWEEP; any search term makes it a NAMED find");
__RETIRED("§1.10 F-04.62 — A DELIBERATE REFUSAL IS NEVER A SEARCH FAILURE: the paper opens REFUSED and disowns the failure reading in its first clause; it is not ERROR-prefixed; the hand's two genuine ERROR forms are byte-present and unchanged");
__RETIRED("§1.11 THE PAPER STATES WHAT IT REFUSED, WHY, AND THE LAWFUL PATH (F-06.92's pattern; F-04.27's affordance — a false refusal costs one narrowing step, never a dead end)");
__RETIRED("§1.12 A-3, RULED: THE PLAIN LEG IS ABSENT, NOT DORMANT — historyGate authors no plain clause anywhere, so the paper cannot reach Victor's composer or a vendor by any path");
__RETIRED("§1.13 THE REFUSAL IS RECORDED UNDER ITS OWN HAND NAME, never donna_history — every reader that counts that name means \"a deep read happened\", and a refusal keeping it would leave a hollow RED over a wire that refused correctly");
__RETIRED("§1.14 THE SITING IS THE WORK LOOP (fork A-1(b)) and executeHistory still has exactly ONE caller — the predicate reads the ask, the chain and the ids where they are formed");
__RETIRED("§1.15 THE FAIL-OPEN/FAIL-CLOSED CONTRAST IS STATED WHERE BOTH ARE READ: the provenance hold sits at this same seam and fails CLOSED; this gate fails OPEN, and the asymmetry is named in-comment");
sec('§2 THE RELAY SEAM — the echo caught, the honest relay untouched (fork B-2(α))');

const REFUSED_TARA = [
  { field: 'city', stands: 'Jaipur', said: 'Udaipur' },
  { field: 'wedding date', stands: '5 March 2027', said: '5 December 2027' },
];
const DOOR_SENTENCE = "Not written — the record already stands: the city stays Jaipur (you said Udaipur), the wedding date stays 5 March 2027 (you said 5 December 2027). If either should change, say so and I'll change it.";
const ECHO = 'Listen Harvey — Lead updated: Tara Relay Test, Udaipur, 5 Dec 2027, phone on file.';
const HONEST = 'Listen Harvey — Matched the existing Tara Relay Test — her record holds Jaipur, 5 March 2027; the new city and date were not written.';

T('§2.1 THE ECHO IS DETECTED: the relay asserts the value the door refused and never names the value that stands',
  seam.echoedRefusals(ECHO, REFUSED_TARA).length === 1
  && seam.echoedRefusals(ECHO, REFUSED_TARA)[0].field === 'city');

T('§2.2 THE HONEST RELAY IS NOT DETECTED: it carries the standing value, so however many facts were refused it is never a contradiction',
  seam.echoedRefusals(HONEST, REFUSED_TARA).length === 0);

T('§2.3 FAIL-OPEN BY CONSTRUCTION: a hand with no refused array can never be detected — no inference fallback exists (F-06.102: structured fields, never prose)',
  seam.echoedRefusals(ECHO, undefined).length === 0
  && seam.echoedRefusals(ECHO, null).length === 0
  && seam.echoedRefusals(ECHO, []).length === 0);

T('§2.4 A HALF-FORMED FACT NEVER CONVICTS (a missing stands or said is silence, not a contradiction)',
  seam.echoedRefusals('Udaipur it is.', [{ field: 'city', stands: '', said: 'Udaipur' }]).length === 0
  && seam.echoedRefusals('Udaipur it is.', [{ field: 'city', stands: 'Jaipur', said: '' }]).length === 0);

T('§2.5 HER BYTES ARE VERBATIM-FIRST AND BYTE-EXACT: the append never edits, reorders or drops one character of hers',
  seam.appendDeedTail(ECHO, DOOR_SENTENCE).startsWith(ECHO)
  && seam.stripDeedTail(seam.appendDeedTail(ECHO, DOOR_SENTENCE)) === ECHO);

T('§2.6 THE TAIL IS THE DOOR\'S ALREADY-VETOED SENTENCE, after the founder-vetoed seam and nothing else',
  seam.appendDeedTail(ECHO, DOOR_SENTENCE) === ECHO + seam.RELAY_DEED_SEAM + DOOR_SENTENCE);

T('§2.7 THE SEAM IS THE VETOED V1 BYTES: newline, em-dash lead-in, "from the file: " — a second voice on the wire, never a continuation of hers',
  seam.RELAY_DEED_SEAM === '\n— from the file: ');

T('§2.8 IDEMPOTENT: a text already carrying the seam is returned untouched — no second pass can double a tail onto a relay',
  seam.appendDeedTail(seam.appendDeedTail(ECHO, DOOR_SENTENCE), DOOR_SENTENCE)
    === seam.appendDeedTail(ECHO, DOOR_SENTENCE));

T('§2.9 A CLEAN TURN APPENDS NOTHING: an empty door sentence returns the voiced text byte-identical (the cost fires only on a detected contradiction)',
  seam.appendDeedTail(HONEST, '') === HONEST && seam.appendDeedTail(HONEST, '   ') === HONEST);

T('§2.10 stripDeedTail IS TOTAL: a text with no seam is returned whole, so the arms\' extraction is byte-safe on every historic turn',
  seam.stripDeedTail(HONEST) === HONEST && seam.stripDeedTail('') === '');

__RETIRED("§2.11 THE SEAM HAS ONE HOME");

__RETIRED("§2.12 THE DETECTION IS WIRED");

// ───────────────────────────────────────────────────────────────────────────────
sec('§3 THE CARRIER, THE COVERAGE BOUNDARY, AND THE LAWS BOTH CURES SHIP INSIDE');

T('§3.1 THE STRUCTURED CARRIER IS ADDITIVE ON ToolOutcome and re-exported from ONE home (fork B-1(a); (b) refused on F-04.36)',
  /refused\?: RefusedFact\[\] \| null;/.test(SRC.types)
  && /import type \{ RefusedFact \} from '\.\/relaySeam\.js';/.test(SRC.types)
  && (SRC.seam.match(/^export type RefusedFact = /mg) || []).length === 1);

/* CE-45 LCV-16 LSP_5 (A-45.2): the span below was removed and its 9 cells retired at site: K6/L5-c: the refused-authoring door (donnaLead.ts), its coverage law over those sites (now 0), historyGate, Donna's soul and static prefix, and loop.ts's relay dispatch are deleted; §3.1 (the carrier on snapshotTypes, kept) stays */
__RETIRED("§3.2 THE DOOR AUTHORS refused FROM THE GUARDS IT HAS ALREADY EVALUATED — one evaluation, two renderings, no second home to drift");
__RETIRED("§3.3 BOTH SINGLE-MATCH RETURNS CARRY IT, and fail-open: no refusal ⇒ no array ⇒ the relay can never be detected on that hand");
__RETIRED("§3.4 THE DATE FACT IS PRECISION-HONEST: the structured value comes from humanWeddingDate, the same renderer the sentence uses — a month-precision row can never enter the carrier as a day the estate never had");
__RETIRED("§3.5 B-3 — THE COVERAGE BOUNDARY IS STATED AS LAW, against `refused` and never conflated with `plain`");
__RETIRED("§3.5b AND THE BOUNDARY IS TRUE AT THIS COMMIT — derived, not asserted: refused at two sites, plain at three");
__RETIRED("§3.6 F-06.85 BOTH DIRECTIONS — the seam names the arms it conditions, and the gate names the soul sentence it conditions with the reverse pointer FILED AS OWED (W-1 shut)");
__RETIRED("§3.7 W-1 HELD SHUT: donnaSoul.ts is byte-free of both cures, and DONNA_STATIC_PREFIX is composed from DONNA_SOUL + the cabinet text untouched — the cache window is not invalidated by this delivery");
__RETIRED("§3.8 THE DISTIL NEEDS NO READER CHANGE: the annotated text IS the persisted listen_harvey_talk result, so donnaMessages re-teaches the correction with zero reader bytes shipped");
__RETIRED("§3.9 FORK C IS NOT DOUBLED: a receipt already carried at the seam is not repeated in the composer's payload");
console.log(`\n${fail === 0 ? 'GREEN' : 'RED'} — b06_f0613_relay_bench ${pass}/${pass + fail}`);
if (fail === 0) {
  console.log('\n       The breadth sweep no longer hauls eight dossiers to answer a question about');
  console.log('       eight timestamps, and a relay that speaks over its own receipt no longer');
  console.log('       ships alone. Her words are untouched; the file speaks after them.');
  console.log('       THE LIVE VERDICT IS EVENING FIVE\'S — declared here, never claimed.');
}
process.exit(fail === 0 ? 0 : 1);
